# Part 1 — Foundations, Risk Architecture, Provider Classification, and Governance

### 1 — AI-LLM-001 — Large Language Model Provider Risk Definition

Large Language Model (LLM) provider risk is the risk introduced when an organization depends on an external provider for access to, operation of, hosting of, or services built around a large language model.

LLM provider risk can arise from:

* model security;
* data processing;
* prompt handling;
* output handling;
* model training;
* model changes;
* service availability;
* API security;
* provider infrastructure;
* subprocessors;
* model supply chain;
* regulatory exposure;
* intellectual property;
* concentration;
* vendor lock-in; and
* provider failure.

LLM provider risk is therefore broader than conventional SaaS vendor risk.

---

### 2 — AI-LLM-002 — LLM Provider Risk Governance Objective

The objective of LLM provider risk governance is to ensure that external LLM dependencies are:

* identified;
* understood;
* assessed;
* authorized;
* securely integrated;
* monitored;
* reassessed when material conditions change; and
* capable of being restricted, replaced, or terminated when required.

---

### 3 — AI-LLM-003 — LLM Provider Versus AI Vendor

An LLM provider is a specific category of AI vendor that supplies access to a language model or model-related capability.

Examples of provider relationships include:

* direct model API;
* hosted LLM;
* managed enterprise LLM;
* foundation-model service;
* cloud-hosted LLM;
* self-hostable model supplied by a provider;
* fine-tuning service;
* inference service; and
* LLM-powered application.

An organization should identify the actual role performed by the provider rather than treating all AI vendors as equivalent.

---

### 4 — AI-LLM-004 — LLM Provider Role Classification

An LLM provider may perform one or more roles:

1. model developer;
2. model owner;
3. model distributor;
4. model host;
5. inference provider;
6. API provider;
7. fine-tuning provider;
8. application provider;
9. infrastructure provider;
10. model marketplace operator; or
11. intermediary between the organization and another model provider.

The role determines which risks and controls should be assessed.

---

### 5 — AI-LLM-005 — Direct LLM Provider Dependency

A direct LLM dependency exists when the organization's application communicates directly with the provider's model or API.

The organization should identify:

* endpoint;
* model;
* authentication;
* data transmitted;
* provider processing;
* response handling;
* logging;
* retention;
* model version;
* service tier; and
* contractual relationship.

---

### 6 — AI-LLM-006 — Indirect LLM Provider Dependency

An indirect LLM dependency exists when another application or service uses an external LLM provider on the organization's behalf.

For example:

**Organization → AI SaaS → LLM Provider → Cloud Infrastructure**

The organization should identify the underlying LLM dependency where material risk could propagate through the service.

---

### 7 — AI-LLM-007 — LLM Dependency Mapping

Material LLM dependencies should be mapped through the relevant technology stack.

A dependency map may identify:

**Business Process → Application → AI Service → LLM → Hosting Provider → Infrastructure → Supporting Services**

The objective is to identify hidden dependencies and potential correlated failures.

---

### 8 — AI-LLM-008 — LLM Provider Criticality

An LLM provider should be classified according to the impact of its failure or compromise on the organization.

Criticality may depend on:

* business-process dependency;
* transaction volume;
* customer impact;
* sensitive data;
* regulatory significance;
* automation level;
* availability requirements;
* replacement difficulty; and
* concentration.

This is an internal governance classification and is not itself a regulatory classification.

---

### 9 — AI-LLM-009 — LLM Provider Risk Dimensions

LLM provider risk should be evaluated across multiple dimensions:

* cybersecurity;
* privacy;
* data governance;
* model security;
* model reliability;
* AI safety;
* availability;
* resilience;
* regulatory;
* contractual;
* intellectual property;
* supply chain;
* concentration;
* geopolitical;
* financial;
* reputational; and
* exit/portability.

---

### 10 — AI-LLM-010 — LLM Cybersecurity Risk

Cybersecurity risk may arise from:

* compromised provider infrastructure;
* compromised API credentials;
* unauthorized access;
* insecure integrations;
* vulnerable model-serving systems;
* malicious model changes;
* data leakage;
* compromised plugins;
* supply-chain attacks; and
* provider-side security incidents.

---

### 11 — AI-LLM-011 — LLM API Security Risk

Where an LLM is accessed through an API, the organization should assess:

* authentication;
* authorization;
* API-key management;
* rate limiting;
* quotas;
* TLS;
* endpoint security;
* logging;
* abuse prevention;
* credential rotation; and
* API-version management.

---

### 12 — AI-LLM-012 — LLM Credential Risk

LLM API credentials can provide direct access to AI services and may also expose sensitive organizational data through prompts.

Credentials should therefore be protected through:

* secure storage;
* least privilege;
* rotation;
* monitoring;
* revocation;
* separation;
* secret-management systems; and
* controlled issuance.

---

### 13 — AI-LLM-013 — LLM API Key Exposure

API keys should not be embedded unnecessarily in:

* source code;
* client-side applications;
* public repositories;
* documentation;
* browser applications;
* tickets; or
* unprotected configuration files.

Where exposure is suspected, the credential should be treated as potentially compromised and handled according to incident procedures.

---

### 14 — AI-LLM-014 — LLM Provider Authentication

The organization should evaluate the provider's authentication mechanisms.

Relevant capabilities may include:

* multifactor authentication;
* federated identity;
* service identities;
* role-based access;
* API credentials;
* workload identities; and
* privileged-access controls.

---

### 15 — AI-LLM-015 — LLM Provider Authorization

Authorization should restrict access according to:

* user;
* application;
* environment;
* model;
* function;
* data;
* administrative role; and
* permitted action.

An authenticated user should not automatically receive unrestricted access to every model or capability.

---

### 16 — AI-LLM-016 — LLM Provider Least Privilege

Access to external LLM services should follow least-privilege principles.

Examples include:

* separate development and production credentials;
* restricted model access;
* restricted administrative access;
* application-specific credentials;
* limited API scopes; and
* restricted tool permissions.

---

### 17 — AI-LLM-017 — LLM Provider Administrative Access

The organization should understand how provider personnel can access customer environments or data.

Assessment should consider:

* authorized personnel;
* access conditions;
* privileged access;
* monitoring;
* logging;
* approval;
* emergency access;
* access revocation; and
* geographic location.

---

### 18 — AI-LLM-018 — LLM Provider Support Access

Customer-support processes can create an additional access path to organizational information.

The organization should assess whether support personnel can access:

* prompts;
* uploaded files;
* outputs;
* account information;
* logs;
* configuration;
* incident data; or
* other customer content.

---

### 19 — AI-LLM-019 — LLM Data Processing Model

The organization should establish how data is processed by the LLM provider.

The assessment should identify:

* input;
* processing;
* storage;
* inference;
* logging;
* monitoring;
* backup;
* support access;
* model-improvement processing; and
* deletion.

---

### 20 — AI-LLM-020 — LLM Prompt Data Classification

Prompts should be classified according to the information they may contain.

Prompts can contain:

* personal data;
* confidential information;
* trade secrets;
* source code;
* credentials;
* customer information;
* financial information;
* security information; and
* proprietary business information.

The organization should not classify prompts as low risk merely because they are text.

---

### 21 — AI-LLM-021 — LLM Input Data Governance

Approved LLM use should define what types of information may be submitted to the provider.

Controls may include:

* data classification;
* DLP;
* prompt filtering;
* user restrictions;
* application controls;
* data masking;
* pseudonymization; and
* technical blocking.

---

### 22 — AI-LLM-022 — LLM Sensitive Data Restrictions

Where organizational policy or applicable requirements restrict external processing of sensitive data, the LLM deployment should enforce those restrictions.

Possible approaches include:

* blocking;
* masking;
* tokenization;
* pseudonymization;
* dedicated environments;
* approved enterprise services; or
* alternative processing.

---

### 23 — AI-LLM-023 — LLM Data Minimization

The organization should minimize information submitted to an external LLM to what is necessary for the approved purpose.

For example, a document-classification task may not require transmitting unrelated customer identifiers.

---

### 24 — AI-LLM-024 — LLM Prompt Redaction

Where appropriate, prompts should be processed to remove or transform unnecessary sensitive information before transmission.

Redaction mechanisms should be tested for:

* completeness;
* bypass;
* false positives;
* false negatives; and
* preservation of task functionality.

---

### 25 — AI-LLM-025 — LLM Data Masking

Sensitive fields may be masked before transmission to the provider.

The organization should ensure that masking:

* is applied consistently;
* does not expose the original information;
* preserves required functionality; and
* can be reversed only by authorized processes where reversibility is required.

---

### 26 — AI-LLM-026 — LLM Pseudonymization

Where appropriate, identifiers may be pseudonymized before being submitted to an external LLM.

The organization should separately protect the mapping between pseudonyms and original identities.

---

### 27 — AI-LLM-027 — LLM Provider Data Retention

The organization should determine how long the provider retains:

* prompts;
* files;
* outputs;
* logs;
* telemetry;
* account information;
* security records; and
* backups.

Different data categories may have different retention periods.

---

### 28 — AI-LLM-028 — LLM Zero-Retention Claims

Claims that an LLM service provides “zero retention” should be validated against:

* contractual terms;
* service documentation;
* technical configuration;
* logging;
* abuse monitoring;
* backup architecture;
* support processes; and
* applicable service tier.

“Zero retention” should not be interpreted as an absolute absence of every form of provider-side data processing.

---

### 29 — AI-LLM-029 — LLM Training Data Use

The organization should establish whether its prompts, files, outputs, feedback, or telemetry may be used to:

* train models;
* fine-tune models;
* improve services;
* evaluate models;
* detect abuse;
* develop products; or
* support other provider functions.

---

### 30 — AI-LLM-030 — LLM Customer Data Training Restriction

Where the organization prohibits customer data from being used for model training or improvement, that restriction should be technically and contractually supported where feasible.

The organization should verify that the selected service configuration implements the intended restriction.

---

### 31 — AI-LLM-031 — LLM Human Review of Customer Content

The organization should determine whether provider personnel may review customer prompts, files, or outputs.

Where human review is possible, the organization should understand:

* purpose;
* authorization;
* scope;
* retention;
* personnel access;
* confidentiality controls; and
* opt-out or configuration mechanisms where available.

---

### 32 — AI-LLM-032 — LLM Abuse Monitoring Data

Providers may process certain customer interactions for security, abuse prevention, or service protection.

The organization should understand such processing separately from model-training use.

This distinction is important because a prohibition on training does not necessarily mean that no provider-side processing occurs.

---

### 33 — AI-LLM-033 — LLM Data Secondary Use

The organization should identify secondary uses of customer data beyond the primary inference purpose.

Potential uses include:

* analytics;
* product improvement;
* abuse detection;
* benchmarking;
* research;
* quality assurance; and
* support.

Each use should be evaluated against organizational and applicable legal requirements.

---

### 34 — AI-LLM-034 — LLM Data Ownership and Control

Contracts and service documentation should clarify relevant rights concerning:

* customer inputs;
* customer outputs;
* provider-generated metadata;
* derived data;
* embeddings;
* fine-tuned models; and
* logs.

Ownership terminology should be distinguished from rights to process or use information.

---

### 35 — AI-LLM-035 — LLM Output Rights

The organization should evaluate contractual and legal treatment of model outputs where outputs are important to business processes.

Assessment may consider:

* contractual rights;
* licensing;
* confidentiality;
* downstream use;
* intellectual-property issues;
* third-party content; and
* provider restrictions.

---

### 36 — AI-LLM-036 — LLM Intellectual-Property Risk

LLM provider relationships may create intellectual-property risk involving:

* training data;
* prompts;
* outputs;
* model weights;
* generated code;
* third-party content;
* datasets;
* licenses; and
* provider contractual terms.

Legal analysis should be obtained where the organization's intended use creates material IP exposure.

---

### 37 — AI-LLM-037 — LLM Copyright and Licensing Assessment

The organization should assess relevant licensing and intellectual-property considerations for the intended LLM use.

The assessment should distinguish:

* provider contractual terms;
* model license;
* third-party content;
* generated output;
* organizational content; and
* applicable law.

---

### 38 — AI-LLM-038 — LLM Generated-Code Risk

When LLMs generate source code, the organization should assess:

* security;
* licensing;
* provenance;
* vulnerabilities;
* malicious code;
* dependency insertion;
* secrets;
* quality; and
* human review.

Generated code should follow the organization's secure-development controls.

---

### 39 — AI-LLM-039 — LLM Output Reliability Risk

LLM outputs may contain:

* factual errors;
* hallucinations;
* omissions;
* inconsistent responses;
* unsafe recommendations;
* outdated information; or
* fabricated references.

Reliability controls should therefore be aligned with the consequences of the use case.

---

### 40 — AI-LLM-040 — LLM Hallucination Risk

Hallucination risk should be assessed where incorrect output could cause material harm.

Controls may include:

* retrieval augmentation;
* grounding;
* source validation;
* confidence mechanisms;
* human review;
* deterministic processing where possible;
* output constraints; and
* post-processing validation.

---

### 41 — AI-LLM-041 — LLM Grounding Controls

Where factual accuracy is material, the organization should evaluate mechanisms that ground model outputs in trusted information sources.

Potential mechanisms include:

* retrieval-augmented generation;
* approved knowledge bases;
* authoritative datasets;
* document retrieval;
* citation requirements; and
* controlled context.

---

### 42 — AI-LLM-042 — LLM Retrieval Dependency Risk

If an LLM application uses retrieval-augmented generation, vendor risk extends to:

* vector databases;
* retrieval services;
* source repositories;
* embedding models;
* indexing pipelines;
* access controls; and
* source-data integrity.

---

### 43 — AI-LLM-043 — LLM Embedding Dependency

Where embeddings are generated by an external provider, the organization should assess:

* data transmission;
* retention;
* model use;
* geographic processing;
* provider access;
* portability; and
* security.

---

### 44 — AI-LLM-044 — LLM Context Window Dependency

Changes to an LLM provider's context-window capability may affect:

* application functionality;
* data transmission;
* prompt construction;
* cost;
* performance; and
* security controls.

Material changes should therefore be assessed where they affect the organization's use case.

---

### 45 — AI-LLM-045 — LLM Token and Usage Limits

Organizations should understand provider limits affecting:

* tokens;
* requests;
* throughput;
* concurrent users;
* file size;
* context;
* API calls; and
* monthly usage.

Unexpected limits can create operational and availability risk.

---

### 46 — AI-LLM-046 — LLM Rate-Limit Risk

Rate limiting should be evaluated as part of availability and operational planning.

The organization should determine:

* provider limits;
* application demand;
* burst behavior;
* retry behavior;
* throttling;
* fallback;
* quota management; and
* customer impact.

---

### 47 — AI-LLM-047 — LLM Cost-Control Risk

Uncontrolled LLM usage can create financial and operational exposure.

Controls may include:

* budgets;
* quotas;
* rate limits;
* per-user limits;
* per-application limits;
* anomaly detection;
* alerts; and
* approval thresholds.

---

### 48 — AI-LLM-048 — LLM Cost Anomaly Detection

The organization should monitor abnormal increases in LLM consumption.

Potential indicators include:

* sudden token increases;
* unexpected API traffic;
* unusual users;
* unusual models;
* repeated requests;
* automated loops; and
* credential compromise.

---

### 49 — AI-LLM-049 — LLM Denial-of-Wallet Risk

An attacker or malfunctioning application may generate excessive LLM requests and create substantial provider charges.

Controls may include:

* API quotas;
* budget thresholds;
* rate limits;
* application controls;
* credential isolation;
* anomaly detection; and
* automated shutdown.

---

### 50 — AI-LLM-050 — LLM Service Availability

Availability should be assessed according to the criticality of the organization's dependency.

Assessment may include:

* historical availability;
* SLA;
* outage frequency;
* regional redundancy;
* maintenance;
* capacity;
* rate limits;
* service dependencies; and
* fallback capability.

---

### 51 — AI-LLM-051 — LLM Provider Outage Risk

An LLM provider outage can affect all applications that depend on the provider.

The organization should identify:

* affected business processes;
* maximum tolerable disruption;
* fallback;
* alternate models;
* manual procedures; and
* recovery requirements.

---

### 52 — AI-LLM-052 — LLM Provider Regional Outage

Where a provider operates across multiple regions, the organization should assess whether its selected configuration depends on a single geographic region.

Regional concentration may create correlated availability and regulatory risks.

---

### 53 — AI-LLM-053 — LLM Multi-Region Resilience

Where business criticality justifies it, organizations may evaluate:

* multi-region deployment;
* alternate endpoints;
* alternate providers;
* alternate models;
* local inference; or
* manual fallback.

---

### 54 — AI-LLM-054 — LLM Multi-Provider Resilience

Organizations may consider multiple LLM providers where concentration creates material operational risk.

The assessment should also consider the additional complexity of:

* security;
* identity;
* data controls;
* monitoring;
* testing;
* model behavior; and
* governance.

---

### 55 — AI-LLM-055 — LLM Provider Dependency Concentration

Concentration should be assessed where multiple applications depend on:

* the same LLM provider;
* the same model family;
* the same cloud;
* the same API;
* the same region; or
* the same infrastructure.

---

### 56 — AI-LLM-056 — LLM Correlated Failure

Apparently separate AI applications may experience simultaneous failure when they share a common LLM dependency.

The dependency map should therefore be used to identify correlated failure scenarios.

---

### 57 — AI-LLM-057 — LLM Provider Business Continuity

Critical LLM providers should be evaluated for business-continuity and disaster-recovery capabilities appropriate to the service.

Evidence may include:

* continuity plans;
* recovery objectives;
* redundancy;
* testing;
* incident response; and
* recovery evidence.

---

### 58 — AI-LLM-058 — LLM Provider Incident Management

The organization should determine how the provider handles:

* cybersecurity incidents;
* privacy incidents;
* service outages;
* model incidents;
* supply-chain compromise;
* abuse;
* data leakage; and
* customer notification.

---

### 59 — AI-LLM-059 — LLM Incident Notification

Material LLM provider incidents should be subject to defined notification mechanisms.

Where appropriate, contracts should specify:

* notification channel;
* incident categories;
* timing;
* minimum information;
* updates;
* cooperation; and
* evidence preservation.

---

### 60 — AI-LLM-060 — LLM Incident Notification Versus Regulatory Reporting

A provider's notification to the organization does not automatically satisfy the organization's own legal or regulatory reporting responsibilities.

The organization should independently determine whether notification obligations apply.

---

### 61 — AI-LLM-061 — LLM Incident Evidence

The organization should preserve relevant evidence following a material provider incident.

Potential evidence includes:

* provider notices;
* affected model;
* timestamps;
* logs;
* API records;
* affected data;
* configuration;
* communications; and
* remediation information.

---

### 62 — AI-LLM-062 — LLM Provider Root-Cause Analysis

For material incidents, the organization should seek sufficient information to understand:

* root cause;
* affected systems;
* affected model;
* attack vector;
* data exposure;
* containment;
* remediation; and
* recurrence risk.

---

### 63 — AI-LLM-063 — LLM Provider Post-Incident Reassessment

A material provider incident should be evaluated to determine whether the organization's vendor risk assessment remains valid.

The review may consider:

* risk tier;
* controls;
* contractual protections;
* monitoring;
* residual risk;
* business necessity; and
* exit feasibility.

This is an internal governance control unless a separate legal, regulatory, or contractual requirement mandates reassessment.

---

### 64 — AI-LLM-064 — LLM Model Version Governance

The organization should maintain visibility into the model version or model identifier used for material applications where technically feasible.

Version information supports:

* reproducibility;
* incident investigation;
* change assessment;
* testing;
* rollback; and
* auditability.

---

### 65 — AI-LLM-065 — LLM Model Change Risk

Model changes can alter:

* outputs;
* safety behavior;
* refusals;
* reasoning behavior;
* latency;
* context handling;
* data processing;
* tool interaction;
* performance; and
* security characteristics.

A model update should therefore be treated as a potential dependency change.

---

### 66 — AI-LLM-066 — LLM Model Change Notification

For material applications, the organization should seek mechanisms for receiving notification of significant model changes.

The required notification mechanism should reflect contractual capability and business criticality.

---

### 67 — AI-LLM-067 — LLM Model Change Assessment

Material model changes should be evaluated for:

* security;
* privacy;
* performance;
* safety;
* compliance;
* compatibility;
* business impact; and
* downstream automation.

---

### 68 — AI-LLM-068 — LLM Model Regression Testing

Where the organization controls the application consuming the LLM, model changes should be tested against defined regression criteria where appropriate.

Testing may include:

* functional accuracy;
* security;
* safety;
* hallucination;
* prompt injection;
* refusal behavior;
* latency; and
* application compatibility.

---

### 69 — AI-LLM-069 — LLM Model Rollback

Where technically feasible, critical LLM applications should maintain an appropriate rollback or alternative-model capability.

Rollback should be tested when the business impact justifies it.

---

### 70 — AI-LLM-070 — LLM Model Deprecation

The organization should monitor provider announcements regarding:

* model retirement;
* version sunset;
* API retirement;
* feature removal;
* service discontinuation; and
* migration requirements.

---

### 71 — AI-LLM-071 — LLM Model Lifecycle

The organization should track the lifecycle of material models:

**Selection → Validation → Production → Update → Monitoring → Deprecation → Replacement**

Each stage should have appropriate governance.

---

### 72 — AI-LLM-072 — LLM Provider Model Selection

Model selection should consider:

* business requirements;
* data sensitivity;
* security;
* performance;
* safety;
* regulatory exposure;
* cost;
* availability;
* provider assurance; and
* exit feasibility.

---

### 73 — AI-LLM-073 — LLM Model Approval

Material models should receive appropriate approval before production use.

Approval should identify:

* model;
* provider;
* use case;
* data;
* environment;
* risk;
* controls;
* owner; and
* approval authority.

---

### 74 — AI-LLM-074 — LLM Model Allowlist

Organizations may maintain an approved-model allowlist.

The allowlist may identify:

* provider;
* model;
* version;
* approved use case;
* data classification;
* environment;
* restrictions; and
* approval status.

---

### 75 — AI-LLM-075 — LLM Model Denylist

Where required, organizations may maintain a denylist for models that are:

* unauthorized;
* unsupported;
* materially vulnerable;
* prohibited by policy;
* unsuitable for sensitive data; or
* otherwise outside approved use.

---

### 76 — AI-LLM-076 — LLM Model Access Segmentation

Different models may require different access controls based on:

* capability;
* data sensitivity;
* cost;
* business criticality;
* autonomy;
* security; and
* regulatory exposure.

---

### 77 — AI-LLM-077 — LLM Provider Service-Tier Risk

Different provider service tiers may offer materially different:

* retention;
* security;
* logging;
* support;
* model access;
* privacy;
* contractual;
* regional; and
* administrative capabilities.

The actual purchased tier should therefore be assessed.

---

### 78 — AI-LLM-078 — LLM Consumer Versus Enterprise Service

Consumer-oriented LLM services should not automatically be treated as equivalent to enterprise offerings.

The organization should determine whether the service provides appropriate:

* contractual protection;
* data governance;
* administrative control;
* identity integration;
* logging;
* security;
* privacy; and
* support.

---

### 79 — AI-LLM-079 — LLM Enterprise Configuration Governance

Enterprise LLM configurations should be documented and governed.

Configuration may include:

* data-use settings;
* retention;
* model selection;
* user access;
* administrative roles;
* integrations;
* logging;
* regional settings; and
* security controls.

---

### 80 — AI-LLM-080 — LLM Configuration Drift

The organization should monitor material LLM configuration for unauthorized or unexpected changes.

Drift may affect:

* data retention;
* model training;
* user access;
* model selection;
* logging;
* region;
* API permissions; and
* integrations.

---

### 81 — AI-LLM-081 — LLM Provider Configuration Evidence

Material configuration settings should be retained as evidence sufficient to support:

* audits;
* investigations;
* compliance;
* troubleshooting;
* incident response; and
* change reconstruction.

---

### 82 — AI-LLM-082 — LLM Provider Data Residency

Where geographic data-processing requirements apply, the organization should establish where relevant LLM data is:

* transmitted;
* processed;
* stored;
* backed up;
* accessed by support personnel; and
* processed by subprocessors.

---

### 83 — AI-LLM-083 — LLM Provider Geographic Restrictions

Where organizational or legal requirements restrict processing to specific jurisdictions, the LLM service should support enforceable geographic controls where required.

Geographic claims should be validated against the actual service architecture.

---

### 84 — AI-LLM-084 — LLM Cross-Border Processing

Cross-border processing should be evaluated according to applicable:

* privacy requirements;
* data-transfer mechanisms;
* contractual requirements;
* data-sovereignty requirements;
* sector requirements; and
* organizational policy.

---

### 85 — AI-LLM-085 — LLM Subprocessor Risk

LLM providers may rely on subprocessors for:

* cloud infrastructure;
* content moderation;
* security;
* support;
* storage;
* analytics;
* model hosting; or
* other services.

Material subprocessors should therefore be incorporated into the provider-risk assessment.

---

### 86 — AI-LLM-086 — LLM Fourth-Party Risk

The organization should identify material dependencies beyond the direct LLM provider where they can affect:

* confidentiality;
* integrity;
* availability;
* privacy;
* model security;
* regulatory exposure; or
* resilience.

---

### 87 — AI-LLM-087 — LLM Supply-Chain Risk

The LLM supply chain may include:

**Training Data → Training Infrastructure → Model Weights → Fine-Tuning → Model Serving → API → Application**

A compromise at any relevant stage may affect the final AI service.

---

### 88 — AI-LLM-088 — LLM Model Provenance

Where material, the organization should seek sufficient information concerning:

* model origin;
* provider;
* version;
* training or adaptation;
* distribution;
* model artifacts;
* dependencies; and
* integrity mechanisms.

---

### 89 — AI-LLM-089 — LLM Model Integrity

Where model integrity is material, controls should protect against:

* unauthorized modification;
* malicious replacement;
* tampering;
* compromised distribution;
* unauthorized fine-tuning; and
* model artifact substitution.

---

### 90 — AI-LLM-090 — LLM Model Artifact Verification

Where model artifacts are provided directly to the organization, appropriate integrity mechanisms may include:

* cryptographic hashes;
* digital signatures;
* trusted repositories;
* controlled distribution;
* version identifiers; and
* verification procedures.

---

### 91 — AI-LLM-091 — LLM Provider Secure Development

Where the provider develops supporting software, the organization should assess relevant secure-development practices.

Assessment may include:

* code review;
* vulnerability scanning;
* dependency management;
* secrets management;
* security testing;
* release controls; and
* remediation.

---

### 92 — AI-LLM-092 — LLM Provider Vulnerability Management

The organization should assess whether the provider has processes for:

* vulnerability discovery;
* prioritization;
* remediation;
* patching;
* verification;
* disclosure; and
* customer notification.

---

### 93 — AI-LLM-093 — LLM Provider Penetration Testing Evidence

Where available and appropriate, the organization may assess provider penetration-testing evidence.

The evidence should be evaluated for:

* scope;
* date;
* methodology;
* systems covered;
* findings;
* remediation; and
* independence.

---

### 94 — AI-LLM-094 — LLM Provider Security Assurance

Relevant assurance may include:

* SOC reports;
* ISO certifications;
* independent assessments;
* penetration testing;
* security attestations; and
* other recognized assurance mechanisms.

The organization should assess the scope and limitations of each source.

---

### 95 — AI-LLM-095 — LLM Provider Assurance Scope

Provider assurance should be checked against:

* actual service;
* model;
* region;
* service tier;
* infrastructure;
* subprocessors;
* customer configuration; and
* applicable requirements.

A corporate certification should not automatically be treated as comprehensive assurance over every LLM service.

---

### 96 — AI-LLM-096 — LLM Provider Assurance Gap

Where assurance evidence does not cover material requirements, the organization should identify the gap and determine appropriate treatment.

Possible treatments include:

* additional evidence;
* contractual control;
* technical control;
* compensating control;
* restricted use; or
* risk acceptance.

---

### 97 — AI-LLM-097 — LLM Provider Questionnaire

An LLM-specific provider questionnaire should address at least:

* data processing;
* training use;
* retention;
* security;
* model governance;
* model changes;
* incidents;
* subprocessors;
* geographic processing;
* resilience;
* regulatory exposure;
* IP;
* supply chain; and
* exit.

---

### 98 — AI-LLM-098 — LLM Provider Evidence Validation

Provider questionnaire responses should be validated against objective evidence where risk warrants it.

Evidence may include:

* contracts;
* technical documentation;
* configuration;
* independent assurance;
* testing;
* architecture;
* security reports; and
* formal provider policies.

---

### 99 — AI-LLM-099 — LLM Provider Marketing Claim Validation

Claims such as:

* “secure”;
* “private”;
* “enterprise-grade”;
* “compliant”;
* “zero retention”; or
* “safe”

should be translated into specific, testable requirements.

Marketing language should not be treated as independent assurance.

---

### 100 — AI-LLM-100 — LLM Provider Contractual Security Requirements

Material LLM provider agreements should establish appropriate requirements covering:

* security;
* data use;
* retention;
* deletion;
* incident notification;
* subprocessors;
* confidentiality;
* audit;
* resilience;
* model changes; and
* termination.

---

### 101 — AI-LLM-101 — LLM Data-Use Contract

Contracts should clearly establish permitted and prohibited uses of organizational data.

Where relevant, provisions should distinguish:

* inference;
* service improvement;
* training;
* fine-tuning;
* abuse monitoring;
* analytics;
* human review; and
* other secondary uses.

---

### 102 — AI-LLM-102 — LLM Model-Training Contract

Where organizational data may not be used for model training or improvement, the restriction should be explicitly reflected in the applicable contractual and service configuration where feasible.

---

### 103 — AI-LLM-103 — LLM Retention Contract

Material agreements should define applicable retention requirements for:

* prompts;
* files;
* outputs;
* logs;
* backups;
* metadata;
* derived data; and
* other relevant information.

---

### 104 — AI-LLM-104 — LLM Deletion Contract

Where deletion is required, contractual provisions should establish:

* deletion scope;
* deletion timing;
* backups;
* subprocessors;
* derived information;
* certification where appropriate; and
* legal exceptions.

---

### 105 — AI-LLM-105 — LLM Incident Contract

Material contracts should establish provider responsibilities for security and privacy incidents.

The agreement should define, where appropriate:

* notification;
* cooperation;
* evidence;
* investigation;
* remediation;
* communications; and
* regulatory support.

---

### 106 — AI-LLM-106 — LLM Model Change Contract

Where material model changes could affect the organization's risk, contractual terms should address appropriate notification and change-management mechanisms.

---

### 107 — AI-LLM-107 — LLM Audit Rights

Material provider relationships should establish appropriate assurance or audit mechanisms.

Possible mechanisms include:

* assurance reports;
* questionnaires;
* independent audits;
* documentation;
* technical assessments; and
* contractual audit rights.

---

### 108 — AI-LLM-108 — LLM Regulatory Cooperation

Where appropriate, contracts should require reasonable provider cooperation in supporting the organization's applicable regulatory and compliance obligations.

The organization remains responsible for determining its own legal obligations.

---

### 109 — AI-LLM-109 — LLM SLA Requirements

SLAs for critical LLM services should consider:

* availability;
* latency;
* support;
* incident response;
* restoration;
* maintenance;
* API performance; and
* service discontinuation.

---

### 110 — AI-LLM-110 — LLM Exit Requirements

Critical LLM dependencies should have appropriate exit provisions covering:

* data export;
* configuration;
* model migration;
* application migration;
* termination;
* deletion;
* transition support; and
* replacement.

---

### 111 — AI-LLM-111 — LLM Portability Assessment

Portability should be evaluated for:

* prompts;
* data;
* embeddings;
* configurations;
* workflows;
* fine-tuning artifacts;
* model integrations; and
* application logic.

Portability should be assessed technically and contractually.

---

### 112 — AI-LLM-112 — LLM Lock-In Risk

LLM provider lock-in may arise from:

* proprietary APIs;
* proprietary formats;
* provider-specific tools;
* provider-specific embeddings;
* proprietary fine-tuning;
* unique model capabilities;
* accumulated prompts;
* orchestration dependencies; and
* contractual restrictions.

---

### 113 — AI-LLM-113 — LLM Exit Feasibility

Exit feasibility should consider:

* alternate providers;
* alternate models;
* application compatibility;
* migration effort;
* testing;
* data portability;
* cost;
* staffing; and
* regulatory requirements.

---

### 114 — AI-LLM-114 — LLM Alternative Provider Assessment

For critical use cases, organizations should identify feasible alternatives where practical.

Assessment may compare:

* functionality;
* security;
* privacy;
* performance;
* availability;
* regulatory requirements;
* cost;
* interoperability; and
* migration complexity.

---

### 115 — AI-LLM-115 — LLM Fallback Strategy

Critical applications should define an appropriate fallback strategy.

Potential approaches include:

* alternate LLM;
* alternate provider;
* smaller local model;
* deterministic logic;
* manual process;
* cached information; or
* temporary service suspension.

---

### 116 — AI-LLM-116 — LLM Fallback Testing

Fallback mechanisms should be tested where failure of the primary provider would materially affect business operations.

Testing should validate:

* availability;
* functionality;
* security;
* data handling;
* performance;
* model behavior; and
* operational readiness.

---

### 117 — AI-LLM-117 — LLM Provider Renewal Assessment

Before renewing a material LLM provider relationship, the organization should review:

* current risk;
* incidents;
* model changes;
* assurance;
* contract changes;
* regulatory developments;
* performance;
* concentration; and
* exit feasibility.

---

### 118 — AI-LLM-118 — LLM Provider Governance Metrics

Metrics may include:

* approved LLM providers;
* critical LLM dependencies;
* current assessments;
* assurance coverage;
* unresolved findings;
* incidents;
* model changes;
* concentration;
* renewal status; and
* exit readiness.

---

### 119 — AI-LLM-119 — LLM Provider Key Risk Indicators

Potential KRIs include:

* repeated provider incidents;
* material service outages;
* expired assurance;
* significant model changes;
* increasing concentration;
* unresolved critical vulnerabilities;
* regulatory events;
* deteriorating service performance; and
* declining portability.

---

### 120 — AI-LLM-120 — LLM Provider Risk Reassessment Trigger

The organization should define events that trigger LLM provider reassessment, such as:

* material security incident;
* privacy incident;
* material model change;
* new subprocessor;
* significant service change;
* regulatory development;
* ownership change;
* data-sensitivity expansion;
* increased business criticality; or
* significant deterioration in exit feasibility.

These are governance triggers and are not automatically legal requirements.

---

### 121 — AI-LLM-121 — LLM Provider Governance Principle

An external LLM should be governed as a material technology and supply-chain dependency rather than merely as an API endpoint.

The assessment should encompass:

**Provider → Model → Data → API → Infrastructure → Subprocessors → Application → Business Process → Regulatory Context**

---

### 122 — AI-LLM-122 — LLM Provider Risk Decision Principle

The decision to use an external LLM should consider:

* business necessity;
* use-case risk;
* data sensitivity;
* provider assurance;
* model security;
* contractual protection;
* resilience;
* regulatory requirements;
* concentration; and
* exit feasibility.

---

### 123 — AI-LLM-123 — LLM Provider Evidence Principle

The organization should distinguish:

**Provider Claim → Provider Documentation → Contractual Commitment → Technical Evidence → Independent Assurance**

These evidence types provide different levels and forms of assurance and should not be treated as interchangeable.

---

### 124 — AI-LLM-124 — LLM Provider Responsibility Principle

The provider remains responsible for controls within its contractual and technical scope, while the organization remains responsible for managing the risks arising from its own use of the provider.

Outsourcing the model does not automatically outsource organizational accountability.

---

### 125 — AI-LLM-125 — LLM Provider Governance Principle

LLM provider risk management should integrate:

**Due Diligence → Contracting → Technical Controls → Model Governance → Monitoring → Incident Response → Reassessment → Resilience → Exit**

The internal `AI-LLM-*` identifiers in this knowledge base are taxonomy identifiers for governance purposes and are not official legal or regulatory control references.


