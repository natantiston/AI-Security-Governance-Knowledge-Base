# 12.16 — Infrastructure-as-Code Security for AI

## Part 1 — Infrastructure-as-Code Security for AI Foundations

### 1. Definition of Infrastructure-as-Code Security for AI

Infrastructure-as-Code Security for AI is the discipline of securing the declarative and programmatic definitions used to create, configure, modify, and manage infrastructure supporting AI systems.

IaC may define:

* Cloud infrastructure
* Networks
* Kubernetes clusters
* Compute resources
* GPU infrastructure
* Storage
* Databases
* Identity and access
* Security controls
* AI development environments
* Training infrastructure
* Model-serving infrastructure

IaC security ensures that infrastructure cannot become an uncontrolled pathway for introducing security weaknesses into AI environments.

---

### 2. Purpose of Infrastructure-as-Code Security for AI

The purpose is to ensure that infrastructure supporting AI systems is:

* Securely designed
* Consistently configured
* Authorized
* Reproducible
* Version controlled
* Tested
* Traceable
* Continuously monitored
* Protected against unauthorized changes

The fundamental lifecycle is:

```text id="iac2a"
Infrastructure Requirement
          |
          v
Architecture
          |
          v
IaC Development
          |
          v
Security Validation
          |
          v
Plan / Review
          |
          v
Controlled Deployment
          |
          v
Runtime
          |
          v
Drift Detection
          |
          v
Continuous Assurance
```

---

### 3. AI IaC Security vs Traditional IaC Security

Traditional IaC security focuses primarily on infrastructure configuration.

AI IaC security must additionally account for AI-specific infrastructure requirements, including:

* GPU resources
* Model-serving infrastructure
* Training clusters
* AI data stores
* Model registries
* Vector databases
* AI APIs
* High-performance networking
* Large-scale compute
* Sensitive model and dataset storage

An insecure infrastructure definition can therefore compromise not only infrastructure but also AI models, datasets, pipelines, and workloads.

---

### 4. Infrastructure-as-Code as an AI Security Boundary

IaC represents a powerful security boundary because it can define the permissions, connectivity, storage, compute, and services available to AI workloads.

A malicious or incorrect IaC change could:

* Expose a database
* Open a network path
* Grant excessive privileges
* Disable logging
* Expose model storage
* Expose training data
* Create insecure compute resources

Therefore, IaC must be treated as security-sensitive code.

---

### 5. AI Infrastructure Lifecycle

The AI infrastructure lifecycle begins before infrastructure deployment.

```text id="iac5b"
Business Requirement
       |
       v
AI Architecture
       |
       v
Infrastructure Design
       |
       v
IaC Development
       |
       v
Security Testing
       |
       v
Plan Validation
       |
       v
Approval
       |
       v
Deployment
       |
       v
Monitoring
       |
       v
Change / Retirement
```

Security must apply across every stage.

---

### 6. Infrastructure Definition Security

Infrastructure definitions must be protected like application source code.

Examples include:

* Terraform
* Kubernetes manifests
* Helm charts
* CloudFormation
* ARM/Bicep
* Pulumi
* Ansible
* Other infrastructure automation definitions

Unauthorized changes to these definitions can directly modify the AI security posture.

---

### 7. IaC Source Code Security

IaC source repositories must use appropriate security controls.

Controls should include:

* Access control
* Branch protection
* Code review
* Change tracking
* Secret scanning
* Security analysis

---

### 8. IaC Version Control

Infrastructure definitions must be version controlled.

Version control provides:

* Change history
* Attribution
* Rollback
* Review
* Reproducibility
* Auditability

Infrastructure deployed to production should correspond to an identifiable source revision.

---

### 9. IaC Change Traceability

Every material infrastructure change should be traceable to:

* Request
* Source change
* Author
* Reviewer
* Security validation
* Deployment
* Resulting infrastructure state

```text id="iac9c"
Change Request
      |
      v
IaC Commit
      |
      v
Security Review
      |
      v
Deployment
      |
      v
Infrastructure State
```

---

### 10. Secure-by-Design Infrastructure

AI infrastructure should be designed securely before IaC implementation.

Security architecture should define:

* Trust boundaries
* Network segmentation
* Identity
* Data protection
* Compute isolation
* Secrets
* Logging
* Monitoring
* Recovery

IaC should implement the approved architecture rather than define security implicitly.

---

### 11. Infrastructure Security Baseline

A standardized security baseline should define minimum security requirements for AI infrastructure.

The baseline may include:

* Encryption
* Identity controls
* Network segmentation
* Logging
* Monitoring
* Secure storage
* Least privilege
* Backup
* Vulnerability management

---

### 12. AI Infrastructure Classification

Infrastructure should be classified according to the AI capability it supports.

Examples include:

* AI development
* AI testing
* Model training
* Model evaluation
* Model registry
* Model serving
* AI data processing
* AI production

Classification determines the applicable security requirements.

---

### 13. AI Infrastructure Risk Classification

Infrastructure risk should consider:

* Data sensitivity
* Model sensitivity
* Internet exposure
* Privilege
* Business criticality
* Computational power
* Lateral movement potential

```text id="iac13d"
Infrastructure
      |
      +--> Data
      +--> Model
      +--> Identity
      +--> Network
      +--> Compute
      |
      v
Security Risk
```

---

### 14. Infrastructure Identity Security

Infrastructure resources must have controlled identities.

Examples include:

* Virtual machines
* Kubernetes workloads
* Cloud services
* Automation pipelines
* Serverless functions
* AI services

Each identity should receive only the permissions required.

---

### 15. Infrastructure Authentication

Infrastructure management interfaces must require strong authentication.

Authentication should apply to:

* Cloud platforms
* Kubernetes
* IaC repositories
* Deployment systems
* Infrastructure APIs

---

### 16. Infrastructure Authorization

Infrastructure actions must be explicitly authorized.

Authorization should control the ability to:

* Create resources
* Modify resources
* Delete resources
* Change networking
* Change security controls
* Access infrastructure secrets

---

### 17. Least Privilege for IaC

IaC execution identities should receive only the permissions necessary to perform their authorized deployments.

A pipeline deploying an AI inference service should not automatically have unrestricted control over the entire cloud account.

---

### 18. Separation of Duties in IaC

Infrastructure creation, security approval, and production authorization should be separated where risk warrants.

```text id="iac18e"
IaC Developer
     |
     v
Security Validation
     |
     v
Deployment Authority
     |
     v
Production Infrastructure
```

The system creating the infrastructure should not automatically be the sole authority deciding that the infrastructure is secure.

---

### 19. AI Network Infrastructure Security

IaC must securely define the networks supporting AI workloads.

This includes:

* Virtual networks
* Subnets
* Security groups
* Firewalls
* Network policies
* Routing
* Private endpoints

---

### 20. AI Network Segmentation

AI infrastructure should separate major trust zones.

```text id="iac20f"
                 AI Environment
                       |
        +--------------+--------------+
        |              |              |
        v              v              v
   Development      Training       Production
        |              |              |
        X--------------X--------------X
              Controlled Flows
```

Connectivity between zones should be explicitly authorized.

---

### 21. AI Data Infrastructure Security

Infrastructure definitions must protect systems storing AI datasets.

Controls should address:

* Storage access
* Encryption
* Network exposure
* Backup
* Retention
* Identity
* Logging

---

### 22. Model Storage Infrastructure Security

Model repositories and storage infrastructure should receive dedicated security requirements.

IaC should enforce:

* Access control
* Encryption
* Network restrictions
* Logging
* Backup
* Immutability where appropriate

---

### 23. Model Registry Infrastructure Security

Infrastructure supporting model registries must be securely defined.

Security controls should protect:

* Model artifacts
* Registry APIs
* Metadata
* Credentials
* Administrative interfaces

---

### 24. AI Compute Infrastructure Security

Compute resources supporting AI workloads should be securely configured.

Controls should include:

* Hardened operating systems
* Identity
* Network segmentation
* Resource permissions
* Logging
* Vulnerability management

---

### 25. GPU Infrastructure Security

GPU infrastructure introduces additional security considerations.

IaC should control:

* GPU allocation
* Device access
* Driver configuration
* Accelerator libraries
* Node placement
* Workload isolation

---

### 26. GPU Resource Isolation

Where GPUs are shared across AI workloads, infrastructure architecture should prevent unauthorized cross-workload access.

GPU resources should be assigned according to workload identity and authorization.

---

### 27. Infrastructure Configuration Security

IaC configuration must be validated against approved security standards.

Potential security-sensitive configuration includes:

* Public exposure
* Encryption
* IAM permissions
* Firewall rules
* Storage permissions
* Logging
* Network routes

---

### 28. IaC Secrets Security

Secrets must not be embedded directly in IaC source code.

Examples include:

* Cloud access keys
* Database credentials
* API keys
* Private keys
* Tokens

Secrets should be retrieved from approved secrets-management systems.

---

### 29. IaC Parameter Security

Infrastructure parameters must be validated to prevent unsafe configurations.

Parameters controlling:

* Network access
* Resource privileges
* Public exposure
* Encryption
* Identity

should receive appropriate validation.

---

### 30. Infrastructure Dependency Security

IaC often depends on:

* Modules
* Providers
* Plugins
* Packages
* Templates
* External components

These dependencies must be treated as supply-chain components.

---

### 31. Trusted IaC Modules

Organizations should establish trusted modules for commonly deployed AI infrastructure.

Trusted modules should have:

* Defined ownership
* Security baselines
* Version control
* Security testing
* Controlled updates

---

### 32. IaC Module Versioning

Infrastructure modules must use controlled versions.

Uncontrolled references to changing modules can introduce unexpected infrastructure changes.

---

### 33. IaC Provider Security

Infrastructure providers and plugins must be controlled and monitored.

Provider security should include:

* Approved sources
* Version management
* Vulnerability monitoring
* Integrity verification
* Change management

---

### 34. IaC Static Security Analysis

IaC definitions should undergo automated security analysis before deployment.

Analysis should identify:

* Excessive privileges
* Public exposure
* Missing encryption
* Weak network controls
* Insecure storage
* Misconfigured security groups

---

### 35. IaC Policy Validation

Infrastructure definitions should be evaluated against machine-enforceable security policies.

```text id="iac35e"
IaC
 |
 v
Policy Engine
 |
 +--> Identity Policy
 +--> Network Policy
 +--> Data Policy
 +--> Encryption Policy
 +--> Logging Policy
 |
 v
Security Decision
```

---

### 36. IaC Threat Modeling

High-risk AI infrastructure should undergo threat modeling before implementation.

Threat modeling should consider:

* Infrastructure compromise
* Privilege escalation
* Network exposure
* Data access
* Container escape
* Credential theft
* Lateral movement

---

### 37. IaC Plan Security

Infrastructure plans should be reviewed before changes are applied.

The plan should identify:

* Resources to create
* Resources to modify
* Resources to delete
* Permission changes
* Network changes
* Security-control changes

---

### 38. IaC Change Impact Analysis

Material infrastructure changes should receive security impact analysis.

Changes to high-risk components should trigger stronger validation.

Examples include changes to:

* IAM
* Firewalls
* Storage
* Kubernetes
* Model-serving infrastructure
* Data access

---

### 39. IaC Security Testing

IaC should undergo security testing before production deployment.

Testing may include:

* Static analysis
* Policy validation
* Configuration testing
* Architecture validation
* Negative testing
* Compliance checks

---

### 40. Infrastructure Security Gates

Security gates should prevent non-compliant infrastructure from being deployed.

```text id="iac40f"
IaC Change
    |
    v
Security Tests
    |
    v
Policy Evaluation
    |
   / \
 Pass Fail
  |     |
  v     v
Deploy  Block
```

---

### 41. Risk-Based IaC Security

Not all infrastructure changes require identical controls.

Higher-risk changes should receive stronger:

* Testing
* Review
* Approval
* Monitoring
* Rollback requirements

---

### 42. Fail-Closed IaC Security

Mandatory security validation should fail closed.

If a required security check cannot establish compliance, deployment should not automatically proceed.

---

### 43. Infrastructure Deployment Authorization

Production infrastructure deployment must require appropriate authorization.

Authorization should consider:

* Change identity
* Risk
* Security validation
* Environment
* Approval status

---

### 44. Infrastructure State Security

Actual infrastructure state should be monitored against the approved IaC state.

```text id="iac44g"
Approved IaC State
        |
        v
Actual Infrastructure
        |
        v
State Comparison
      /     \
   Match    Drift
     |        |
     v        v
 Continue   Investigate
```

---

### 45. Infrastructure Drift Detection

Infrastructure drift occurs when deployed resources differ from the approved IaC definition.

Drift can result from:

* Manual changes
* Unauthorized changes
* Compromise
* Emergency modifications
* Configuration errors

---

### 46. Infrastructure Change Monitoring

Changes to AI infrastructure should be continuously monitored.

Monitoring should identify:

* Resource creation
* Resource deletion
* Permission changes
* Network changes
* Security-control changes
* Configuration changes

---

### 47. Infrastructure Recovery

IaC should support rapid recovery from infrastructure failure or compromise.

Infrastructure definitions should allow authorized teams to reconstruct known-good environments.

---

### 48. Infrastructure Rollback

IaC changes should support controlled rollback where technically feasible.

Rollback should restore a known-good configuration rather than simply reverse the most recent command.

---

### 49. Continuous IaC Security Assurance

Infrastructure security must continue after deployment.

The lifecycle should be:

```text id="iac49h"
IaC
 |
 v
Validate
 |
 v
Approve
 |
 v
Deploy
 |
 v
Monitor
 |
 v
Detect Drift
 |
 +---- No Drift ---> Continue
 |
 +---- Drift ------> Reassess
```

Infrastructure security therefore becomes a continuous assurance process rather than a one-time configuration review.

---

### 50. Core Principle of Infrastructure-as-Code Security for AI

**Infrastructure-as-Code for AI must be treated as security-sensitive code that defines the identities, networks, compute, storage, permissions, and trust boundaries on which AI systems depend. Every infrastructure definition must therefore be version-controlled, security-tested, risk-assessed, independently validated where required, traceable to deployed state, and continuously monitored for drift so that an unauthorized or compromised infrastructure change cannot silently establish new pathways to AI models, datasets, workloads, credentials, or enterprise systems.**

## Part 2 — Infrastructure-as-Code Security for AI Controls

### 51. AI Infrastructure-as-Code Security Control Framework

The AI Infrastructure-as-Code Security Control Framework establishes technical controls for securing infrastructure definitions, modules, providers, deployment pipelines, infrastructure state, and deployed AI environments.

The framework should protect the complete IaC trust chain:

```text id="iac51a"
IaC Source
    |
    v
Module / Provider
    |
    v
Security Validation
    |
    v
Plan
    |
    v
Approval
    |
    v
Deployment
    |
    v
Infrastructure State
    |
    v
Drift Detection
    |
    v
Continuous Assurance
```

---

### 52. IaC Inventory Control

The enterprise must maintain an inventory of infrastructure-as-code assets supporting AI systems.

The inventory should identify:

* IaC repository
* IaC technology
* Modules
* Providers
* Environment
* AI system
* Owner
* Deployment pipeline
* Current version
* Security status

---

### 53. IaC Source Repository Control

Infrastructure definitions must be stored in approved source repositories.

Repository controls should include:

* Authentication
* Authorization
* Branch protection
* Change tracking
* Code review
* Audit logging

---

### 54. IaC Version Control

All production infrastructure definitions must be version controlled.

Each deployed infrastructure state should correspond to an identifiable source revision.

---

### 55. IaC Branch Protection Control

Production IaC branches must be protected from unauthorized direct modification.

Controls should require appropriate:

* Pull requests
* Reviews
* Status checks
* Security validation

---

### 56. IaC Code Review Control

Material infrastructure changes must undergo appropriate peer or security review.

Higher-risk changes should require additional independent review.

---

### 57. IaC Security Scanning Control

IaC definitions must undergo automated security scanning.

Scanning should identify:

* Public resources
* Excessive permissions
* Missing encryption
* Insecure network rules
* Weak authentication
* Exposed storage

---

### 58. IaC Policy-as-Code Control

Infrastructure policies should be automatically evaluated before deployment.

Policies may enforce:

* Approved regions
* Encryption
* Network restrictions
* Identity requirements
* Resource tagging
* Logging
* Backup

---

### 59. IaC Secrets Scanning Control

IaC repositories must be scanned for exposed credentials and secrets.

Detected secrets should prevent unauthorized promotion according to security policy.

---

### 60. IaC Dependency Inventory Control

Infrastructure modules, providers, plugins, and external components must be inventoried.

Dependencies should have identifiable:

* Name
* Version
* Source
* Owner
* Security status

---

### 61. IaC Module Approval Control

Only approved infrastructure modules should be used in controlled AI environments.

Approved modules should have defined:

* Ownership
* Version
* Security baseline
* Source
* Validation status

---

### 62. IaC Module Version Pinning Control

Critical infrastructure modules should use controlled versions.

Unbounded module versions can introduce unexpected infrastructure changes.

---

### 63. IaC Provider Approval Control

Cloud providers, plugins, and infrastructure providers must be approved before use in production IaC.

---

### 64. IaC Provider Version Control

Infrastructure providers should use controlled versions.

Provider updates should undergo security and compatibility validation.

---

### 65. IaC Provider Integrity Control

Provider binaries and plugins should be obtained from trusted sources and verified for integrity where supported.

---

### 66. IaC Dependency Vulnerability Control

IaC modules and providers should be assessed for known security vulnerabilities.

Dependency vulnerabilities should be evaluated according to the AI infrastructure risk.

---

### 67. AI Infrastructure Configuration Baseline Control

AI infrastructure should comply with approved configuration baselines.

Baseline requirements may cover:

* Encryption
* IAM
* Network controls
* Logging
* Monitoring
* Backup
* Secure protocols

---

### 68. Cloud Resource Security Control

Cloud resources provisioned through IaC must comply with enterprise security requirements.

This includes:

* Compute
* Storage
* Databases
* Networking
* Load balancers
* AI services

---

### 69. AI Compute Security Control

AI compute infrastructure must be securely configured.

Controls should address:

* Instance identity
* Network placement
* Operating-system security
* Resource permissions
* Logging
* Vulnerability management

---

### 70. GPU Infrastructure Control

GPU infrastructure provisioned through IaC must be explicitly controlled.

Controls should validate:

* GPU type
* Allocation
* Device permissions
* Node configuration
* Driver requirements
* Workload isolation

---

### 71. AI Network Security Control

IaC must enforce secure network configurations for AI environments.

Controls should cover:

* Subnets
* Security groups
* Firewalls
* Routes
* Private endpoints
* Network policies

---

### 72. Network Exposure Control

IaC security scanning must detect unintended public exposure.

High-risk resources should not be publicly accessible unless explicitly authorized.

---

### 73. AI Network Segmentation Control

IaC must enforce segmentation between appropriate AI environments and workloads.

```text id="iac73a"
Development
     |
     X
Training
     |
     X
Production
```

Required communication paths should be explicitly defined.

---

### 74. AI Data Storage Security Control

Storage resources containing AI datasets must be securely provisioned.

Controls should include:

* Encryption
* Access control
* Network restrictions
* Logging
* Backup
* Retention

---

### 75. Model Storage Security Control

Infrastructure supporting model storage must enforce appropriate:

* Authentication
* Authorization
* Encryption
* Integrity
* Network security
* Audit logging

---

### 76. Model Registry Infrastructure Control

Infrastructure supporting model registries must be deployed using approved security configurations.

Registry infrastructure should be isolated from unnecessary network and administrative access.

---

### 77. AI Database Security Control

Databases supporting AI applications must be securely provisioned through IaC.

Controls should address:

* Authentication
* Encryption
* Network access
* Backup
* Logging
* Least privilege

---

### 78. Vector Database Security Control

Vector databases and retrieval infrastructure must receive dedicated security controls.

IaC should enforce:

* Network restrictions
* Authentication
* Authorization
* Encryption
* Backup
* Monitoring

---

### 79. AI API Infrastructure Control

Infrastructure supporting AI APIs must be configured securely.

Controls should address:

* TLS
* Authentication
* Authorization
* Rate limiting
* Network exposure
* Logging

---

### 80. IAM Configuration Control

IaC must securely define identities, roles, policies, and permissions supporting AI infrastructure.

Excessive privileges should be automatically detected.

---

### 81. Least-Privilege Infrastructure Control

Infrastructure resources should receive only the permissions required for their function.

```text id="iac81b"
AI Workload
     |
     v
Required Resource
     |
     v
Minimum Permission
     |
     X
Unnecessary Privilege
```

---

### 82. IAM Policy Validation Control

IAM policies defined through IaC must be analyzed for:

* Wildcards
* Excessive permissions
* Cross-account access
* Privilege escalation paths
* Sensitive resource access

---

### 83. Service Account Security Control

Service accounts supporting AI infrastructure must use controlled permissions.

Unused service accounts should be removed or disabled.

---

### 84. Infrastructure Authentication Control

Management interfaces and infrastructure services must use approved authentication mechanisms.

Weak or deprecated authentication methods should be blocked.

---

### 85. Infrastructure Encryption Control

IaC must enforce encryption for appropriate:

* Data storage
* Network communication
* Backups
* Model artifacts
* Secrets

---

### 86. Encryption Key Management Control

Encryption keys must be managed through approved key-management systems.

Keys should not be embedded directly into IaC definitions.

---

### 87. IaC Secrets Management Control

Sensitive values should be retrieved from approved secrets-management systems rather than stored directly in source code.

---

### 88. Secret Reference Validation Control

IaC should reference approved secret identifiers rather than containing secret values.

Automated validation should detect embedded credentials.

---

### 89. Infrastructure State Security Control

IaC state files can contain highly sensitive information and must be protected.

Controls should include:

* Encryption
* Access control
* Versioning
* Backup
* Integrity protection

---

### 90. Infrastructure State Access Control

Only authorized identities should access infrastructure state.

State access should follow least privilege.

---

### 91. Infrastructure State Integrity Control

Unauthorized modifications to infrastructure state must be detectable.

State integrity should be correlated with the approved IaC configuration.

---

### 92. IaC Plan Validation Control

Infrastructure plans must be generated and security-validated before deployment.

The plan should identify security-sensitive changes.

---

### 93. IaC Destructive Change Control

Changes that delete or replace critical AI infrastructure should require enhanced controls.

Examples include deletion of:

* Production model storage
* AI datasets
* Model registries
* Security logs
* Backup infrastructure

---

### 94. IaC Change Impact Control

Material IaC changes must receive security impact analysis.

Risk should consider the affected:

* AI systems
* Data
* Models
* Identity
* Networks
* Compute

---

### 95. IaC Security Gate Control

IaC security gates must prevent non-compliant infrastructure from reaching controlled environments.

```text id="iac95c"
IaC Change
    |
    v
Security Scanning
    |
    v
Policy Validation
    |
    v
Risk Evaluation
   /       \
 Pass      Fail
  |          |
  v          v
Deploy     Block
```

---

### 96. Risk-Adaptive IaC Gate Control

Security gate requirements should increase with infrastructure risk.

High-impact changes should receive stronger validation and approval.

---

### 97. Mandatory Gate Failure Control

Mandatory IaC security gates must fail closed.

If required security evidence is unavailable, deployment should not automatically proceed.

---

### 98. IaC Deployment Authorization Control

Production infrastructure changes must require authorized deployment identities.

The identity executing the deployment must be attributable.

---

### 99. Separation of Duties Control

IaC architecture should separate:

* Development
* Security validation
* Approval
* Production deployment

where required by risk.

---

### 100. IaC Deployment Environment Control

Development and production IaC environments must be appropriately separated.

Development credentials must not automatically authorize production deployment.

---

### 101. Infrastructure Provisioning Identity Control

Automated provisioning systems must use dedicated identities.

Shared administrative credentials should not be used for automated AI infrastructure deployment.

---

### 102. Short-Lived IaC Credential Control

Infrastructure deployment pipelines should use short-lived credentials where practical.

This reduces the impact of credential compromise.

---

### 103. Infrastructure Deployment Logging Control

Every IaC deployment should generate auditable records.

Records should identify:

* Actor
* Source revision
* Deployment identity
* Environment
* Change
* Timestamp
* Result

---

### 104. Infrastructure State Monitoring Control

Deployed infrastructure must be continuously monitored against approved IaC state.

---

### 105. Infrastructure Drift Detection Control

Drift detection must identify unauthorized differences between:

* Approved IaC
* Planned state
* Actual infrastructure

---

### 106. Drift Response Control

Detected security-relevant drift must trigger appropriate action.

Actions may include:

* Alert
* Investigation
* Automatic remediation
* Resource isolation
* Rollback

---

### 107. Unauthorized Manual Change Control

Manual production infrastructure changes should be restricted.

Where emergency manual changes are necessary, they must be documented and reconciled with IaC afterward.

---

### 108. Infrastructure Configuration Integrity Control

Critical infrastructure configurations must be protected against unauthorized modification.

This includes:

* Firewall rules
* IAM
* Storage
* Network routes
* Security policies
* Kubernetes configuration

---

### 109. AI Infrastructure Runtime Security Control

Infrastructure supporting AI workloads must be continuously assessed after deployment.

Runtime controls should monitor:

* Configuration
* Identity
* Network behavior
* Resource use
* Security events

---

### 110. Infrastructure Vulnerability Management Control

Provisioned AI infrastructure must be continuously assessed for vulnerabilities.

This should include:

* Operating systems
* Cloud services
* Kubernetes nodes
* Infrastructure components
* GPU drivers where applicable

---

### 111. Infrastructure Patch Control

Security patches must be applied according to risk-based remediation requirements.

Critical exposed infrastructure should receive accelerated treatment.

---

### 112. IaC Security Evidence Control

Security validation results must be retained as evidence.

Evidence may include:

* Scan results
* Policy evaluations
* IaC plans
* Approvals
* Deployment records
* Drift reports

---

### 113. Infrastructure Traceability Control

The enterprise must be able to trace infrastructure resources back to their IaC definitions.

```text id="iac113d"
Production Resource
       |
       v
Deployment
       |
       v
IaC Revision
       |
       v
Change Request
       |
       v
Author / Approver
```

---

### 114. IaC Compliance Control

IaC deployments must be continuously evaluated against enterprise security and regulatory requirements.

Non-compliant infrastructure should be identified and tracked.

---

### 115. Automated IaC Security Enforcement Control

Security controls should automatically prevent prohibited infrastructure configurations.

Examples include:

* Public storage
* Excessive IAM permissions
* Unencrypted data
* Unrestricted network access
* Unauthorized regions
* Unapproved resource types

---

### 116. IaC Remediation and Rollback Control

The enterprise must be capable of restoring infrastructure to an approved secure state.

Rollback or automated remediation should be risk-controlled to avoid causing additional operational impact.

---

### 117. Core Engineering Principle

**Infrastructure-as-Code security must transform infrastructure definitions into continuously validated security-controlled deployment artifacts. Every AI infrastructure change must be attributable, version-controlled, security-tested, risk-assessed, authorized, and traceable to the resulting infrastructure state, while unauthorized configuration drift must be detectable and capable of containment or remediation. The ability to provision infrastructure must never provide automatic authority to establish its security posture.**


