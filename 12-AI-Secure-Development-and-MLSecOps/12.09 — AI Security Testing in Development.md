# 12.09 — AI Security Testing in Development

## Part 1 — Foundations

### 1. Definition of AI Security Testing in Development

AI Security Testing in Development is the systematic process of identifying and validating security weaknesses in AI applications, models, datasets, dependencies, infrastructure, pipelines, and supporting components before they reach production.

It extends conventional software security testing by addressing AI-specific attack surfaces such as:

* Prompt injection
* Model manipulation
* Dataset poisoning
* Unsafe model loading
* AI supply-chain compromise
* Retrieval abuse
* Tool abuse
* Output manipulation
* Model extraction
* AI-specific authorization failures

The objective is to identify security weaknesses while they are still inexpensive and practical to remediate.

---

### 2. Purpose of AI Security Testing in Development

The primary purpose is to establish evidence that security requirements and threat mitigations actually work before deployment.

AI security testing should answer questions such as:

* Can an attacker bypass authentication?
* Can untrusted input manipulate trusted instructions?
* Can unauthorized data be retrieved?
* Can the model invoke a restricted tool?
* Can a malicious artifact enter the build?
* Can a compromised dependency reach production?
* Can sensitive information be extracted?
* Can security controls be bypassed through model behavior?

Testing transforms security assumptions into measurable evidence.

---

### 3. AI Security Testing vs Functional Testing

Functional testing determines whether the AI system performs its intended business functions.

Security testing determines whether those functions can be abused or compromised.

For example:

| Testing Type | Question                                                |
| ------------ | ------------------------------------------------------- |
| Functional   | Does the chatbot answer correctly?                      |
| Security     | Can a user manipulate it into exposing restricted data? |
| Functional   | Can the model call the payment API?                     |
| Security     | Can an unauthorized user cause a payment action?        |
| Functional   | Can the RAG system retrieve documents?                  |
| Security     | Can it retrieve documents belonging to another tenant?  |

An AI system can be functionally correct while remaining fundamentally insecure.

---

### 4. AI Security Testing as a Lifecycle Discipline

Security testing should occur continuously throughout development rather than only immediately before production.

```text id="ast4a"
Requirements
     |
     v
Design
     |
     v
Threat Modeling
     |
     v
Development
     |
     v
Build
     |
     v
Model Testing
     |
     v
Integration Testing
     |
     v
Security Gates
     |
     v
Deployment
```

Testing depth should increase according to system risk and attack surface.

---

### 5. Shift-Left AI Security Testing

Shift-left testing moves security validation toward the earliest practical development stages.

Developers should be able to detect weaknesses during:

* Coding
* Prompt development
* Dependency selection
* Dataset preparation
* Model integration
* Infrastructure development

Early detection reduces remediation cost and prevents vulnerable designs from becoming embedded in later stages.

---

### 6. Continuous AI Security Testing

AI systems require continuous security validation because their behavior and dependencies can change.

Changes may occur in:

* Models
* Prompts
* Datasets
* Dependencies
* APIs
* Tools
* Configurations
* Infrastructure

A previously secure test result should therefore not be treated as permanent evidence of security.

---

### 7. AI Security Testing Scope

The testing scope should encompass the complete AI system.

```text id="ast7b"
                 AI SYSTEM
                    |
       +------------+------------+
       |            |            |
       v            v            v
 Application      Model        Data
       |            |            |
       +------------+------------+
                    |
       +------------+------------+
       |            |            |
       v            v            v
     APIs         Tools      Retrieval
       |            |            |
       +------------+------------+
                    |
       +------------+------------+
       |            |            |
       v            v            v
   Dependencies   Pipeline   Infrastructure
```

Testing only the model is insufficient when vulnerabilities exist elsewhere in the architecture.

---

### 8. AI Security Test Objectives

Each security test should have a clearly defined objective.

Objectives may include:

* Prevent unauthorized access
* Protect sensitive data
* Preserve model integrity
* Prevent privilege escalation
* Prevent tool abuse
* Validate tenant isolation
* Detect malicious input
* Protect supply-chain integrity
* Verify security controls

The objective should be linked to a security requirement or threat wherever possible.

---

### 9. Threat-Driven Security Testing

Threat modeling should determine which security tests are required.

```text id="ast9c"
Threat Model
     |
     v
Threat Scenario
     |
     v
Security Requirement
     |
     v
Test Case
     |
     v
Test Result
     |
     v
Security Evidence
```

This ensures that testing addresses realistic attack scenarios rather than arbitrary test cases.

---

### 10. Risk-Based Testing

Testing depth should correspond to AI system risk.

A low-risk internal application may require standard security testing.

A high-risk AI system may require:

* Extensive adversarial testing
* Independent testing
* Continuous testing
* Stronger release gates
* Negative testing
* Abuse simulation
* Runtime validation

Risk should determine the intensity of testing rather than applying identical testing requirements to every system.

---

### 11. Security Test Categories

AI security testing should combine multiple categories.

| Category               | Primary Focus                  |
| ---------------------- | ------------------------------ |
| SAST                   | Source-code vulnerabilities    |
| SCA                    | Dependency vulnerabilities     |
| Secret Scanning        | Credential exposure            |
| DAST                   | Runtime application weaknesses |
| API Testing            | Interface security             |
| Model Testing          | Model-specific risks           |
| Dataset Testing        | Data integrity and poisoning   |
| Adversarial Testing    | AI manipulation                |
| Infrastructure Testing | Environment security           |
| Configuration Testing  | Security baseline              |
| Authorization Testing  | Access control                 |
| Supply-Chain Testing   | Component trust                |

No single testing method provides complete AI security assurance.

---

### 12. Static Application Security Testing for AI

SAST should be applied to AI application source code.

Testing should identify issues such as:

* Injection vulnerabilities
* Unsafe command execution
* Insecure file handling
* Weak authorization
* Hardcoded credentials
* Unsafe API usage
* Dangerous deserialization

AI-specific code paths should receive the same security scrutiny as conventional application code.

---

### 13. Software Composition Analysis

AI applications frequently depend on large software ecosystems.

SCA should identify:

* Known vulnerabilities
* Outdated dependencies
* Transitive dependencies
* Unapproved packages
* License concerns
* Dependency conflicts

Dependency findings should be evaluated according to actual exploitability and system exposure.

---

### 14. Secret Detection Testing

Security testing should identify credentials accidentally introduced into development artifacts.

Potential locations include:

* Source code
* Configuration files
* Notebooks
* Prompts
* Test data
* Scripts
* CI/CD configuration
* Container layers

Secrets discovered during development should be treated as potentially compromised and rotated where appropriate.

---

### 15. API Security Testing

AI APIs should be tested for:

* Authentication bypass
* Authorization bypass
* Injection
* Excessive data exposure
* Rate-limit weaknesses
* Improper error handling
* Object-level authorization failures

API testing should evaluate both conventional API security and AI-specific interaction patterns.

---

### 16. Authentication Testing

Authentication mechanisms should be tested to verify that unauthorized identities cannot access protected AI capabilities.

Testing should include:

* Invalid credentials
* Expired credentials
* Revoked credentials
* Token manipulation
* Session abuse
* Authentication bypass

Workload and service identities should also be tested where they represent significant attack paths.

---

### 17. Authorization Testing

Authorization testing should verify that authenticated users and workloads can perform only permitted actions.

Testing should cover:

* Horizontal privilege escalation
* Vertical privilege escalation
* Object-level authorization
* Function-level authorization
* Tool authorization
* Data authorization

```text id="ast17d"
Authenticated Identity
          |
          v
Requested Resource
          |
          v
Requested Action
          |
          v
Authorization Decision
          |
      +---+---+
      |       |
     Allow   Deny
```

---

### 18. Least-Privilege Testing

Security tests should verify that AI applications and workloads do not possess unnecessary privileges.

Testing should attempt to determine whether a compromised component can:

* Access unrelated data
* Invoke unnecessary APIs
* Execute privileged operations
* Access administrative functions
* Modify security configurations

The purpose is to validate the actual privilege boundary rather than merely confirm that a least-privilege policy exists.

---

### 19. Prompt Injection Testing

Prompt injection testing evaluates whether untrusted input can manipulate the AI system into violating intended security behavior.

Tests may attempt to:

* Override system instructions
* Reveal protected information
* Alter task objectives
* Bypass application restrictions
* Trigger unauthorized tools

Testing should include both direct and indirect injection scenarios.

---

### 20. Indirect Prompt Injection Testing

Indirect prompt injection occurs when malicious instructions originate from content the AI retrieves or processes rather than directly from the user.

Potential sources include:

* Web pages
* Documents
* Emails
* Knowledge bases
* Uploaded files
* Database records

Testing should determine whether untrusted content can influence trusted system behavior.

---

### 21. Instruction Hierarchy Testing

Security testing should verify that trusted instructions maintain appropriate precedence over untrusted content.

Tests should evaluate interactions between:

* System instructions
* Developer instructions
* Application policies
* User input
* Retrieved content
* Tool output

The objective is to determine whether untrusted content can improperly obtain higher authority.

---

### 22. Context Isolation Testing

AI systems should be tested for unintended context leakage.

Testing should verify separation between:

* Users
* Sessions
* Tenants
* Applications
* Conversations
* Retrieved data
* Persistent memory

A successful request by one identity should not provide access to another identity's context.

---

### 23. Output Validation Testing

Model outputs should be tested before they are trusted by downstream systems.

Testing should determine whether malicious or malformed model output can cause:

* Command execution
* SQL injection
* HTML injection
* Workflow manipulation
* Unauthorized API requests
* Security-policy bypass

The fundamental assumption should be:

**Model output is untrusted until validated.**

---

### 24. Structured Output Security Testing

When AI applications use structured outputs such as JSON or function-call arguments, testing should validate both syntax and security semantics.

Tests should include:

* Invalid types
* Unexpected fields
* Missing fields
* Boundary values
* Unauthorized values
* Malicious parameters

Schema validation alone does not guarantee that the requested action is authorized.

---

### 25. Tool Invocation Security Testing

AI applications capable of tool use should undergo dedicated security testing.

Testing should determine whether the model can cause:

* Unauthorized tool invocation
* Privileged actions
* Parameter manipulation
* Excessive resource use
* Destructive operations

The authorization layer should be tested independently from model behavior.

---

### 26. Tool Parameter Testing

Tool parameters should be tested against malicious and unexpected values.

Testing should include:

* Boundary values
* Invalid identifiers
* Unauthorized resources
* Unexpected commands
* Excessive quantities
* Cross-tenant references

The goal is to determine whether tool validation remains effective even when the model generates malicious parameters.

---

### 27. High-Risk Action Testing

High-impact actions require stronger negative testing.

Examples include:

* Financial transactions
* Data deletion
* Privileged changes
* Production deployments
* Account modification
* External communication

Testing should verify that required approvals and authorization controls cannot be bypassed through AI-generated requests.

---

### 28. RAG Security Testing

Retrieval-Augmented Generation systems should be tested for security weaknesses in the complete retrieval chain.

Testing should examine:

* Retrieval authorization
* Document access
* Tenant isolation
* Retrieval manipulation
* Malicious documents
* Data leakage
* Embedding behavior

```text id="ast28e"
User
 |
 v
Authorization
 |
 v
Retriever
 |
 v
Vector Store
 |
 v
Retrieved Content
 |
 v
Context
 |
 v
Model
 |
 v
Output
```

Each stage represents a potential attack surface.

---

### 29. Vector Store Security Testing

Vector stores should be tested for:

* Unauthorized retrieval
* Cross-tenant access
* Metadata leakage
* Index manipulation
* Embedding exposure
* Access-control bypass

Security testing should treat vector representations as potentially sensitive data.

---

### 30. AI Memory Security Testing

Persistent AI memory should be tested for:

* Unauthorized retrieval
* Memory poisoning
* Cross-user leakage
* Cross-tenant leakage
* Improper deletion
* Excessive retention

Testing should validate both read and write security.

---

### 31. Dataset Security Testing

Datasets should undergo security testing before being used for training or evaluation.

Testing may examine:

* Integrity
* Provenance
* Schema
* Malicious content
* Sensitive information
* Unexpected modifications
* Poisoning indicators

Dataset testing should be proportional to the model's risk and the dataset's importance.

---

### 32. Dataset Poisoning Testing

Security teams should test whether malicious or manipulated training data could influence model behavior.

Testing may evaluate:

* Malicious labels
* Trigger patterns
* Anomalous records
* Distribution manipulation
* Targeted data injection

High-impact models should receive more sophisticated poisoning analysis.

---

### 33. Model Integrity Testing

Model artifacts should be tested to verify that they correspond to approved versions.

Testing should validate:

* Hash
* Signature
* Version
* Provenance
* Registry status

Integrity failures should result in rejection or escalation according to risk.

---

### 34. Model Loading Security Testing

AI systems should be tested for unsafe model loading behavior.

Testing should examine whether untrusted model files can:

* Execute unintended code
* Exploit deserialization
* Load malicious components
* Access unauthorized resources

Model loading should occur within appropriately isolated environments.

---

### 35. Model Extraction Testing

Where model confidentiality is important, systems should be tested against model extraction attempts.

Testing may examine:

* Excessive querying
* Systematic probing
* API abuse
* Repeated inference requests
* Reconstruction patterns

Controls should balance security with legitimate application functionality.

---

### 36. Data Extraction Testing

AI systems should be tested for unintended disclosure of sensitive training or contextual information.

Testing may target:

* Memorized information
* System instructions
* Retrieved documents
* User data
* Credentials
* Sensitive prompts

Testing should focus on realistic information-exposure scenarios.

---

### 37. AI Abuse Testing

Security testing should include misuse scenarios beyond traditional vulnerability exploitation.

Examples include:

* Automated abuse
* Resource exhaustion
* Malicious content generation
* Repeated probing
* Tool misuse
* Policy circumvention

Abuse testing helps determine whether the AI system can be exploited without a conventional software vulnerability.

---

### 38. Model Behavior Security Testing

Security testing should examine whether model behavior creates security consequences.

Examples include:

* Incorrect authorization recommendations
* Unsafe instructions
* Security-control circumvention
* Manipulative responses
* Unsafe tool recommendations

Model behavior should be evaluated in relation to the application's actual authority boundaries.

---

### 39. Security Testing of External AI Providers

External AI services should be evaluated as security dependencies.

Testing and assessment should consider:

* API authentication
* Data transmission
* Provider isolation
* Response handling
* Failure behavior
* Data retention assumptions
* Security-control boundaries

The organization should not assume that a third-party model provider satisfies enterprise security requirements automatically.

---

### 40. AI Dependency Security Testing

Dependencies should be tested continuously for newly discovered vulnerabilities.

Testing should include:

* Direct dependencies
* Transitive dependencies
* AI frameworks
* Inference libraries
* Plugins
* Model-serving components

Critical dependency findings should influence release decisions.

---

### 41. Container Security Testing

Containerized AI applications should undergo:

* Image vulnerability scanning
* Configuration testing
* Privilege testing
* Runtime isolation testing
* Network exposure testing
* Secret detection

Testing should occur before deployment to trusted environments.

---

### 42. Infrastructure Security Testing

Infrastructure supporting AI workloads should be security-tested.

Testing may evaluate:

* IAM configuration
* Network segmentation
* Storage permissions
* Public exposure
* Encryption
* Administrative access
* Compute isolation

AI security testing should include the infrastructure on which AI capabilities depend.

---

### 43. Configuration Security Testing

AI configurations should be tested against approved security baselines.

Testing should identify:

* Excessive permissions
* Disabled controls
* Unsafe defaults
* Unapproved providers
* Unrestricted tools
* Insecure network settings

Configuration testing should occur automatically where practical.

---

### 44. Security Regression Testing

Previously discovered vulnerabilities should become regression tests when appropriate.

```text id="ast44f"
Security Finding
      |
      v
Remediation
      |
      v
Regression Test
      |
      v
CI/CD
      |
      v
Future Releases
```

This prevents previously fixed security weaknesses from returning during future development.

---

### 45. Negative Security Testing

AI security testing should deliberately attempt actions that the system is supposed to reject.

Examples include:

* Unauthorized data retrieval
* Unauthorized tool use
* Privilege escalation
* Prompt injection
* Malicious file uploads
* Invalid API requests

Security is demonstrated not only by successful legitimate behavior, but by reliable rejection of prohibited behavior.

---

### 46. Security Test Automation

Security testing should be automated wherever practical.

Automation can provide:

* Faster feedback
* Consistent execution
* Repeatability
* Continuous validation
* Automated security gates
* Evidence generation

Human review remains important for complex, high-risk, or context-dependent security scenarios.

---

### 47. Security Test Evidence

Every material security test should produce evidence sufficient to demonstrate what was tested and what happened.

Evidence should include:

* Test identity
* System/version
* Test scenario
* Expected result
* Actual result
* Timestamp
* Test environment
* Security status

Evidence should be protected from unauthorized modification.

---

### 48. Security Test Traceability

Security tests should connect directly to the security requirements and threats they validate.

```text id="ast48g"
Threat
   |
   v
Risk
   |
   v
Requirement
   |
   v
Control
   |
   v
Security Test
   |
   v
Evidence
```

This enables teams to determine whether important security requirements have actually been validated.

---

### 49. AI Security Testing Principles

Effective AI security testing should follow these principles:

1. **Test the complete AI system, not only the model.**
2. **Test both expected and prohibited behavior.**
3. **Use threat modeling to determine test priorities.**
4. **Treat model outputs as untrusted.**
5. **Test authorization independently from AI reasoning.**
6. **Test datasets, models, applications, dependencies, and infrastructure.**
7. **Automate repeatable security validation.**
8. **Preserve evidence for material tests.**
9. **Convert important findings into regression tests.**
10. **Repeat testing when material changes occur.**

---

### 50. Core Principle of AI Security Testing in Development

**AI security testing must provide continuous, risk-based, threat-driven evidence that the AI system, its models, data, dependencies, infrastructure, interfaces, and security controls resist realistic attack and misuse scenarios before and throughout the lifecycle.**

The fundamental testing relationship is:

```text id="ast50h"
Threat Model
      |
      v
Security Requirement
      |
      v
Test Objective
      |
      v
Attack / Negative Scenario
      |
      v
Test Execution
      |
      v
Expected vs Actual Result
      |
      v
Finding
      |
      v
Remediation
      |
      v
Regression Test
      |
      v
Security Gate
      |
      v
Evidence
      |
      v
Continuous Validation
```

**A security test is valuable only when it validates a meaningful security property and produces actionable evidence about whether that property remains effective.** For AI systems, this requires testing not only conventional software vulnerabilities but also the interactions among models, prompts, data, retrieval, tools, identities, dependencies, infrastructure, and human decisions.

## Part 2 — AI Security Testing Control Framework

### 51. AI Security Testing Control Framework

The AI Security Testing Control Framework establishes the controls required to systematically test AI applications, models, datasets, pipelines, dependencies, infrastructure, and security boundaries.

The framework should connect threats and requirements to repeatable security validation.

```text id="astc51a"
Threat Model
     |
     v
Security Requirement
     |
     v
Test Objective
     |
     v
Test Case
     |
     v
Execution
     |
     v
Result
     |
     v
Security Gate
     |
     v
Evidence
```

Controls should support preventive validation, detection of weaknesses, remediation, regression testing, and continuous assurance.

---

### 52. AI Security Test Planning Control

Every in-scope AI system should have a documented security testing plan proportionate to its risk.

The plan should define:

* Testing scope
* Security objectives
* Test categories
* Required tools
* Test frequency
* Test owners
* Acceptance criteria
* Evidence requirements
* Escalation requirements

Testing plans should be updated when the AI system materially changes.

---

### 53. Security Test Scope Control

The testing scope should cover the complete security-relevant architecture.

It should consider:

* Application
* Model
* Dataset
* Prompt
* Retrieval
* Memory
* Tools
* APIs
* Dependencies
* Containers
* Infrastructure
* CI/CD
* External providers

Testing only the model should not be considered sufficient for an integrated AI system.

---

### 54. Security Test Classification Control

Security tests should be categorized consistently.

Categories may include:

* Application security testing
* API security testing
* Model security testing
* Dataset security testing
* Adversarial testing
* Authorization testing
* Supply-chain testing
* Infrastructure testing
* Configuration testing
* Runtime testing

Classification enables consistent reporting and coverage analysis.

---

### 55. Security Test Requirement Control

Every material AI security requirement should have an associated validation mechanism.

The mapping should identify:

* Requirement
* Test
* Expected result
* Acceptance criteria
* Evidence
* Test owner

Critical requirements should not remain untested without formally accepted risk.

---

### 56. Threat-Based Test Selection Control

Threat-model findings should determine the security tests required for the system.

```text id="astc56b"
Threat
  |
  v
Attack Scenario
  |
  v
Security Property
  |
  v
Test Case
```

This prevents security testing from becoming a generic checklist disconnected from the actual threat environment.

---

### 57. Risk-Based Test Prioritization Control

Security testing should prioritize threats according to risk.

Factors should include:

* Impact
* Likelihood
* Exploitability
* Exposure
* Asset criticality
* Control effectiveness

High-risk attack paths should receive deeper and more frequent testing.

---

### 58. Security Test Environment Control

Security testing should occur in controlled environments that adequately represent the security properties of production.

The environment should provide:

* Appropriate isolation
* Controlled data
* Test identities
* Test credentials
* Representative configurations
* Safe test targets

Production testing should require explicit authorization and safeguards.

---

### 59. Test Data Security Control

Security test data should itself be protected.

Controls should address:

* Sensitive information
* Test credentials
* Customer data
* Synthetic data
* Data retention
* Test-data access

Production data should not be copied into testing environments without appropriate authorization and protection.

---

### 60. Test Identity Control

Security tests should use controlled identities representing realistic security roles.

Test identities should support scenarios such as:

* Unauthenticated user
* Standard user
* Privileged user
* Cross-tenant user
* Service account
* Restricted workload

This allows authorization boundaries to be tested explicitly.

---

### 61. Authentication Security Testing Control

Authentication controls should be tested against realistic failure and attack scenarios.

Testing should include:

* Invalid credentials
* Expired credentials
* Revoked credentials
* Token manipulation
* Session abuse
* Authentication bypass

Test results should demonstrate that unauthorized identities cannot obtain protected AI capabilities.

---

### 62. Authorization Security Testing Control

Authorization testing should validate access decisions at every material AI security boundary.

Tests should cover:

* Function-level authorization
* Object-level authorization
* Data authorization
* Tool authorization
* Model authorization
* Administrative authorization

Authorization should be evaluated independently from the AI model's response.

---

### 63. Least-Privilege Testing Control

Testing should verify that AI components operate within their intended privilege boundaries.

Tests should attempt to determine whether a compromised component can:

* Access unrelated data
* Invoke unauthorized tools
* Modify protected resources
* Access administrative interfaces
* Escalate privileges

The goal is to validate actual privilege enforcement rather than policy documentation.

---

### 64. Input Validation Testing Control

AI application inputs should be tested for malicious, malformed, unexpected, and boundary conditions.

Testing may include:

* Oversized input
* Invalid types
* Malicious content
* Injection payloads
* Encoding manipulation
* Unexpected structures

Input validation should occur before sensitive processing.

---

### 65. Prompt Input Security Testing Control

Prompt interfaces should be tested against malicious user-controlled input.

Tests should determine whether attackers can:

* Override instructions
* Alter application objectives
* Extract sensitive context
* Circumvent restrictions
* Manipulate tool behavior

Prompt security tests should reflect the actual architecture and instruction hierarchy.

---

### 66. Prompt Template Integrity Control

Security-sensitive prompt templates should be tested for unauthorized modification.

Testing should validate:

* Approved version
* Integrity
* Source
* Deployment state
* Security review

A modified system prompt should be capable of triggering security validation.

---

### 67. Instruction Separation Testing Control

Testing should verify that trusted instructions remain distinct from untrusted content.

Test scenarios should include:

* Malicious user input
* Malicious retrieved documents
* Malicious tool output
* Uploaded files
* External web content

The objective is to determine whether untrusted content can obtain instruction-level authority.

---

### 68. Context Isolation Testing Control

Context boundaries should be tested across:

* Users
* Sessions
* Tenants
* Applications
* Conversations
* Retrieved content

Tests should attempt to force unintended context sharing.

---

### 69. Prompt Injection Testing Control

Direct prompt injection testing should be mandatory for applicable AI applications.

Tests should attempt to manipulate the model into:

* Revealing restricted information
* Ignoring security instructions
* Changing task objectives
* Calling unauthorized tools
* Bypassing application controls

Testing should evaluate both successful and partially successful attack outcomes.

---

### 70. Indirect Prompt Injection Testing Control

Indirect prompt injection should be tested through untrusted content sources.

Examples include:

* Documents
* Web content
* Emails
* Knowledge bases
* Search results
* Retrieved records

Testing should determine whether malicious content can influence downstream AI behavior.

---

### 71. Output Validation Testing Control

AI-generated outputs should be tested before they are consumed by security-sensitive components.

Tests should evaluate whether outputs can cause:

* Command injection
* SQL injection
* HTML injection
* Workflow manipulation
* Unauthorized API requests
* Security-control bypass

---

### 72. Structured Output Security Control

Structured model outputs should be tested against schema and semantic requirements.

Tests should include:

* Invalid fields
* Unexpected fields
* Incorrect types
* Boundary values
* Unauthorized values
* Malicious parameters

A valid schema must not be confused with a valid authorization decision.

---

### 73. Model Invocation Security Control

Model invocation should be tested for:

* Unauthorized model access
* Model substitution
* Incorrect model version
* Unapproved provider
* Unsafe configuration
* Excessive model permissions

Applications should invoke only approved models under approved conditions.

---

### 74. Model Integrity Testing Control

Model integrity should be verified before deployment.

Tests should validate:

* Hash
* Signature
* Version
* Registry status
* Provenance

Integrity failures should result in rejection or escalation according to system risk.

---

### 75. Unsafe Model Loading Testing Control

Model loading mechanisms should be tested for unsafe deserialization and malicious artifact behavior.

Testing should determine whether untrusted model artifacts can:

* Execute unintended code
* Load unauthorized components
* Access restricted resources
* Escape isolation boundaries

Model loading should occur within appropriate security boundaries.

---

### 76. Model Artifact Security Testing Control

Model artifacts should be scanned before being promoted to trusted repositories.

Testing may identify:

* Malicious payloads
* Suspicious files
* Unsafe serialization
* Integrity violations
* Unexpected dependencies

Artifact testing should occur before production authorization.

---

### 77. Model Provenance Testing Control

Security testing should validate whether model provenance is complete and trustworthy.

Testing should determine whether the organization can identify:

* Dataset
* Training code
* Dependencies
* Configuration
* Training environment
* Training run
* Model version

Missing provenance should be treated as a supply-chain assurance gap.

---

### 78. Model Extraction Testing Control

Where model confidentiality is important, systems should be tested against extraction attempts.

Testing may simulate:

* Systematic querying
* Excessive inference
* Output collection
* API probing
* Reconstruction attempts

Rate limiting and abuse controls should be evaluated as part of these tests.

---

### 79. Model Manipulation Testing Control

Models should be tested for security-relevant manipulation scenarios.

Depending on the system, testing may include:

* Backdoor behavior
* Adversarial inputs
* Malicious fine-tuning
* Unexpected behavior under crafted inputs
* Model replacement

The testing approach should reflect the model's use and risk.

---

### 80. Dataset Security Testing Control

Datasets should be tested for:

* Integrity
* Provenance
* Unauthorized modification
* Sensitive information
* Malicious content
* Schema anomalies

Security testing should occur before security-sensitive datasets enter trusted pipelines.

---

### 81. Dataset Poisoning Testing Control

Where applicable, testing should evaluate whether manipulated training data can influence model behavior.

Test scenarios may include:

* Malicious labels
* Trigger patterns
* Injected records
* Distribution manipulation
* Targeted poisoning

High-impact systems should receive stronger poisoning assurance.

---

### 82. Dataset Access-Control Testing Control

Dataset authorization should be tested using multiple identities.

Testing should verify that:

* Unauthorized users cannot access restricted datasets
* Users cannot access another tenant's data
* Training workloads receive only approved data
* Administrative access is restricted

---

### 83. Dataset Provenance Testing Control

Testing should verify that dataset sources and transformations can be reconstructed.

The test should validate:

```text id="astc83c"
Source
  |
  v
Collection
  |
  v
Transformation
  |
  v
Validation
  |
  v
Storage
  |
  v
Training
```

Breaks in provenance should be identified as security assurance weaknesses.

---

### 84. Retrieval Authorization Testing Control

RAG systems should be tested to ensure that retrieval authorization is enforced independently.

Testing should attempt:

* Unauthorized document access
* Cross-tenant retrieval
* Restricted-document retrieval
* Metadata exposure
* Query manipulation

---

### 85. Vector Store Security Testing Control

Vector stores should be tested for:

* Access-control bypass
* Cross-tenant exposure
* Metadata leakage
* Unauthorized index access
* Manipulated embeddings
* Retrieval poisoning

Testing should consider both the vector database and the retrieval application.

---

### 86. AI Memory Security Testing Control

Persistent memory should be tested for:

* Unauthorized reads
* Unauthorized writes
* Cross-user leakage
* Cross-tenant leakage
* Memory poisoning
* Improper deletion

Security testing should validate the complete memory lifecycle.

---

### 87. Tool Access Testing Control

AI tools should be tested to verify that unauthorized tools cannot be invoked.

Tests should include:

* Restricted tool requests
* Privileged tool requests
* Tool substitution
* Unauthorized identities
* Policy bypass attempts

---

### 88. Tool Parameter Testing Control

Tool parameters should be subjected to malicious and boundary-value testing.

Tests should include:

* Invalid resource identifiers
* Unauthorized resources
* Extreme values
* Malicious strings
* Cross-tenant identifiers
* Unexpected commands

---

### 89. High-Risk Tool Testing Control

High-risk tools should receive stronger security testing.

Testing should verify:

* Authorization
* Approval requirements
* Transaction limits
* Identity binding
* Auditability
* Emergency revocation

Examples include financial, administrative, destructive, and production-management tools.

---

### 90. API Security Testing Control

AI APIs should undergo comprehensive security testing.

Testing should cover:

* Authentication
* Authorization
* Input validation
* Output handling
* Rate limiting
* Object-level access
* Error handling
* Abuse resistance

---

### 91. Rate-Limiting Testing Control

Rate limits should be tested under realistic and abusive traffic.

Testing should verify that attackers cannot:

* Exhaust resources
* Perform uncontrolled model extraction
* Bypass quotas
* Overload downstream systems

Rate-limit failures should produce observable security events.

---

### 92. Resource-Quota Testing Control

AI resource limits should be tested for enforcement.

Resources may include:

* Tokens
* GPU capacity
* CPU
* Memory
* Storage
* API calls
* Tool transactions

Tests should determine whether resource limits can be bypassed through repeated or concurrent requests.

---

### 93. Dependency Vulnerability Testing Control

AI dependencies should be continuously tested for known vulnerabilities.

Testing should include:

* Direct dependencies
* Transitive dependencies
* AI frameworks
* Inference libraries
* Plugins

Critical vulnerabilities should influence security gate decisions.

---

### 94. Software Supply-Chain Testing Control

Software supply-chain security should be tested from source through deployment.

Testing should validate:

* Source integrity
* Dependency provenance
* Build integrity
* Artifact integrity
* Registry security
* Deployment provenance

---

### 95. Model Supply-Chain Testing Control

External and internally produced models should undergo supply-chain security validation.

Testing should verify:

* Source authenticity
* Model provenance
* Artifact integrity
* Security evaluation
* Approved provider status
* Model version

---

### 96. Container Security Testing Control

AI containers should undergo security testing before deployment.

Testing should include:

* Image vulnerability scanning
* Privilege testing
* Runtime isolation
* Network exposure
* Secret detection
* Image integrity

---

### 97. Infrastructure Security Testing Control

AI infrastructure should be tested against security requirements.

Testing should include:

* IAM
* Network segmentation
* Storage permissions
* Public exposure
* Encryption
* Administrative access
* Compute isolation

---

### 98. Infrastructure-as-Code Security Testing Control

IaC should undergo automated security testing.

Testing should identify:

* Excessive permissions
* Public exposure
* Insecure configurations
* Disabled security controls
* Hardcoded secrets

Tests should run before infrastructure deployment.

---

### 99. Secrets Security Testing Control

Security testing should search for secrets throughout AI development.

Potential locations include:

* Source code
* Notebooks
* Configuration
* CI/CD
* Containers
* Prompts
* Test data
* Logs

Detected secrets should be handled according to incident and credential-management procedures.

---

### 100. Configuration Security Testing Control

AI configurations should be tested against approved security baselines.

Testing should identify:

* Unsafe defaults
* Excessive privileges
* Unapproved providers
* Unrestricted tools
* Weak authentication
* Insecure network configuration

---

### 101. Security Baseline Validation Control

AI development and test environments should be automatically evaluated against security baselines.

Validation may include:

* Identity configuration
* Network configuration
* Storage security
* Logging
* Encryption
* Access control
* Runtime restrictions

---

### 102. CI/CD Security Testing Control

CI/CD pipelines should be security-tested as privileged infrastructure.

Testing should cover:

* Pipeline identity
* Pipeline permissions
* Secrets
* Build isolation
* Configuration
* Artifact handling
* Deployment authority

---

### 103. Build Environment Security Testing Control

Build environments should be tested for isolation and privilege weaknesses.

Testing should verify that compromised build code cannot easily:

* Access production
* Obtain unrelated credentials
* Modify trusted artifacts
* Access restricted infrastructure

---

### 104. Artifact Integrity Testing Control

Production-bound artifacts should undergo integrity validation.

Testing should verify:

* Hash consistency
* Signature validity
* Provenance
* Registry identity
* Version consistency

---

### 105. Security Gate Testing Control

Security gates themselves should be tested.

Testing should verify that:

* Failed checks actually block deployment
* Unknown results do not bypass critical controls
* Gate decisions are logged
* Bypass attempts are detected
* Approved exceptions are correctly enforced

A security gate that can be bypassed is itself a security vulnerability.

---

### 106. Deployment Authorization Testing Control

Deployment authorization mechanisms should be tested against unauthorized deployment attempts.

Testing should attempt to bypass:

* Approval requirements
* Role restrictions
* Environment restrictions
* Security gates
* Artifact validation

---

### 107. Deployment Environment Verification Control

Testing should verify that approved artifacts are deployed only to authorized environments.

Tests should identify whether:

* Development artifacts can reach production
* Unapproved registries can be used
* Wrong configurations can be deployed
* Unauthorized workloads can access production

---

### 108. Runtime Security Testing Control

AI applications should undergo security testing after deployment.

Runtime testing should validate:

* Authentication
* Authorization
* API security
* Model integrity
* Tool controls
* Configuration
* Network controls

---

### 109. Runtime Model Integrity Testing Control

The running model should be tested to confirm that it corresponds to the approved artifact.

Testing should verify:

```text id="astc109d"
Approved Model
      |
      v
Approved Artifact
      |
      v
Deployed Artifact
      |
      v
Running Model
```

Any mismatch should trigger investigation.

---

### 110. AI Abuse Testing Control

AI systems should be tested against realistic abuse scenarios.

Testing may include:

* Automated probing
* Excessive inference
* Repeated injection
* Tool misuse
* Data extraction
* Resource exhaustion

---

### 111. Security Regression Testing Control

Security regression tests should be retained for previously discovered vulnerabilities.

Every relevant future release should execute applicable regression tests.

This creates a persistent security memory for the development lifecycle.

---

### 112. Continuous Adversarial Testing Control

High-risk AI systems should receive recurring adversarial security testing.

Testing should evolve with:

* New threats
* New models
* New attack techniques
* New tools
* New integrations

Static adversarial test suites should not become the sole source of assurance.

---

### 113. Security Test Automation Control

Repeatable security tests should be automated where practical.

Automation should provide:

* Consistent execution
* Fast feedback
* Repeatability
* CI/CD integration
* Security gate integration
* Evidence generation

Manual testing remains necessary for complex and high-risk scenarios.

---

### 114. Security Test Result Integrity Control

Test results should be protected against unauthorized modification.

Results should include:

* Test identity
* Test version
* Target version
* Execution time
* Result
* Environment
* Tester or automation identity

This prevents false evidence from entering security decisions.

---

### 115. Security Test Evidence Control

Security tests should generate auditable evidence.

Evidence should demonstrate:

* What was tested
* Why it was tested
* Which version was tested
* Expected result
* Actual result
* Test outcome
* Relevant findings

Evidence should remain linked to the applicable requirement and threat.

---

### 116. Security Test Failure Enforcement Control

Critical security test failures should produce defined enforcement actions.

Depending on risk, a failure may:

* Block a build
* Block artifact promotion
* Block deployment
* Generate an incident
* Require remediation
* Require formal risk acceptance

A failed critical security test should not be silently ignored.

---

### 117. Core Engineering Principle

**AI security testing must be an enforceable lifecycle control in which threat-driven and risk-based tests continuously validate security requirements across applications, models, datasets, prompts, tools, APIs, dependencies, pipelines, infrastructure, and runtime environments, with failures producing appropriate security actions and evidence.**

The control relationship is:

```text id="astc117e"
Threat
   |
   v
Requirement
   |
   v
Security Test
   |
   v
Execution
   |
   +--------+
   |        |
   v        v
 Pass     Fail
   |        |
   v        v
Continue  Remediate /
          Block /
          Escalate
             |
             v
        Retest
             |
             v
          Evidence
```

**The essential control principle is that security testing must have consequences.** A test that produces a result but cannot influence remediation, release, deployment, or risk decisions provides limited security assurance. For material AI risks, testing must be connected to enforceable gates, accountable remediation, regression protection, and continuous validation.



