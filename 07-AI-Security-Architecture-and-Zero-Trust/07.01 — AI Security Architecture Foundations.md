# 07.01 — AI Security Architecture Foundations

## Part 1 — Foundations, Principles, Components, and Security Architecture Thinking

### 1. Introduction

AI security architecture is the structured design of security controls, trust relationships, identities, data flows, infrastructure, applications, models, and operational processes that protect an AI system throughout its lifecycle.

A traditional application security architecture might focus primarily on:

* Applications.
* Servers.
* Databases.
* Networks.
* Users.
* APIs.

An AI security architecture must extend that view.

An AI system may additionally introduce:

* Foundation models.
* Machine-learning models.
* Training data.
* Fine-tuning data.
* Embeddings.
* Vector databases.
* Prompt processing.
* Retrieval-augmented generation (RAG).
* AI agents.
* Tool integrations.
* Model APIs.
* Model registries.
* AI development pipelines.
* Model-serving infrastructure.

Therefore:

> **AI security architecture is not simply traditional cybersecurity applied to an AI application. It requires understanding the unique components, data flows, trust boundaries, and attack surfaces created by AI.**

---

# 2. Why AI Security Architecture Matters

AI systems increasingly interact with:

* Sensitive enterprise data.
* Internal applications.
* Customers.
* Employees.
* External APIs.
* Cloud platforms.
* Business workflows.
* Critical systems.

As AI becomes more capable, the security impact of an architecture decision increases.

For example:

```text
Traditional chatbot
        ↓
Provides information

AI agent
        ↓
Reads information
        ↓
Calls APIs
        ↓
Changes records
        ↓
Executes business actions
```

The second architecture introduces significantly greater security consequences.

The key question becomes:

> **What is the maximum authority we are giving the AI system, and what security architecture prevents that authority from being abused?**

---

# 3. Security Architecture vs. Security Controls

These concepts are related but different.

### Security Control

A specific safeguard.

Examples:

* MFA.
* Encryption.
* Firewall.
* DLP.
* RBAC.
* Logging.

### Security Architecture

The structured arrangement of:

* Components.
* Trust boundaries.
* Security controls.
* Identity.
* Network paths.
* Data flows.
* Dependencies.
* Enforcement points.

For example:

> MFA is a security control.

But:

> Designing an architecture where every privileged AI service identity must authenticate through centralized identity infrastructure and receive short-lived credentials is an architectural security decision.

---

# 4. Architecture as a Security Decision

Architecture determines:

> **Where security controls can be applied and how much damage can occur if a control fails.**

Consider two architectures.

### Architecture A

```text
AI Application
      ↓
Database
```

The AI application has direct database access.

### Architecture B

```text
AI Application
      ↓
Policy Enforcement
      ↓
Application API
      ↓
Authorization
      ↓
Data Service
      ↓
Database
```

Architecture B creates additional enforcement and authorization boundaries.

This illustrates an important principle:

> **Security should be designed into the architecture rather than added after deployment.**

---

# 5. AI Security Architecture Objectives

A secure AI architecture should seek to provide:

### Confidentiality

Prevent unauthorized access to:

* Prompts.
* Data.
* Models.
* Credentials.
* Outputs.

### Integrity

Protect:

* Models.
* Training data.
* Configuration.
* Prompts.
* Policies.
* AI-generated actions.

### Availability

Ensure AI services remain:

* Available.
* Resilient.
* Recoverable.

### Authenticity

Ensure:

> Users, services, models, APIs, and systems are what they claim to be.

### Accountability

Ensure:

> Actions can be traced to the responsible identity, system, or process.

---

# 6. The AI Security Architecture Model

A useful high-level model is:

```text
                  USERS
                    ↓
             Identity Layer
                    ↓
             AI Application
                    ↓
              AI Gateway
                    ↓
          ┌─────────┼─────────┐
          ↓         ↓         ↓
       Model      RAG       Tools
          ↓         ↓         ↓
      Model       Vector    Enterprise
      Runtime     Store      APIs
          ↓         ↓         ↓
          └─────────┼─────────┘
                    ↓
               Data Layer
                    ↓
             Infrastructure
```

Security controls exist across all these layers.

---

# 7. AI Architecture Layers

A useful way to understand AI security architecture is through layers.

## Layer 1 — User

Includes:

* Employees.
* Customers.
* Administrators.
* Developers.
* AI operators.

---

## Layer 2 — Application

Includes:

* Chat applications.
* AI assistants.
* Agent frameworks.
* Business applications.

---

## Layer 3 — AI/Model

Includes:

* Foundation models.
* Fine-tuned models.
* Machine-learning models.
* Model-serving infrastructure.

---

## Layer 4 — Data

Includes:

* Training data.
* Prompts.
* Documents.
* Embeddings.
* Vector databases.
* Enterprise databases.

---

## Layer 5 — Integration

Includes:

* APIs.
* Plugins.
* Tools.
* SaaS applications.
* Enterprise systems.

---

## Layer 6 — Infrastructure

Includes:

* Cloud.
* Containers.
* Kubernetes.
* Virtual machines.
* GPUs.
* Storage.
* Networking.

---

## Layer 7 — Security and Governance

Cross-cutting capabilities include:

* IAM.
* Zero Trust.
* Logging.
* Monitoring.
* Encryption.
* DLP.
* Security operations.
* Governance.
* Risk management.

---

# 8. AI Security Is Cross-Layer

An AI vulnerability rarely exists in isolation.

Example:

> Prompt injection.

At first glance, it looks like a model problem.

But exploitation may involve:

```text
Prompt Injection
      ↓
AI Model
      ↓
Agent
      ↓
Tool Invocation
      ↓
API
      ↓
Identity
      ↓
Enterprise Data
```

Therefore:

> **AI security must protect the entire execution chain, not only the model.**

---

# 9. AI Attack Surface

The AI attack surface includes all components through which an attacker could:

* Enter the system.
* Influence behavior.
* Access information.
* Modify data.
* Execute actions.
* Compromise infrastructure.

Examples include:

* User interfaces.
* APIs.
* Model endpoints.
* Prompt interfaces.
* Plugins.
* Tool connections.
* Data pipelines.
* Vector databases.
* Model registries.
* Cloud infrastructure.
* CI/CD pipelines.

---

# 10. Traditional vs. AI Attack Surface

Traditional application:

```text
Internet
   ↓
Web Application
   ↓
API
   ↓
Database
```

AI-enabled application:

```text
Internet
   ↓
Web Application
   ↓
AI Gateway
   ↓
Prompt
   ↓
Model
   ↓
RAG
   ↓
Vector Database
   ↓
Tool/API
   ↓
Enterprise System
```

The AI architecture introduces additional trust and security considerations.

---

# 11. Trust Boundaries

A trust boundary is a point where:

> **The level of trust, authority, ownership, or security assumptions changes.**

Examples:

```text
Internet
   |
   | Trust Boundary
   ↓
Enterprise Network
```

Another:

```text
Enterprise
   |
   | Trust Boundary
   ↓
Third-Party AI Provider
```

Another:

```text
AI Agent
   |
   | Trust Boundary
   ↓
Financial System
```

Each boundary requires explicit security controls.

---

# 12. AI Trust Must Be Explicit

A dangerous assumption is:

> "The AI is inside our environment, so we trust it."

The AI system should not automatically receive trust simply because it operates internally.

Instead:

> **Trust should be established based on identity, authorization, context, policy, and verified behavior.**

This becomes especially important for Zero Trust.

---

# 13. Zero Trust Foundation

A fundamental Zero Trust principle is:

> **Never trust implicitly; continuously verify.**

Applied to AI:

Do not assume that:

* An AI agent is trustworthy.
* An internal model is safe.
* A service account is legitimate.
* A tool call is authorized.
* Retrieved data should automatically be trusted.

Instead verify:

* Who?
* What?
* Why?
* What data?
* What action?
* Under what policy?
* Under what context?

---

# 14. Identity Is Central

AI security architecture increasingly depends on machine identities.

An AI system may have:

* Human users.
* Application identities.
* Service accounts.
* Workload identities.
* API identities.

Example:

```text
Human User
     ↓
User Identity
     ↓
AI Application
     ↓
Workload Identity
     ↓
Enterprise API
```

Each identity should have clearly defined permissions.

---

# 15. Least Privilege

AI systems should receive:

> **Only the minimum permissions necessary to perform their approved function.**

Example:

A customer-service AI should perhaps be allowed to:

* Read customer order status.

But not necessarily:

* Delete customer accounts.
* Modify billing information.
* Create administrator accounts.

---

# 16. AI Agent Authority

This becomes particularly important with AI agents.

An AI agent may be able to:

```text
Read
 ↓
Write
 ↓
Execute
 ↓
Transact
```

Each additional capability increases potential impact.

Therefore:

> **AI autonomy and privilege should be explicitly governed.**

---

# 17. The Principle of Bounded Autonomy

A useful architecture principle is:

> **Give AI enough authority to perform its intended function, but constrain the authority so that failures or compromise have limited impact.**

For example:

```text
AI Agent
   ↓
Read Customer Data
   ↓
Generate Recommendation
   ↓
Human Approval
   ↓
Execute Transaction
```

Instead of:

```text
AI Agent
   ↓
Full Transaction Authority
```

---

# 18. Human Oversight as an Architectural Control

Human oversight is not merely a policy requirement.

It can be implemented architecturally.

For example:

```text
AI Decision
     ↓
Risk Classification
     ↓
High-Risk?
   ↙       ↘
 Yes        No
 ↓           ↓
Human       Automated
Review      Execution
 ↓
Approval
 ↓
Action
```

This converts governance requirements into technical enforcement.

---

# 19. Defense in Depth

AI security should not rely on one control.

Example:

```text
Authentication
      ↓
Authorization
      ↓
Network Segmentation
      ↓
Input Controls
      ↓
Model Controls
      ↓
Output Validation
      ↓
DLP
      ↓
Logging
      ↓
Monitoring
```

If one control fails:

> Other controls can reduce the potential impact.

---

# 20. Secure-by-Design

Secure-by-design means security is considered during architecture and design rather than after deployment.

Instead of:

> Build → Deploy → Security Review → Fix

Prefer:

> Requirements → Threat Modeling → Secure Architecture → Build → Test → Deploy

---

# 21. Threat Modeling

AI architecture should be threat-modeled before deployment.

Ask:

* What are the assets?
* Who are the threat actors?
* What are the trust boundaries?
* Where does untrusted input enter?
* Where does sensitive data flow?
* What privileges exist?
* What external services are trusted?
* What happens if the model is compromised?
* What happens if the AI produces a malicious or incorrect instruction?

---

# 22. AI-Specific Threat Modeling

Traditional threat modeling should be expanded for AI.

Consider:

* Prompt injection.
* Data poisoning.
* Model theft.
* Model extraction.
* Sensitive information disclosure.
* Insecure output handling.
* Excessive agency.
* Supply-chain compromise.
* RAG poisoning.
* Malicious tools.
* Model manipulation.

The important principle is:

> **Threat modeling should follow the AI system's actual architecture and data flows.**

---

# 23. Secure Architecture Principle: Minimize Exposure

A secure AI architecture should minimize:

* Unnecessary network exposure.
* Unnecessary data access.
* Excessive privileges.
* Unnecessary integrations.
* Long-lived credentials.
* Uncontrolled model endpoints.

The architectural question is:

> **What can we remove rather than merely protect?**

---

# 24. Secure Architecture Principle: Minimize Blast Radius

If an AI component is compromised:

> **How much of the enterprise can the attacker reach?**

This is the **blast radius** question.

Example:

### Poor architecture

```text
AI Agent
   ↓
Full Enterprise Network
```

### Better architecture

```text
AI Agent
   ↓
Restricted Segment
   ↓
Approved Gateway
   ↓
Specific API
```

The second design limits potential impact.

---

# 25. Secure Architecture Principle: Explicit Boundaries

Important boundaries should be clearly defined between:

* Users.
* AI applications.
* Models.
* Data.
* Tools.
* APIs.
* Providers.
* Administrative systems.

Each boundary should have:

* Identity.
* Authentication.
* Authorization.
* Monitoring.
* Policy enforcement.

---

# 26. Secure Architecture Principle: Fail Securely

If the AI system encounters:

* Uncertainty.
* Authorization failure.
* Tool failure.
* Policy violation.
* Suspicious input.

The secure behavior should be:

> **Deny, restrict, or escalate rather than automatically proceed.**

For example:

```text
Authorization Failure
        ↓
      DENY
        ↓
     LOG
        ↓
    ALERT
```

rather than:

```text
Authorization Failure
        ↓
"Try anyway"
```

---

# 27. Secure Architecture Principle: Assume Compromise

A mature architecture assumes that:

* A credential could be stolen.
* A model could behave unexpectedly.
* A tool could be compromised.
* A provider could experience an incident.
* A user could be malicious.
* A prompt could be hostile.

Therefore:

> **Architecture should remain resilient even when individual components cannot be fully trusted.**

---

# 28. Security Architecture and Governance

Security architecture translates governance requirements into technical design.

For example:

### Governance Requirement

> High-risk AI actions require human approval.

### Architecture

```text
AI Agent
   ↓
Policy Engine
   ↓
Risk Classification
   ↓
Human Approval
   ↓
Execution API
```

This is the connection between:

> **GRC policy and technical architecture.**

---

# 29. GRC Professional's Role

A GRC professional does not necessarily design every firewall rule or Kubernetes policy.

Instead, the GRC/security professional should be able to:

* Understand the architecture.
* Identify trust boundaries.
* Identify security risks.
* Challenge excessive privileges.
* Verify security requirements.
* Assess control coverage.
* Review architecture evidence.
* Determine residual risk.
* Communicate risks to stakeholders.

---

# 30. Architecture Review Questions

During an AI security architecture review, ask:

### Identity

> What identities interact with the AI system?

### Access

> What can each identity do?

### Data

> What data can the AI access?

### Network

> What systems can it reach?

### APIs

> What actions can it invoke?

### Model

> Where does the model execute?

### Provider

> Which components are third-party?

### Monitoring

> Can we detect misuse?

### Recovery

> What happens if the system is compromised?

---

# 31. Architecture Documentation

A secure AI architecture should be documented.

Useful artifacts include:

* Architecture diagrams.
* Data-flow diagrams.
* Trust-boundary diagrams.
* Network diagrams.
* Identity models.
* API inventories.
* Data classifications.
* Threat models.
* Control mappings.
* Risk assessments.

Documentation should reflect the **actual deployed architecture**, not merely the intended design.

---

# 32. Architecture vs. Reality

One of the most important assessment principles:

> **Do not assess only the documented architecture. Assess whether the implemented environment matches the approved architecture.**

For example:

Architecture document:

> AI has read-only access.

Production configuration:

> AI service account has write permissions.

This is an architecture/control gap.

---

# 33. Architecture Drift

AI environments can change rapidly.

Examples:

* New model.
* New API.
* New data source.
* New plugin.
* New agent capability.
* New cloud service.

If architecture documentation is not updated:

> **The organization may lose visibility into the actual attack surface.**

---

# 34. AI Security Architecture Lifecycle

A useful lifecycle is:

```text
Requirements
    ↓
Architecture
    ↓
Threat Modeling
    ↓
Security Design
    ↓
Implementation
    ↓
Security Testing
    ↓
Approval
    ↓
Deployment
    ↓
Monitoring
    ↓
Change Management
    ↓
Reassessment
```

---

# 35. Architecture Security Requirements

Security requirements should be defined before implementation.

Examples:

> AI workloads must use approved identity services.

> AI systems must enforce least privilege.

> Sensitive data must be encrypted.

> High-risk actions require authorization.

> AI workloads must be isolated from unauthorized network segments.

> Administrative access must be logged and monitored.

These become:

> **Architecture requirements.**

---

# 36. Architecture Control Mapping

A GRC professional can map:

```text
Requirement
    ↓
Architecture Principle
    ↓
Security Control
    ↓
Technical Implementation
    ↓
Evidence
    ↓
Assessment
```

Example:

```text
Requirement:
Least privilege

        ↓

Architecture:
Dedicated workload identity

        ↓

Control:
RBAC

        ↓

Implementation:
Restricted API permissions

        ↓

Evidence:
IAM configuration

        ↓

Assessment:
Effective
```

This is powerful GRC traceability.

---

# 37. Secure AI Architecture Is a System

The most important lesson from this section is:

> **AI security cannot be reduced to protecting the model.**

A secure AI system must protect:

```text
User
 ↓
Identity
 ↓
Application
 ↓
AI Gateway
 ↓
Model
 ↓
Data
 ↓
Tools
 ↓
APIs
 ↓
Infrastructure
 ↓
Operations
```

A weakness anywhere in this chain can affect the overall security posture.

---

# 38. Part 1 Interview Anchor

If asked:

> **"What is AI security architecture?"**

Use:

> **"AI security architecture is the structured design of identities, trust boundaries, data flows, infrastructure, models, applications, APIs and security controls that protect an AI system throughout its lifecycle. I look beyond the model itself and assess the complete execution chain, including how users interact with the AI, what data it can access, what tools and APIs it can invoke, what infrastructure hosts it, and how security controls limit and monitor that authority."**

---

# 39. Senior GRC Anchor

Remember:

> **Architecture defines trust.**

> **Identity defines who or what is acting.**

> **Authentication verifies identity.**

> **Authorization defines what it can do.**

> **Segmentation limits where it can go.**

> **Isolation limits blast radius.**

> **Encryption protects data.**

> **Monitoring provides visibility.**

> **Governance determines whether the architecture is acceptable.**

---

# 40. Final Mental Model

For **07.01**, memorize this:

```text id="q7x2pa"
              AI SYSTEM
                  ↓
          WHAT CAN IT ACCESS?
                  ↓
          WHO CAN CONTROL IT?
                  ↓
          WHAT CAN IT EXECUTE?
                  ↓
          WHERE CAN IT CONNECT?
                  ↓
          WHAT DATA CAN IT SEE?
                  ↓
          WHAT HAPPENS IF COMPROMISED?
                  ↓
       HOW DO WE LIMIT THE BLAST RADIUS?
                  ↓
          HOW DO WE DETECT IT?
                  ↓
          HOW DO WE RESPOND?
```

The fundamental architectural principle is:

> **Never give an AI system more trust, access, connectivity, or authority than its approved business purpose requires.**


