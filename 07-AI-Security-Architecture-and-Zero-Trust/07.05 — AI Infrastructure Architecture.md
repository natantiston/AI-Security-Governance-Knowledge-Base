# 07.05 — AI Infrastructure Architecture

## Part 1 — Foundations, Components, and Infrastructure Security Model

### 338. AI Infrastructure Architecture

**AI infrastructure architecture** defines the technical foundation on which AI systems, models, applications, agents, data services, and security controls operate.

It answers a fundamental security question:

> **What infrastructure is required to run AI securely, and how should that infrastructure be structured, isolated, protected, monitored, and governed?**

AI infrastructure may include:

```text
AI Applications
       ↓
AI Services / APIs
       ↓
Model Runtime
       ↓
Compute
       ↓
Storage
       ↓
Network
       ↓
Identity
       ↓
Security Services
```

Unlike traditional applications, AI infrastructure often introduces specialized components such as:

* GPU clusters.
* Model serving platforms.
* Vector databases.
* Model registries.
* Feature stores.
* AI pipelines.
* Inference endpoints.
* Training environments.
* Agent execution environments.
* Prompt and response processing services.

The security architecture must account for the complete infrastructure stack.

---

### 339. AI Infrastructure Stack

A useful conceptual model is:

```text
┌──────────────────────────────────────┐
│          AI Applications             │
├──────────────────────────────────────┤
│       Agents / AI Services           │
├──────────────────────────────────────┤
│      Model Serving / Runtime         │
├──────────────────────────────────────┤
│      Models / Model Artifacts        │
├──────────────────────────────────────┤
│       AI Data / Vector Stores        │
├──────────────────────────────────────┤
│      Containers / Kubernetes         │
├──────────────────────────────────────┤
│       Compute / GPU / CPU            │
├──────────────────────────────────────┤
│       Operating Systems              │
├──────────────────────────────────────┤
│       Network / Virtual Network      │
├──────────────────────────────────────┤
│       Physical / Cloud Platform      │
└──────────────────────────────────────┘
```

Security controls should be implemented across every layer.

---

### 340. AI Infrastructure Is More Than Compute

A common misconception is:

> AI infrastructure = GPUs.

GPUs are important, but an enterprise AI platform may depend on many additional components.

For example:

```text
GPU
 ↓
Container Runtime
 ↓
Kubernetes
 ↓
Model Server
 ↓
API Gateway
 ↓
Identity
 ↓
Vector Database
 ↓
Enterprise Applications
```

A weakness in any component can affect the security of the overall AI service.

---

### 341. Infrastructure Security Objective

The primary objective is to ensure that AI infrastructure provides:

```text
Confidentiality
Integrity
Availability
Authenticity
Isolation
Traceability
Resilience
Governance
```

These objectives apply not only to business data but also to:

* Models.
* Model weights.
* Prompts.
* Embeddings.
* System instructions.
* Training datasets.
* Configuration.
* Secrets.
* Logs.
* AI-generated artifacts.

---

### 342. AI Infrastructure Domains

An enterprise AI architecture can be divided into major domains:

```text
1. Compute
2. Storage
3. Network
4. Virtualization
5. Containers
6. Orchestration
7. Model Serving
8. Model Registry
9. Data Platforms
10. AI APIs
11. Identity
12. Secrets
13. Monitoring
14. Security Operations
15. Management Plane
```

Each domain introduces different security requirements.

---

### 343. AI Compute Architecture

AI workloads can run on:

* CPUs.
* GPUs.
* TPUs.
* Specialized AI accelerators.
* On-premises servers.
* Virtual machines.
* Containers.
* Kubernetes clusters.
* Cloud-managed AI services.

A simplified architecture is:

```text
AI Workload
     ↓
Compute Scheduler
     ↓
GPU / CPU
     ↓
Host Infrastructure
```

The security model must protect both the workload and the underlying infrastructure.

---

### 344. GPU Security

GPUs are particularly important because AI workloads may process large volumes of sensitive information.

Security considerations include:

* GPU isolation.
* Memory protection.
* Driver security.
* Firmware security.
* Device access controls.
* Multi-tenant separation.
* Secure configuration.
* Monitoring.
* Patch management.

A compromised GPU environment could affect multiple AI workloads if isolation is inadequate.

---

### 345. GPU Memory Exposure

AI workloads may temporarily place information into GPU memory.

Potentially sensitive information can include:

```text
Prompts
Training Data
Model Parameters
Intermediate Results
Embeddings
User Information
```

Therefore, GPU memory should be considered part of the AI data-processing environment.

---

### 346. GPU Sharing

Organizations may share GPU resources:

```text
┌───────────────┐
│ GPU Cluster   │
├───────────────┤
│ Workload A    │
│ Workload B    │
│ Workload C    │
└───────────────┘
```

Security architecture should determine whether workloads can safely share physical or logical GPU resources.

Higher-risk workloads may require dedicated infrastructure.

---

### 347. Dedicated vs Shared AI Infrastructure

Two common models are:

**Shared:**

```text
Multiple Workloads
       ↓
Shared GPU Cluster
```

**Dedicated:**

```text
Sensitive AI Workload
       ↓
Dedicated GPU Cluster
```

Shared infrastructure can improve utilization and reduce cost, while dedicated infrastructure can provide stronger isolation.

The correct choice depends on:

* Data sensitivity.
* Regulatory requirements.
* Tenant separation.
* Threat model.
* Business criticality.
* Performance requirements.

---

### 348. AI Infrastructure Isolation

Isolation should be considered at multiple levels:

```text
Physical
   ↓
Virtual
   ↓
Network
   ↓
Container
   ↓
Process
   ↓
Application
   ↓
Data
```

Strong architecture does not rely on a single isolation mechanism.

---

### 349. Physical Isolation

Physical isolation may be appropriate for highly sensitive AI workloads.

Example:

```text
Restricted AI
     ↓
Dedicated Server
     ↓
Dedicated GPU
     ↓
Dedicated Storage
```

This can reduce multi-tenancy risk.

---

### 350. Virtual Isolation

Virtual machines can separate workloads:

```text
Physical Host
      │
 ┌────┼────┐
 ↓    ↓    ↓
VM-A VM-B VM-C
```

Each VM can have independent:

* Operating system.
* Identity.
* Network configuration.
* Storage.
* Security policies.

---

### 351. Container Isolation

AI applications are frequently packaged in containers.

```text
Container
   ↓
AI Application
   ↓
Model Runtime
```

Containers improve portability and deployment consistency, but they should not automatically be treated as equivalent to strong physical isolation.

---

### 352. Container Security

AI container security should address:

* Image provenance.
* Vulnerability scanning.
* Minimal base images.
* Runtime privileges.
* Secrets.
* Network access.
* File-system access.
* Resource limits.
* Image signing.
* Runtime monitoring.

A compromised container should have limited ability to affect the underlying host.

---

### 353. Container Image Supply Chain

A typical AI image flow is:

```text
Developer
   ↓
Source Code
   ↓
Build Pipeline
   ↓
Container Image
   ↓
Registry
   ↓
Deployment
```

Each stage represents a supply-chain trust relationship.

---

### 354. Image Provenance

The organization should know:

> **Where did this AI container image come from?**

Important information can include:

* Source repository.
* Build system.
* Build timestamp.
* Dependencies.
* Base image.
* Security scan results.
* Signing information.
* Approver.

---

### 355. Container Image Registry

The registry becomes a critical infrastructure component.

```text
Build Pipeline
      ↓
Image Registry
      ↓
Deployment Platform
      ↓
AI Workload
```

Registry security should include:

* Authentication.
* Authorization.
* Image scanning.
* Signing.
* Immutable tags where appropriate.
* Access logging.
* Retention controls.

---

### 356. Kubernetes for AI

Kubernetes can orchestrate AI workloads:

```text
Kubernetes Cluster
       │
 ┌─────┼────────┐
 ↓     ↓        ↓
AI Pod Model Pod API Pod
```

Security considerations include:

* Namespace isolation.
* RBAC.
* Network policies.
* Pod security.
* Secrets management.
* Service accounts.
* Admission controls.
* Image validation.
* Resource quotas.

---

### 357. AI Namespace Architecture

Organizations can separate AI workloads using namespaces:

```text
Kubernetes Cluster
│
├── ai-development
├── ai-testing
├── ai-staging
└── ai-production
```

Each namespace can have different:

* Policies.
* Identities.
* Network access.
* Resource limits.
* Security controls.

---

### 358. Kubernetes Service Account

An AI workload should use a dedicated service identity.

```text
AI Pod
  ↓
Service Account
  ↓
Authorized Resources
```

Avoid using highly privileged shared service accounts.

---

### 359. Kubernetes RBAC

Role-based access control can restrict what AI workloads can do.

Example:

```text
AI Service Account
      ↓
Read:
  Model Registry

Read:
  Vector Store

No Access:
  Kubernetes Secrets

No Access:
  Cluster Administration
```

The workload receives only the permissions required for its function.

---

### 360. Kubernetes Network Policy

Network policies can control which workloads communicate.

```text
AI Pod
  │
  ├──→ Model Service ✓
  ├──→ Vector DB   ✓
  ├──→ HR DB       ✗
  └──→ Internet    ✗
```

This reduces lateral movement opportunities.

---

### 361. AI Workload Scheduling

AI workloads may require specialized scheduling:

```text
AI Job
  ↓
Scheduler
  ↓
GPU Node
  ↓
Execution
```

Security policies should prevent unauthorized workloads from consuming restricted GPU resources.

---

### 362. Resource Quotas

AI workloads can consume substantial infrastructure resources.

Controls can limit:

```text
CPU
GPU
Memory
Storage
Network
Execution Time
```

Resource limits reduce the risk of:

* Denial of service.
* Runaway workloads.
* Resource exhaustion.
* Unauthorized mining or computation.

---

### 363. AI Denial-of-Service

AI workloads can be particularly expensive to operate.

An attacker may intentionally generate:

```text
Large Prompts
Large Outputs
High Request Volume
Expensive Model Calls
Repeated Inference
Long Agent Loops
```

This can create both availability and financial impact.

---

### 364. AI Resource Governance

Resource governance can include:

```text
Rate Limits
Quota
Concurrency Limits
Token Limits
GPU Limits
Timeouts
Budget Alerts
Automatic Shutdown
```

Security and financial governance therefore intersect.

---

### 365. AI Storage Architecture

AI infrastructure may require several storage types:

```text
Object Storage
Block Storage
File Storage
Databases
Vector Databases
Model Registries
Caches
Backup Storage
```

Each storage type should have an appropriate security model.

---

### 366. Model Storage

Model artifacts may include:

```text
Model Weights
Configuration
Tokenizer
Adapters
Fine-Tuning Artifacts
Metadata
```

These assets should be protected against unauthorized modification.

---

### 367. Model Integrity

An attacker who modifies model artifacts may influence AI behavior.

Therefore:

```text
Model Registry
      ↓
Integrity Verification
      ↓
Approved Model
      ↓
Production
```

can be incorporated into the deployment process.

---

### 368. Model Signing

A mature architecture can use signed model artifacts:

```text
Model Build
    ↓
Security Validation
    ↓
Signing
    ↓
Model Registry
    ↓
Signature Verification
    ↓
Deployment
```

This helps establish model provenance and integrity.

---

### 369. Model Registry Access

Access should be based on role and purpose.

Example:

| Role               | Permission            |
| ------------------ | --------------------- |
| Developer          | Submit model          |
| Security           | Review                |
| Model Owner        | Approve               |
| Deployment Service | Deploy approved model |
| Auditor            | Read metadata         |
| General User       | No direct access      |

---

### 370. Vector Database Architecture

A RAG system may use:

```text
Documents
   ↓
Embedding
   ↓
Vector Database
   ↓
Similarity Search
   ↓
Retrieved Context
   ↓
AI Model
```

The vector database therefore becomes part of the AI security boundary.

---

### 371. Vector Database Security

Security controls may include:

* Authentication.
* Authorization.
* Tenant isolation.
* Encryption.
* Index protection.
* Query monitoring.
* Data retention.
* Deletion controls.

The vector database should not be treated as merely a performance component.

---

### 372. Embedding Security

Embeddings can encode information derived from source documents.

Therefore, an embedding store may still represent sensitive enterprise information even when the original document is not directly stored.

A useful principle is:

> **Transformed data can remain sensitive data.**

---

### 373. Data Lineage in AI Infrastructure

An enterprise should be able to trace:

```text
Source Document
      ↓
Processing
      ↓
Embedding
      ↓
Vector Store
      ↓
Retrieval
      ↓
Prompt
      ↓
Model
      ↓
Output
```

This creates end-to-end lineage.

---

### 374. AI Cache Security

AI platforms may cache:

* Prompts.
* Responses.
* Embeddings.
* Sessions.
* Authentication tokens.
* Model outputs.

Caching can introduce unexpected persistence.

Security architecture should define:

```text
What is cached?
Why?
For how long?
Who can access it?
How is it deleted?
```

---

### 375. AI Session Architecture

A user session may involve:

```text
User
 ↓
AI Gateway
 ↓
Application
 ↓
Model
 ↓
Memory / Session Store
```

Session data should be isolated between users.

---

### 376. Memory Isolation

If AI applications use persistent memory:

```text
User A
 ↓
Memory A

User B
 ↓
Memory B
```

The architecture must prevent:

```text
User A
 ↓
Memory B
```

This is particularly important for multi-user AI assistants.

---

### 377. AI Network Architecture

A typical secure AI network architecture may include:

```text
Internet
   ↓
WAF
   ↓
API Gateway
   ↓
AI Application Zone
   ↓
AI Processing Zone
   ↓
Data Zone
```

Each zone can have different network policies.

---

### 378. AI DMZ

Internet-facing AI services may be placed behind a controlled perimeter:

```text
Internet
   ↓
WAF
   ↓
Load Balancer
   ↓
AI API Gateway
   ↓
Internal AI Services
```

The model runtime should not necessarily be directly exposed to the Internet.

---

### 379. AI Private Network

For sensitive workloads:

```text
User
 ↓
Corporate Network
 ↓
Private AI Gateway
 ↓
Private AI Cluster
 ↓
Private Data
```

This can reduce public exposure.

---

### 380. AI Egress Control

AI workloads may need outbound connectivity.

However:

```text
AI Workload
     ↓
Internet
```

should not automatically mean unrestricted Internet access.

A controlled pattern is:

```text
AI Workload
     ↓
Egress Gateway
     ↓
Approved Destinations
```

---

### 381. Egress Allowlisting

Organizations can restrict AI workloads to approved destinations:

```text
Approved:
api.company.com
model-provider.example
updates.example

Blocked:
Unknown Internet Destinations
```

This can significantly reduce data-exfiltration opportunities.

---

### 382. AI Ingress Control

Inbound access should also be controlled.

```text
External Request
      ↓
WAF
      ↓
API Gateway
      ↓
Authentication
      ↓
Authorization
      ↓
AI Service
```

The model endpoint itself should not necessarily be publicly accessible.

---

### 383. Management Plane vs Data Plane

AI infrastructure should distinguish between:

**Management plane:**

```text
Configuration
Deployment
IAM
Policy
Monitoring
Administration
```

**Data plane:**

```text
Prompts
Inference
Model Requests
Data Retrieval
Tool Calls
Outputs
```

These planes should have appropriately separated access.

---

### 384. Management Plane Security

Compromise of the management plane can have a large blast radius.

For example:

```text
Admin
 ↓
AI Platform
 ↓
All AI Workloads
```

Administrative access should therefore receive stronger controls such as:

* MFA.
* Privileged access management.
* Strong authentication.
* Network restrictions.
* Session recording where appropriate.
* Just-in-time access.
* Administrative logging.

---

### 385. Data Plane Security

The data plane should enforce:

```text
Identity
Authorization
Data Classification
Network Policy
DLP
Rate Limits
Monitoring
```

Data-plane controls should remain effective even if an individual AI application behaves unexpectedly.

---

### 386. Control Plane Separation

A strong architecture avoids allowing ordinary AI workloads to modify their own security configuration.

Weak:

```text
AI Workload
   ↓
AI Platform Administration
```

Stronger:

```text
AI Workload
   ↓
Data Plane Only

Security Administrator
   ↓
Management Plane
```

---

### 387. Infrastructure Identity

Every significant AI infrastructure component should have an identifiable security principal.

Examples:

```text
AI Application Identity
Model Service Identity
Kubernetes Service Account
Database Identity
Storage Identity
Pipeline Identity
Deployment Identity
Administrator Identity
```

This supports accountability.

---

### 388. Workload Identity

Workload identity is preferable to shared static credentials.

```text
AI Workload
     ↓
Workload Identity
     ↓
Short-Lived Credential
     ↓
Resource
```

This reduces credential-management risk.

---

### 389. Infrastructure Secrets

AI infrastructure may contain:

```text
API Keys
Database Credentials
Cloud Credentials
Certificates
Tokens
Signing Keys
Encryption Keys
```

These should not be embedded directly in:

```text
Source Code
Container Images
Model Files
Prompts
Configuration Files
```

---

### 390. Secrets Injection

A safer architecture is:

```text
AI Workload
      ↓
Identity Verification
      ↓
Secrets Manager
      ↓
Secret
      ↓
Runtime
```

Secrets should be retrieved only when required.

---

### 391. Short-Lived Credentials

Where supported:

```text
AI Workload
 ↓
Temporary Credential
 ↓
Resource
 ↓
Credential Expires
```

This limits the useful lifetime of stolen credentials.

---

### 392. AI Infrastructure Logging

Infrastructure logs can include:

```text
Authentication
Authorization
Deployment
Model Loading
API Requests
Network Connections
GPU Usage
Container Events
Administrative Actions
```

These logs support both security operations and audit.

---

### 393. Security Telemetry Architecture

A centralized model is:

```text
AI Infrastructure
       │
       ├── Compute Logs
       ├── Network Logs
       ├── IAM Logs
       ├── Application Logs
       ├── Model Logs
       └── API Logs
               ↓
              SIEM
               ↓
        Security Analytics
               ↓
        Incident Response
```

---

### 394. Infrastructure Monitoring

Monitoring should identify:

* Unauthorized workloads.
* Unexpected GPU utilization.
* Abnormal network traffic.
* Unauthorized model deployment.
* Container changes.
* Excessive resource consumption.
* Configuration changes.
* Suspicious administrative activity.

---

### 395. AI Infrastructure Baseline

A baseline may define:

```text
Normal GPU Utilization
Normal Network Destinations
Normal API Volume
Normal Model Versions
Normal Container Images
Normal Administrative Actions
Normal Data Access
```

Deviation from the baseline can trigger investigation.

---

### 396. Configuration Management

AI infrastructure should have controlled configuration.

Examples:

```text
Network Rules
Kubernetes Policies
Model Configuration
API Settings
Storage Permissions
Identity Policies
Security Controls
```

Configuration should be:

* Version controlled where appropriate.
* Reviewed.
* Approved.
* Tested.
* Monitored for drift.

---

### 397. Infrastructure as Code

AI infrastructure can be deployed using Infrastructure as Code:

```text
Code
 ↓
Review
 ↓
Security Validation
 ↓
Approval
 ↓
Deployment
```

This provides consistency and traceability.

---

### 398. Secure Infrastructure Pipeline

A mature pipeline can include:

```text
Developer
   ↓
Source Repository
   ↓
Code Review
   ↓
SAST / Dependency Scan
   ↓
IaC Security Scan
   ↓
Container Scan
   ↓
Policy Validation
   ↓
Approval
   ↓
Deployment
```

Security becomes integrated into infrastructure delivery.

---

### 399. AI Infrastructure Change Management

Changes may include:

```text
New GPU Cluster
New Model Server
New Container
New Model
New API
New Network Route
New Storage
New Provider
New Identity
```

Material changes should undergo appropriate security and risk review.

---

### 400. Infrastructure Drift

Example:

```text
Approved:
AI Server → Private Network

Actual:
AI Server → Public Internet
```

This is infrastructure drift.

Continuous configuration assessment can identify such deviations.

---

### 401. High-Availability AI Architecture

Business-critical AI systems may require:

```text
Multiple Nodes
Multiple Availability Zones
Load Balancing
Failover
Backup
Disaster Recovery
```

Example:

```text
              Load Balancer
               /         \
              ↓           ↓
          AI Node A    AI Node B
              \           /
               \         /
                Data Layer
```

---

### 402. AI Availability Dependency

Availability should consider dependencies:

```text
AI Application
      ↓
Model Service
      ↓
GPU Cluster
      ↓
Vector DB
      ↓
Storage
      ↓
Identity
```

Failure of one critical dependency can affect the entire AI service.

---

### 403. Single Point of Failure

Security architecture should identify:

```text
Single Model Server
Single GPU Cluster
Single Vector Database
Single Identity Provider
Single Network Gateway
Single Storage System
```

Critical AI workloads should have appropriate resilience.

---

### 404. AI Disaster Recovery

Disaster recovery should consider more than application code.

Recovery may require:

```text
Application
Model
Model Configuration
Vector Data
Training Data
Secrets
Infrastructure Configuration
Identity
Network Configuration
```

A backup that restores only the application may not restore the AI service.

---

### 405. Model Recovery

The organization should determine:

> **Can we restore the exact approved model version used in production?**

Model recovery should preserve:

* Model artifact.
* Version.
* Configuration.
* Dependencies.
* Runtime compatibility.
* Security validation.
* Approval evidence.

---

### 406. AI Backup Security

Backups may contain:

```text
Sensitive Data
Models
Prompts
Vector Stores
Credentials
Configuration
Logs
```

Therefore, backups require:

* Encryption.
* Access control.
* Retention policies.
* Integrity protection.
* Recovery testing.
* Secure deletion where required.

---

### 407. Infrastructure Segmentation Model

A mature AI environment may use:

```text
┌─────────────────────────────────────┐
│ User / Access Zone                  │
└──────────────────┬──────────────────┘
                   ↓
┌─────────────────────────────────────┐
│ AI Application Zone                 │
└──────────────────┬──────────────────┘
                   ↓
┌─────────────────────────────────────┐
│ AI Processing / Model Zone          │
└──────────────────┬──────────────────┘
                   ↓
┌─────────────────────────────────────┐
│ AI Data Zone                        │
└─────────────────────────────────────┘
```

Each zone should have explicit communication rules.

---

### 408. Infrastructure Trust Model

The infrastructure architecture can therefore be viewed as:

```text
User
 ↓
Identity Trust
 ↓
Application Trust
 ↓
AI Workload Trust
 ↓
Model Trust
 ↓
Data Trust
 ↓
Infrastructure Trust
```

Each layer must be validated rather than automatically trusted.

---

### 409. AI Infrastructure Threat Categories

Major threat categories include:

| Threat                    | Potential Impact            |
| ------------------------- | --------------------------- |
| Infrastructure compromise | Loss of AI platform control |
| GPU compromise            | Workload or data exposure   |
| Container compromise      | Lateral movement            |
| Model tampering           | Altered AI behavior         |
| Registry compromise       | Malicious deployment        |
| Vector DB compromise      | Sensitive data exposure     |
| Credential theft          | Unauthorized access         |
| Network exposure          | External attack             |
| Misconfiguration          | Data leakage                |
| Resource exhaustion       | AI outage                   |
| Supply-chain compromise   | Malicious components        |
| Configuration drift       | Security control bypass     |

---

### 410. Infrastructure Security Principles

A strong AI infrastructure architecture should follow:

1. **Least privilege**
2. **Explicit trust**
3. **Strong workload identity**
4. **Defense in depth**
5. **Environment separation**
6. **Network segmentation**
7. **Secure-by-default configuration**
8. **Continuous monitoring**
9. **Configuration integrity**
10. **Supply-chain security**
11. **Data minimization**
12. **Strong isolation**
13. **Controlled egress**
14. **Resilience**
15. **Auditable change management**

---

### 411. AI Infrastructure GRC Perspective

From a GRC perspective, the question is not simply:

> "Is the AI infrastructure secure?"

The assessment should ask:

> **"Can the organization demonstrate that the infrastructure supporting AI workloads is appropriately designed, controlled, monitored, resilient, and governed according to the risk of the AI services operating on it?"**

This connects infrastructure architecture with:

```text
Risk
 ↓
Control
 ↓
Implementation
 ↓
Evidence
 ↓
Testing
 ↓
Assurance
```

---

### 412. Infrastructure Control Mapping

A technical architecture can be translated into GRC controls.

| Architecture Component | Security Concern    | Example Control         |
| ---------------------- | ------------------- | ----------------------- |
| GPU                    | Workload isolation  | GPU access restrictions |
| Kubernetes             | Privilege           | RBAC                    |
| Container              | Supply chain        | Image scanning          |
| Model Registry         | Integrity           | Signing / approval      |
| Vector DB              | Data exposure       | Authorization           |
| Network                | Lateral movement    | Segmentation            |
| API Gateway            | Unauthorized access | Authentication          |
| Secrets Manager        | Credential theft    | Short-lived secrets     |
| SIEM                   | Visibility          | Centralized logging     |
| Backup                 | Recovery            | Encrypted backup        |

---

### 413. Infrastructure Security Evidence

Evidence can include:

```text
Architecture Diagrams
Network Diagrams
Kubernetes Policies
RBAC Configuration
Container Scan Results
Model Registry Records
IAM Policies
Security Group Rules
Network Policies
Secrets Management Configuration
Vulnerability Reports
Patch Records
Logging Configuration
Monitoring Dashboards
Backup Tests
Disaster Recovery Tests
Change Records
```

---

### 414. AI Infrastructure Security Assessment Questions

A GRC professional assessing AI infrastructure should ask:

**Compute**

* Where does the AI workload execute?
* Are GPUs shared?
* Is workload isolation sufficient?

**Containers**

* Are images scanned?
* Are images signed?
* Are privileged containers prohibited?

**Orchestration**

* Are namespaces separated?
* Is RBAC enforced?
* Are network policies implemented?

**Models**

* Are models stored securely?
* Is model integrity verified?
* Is deployment approval enforced?

**Data**

* Where are embeddings stored?
* How are vector stores protected?
* Are backups secured?

**Network**

* Is the model publicly exposed?
* Is egress restricted?
* Are AI zones segmented?

**Identity**

* Does each workload have its own identity?
* Are privileged accounts controlled?
* Are credentials short-lived?

**Monitoring**

* Are infrastructure events logged?
* Is anomalous activity detected?
* Is configuration drift monitored?

**Resilience**

* Can the AI service recover from infrastructure failure?
* Are model artifacts backed up?
* Has recovery been tested?

---

### 415. AI Infrastructure Security Architecture Pattern

A foundational enterprise pattern can be represented as:

```text
                         USERS
                           │
                           ▼
                    ┌──────────────┐
                    │ Identity /   │
                    │ Access       │
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ WAF / API    │
                    │ Gateway      │
                    └──────┬───────┘
                           │
                           ▼
              ┌─────────────────────────┐
              │ AI APPLICATION ZONE      │
              │                         │
              │ Agents / AI Services    │
              └────────────┬────────────┘
                           │
                           ▼
              ┌─────────────────────────┐
              │ AI PROCESSING ZONE       │
              │                         │
              │ Model Runtime           │
              │ GPU / CPU               │
              │ Containers              │
              └────────────┬────────────┘
                           │
               ┌───────────┴───────────┐
               ▼                       ▼
      ┌────────────────┐       ┌────────────────┐
      │ MODEL ZONE     │       │ DATA ZONE      │
      │                │       │                │
      │ Model Registry │       │ Vector DB      │
      │ Model Artifacts│       │ Object Storage │
      └────────────────┘       └────────────────┘

        SECURITY / MANAGEMENT PLANE
        IAM │ KMS │ Secrets │ SIEM │ Monitoring
        Vulnerability Management │ Policy │ Audit
```

This architecture establishes distinct infrastructure zones and creates enforceable security boundaries around AI processing, models, and data.

# 07.05 — AI Infrastructure Architecture

## Part 2 — Infrastructure Security Controls and Operational Architecture

### 416. AI Infrastructure Security Control Layers

AI infrastructure security should be implemented as **multiple control layers**, rather than relying on a single security technology.

```text
┌─────────────────────────────────────┐
│ Governance & Policy                 │
├─────────────────────────────────────┤
│ Identity & Access                   │
├─────────────────────────────────────┤
│ Network Security                    │
├─────────────────────────────────────┤
│ Compute & Host Security             │
├─────────────────────────────────────┤
│ Container & Orchestration Security  │
├─────────────────────────────────────┤
│ Model & Artifact Security           │
├─────────────────────────────────────┤
│ Data & Storage Security              │
├─────────────────────────────────────┤
│ Monitoring & Detection              │
├─────────────────────────────────────┤
│ Resilience & Recovery               │
└─────────────────────────────────────┘
```

Each layer should have defined:

* Security objectives.
* Control owners.
* Technical controls.
* Monitoring requirements.
* Evidence requirements.
* Testing procedures.

---

### 417. Infrastructure Security Baseline

An organization should establish an **AI infrastructure security baseline**.

The baseline defines the minimum acceptable configuration for AI environments.

For example:

```text
AI Production Baseline

✓ MFA for administrators
✓ No unrestricted Internet access
✓ Approved container images only
✓ Vulnerability scanning
✓ Encryption enabled
✓ Centralized logging
✓ Network segmentation
✓ RBAC enforced
✓ Secrets stored in approved vault
✓ Approved model versions only
✓ Backup enabled
✓ Configuration monitoring
```

The baseline becomes the reference point for security assessments.

---

### 418. AI Development Infrastructure

Development environments should be separated from production.

```text
Development
     ↓
Testing
     ↓
Staging
     ↓
Production
```

A developer should not automatically have administrative access to production AI infrastructure.

---

### 419. Environment Separation

A stronger architecture separates environments using multiple controls:

```text
DEV
 │
 ├── Separate Identity
 ├── Separate Network
 ├── Separate Resources
 └── Separate Data

TEST
 │
 ├── Separate Identity
 ├── Separate Network
 └── Controlled Data

PROD
 │
 ├── Restricted Identity
 ├── Restricted Network
 ├── Protected Data
 └── Strong Monitoring
```

Environment separation reduces the risk that a development compromise becomes a production compromise.

---

### 420. Development-to-Production Promotion

AI infrastructure should use controlled promotion.

```text
Developer
   ↓
Development
   ↓
Security Testing
   ↓
Approval
   ↓
Staging
   ↓
Validation
   ↓
Production
```

Production deployment should not depend solely on an individual developer's manual action.

---

### 421. Production Infrastructure

Production AI environments should generally have stronger controls than development.

Examples include:

* Restricted administrative access.
* Stronger network segmentation.
* Higher logging requirements.
* Formal change approval.
* More restrictive egress.
* Continuous monitoring.
* Backup and recovery.
* Security alerting.

---

### 422. Administrative Access Architecture

Administrative access should follow a privileged-access model.

```text
Administrator
     ↓
MFA
     ↓
Privileged Access Management
     ↓
Approved Session
     ↓
AI Management Plane
```

Administrative access should be:

* Authenticated.
* Authorized.
* Logged.
* Time-controlled where appropriate.
* Reviewed.

---

### 423. Break-Glass Access

Critical AI infrastructure may require emergency access.

A break-glass mechanism should provide exceptional access while maintaining accountability.

```text
Emergency
   ↓
Break-Glass Account
   ↓
Strong Authentication
   ↓
Emergency Access
   ↓
Detailed Logging
   ↓
Post-Event Review
```

Emergency access should not become a normal administrative mechanism.

---

### 424. Infrastructure Privilege Model

A useful privilege hierarchy is:

```text
Viewer
  ↓
Operator
  ↓
Developer
  ↓
AI Administrator
  ↓
Platform Administrator
  ↓
Security Administrator
```

Roles should be separated according to actual responsibilities.

---

### 425. Separation of Duties

Critical AI infrastructure activities should not necessarily be controlled by one person.

For example:

```text
Developer
   ↓
Build Model

Security
   ↓
Validate Model

Model Owner
   ↓
Approve Model

Platform
   ↓
Deploy Model
```

This reduces the possibility of unauthorized changes reaching production.

---

### 426. AI Infrastructure Network Zones

A mature environment may use multiple security zones:

```text
Internet
   │
   ▼
DMZ
   │
   ▼
Application Zone
   │
   ▼
AI Processing Zone
   │
   ├──── Model Zone
   │
   └──── Data Zone
```

Administrative access can use a separate management path.

---

### 427. Management Network

The management plane should ideally use a controlled management network.

```text
Administrator
      ↓
Management Network
      ↓
Management Services
      ↓
AI Infrastructure
```

Ordinary application traffic should not automatically provide administrative access.

---

### 428. Network Microsegmentation

Within an AI environment, microsegmentation can restrict communication between individual workloads.

```text
AI-App-A
   │
   └──→ Model-Service-A ✓

AI-App-A
   │
   └──→ Model-Service-B ✗
```

This reduces lateral movement.

---

### 429. East-West Traffic

AI infrastructure must consider **east-west traffic**, not only Internet-facing traffic.

East-west traffic includes:

```text
Container → Container
Pod → Pod
Model → Vector DB
Application → Model
AI Service → Storage
```

Monitoring and segmentation should cover these internal communications.

---

### 430. North-South Traffic

North-south traffic represents communication entering or leaving the AI environment.

```text
User
 ↓
Internet / Corporate Network
 ↓
AI Gateway
 ↓
AI Platform
```

Perimeter controls should protect this traffic.

---

### 431. AI Firewall Architecture

Firewalls can enforce communication rules between AI infrastructure zones.

Example:

| Source        | Destination   | Action     |
| ------------- | ------------- | ---------- |
| Internet      | WAF           | Allow      |
| WAF           | API Gateway   | Allow      |
| API Gateway   | AI App        | Allow      |
| AI App        | Model Service | Allow      |
| Model Service | Vector DB     | Allow      |
| Model Service | HR Database   | Deny       |
| AI Workload   | Internet      | Restricted |

The actual rules should be based on business requirements and risk.

---

### 432. AI API Gateway

An API gateway can become a central enforcement point.

```text
Client
  ↓
API Gateway
  ├── Authentication
  ├── Authorization
  ├── Rate Limiting
  ├── Request Validation
  ├── Logging
  └── Routing
  ↓
AI Service
```

This prevents direct exposure of internal AI services where possible.

---

### 433. Rate Limiting

AI infrastructure should enforce limits on requests.

Controls may include:

* Requests per second.
* Requests per minute.
* Concurrent requests.
* Maximum prompt size.
* Maximum response size.
* Maximum tokens.
* Maximum execution time.

Rate limiting supports both security and availability.

---

### 434. AI Gateway Quotas

Different users or applications can receive different quotas.

```text
Executive App
   → High Priority

Employee Assistant
   → Standard

Development Tool
   → Limited

Unknown Client
   → Blocked
```

This can prevent one application from consuming disproportionate resources.

---

### 435. Compute Security

AI compute infrastructure should be hardened.

Controls may include:

* Secure operating-system configuration.
* Patch management.
* Endpoint protection where supported.
* Host firewall.
* Restricted administrative services.
* Secure boot where applicable.
* Hardware security controls.
* Vulnerability scanning.

---

### 436. Host Hardening

A secure AI host should minimize unnecessary services.

```text
AI Host
 ├── Required AI Runtime ✓
 ├── Required GPU Driver ✓
 ├── Required Monitoring ✓
 ├── Required Management Agent ✓
 └── Unnecessary Services ✗
```

Reducing the attack surface makes compromise more difficult.

---

### 437. GPU Driver Security

AI infrastructure depends heavily on GPU drivers.

The security lifecycle should include:

```text
Driver Version
      ↓
Security Review
      ↓
Compatibility Testing
      ↓
Deployment
      ↓
Monitoring
```

Driver updates should be tested against the AI workload before production deployment.

---

### 438. Firmware Security

Infrastructure security should also consider:

* Server firmware.
* GPU firmware.
* BIOS/UEFI.
* Network device firmware.
* Storage-controller firmware.

Firmware compromise can bypass higher-level security controls.

---

### 439. Secure Boot

Where supported and appropriate, secure boot mechanisms can help verify that trusted software components are loaded during system startup.

Conceptually:

```text
Boot
 ↓
Firmware Verification
 ↓
Bootloader Verification
 ↓
Operating System
 ↓
AI Runtime
```

This strengthens infrastructure integrity.

---

### 440. Vulnerability Management

AI infrastructure should be incorporated into the enterprise vulnerability-management program.

Assets may include:

```text
Servers
GPUs
Operating Systems
Containers
Kubernetes
AI Frameworks
Model Servers
APIs
Databases
Vector Stores
Network Devices
```

---

### 441. AI Framework Dependencies

AI infrastructure often depends on frameworks and libraries.

Examples include:

* Machine-learning frameworks.
* Python packages.
* GPU libraries.
* Model-serving libraries.
* Container dependencies.
* Operating-system packages.

A vulnerable dependency can become an infrastructure attack path.

---

### 442. Software Bill of Materials

An SBOM can provide visibility into software components.

```text
AI Container
   ↓
SBOM
   ├── OS Packages
   ├── Python Packages
   ├── AI Libraries
   ├── GPU Libraries
   └── Application Dependencies
```

This improves vulnerability identification and supply-chain visibility.

---

### 443. AI Infrastructure Patch Management

Patch management should distinguish between:

```text
Critical Security Patch
        ↓
Emergency Evaluation

Normal Patch
        ↓
Scheduled Testing

Major Upgrade
        ↓
Full Compatibility Testing
```

AI environments require compatibility testing because infrastructure changes can affect GPU drivers, frameworks, models, and inference performance.

---

### 444. Configuration Management Database

A CMDB or equivalent asset inventory should identify important AI infrastructure.

Potential attributes include:

| Attribute           | Example               |
| ------------------- | --------------------- |
| Asset               | AI Production Cluster |
| Owner               | AI Platform Team      |
| Environment         | Production            |
| Criticality         | High                  |
| Data Classification | Confidential          |
| Location            | Cloud Region          |
| Model               | Approved Model vX     |
| Dependencies        | Vector DB             |
| Recovery Tier       | Tier 1                |

---

### 445. AI Asset Inventory

The inventory should cover more than servers.

```text
AI Infrastructure Inventory
│
├── Compute
├── GPUs
├── Containers
├── Kubernetes Clusters
├── Model Servers
├── Models
├── Model Registries
├── Vector Databases
├── Storage
├── APIs
├── Service Accounts
├── Secrets
└── Network Components
```

Unknown infrastructure creates governance and security blind spots.

---

### 446. Shadow AI Infrastructure

Employees or development teams may deploy AI infrastructure outside approved processes.

Examples:

```text
Unapproved Cloud GPU
Unapproved Model Server
Unapproved Vector DB
Personal AI API
Unapproved Container Registry
```

This creates **shadow AI infrastructure**.

---

### 447. Shadow Infrastructure Detection

Organizations can identify unauthorized infrastructure through:

* Cloud inventory.
* Network discovery.
* DNS monitoring.
* API monitoring.
* Expense analysis.
* Identity logs.
* Container registry monitoring.
* Endpoint telemetry.

The goal is to establish visibility before applying controls.

---

### 448. Cloud AI Infrastructure

Cloud environments introduce additional infrastructure components:

```text
Cloud Account
   ↓
Virtual Network
   ↓
Subnet
   ↓
Compute
   ↓
GPU
   ↓
AI Platform
   ↓
Storage / Database
```

Cloud-native security controls should be integrated into the AI architecture.

---

### 449. Cloud Account Separation

Organizations may separate environments by cloud account or subscription:

```text
Production Account
Development Account
Security Account
Logging Account
```

This can provide stronger administrative and billing boundaries.

---

### 450. Cloud Security Groups

Cloud security groups or equivalent controls can restrict traffic.

Example:

```text
AI Model Server
Inbound:
  API Gateway ✓
  Internet ✗

Outbound:
  Vector DB ✓
  Approved API ✓
  Unrestricted Internet ✗
```

---

### 451. Private AI Endpoints

Where supported, AI services can use private connectivity.

```text
Corporate Network
       ↓
Private Connection
       ↓
Private AI Endpoint
       ↓
AI Service
```

This can reduce exposure to the public Internet.

---

### 452. Cloud Storage Security

AI object storage may contain:

* Training datasets.
* Model artifacts.
* Logs.
* Evaluation data.
* Embeddings.
* Backups.

Controls should include:

```text
IAM
Encryption
Access Logging
Versioning
Retention
Data Classification
Public Access Prevention
```

---

### 453. Public Storage Exposure

A major infrastructure risk occurs when sensitive AI storage becomes publicly accessible.

Weak:

```text
Internet
   ↓
Public Bucket
   ↓
Training Data
```

Stronger:

```text
AI Workload
   ↓
IAM Authorization
   ↓
Private Storage
```

---

### 454. AI Data Residency

Infrastructure architecture should account for where AI data is processed and stored.

Questions include:

* Which country hosts the infrastructure?
* Which cloud region is used?
* Where are backups stored?
* Where are logs stored?
* Where are model artifacts replicated?

Data residency requirements may affect architecture choices.

---

### 455. Multi-Region AI Architecture

For highly available services:

```text
              Global Gateway
                 /      \
                ↓        ↓
          Region A     Region B
             ↓            ↓
          AI Cluster    AI Cluster
             \            /
              \          /
                Data Layer
```

Security policies should remain consistent across regions.

---

### 456. Multi-Tenant AI Infrastructure

A shared AI platform may serve multiple business units or customers.

```text
Shared AI Platform
│
├── Tenant A
├── Tenant B
└── Tenant C
```

Tenant isolation becomes a primary security requirement.

---

### 457. Tenant Isolation Controls

Possible controls include:

* Separate identities.
* Separate namespaces.
* Tenant-specific encryption.
* Tenant-aware authorization.
* Logical data separation.
* Network policies.
* Separate vector indexes.
* Resource quotas.

---

### 458. Cross-Tenant Data Access

The architecture should prevent:

```text
Tenant A Request
      ↓
Tenant B Data
```

even if both tenants use the same AI platform.

Authorization must therefore apply to retrieved data as well as application access.

---

### 459. Infrastructure Monitoring Architecture

A centralized monitoring model can be:

```text
Infrastructure
 │
 ├── Host Logs
 ├── Kubernetes Logs
 ├── Network Logs
 ├── IAM Logs
 ├── API Logs
 ├── Model Logs
 └── Storage Logs
        ↓
Central Logging
        ↓
SIEM / Security Analytics
        ↓
Detection
        ↓
Incident Response
```

---

### 460. Security Detection Use Cases

AI infrastructure monitoring can detect:

* Unauthorized model deployment.
* Unexpected GPU consumption.
* Privilege escalation.
* New container images.
* Unknown outbound destinations.
* Abnormal API activity.
* Unexpected data access.
* Configuration changes.
* Suspicious administrative activity.

---

### 461. Infrastructure Alert Prioritization

Not every event requires the same response.

```text
Low
 ↓
Informational

Medium
 ↓
Investigation

High
 ↓
Security Response

Critical
 ↓
Immediate Containment
```

Alert severity should consider asset criticality and business impact.

---

### 462. AI Infrastructure Incident Response

A compromised AI server may require:

```text
Detection
   ↓
Triage
   ↓
Containment
   ↓
Evidence Collection
   ↓
Eradication
   ↓
Recovery
   ↓
Validation
```

AI-specific considerations may include preserving:

* Model versions.
* Model hashes.
* Container images.
* Prompt logs where appropriate.
* Infrastructure configuration.
* Identity events.
* Network connections.

---

### 463. Infrastructure Isolation During Incident

A compromised AI workload may be isolated without immediately shutting down the entire platform.

```text
AI Cluster
│
├── Workload A ✓
├── Workload B ✓
└── Workload C ⚠
       ↓
   Quarantine
```

Segmentation supports more precise containment.

---

### 464. Model Quarantine

If a model artifact is suspected of compromise:

```text
Production Model
       ↓
Security Alert
       ↓
Model Quarantine
       ↓
Integrity Analysis
       ↓
Approved Version
       ↓
Recovery
```

This prevents continued use of a potentially compromised artifact.

---

### 465. Infrastructure Recovery Validation

Recovery should verify:

```text
✓ Correct infrastructure configuration
✓ Correct model version
✓ Correct container image
✓ Correct identity policies
✓ Correct network rules
✓ Correct secrets
✓ Correct data connections
✓ Logging restored
✓ Security monitoring restored
```

Recovery is not complete merely because the service becomes available.

---

### 466. AI Infrastructure Continuous Compliance

Infrastructure compliance should be continuous rather than performed only during annual audits.

```text
Deploy
 ↓
Assess
 ↓
Monitor
 ↓
Detect Drift
 ↓
Remediate
 ↓
Reassess
```

This creates an ongoing control lifecycle.

---

### 467. Policy-as-Code

Infrastructure policies can be automatically evaluated.

Example:

```text
IF
AI workload is production
AND
Internet exposure = true

THEN
FAIL POLICY
```

Other rules can check:

```text
Encryption enabled?
Logging enabled?
Approved image?
Approved region?
Required identity?
Network segmentation?
```

---

### 468. Automated Security Gates

Infrastructure deployment can include automated security gates:

```text
Infrastructure Code
       ↓
Policy Check
       ↓
Security Scan
       ↓
Compliance Check
       ↓
Approval
       ↓
Deployment
```

This prevents known violations from reaching production.

---

### 469. Infrastructure Compliance Dashboard

A governance dashboard may track:

| Metric                    | Example |
| ------------------------- | ------: |
| AI assets inventoried     |     98% |
| Encrypted workloads       |    100% |
| Approved container images |     97% |
| Critical vulnerabilities  |       2 |
| Unrestricted AI endpoints |       0 |
| Logging coverage          |     99% |
| Configuration compliance  |     96% |

These metrics translate technical posture into governance visibility.

---

### 470. AI Infrastructure Risk Indicators

Useful KRIs may include:

* Number of exposed AI endpoints.
* Number of unsupported AI hosts.
* Number of critical vulnerabilities.
* Number of unauthorized models.
* Number of privileged AI accounts.
* Number of unapproved containers.
* Number of configuration violations.
* Number of shadow AI assets.
* Number of security incidents.

---

### 471. Infrastructure Ownership

Every major infrastructure component should have a defined owner.

```text
GPU Cluster
→ Infrastructure Owner

Model Platform
→ AI Platform Owner

Vector Database
→ Data Platform Owner

Network
→ Network Security Owner

Identity
→ IAM Owner

Security Monitoring
→ SOC Owner
```

Ownership prevents control gaps.

---

### 472. Infrastructure RACI

A simplified RACI model can define responsibilities:

| Activity          | AI Team | Infrastructure | Security | GRC |
| ----------------- | ------- | -------------- | -------- | --- |
| Architecture      | R       | R              | C        | C   |
| Deployment        | R       | R              | C        | I   |
| Security Controls | C       | R              | A        | C   |
| Risk Assessment   | C       | C              | R        | A   |
| Monitoring        | C       | R              | A        | I   |
| Compliance        | I       | C              | R        | A   |
| Audit Evidence    | C       | R              | R        | A   |

The exact model should reflect organizational responsibilities.

---

### 473. Infrastructure Security Exceptions

Sometimes a required security baseline cannot be implemented immediately.

An exception process should document:

```text
Requirement
   ↓
Reason for Exception
   ↓
Risk
   ↓
Compensating Control
   ↓
Risk Owner
   ↓
Expiration Date
   ↓
Review
```

Exceptions should be temporary and governed.

---

### 474. Compensating Controls

If a direct control is unavailable, another control may reduce the risk.

Example:

```text
Requirement:
Private AI Endpoint

Unavailable:
Legacy Platform

Compensating Controls:
Strong Firewall
Strict Allowlisting
MFA
Enhanced Monitoring
DLP
```

The residual risk should still be assessed.

---

### 475. Infrastructure Security Testing

Testing should validate whether controls actually work.

Testing may include:

```text
Vulnerability Scanning
Configuration Assessment
Penetration Testing
Container Testing
Cloud Security Assessment
Network Testing
IAM Review
Recovery Testing
```

---

### 476. AI Infrastructure Penetration Testing

Testing should consider infrastructure-specific attack paths:

```text
Internet
 ↓
API Gateway
 ↓
AI Application
 ↓
Container
 ↓
Host
 ↓
Cluster
 ↓
Cloud Account
```

The objective is to determine whether compromise of one component can lead to broader infrastructure compromise.

---

### 477. Infrastructure Threat Modeling

Threat modeling should identify:

```text
Assets
 ↓
Trust Boundaries
 ↓
Threat Actors
 ↓
Attack Paths
 ↓
Security Controls
 ↓
Residual Risk
```

AI infrastructure threat modeling should include both traditional infrastructure threats and AI-specific attack paths.

---

### 478. Infrastructure Architecture Review

Before production deployment, security architecture review should examine:

**Architecture**

* Is the design appropriately segmented?

**Identity**

* Are identities least-privileged?

**Network**

* Is unnecessary connectivity blocked?

**Compute**

* Are hosts and GPUs hardened?

**Containers**

* Are images trusted?

**Models**

* Is model integrity protected?

**Data**

* Is sensitive data appropriately isolated?

**Monitoring**

* Are critical events visible?

**Resilience**

* Can the platform recover?

---

### 479. AI Infrastructure Security Scorecard

A practical assessment can use:

```text
Identity              █████████░ 90%
Network Segmentation  ████████░░ 80%
Compute Security      █████████░ 90%
Container Security    ████████░░ 80%
Model Integrity       ███████░░░ 70%
Data Protection       █████████░ 90%
Monitoring            ████████░░ 80%
Resilience            ███████░░░ 70%
```

The score should identify weaknesses rather than simply produce a compliance percentage.

---

### 480. Infrastructure Risk-Based Prioritization

Not every infrastructure component requires identical controls.

A useful prioritization model is:

```text
Risk =
Likelihood × Impact
```

Factors may include:

* Data sensitivity.
* AI business criticality.
* Internet exposure.
* Privilege level.
* Number of dependent applications.
* Regulatory impact.
* Recovery requirements.

---

### 481. Critical AI Infrastructure

Critical infrastructure components may include:

```text
Production Model Serving
Central AI Gateway
Identity Provider
Model Registry
Vector Database
Production GPU Cluster
Key Management System
Secrets Manager
```

Failure or compromise of these components can affect many AI services simultaneously.

---

### 482. Blast Radius

Architecture should minimize blast radius.

Weak:

```text
One Compromised AI Workload
          ↓
Entire AI Platform
```

Stronger:

```text
One Compromised AI Workload
          ↓
Restricted Segment
          ↓
Limited Resources
```

Segmentation, least privilege, and workload isolation are major blast-radius reduction mechanisms.

---

### 483. Defense in Depth

A secure AI infrastructure should assume that individual controls can fail.

For example:

```text
Authentication
     ↓
Authorization
     ↓
Network Segmentation
     ↓
Workload Isolation
     ↓
Data Authorization
     ↓
Monitoring
```

If one control is bypassed, other controls should continue limiting the attack.

---

### 484. Secure-by-Default AI Infrastructure

The default configuration should be restrictive.

Examples:

```text
Default Network = Deny
Default Storage = Private
Default API = Authenticated
Default Container = Non-Privileged
Default Account = Least Privilege
Default Logging = Enabled
Default Encryption = Enabled
Default Egress = Restricted
```

Security should not depend on administrators remembering to activate every control manually.

---

### 485. AI Infrastructure Architecture Principle

A useful architectural principle is:

> **Every AI infrastructure component should have a defined identity, trust boundary, owner, security baseline, permitted communication paths, monitoring requirements, and recovery strategy.**

This turns infrastructure architecture from a collection of technical components into a governed security system.

