# 17.10 — AI Model Compromise

## Part 1 — AI Model Compromise Foundations, Compromise Concepts, Attack Surface, Integrity and Governance

### 1. AI Model Compromise

**AI Model Compromise** occurs when an unauthorized party gains the ability to modify, replace, manipulate, control, or otherwise undermine the security or trustworthy operation of an AI model or its associated model assets.

A compromise may involve:

- model artifacts;
- model weights;
- configuration;
- deployment;
- inference infrastructure;
- model access;
- model-serving components;
- model repositories;
- model supply chains.

Model compromise should be distinguished from ordinary model failure, model drift, or poor model performance.

### 2. Objective of AI Model Compromise Management

The objective is to prevent, detect, contain, investigate, remediate, and recover from unauthorized changes or control affecting AI models.

The capability should address:

- model integrity;
- model authenticity;
- model access;
- model provenance;
- deployment integrity;
- supply-chain security;
- credential security;
- behavioral validation;
- evidence;
- recovery.

### 3. AI Model

An **AI Model** is a computational artifact that has been developed, trained, configured, or otherwise prepared to perform AI-related inference, prediction, generation, classification, reasoning, or decision-support functions.

Depending on the architecture, the model environment may include:

- weights;
- architecture;
- tokenizer;
- configuration;
- adapters;
- prompts;
- retrieval components;
- inference runtime.

### 4. AI Model Asset

An AI Model Asset is any component required to create, deploy, operate, reproduce, or validate an AI model.

Examples include:

- model weights;
- checkpoints;
- configuration;
- tokenizer;
- adapters;
- training artifacts;
- model container;
- inference runtime;
- model metadata.

Protecting only the model weights may therefore be insufficient.

### 5. AI Model Compromise Event

A Model Compromise Event is an observed condition indicating that a model or model-related asset may have been unauthorizedly modified, replaced, accessed, or controlled.

Examples include:

- unexpected model hash change;
- unauthorized model upload;
- unexplained model-version change;
- suspicious repository activity;
- unauthorized deployment.

An event is not automatically a confirmed compromise.

### 6. AI Model Compromise Incident

A Model Compromise Incident is a confirmed or sufficiently credible security condition involving unauthorized modification, replacement, access, control, or manipulation of a model or its trusted model environment that meets organizational incident criteria.

The distinction between event and incident should be defined by organizational incident-management procedures.

### 7. AI Model Compromise versus AI Model Failure

A **model failure** concerns unacceptable or unexpected behavior.

A **model compromise** concerns unauthorized influence, modification, replacement, access, or control.

A compromised model may continue to perform normally under ordinary tests while containing malicious behavior.

Conversely, a model can fail without being compromised.

### 8. AI Model Compromise versus AI Model Drift

Model drift generally concerns changes in model performance or behavior over time.

Compromise concerns unauthorized security-relevant changes or control.

Drift can therefore occur without compromise, while compromise can exist without immediately observable drift.

### 9. AI Model Compromise versus Model Artifact Corruption

Artifact corruption may result from:

- accidental damage;
- storage failure;
- transmission errors;
- unauthorized modification.

Corruption alone does not establish malicious compromise.

Investigation should determine whether the integrity failure resulted from an attack, operational failure, or another cause.

### 10. AI Model Compromise versus Model Theft

Model theft concerns unauthorized acquisition or copying of a model or model-related intellectual property.

Model compromise concerns unauthorized modification, control, or manipulation.

They can occur independently or together.

### 11. AI Model Compromise versus Model Extraction

Model extraction attempts to reconstruct information about a model through repeated interaction.

Model compromise generally involves unauthorized influence over the model or its trusted operating environment.

An attacker may perform extraction without compromising the model itself.

### 12. AI Model Compromise versus Data Poisoning

Data poisoning manipulates data used to influence model training, fine-tuning, evaluation, or operation.

Model compromise directly targets the model or its trusted artifacts or environment.

The two can be connected:

```text id="d5q8mv"
POISONED DATA
      |
      v
TRAINING / FINE-TUNING
      |
      v
MODEL BEHAVIOR
      |
      v
MODEL ARTIFACT
```

A poisoned training process does not automatically prove that the final model artifact was maliciously modified.

### 13. AI Model Compromise versus Supply-Chain Compromise

Supply-chain compromise occurs when an upstream component, supplier, repository, dependency, or delivery mechanism is compromised.

Model compromise may result from such an attack.

For example:

```text id="n7r3kx"
UPSTREAM SUPPLIER
       |
       v
MODEL REPOSITORY
       |
       v
MODEL ARTIFACT
       |
       v
DEPLOYMENT
       |
       v
PRODUCTION AI
```

The organization's model may therefore be compromised without a direct attack against its production environment.

### 14. AI Model Compromise Attack Surface

The model-compromise attack surface includes all components through which an attacker could alter or control a model.

Potential surfaces include:

- model repositories;
- artifact registries;
- CI/CD pipelines;
- training infrastructure;
- fine-tuning infrastructure;
- model-serving platforms;
- cloud storage;
- developer environments;
- administrative interfaces;
- model APIs;
- deployment systems.

### 15. AI Model Compromise Trust Boundary

A Trust Boundary identifies where model artifacts or model-control information move between environments with different trust assumptions.

Examples include:

- external repository to enterprise repository;
- development to production;
- supplier to organization;
- training environment to serving environment.

Each boundary should have appropriate:

- authentication;
- authorization;
- integrity verification;
- provenance;
- monitoring.

### 16. AI Model Compromise Model Artifact

A Model Artifact is a deployable or reproducible representation of a model.

It may contain:

- weights;
- configuration;
- architecture;
- tokenizer;
- metadata;
- adapters;
- dependencies.

Artifact governance should establish which components are considered security-critical.

### 17. AI Model Compromise Model Weights

Model weights contain learned parameters used during model inference.

Unauthorized modification of weights can potentially alter model behavior without changing the surrounding application.

Weight integrity should therefore be protected and verified.

### 18. AI Model Compromise Model Configuration

Model configuration defines parameters governing how a model is loaded or operated.

Examples may include:

- architecture settings;
- inference parameters;
- context limits;
- adapters;
- safety configurations;
- runtime settings.

A secure model requires protection of configuration as well as weights.

### 19. AI Model Compromise Model Adapter

Adapters, including parameter-efficient fine-tuning components, can materially change model behavior.

Unauthorized adapter modification may therefore create compromise even when the underlying base model remains unchanged.

### 20. AI Model Compromise Model Checkpoint

A checkpoint represents a stored model state.

Checkpoints may be:

- training checkpoints;
- fine-tuning checkpoints;
- evaluation checkpoints;
- production checkpoints.

Unauthorized checkpoint substitution or modification can introduce security risk.

### 21. AI Model Compromise Model Repository

A Model Repository stores or distributes model artifacts.

Security controls should address:

- access;
- authentication;
- authorization;
- integrity;
- versioning;
- provenance;
- change management.

### 22. AI Model Compromise Artifact Registry

An Artifact Registry stores approved model artifacts for controlled deployment.

It should distinguish:

- uploaded artifacts;
- validated artifacts;
- approved artifacts;
- production artifacts.

### 23. AI Model Compromise Model Provenance

Model Provenance records where a model originated and how it was developed, modified, transferred, or approved.

Provenance may include:

- source;
- publisher;
- version;
- build;
- training lineage;
- fine-tuning;
- transfer history.

Provenance does not by itself prove that a model is secure.

### 24. AI Model Compromise Model Authenticity

Model Authenticity concerns whether an artifact is genuinely associated with its claimed source or publisher.

Authentication mechanisms may include:

- digital signatures;
- trusted repositories;
- certificates;
- verified publisher identity.

Authenticity does not establish that the model is safe or free from vulnerabilities.

### 25. AI Model Compromise Model Integrity

Model Integrity concerns whether a model artifact remains unchanged from its approved state, or whether changes are authorized and controlled.

Integrity mechanisms may include:

- cryptographic hashes;
- signatures;
- immutable storage;
- version control;
- access logging.

Integrity verification does not establish behavioral correctness.

### 26. AI Model Compromise Behavioral Integrity

Behavioral Integrity concerns whether the model behaves consistently with its approved security and functional expectations.

A model may pass a hash check while exhibiting malicious or unexpected behavior because:

- the approved artifact was already compromised;
- the wrong artifact was approved;
- malicious behavior is conditional;
- the compromise affects runtime dependencies.

### 27. AI Model Compromise Cryptographic Integrity

Cryptographic Integrity uses cryptographic mechanisms to detect unauthorized artifact modification.

Examples include:

- SHA-family hashes;
- digital signatures;
- signed manifests.

Cryptographic integrity establishes evidence about artifact changes, not overall trustworthiness.

### 28. AI Model Compromise Signature Verification

Signature Verification confirms whether an artifact has a valid digital signature from a trusted signing authority.

Verification should establish:

- signer identity;
- trust chain;
- signature validity;
- artifact association;
- key status.

A valid signature does not prove that the signed artifact is free from malicious functionality.

### 29. AI Model Compromise Hash Verification

Hash Verification compares an artifact's cryptographic digest against an approved reference.

A mismatch may indicate:

- unauthorized modification;
- corruption;
- version change;
- replacement.

A matching hash does not prove that the artifact is safe.

### 30. AI Model Compromise Trusted Model State

A Trusted Model State is a model state for which the organization has sufficient evidence of:

- provenance;
- integrity;
- authorization;
- configuration;
- validation;
- deployment status.

Trusted state should be explicitly defined for critical models.

### 31. AI Model Compromise Model Identity

Model Identity uniquely identifies a model artifact or approved model state.

Identity may include:

- model name;
- version;
- digest;
- publisher;
- artifact identifier;
- environment.

Model identity should be sufficiently precise to prevent ambiguity between similar artifacts.

### 32. AI Model Compromise Model Version

A Model Version identifies a specific release or state of a model.

Security governance should distinguish:

- development;
- testing;
- approved;
- production;
- retired

versions.

### 33. AI Model Compromise Model Version Substitution

Version Substitution occurs when an attacker or unauthorized process replaces an expected model with another artifact.

Potential causes include:

- compromised registry;
- stolen credentials;
- malicious deployment;
- pipeline compromise.

### 34. AI Model Compromise Unauthorized Model Modification

Unauthorized Model Modification occurs when model weights, configuration, adapters, or other model components are changed without approved authorization.

### 35. AI Model Compromise Unauthorized Model Replacement

Unauthorized Model Replacement occurs when an approved model is replaced by another model without appropriate authorization.

This can be more difficult to detect when:

- model names remain similar;
- version identifiers are manipulated;
- repositories are trusted incorrectly.

### 36. AI Model Compromise Unauthorized Model Access

Unauthorized Model Access occurs when an actor gains access to model artifacts or model-management capabilities without appropriate authorization.

Access should be distinguished from successful modification.

### 37. AI Model Compromise Unauthorized Model Control

Unauthorized Model Control occurs when an actor gains sufficient privileges to alter model deployment, configuration, version, or behavior.

### 38. AI Model Compromise Credential Compromise

Credential Compromise occurs when credentials used to access or manage model assets are exposed or misused.

Relevant credentials may include:

- repository credentials;
- cloud identities;
- CI/CD credentials;
- deployment keys;
- API tokens.

### 39. AI Model Compromise Privilege Escalation

Privilege Escalation occurs when an attacker gains permissions beyond those originally authorized.

Potential consequences include:

- model modification;
- deployment;
- artifact deletion;
- configuration changes;
- access to sensitive model assets.

### 40. AI Model Compromise Deployment Integrity

Deployment Integrity concerns whether the model deployed into production is the authorized artifact and configuration.

A secure deployment process should establish:

```text id="w8m4qt"
APPROVED MODEL
      |
      v
INTEGRITY VERIFICATION
      |
      v
AUTHORIZED DEPLOYMENT
      |
      v
RUNTIME VALIDATION
      |
      v
PRODUCTION
```

### 41. AI Model Compromise Runtime Integrity

Runtime Integrity concerns whether the model and its execution environment remain in an approved state while operating.

Relevant components may include:

- model files;
- containers;
- runtime libraries;
- configuration;
- infrastructure;
- model-serving process.

### 42. AI Model Compromise Model-Serving Integrity

Model-Serving Integrity protects the infrastructure responsible for loading and serving the model.

Potential controls include:

- hardened runtime;
- access control;
- image integrity;
- process monitoring;
- configuration management.

### 43. AI Model Compromise Training-Environment Integrity

Training-Environment Integrity protects the infrastructure and artifacts used to create models.

It should address:

- training code;
- datasets;
- checkpoints;
- credentials;
- dependencies;
- compute infrastructure.

### 44. AI Model Compromise Fine-Tuning Integrity

Fine-Tuning Integrity protects the process used to modify an existing model.

Relevant components include:

- base model;
- fine-tuning data;
- adapters;
- training configuration;
- output artifacts.

### 45. AI Model Compromise CI/CD Integrity

CI/CD Integrity ensures that model build and deployment pipelines execute authorized code and promote authorized artifacts.

Potential controls include:

- protected branches;
- signed commits;
- controlled runners;
- artifact verification;
- deployment approval.

### 46. AI Model Compromise Supply-Chain Integrity

Supply-Chain Integrity ensures that model components obtained from external sources are appropriately verified before organizational use.

Verification may consider:

- provenance;
- publisher;
- integrity;
- dependencies;
- vulnerabilities;
- security testing.

### 47. AI Model Compromise Evidence

Relevant evidence may include:

- model hashes;
- signatures;
- repository logs;
- access logs;
- deployment records;
- CI/CD logs;
- configuration history;
- credentials;
- model behavior;
- network telemetry.

Evidence should be protected against unauthorized alteration.

### 48. AI Model Compromise Timeline

A Model Compromise Timeline should reconstruct:

```text id="y4q9mv"
INITIAL ACCESS
      |
      v
CREDENTIAL / CONTROL COMPROMISE
      |
      v
MODEL ACCESS
      |
      v
MODIFICATION / REPLACEMENT
      |
      v
DEPLOYMENT
      |
      v
RUNTIME
      |
      v
DETECTION
      |
      v
CONTAINMENT
      |
      v
RECOVERY
```

The timeline should distinguish confirmed evidence from investigative hypotheses.

### 49. AI Model Compromise Governance Principle

**AI model compromise should be governed as an integrity, authorization, supply-chain, and runtime-security problem rather than merely as a model-performance problem.**

Effective governance should address:

- model identity;
- provenance;
- authenticity;
- artifact integrity;
- access control;
- deployment integrity;
- runtime integrity;
- supply-chain security;
- behavioral validation;
- incident response.

### 50. Core Principle of AI Model Compromise

**An AI model should not be considered trustworthy merely because it is available, correctly named, cryptographically intact, or obtained from a recognized source. Trust should be established through appropriate evidence covering provenance, integrity, authorization, deployment, runtime state, and behavioral expectations.**

Organizations should maintain clear separation between:

- model failure and model compromise;
- model drift and unauthorized modification;
- model theft and model compromise;
- data poisoning and model compromise;
- provenance and trustworthiness;
- authenticity and security;
- cryptographic integrity and behavioral integrity;
- model identity and model safety;
- artifact integrity and runtime integrity;
- supplier assurance and independent assurance;
- model availability and model trust;
- internal governance controls and legal obligations.

Internal `AI-IMR-*` identifiers are knowledge-base taxonomy identifiers. They are not statutory provisions, regulatory article numbers, certification requirements, or legal obligations. Where a model-compromise-related requirement is legally mandatory, the knowledge base should separately identify the authoritative source, applicable jurisdiction, organizational role, affected activity, effective date, and specific requirement.

## Part 2 — AI Model Compromise Controls, Detection, Assessment, Containment, Investigation and Remediation

### 51. AI Model Compromise Management Policy

An **AI Model Compromise Management Policy** establishes organizational requirements for preventing, detecting, investigating, containing, remediating, and recovering from unauthorized model modification or control.

The policy should define:

- scope;
- ownership;
- model criticality;
- approved repositories;
- integrity requirements;
- access requirements;
- incident escalation;
- evidence requirements;
- recovery requirements.

The policy is an internal governance control unless a specific legal, regulatory, contractual, or authoritative requirement establishes a corresponding obligation.

### 52. AI Model Compromise Scope

The scope should cover the model lifecycle and supporting environments, including:

- training;
- fine-tuning;
- evaluation;
- model registration;
- artifact storage;
- deployment;
- serving;
- monitoring;
- retirement.

The scope should also identify relevant third-party model services and dependencies.

### 53. AI Model Compromise Model Register

A **Model Register** provides an authoritative inventory of models used by the organization.

Typical fields include:

- model identifier;
- version;
- owner;
- source;
- repository;
- environment;
- business purpose;
- criticality;
- deployment status;
- integrity reference;
- supplier;
- dependencies.

An incomplete model inventory creates a governance blind spot.

### 54. AI Model Compromise Approved Model Baseline

An **Approved Model Baseline** establishes the known-good state against which deployed or stored models can be compared.

The baseline may include:

- artifact digest;
- signature;
- version;
- configuration;
- dependencies;
- runtime image;
- approved tools;
- expected behavior.

### 55. AI Model Compromise Artifact Hash Control

Artifact Hash Control records cryptographic hashes for approved model artifacts.

A hash mismatch should generate investigation rather than automatically being treated as proof of malicious compromise.

Possible explanations include:

- authorized update;
- corruption;
- version change;
- packaging difference;
- unauthorized modification.

### 56. AI Model Compromise Digital-Signature Control

Digital-Signature Control verifies that model artifacts are signed by an appropriately trusted signing authority.

Verification should include:

- signer identity;
- certificate or key status;
- signature validity;
- artifact association;
- trust chain.

### 57. AI Model Compromise Trusted-Repository Control

A **Trusted-Repository Control** restricts model acquisition and deployment to approved repositories or explicitly authorized sources.

Repository trust should consider:

- publisher identity;
- access controls;
- integrity;
- provenance;
- security history;
- organizational approval.

### 58. AI Model Compromise Repository Access Control

Repository Access Control limits who can:

- upload;
- modify;
- delete;
- approve;
- promote;
- retrieve

model artifacts.

Access should follow least-privilege principles.

### 59. AI Model Compromise Model Approval Control

Model Approval Control establishes authorization before a model enters an approved or production state.

Approval may consider:

- provenance;
- integrity;
- security testing;
- model evaluation;
- licensing;
- dependencies;
- intended use.

### 60. AI Model Compromise Model Promotion Control

Model Promotion Control governs movement between environments.

```text id="a7m4qx"
DEVELOPMENT
     |
     v
SECURITY / EVALUATION
     |
     v
APPROVAL
     |
     v
STAGING
     |
     v
PRODUCTION
```

Promotion should preserve model identity and integrity evidence.

### 61. AI Model Compromise Deployment Authorization

Deployment Authorization ensures that only approved personnel, pipelines, or automation can deploy a model.

Authorization should consider:

- identity;
- role;
- environment;
- model criticality;
- deployment context.

### 62. AI Model Compromise Least-Privilege Control

Least Privilege limits access to model artifacts and model-management functions to the minimum necessary permissions.

Separate permissions may be appropriate for:

- reading;
- uploading;
- modifying;
- approving;
- deploying;
- deleting.

### 63. AI Model Compromise Segregation of Duties

Segregation of Duties reduces the risk that one identity can independently:

1. modify a model;
2. approve the model;
3. deploy the model.

For critical models, these activities should generally be separated where practical.

### 64. AI Model Compromise Privileged-Access Management

Privileged-access management should protect highly sensitive model-management accounts.

Controls may include:

- just-in-time access;
- strong authentication;
- session monitoring;
- privileged approval;
- credential rotation.

### 65. AI Model Compromise Credential Protection

Credentials used to access model repositories, pipelines, and deployment systems should be protected against:

- theft;
- reuse;
- unauthorized sharing;
- long-term exposure.

### 66. AI Model Compromise Secret Management

Secrets associated with model infrastructure should be managed through controlled secret-management mechanisms.

Examples include:

- API keys;
- signing keys;
- repository credentials;
- deployment credentials;
- cloud service identities.

### 67. AI Model Compromise Signing-Key Protection

Signing keys used to establish model authenticity should receive enhanced protection.

Compromise of a trusted signing key can undermine confidence in otherwise valid signatures.

### 68. AI Model Compromise Immutable Artifact Storage

Immutable storage can prevent unauthorized modification of approved model artifacts.

It may support:

- version preservation;
- rollback;
- forensic investigation;
- trusted recovery.

### 69. AI Model Compromise Model-Version Control

Version Control ensures that every approved model state has a unique and traceable identity.

A version record should associate the model with:

- digest;
- source;
- approval;
- deployment;
- change history.

### 70. AI Model Compromise Model Change Control

Model Change Control requires authorization and evidence for changes to:

- weights;
- adapters;
- configuration;
- architecture;
- dependencies;
- deployment parameters.

### 71. AI Model Compromise Model Integrity Monitoring

Integrity Monitoring continuously or periodically checks whether model artifacts differ from approved baselines.

Monitoring frequency should be proportional to:

- criticality;
- exposure;
- change frequency;
- threat environment.

### 72. AI Model Compromise Configuration Integrity Monitoring

Configuration monitoring should detect unauthorized changes to:

- model parameters;
- serving configuration;
- tool permissions;
- environment variables;
- security settings.

### 73. AI Model Compromise Deployment Integrity Monitoring

Deployment monitoring verifies that the model running in production corresponds to the approved artifact.

### 74. AI Model Compromise Runtime Integrity Monitoring

Runtime monitoring assesses whether the serving environment remains consistent with the approved state.

Potential indicators include:

- unexpected processes;
- unauthorized file changes;
- unexpected network connections;
- runtime configuration changes.

### 75. AI Model Compromise Repository Activity Monitoring

Repository monitoring can identify:

- unusual uploads;
- unexpected downloads;
- permission changes;
- deletion activity;
- unusual access locations;
- suspicious administrative actions.

### 76. AI Model Compromise CI/CD Monitoring

CI/CD monitoring should detect unauthorized changes involving:

- source code;
- pipeline definitions;
- build runners;
- model artifacts;
- deployment stages;
- credentials.

### 77. AI Model Compromise Model Deployment Alert

A deployment alert should be generated when model deployment activity violates defined expectations.

Potential triggers include:

- unapproved version;
- unexpected deployment time;
- unauthorized identity;
- unexpected environment;
- failed signature verification.

### 78. AI Model Compromise Integrity Alert Validation

An integrity alert should be validated before incident classification.

Validation may compare:

- change records;
- deployment records;
- approved versions;
- repository history;
- administrative activity.

### 79. AI Model Compromise Behavioral Monitoring

Behavioral Monitoring evaluates whether model behavior deviates materially from approved expectations.

Possible signals include:

- unexpected outputs;
- abnormal refusal behavior;
- unusual tool interactions;
- unexplained security-policy deviations.

Behavioral deviation does not by itself prove model compromise.

### 80. AI Model Compromise Behavioral Baseline

A Behavioral Baseline establishes expected model behavior for relevant use cases.

The baseline may include:

- expected output characteristics;
- refusal behavior;
- tool-use patterns;
- security constraints;
- performance ranges.

### 81. AI Model Compromise Model-Behavior Differential Testing

Differential Testing compares the behavior of:

- current production model;
- approved baseline;
- previous version;
- independently validated reference.

Significant unexplained differences should trigger investigation.

### 82. AI Model Compromise Canary Testing

Canary Testing introduces a model to a limited environment before broad deployment.

This can provide an opportunity to identify:

- behavioral anomalies;
- security-control bypasses;
- unexpected tool behavior;
- deployment issues.

### 83. AI Model Compromise Pre-Deployment Security Testing

Security testing should assess the model and its deployment environment before production use.

Potential testing includes:

- artifact integrity;
- dependency security;
- adversarial evaluation;
- prompt-injection testing;
- behavioral testing;
- access-control validation.

### 84. AI Model Compromise Model Quarantine

Model Quarantine isolates a suspected compromised model from production use while preserving relevant evidence.

Quarantine may include:

- disabling inference;
- preventing further deployment;
- restricting repository access;
- isolating the artifact.

### 85. AI Model Compromise Artifact Quarantine

Artifact Quarantine prevents a suspected model artifact from being promoted or redistributed.

The original artifact should be preserved where required for investigation.

### 86. AI Model Compromise Deployment Quarantine

Deployment Quarantine prevents a potentially compromised model from being deployed to additional environments.

### 87. AI Model Compromise Repository Quarantine

Repository Quarantine may restrict access to affected artifacts or repositories when compromise of the repository itself is suspected.

### 88. AI Model Compromise Credential Revocation

Credentials suspected of enabling unauthorized model access should be revoked or rotated according to incident procedures.

Affected credentials may include:

- repository tokens;
- cloud credentials;
- deployment keys;
- CI/CD identities.

### 89. AI Model Compromise Session Revocation

Active sessions associated with suspected compromise may be terminated.

This can limit continued unauthorized access.

### 90. AI Model Compromise Network Isolation

Network Isolation may restrict communication between compromised model infrastructure and other systems.

Isolation should consider evidence preservation and business continuity.

### 91. AI Model Compromise Pipeline Suspension

A compromised model-development or deployment pipeline may need to be suspended to prevent further propagation.

### 92. AI Model Compromise Promotion Suspension

Model promotion should be suspended when:

- integrity cannot be established;
- provenance is uncertain;
- signing infrastructure is compromised;
- unauthorized changes are suspected.

### 93. AI Model Compromise Tool Disablement

Where a compromised model has access to external tools, selected tools may be disabled to reduce potential impact.

### 94. AI Model Compromise Containment Validation

Containment should be validated rather than assumed.

Validation may confirm that:

- unauthorized access has stopped;
- affected artifacts are isolated;
- deployment has ceased;
- credentials are revoked;
- propagation paths are controlled.

### 95. AI Model Compromise Evidence Preservation

Evidence should be preserved before destructive remediation where practical.

Potential evidence includes:

- model artifacts;
- hashes;
- signatures;
- repository records;
- access logs;
- deployment logs;
- CI/CD records;
- network telemetry.

### 96. AI Model Compromise Artifact Preservation

The suspected artifact should be preserved in a controlled manner so investigators can determine:

- what changed;
- when it changed;
- how it changed;
- whether the change was authorized.

### 97. AI Model Compromise Model-Memory Preservation

Where applicable, relevant model-serving memory, runtime state, or temporary artifacts should be preserved when they may provide evidence of compromise.

### 98. AI Model Compromise Configuration Preservation

Configuration at the time of suspected compromise should be preserved before remediation changes overwrite relevant evidence.

### 99. AI Model Compromise Deployment-State Preservation

The deployed state should be recorded, including:

- model version;
- digest;
- runtime image;
- configuration;
- environment;
- deployment identity.

### 100. AI Model Compromise Evidence Integrity

Evidence should itself be protected against unauthorized modification.

Controls may include:

- cryptographic hashing;
- restricted access;
- immutable storage;
- chain-of-custody records.

### 101. AI Model Compromise Timeline Reconstruction

Investigators should reconstruct:

```text id="c9r5vk"
ACCESS
  |
  v
AUTHENTICATION
  |
  v
PRIVILEGE CHANGE
  |
  v
MODEL ACCESS
  |
  v
MODEL MODIFICATION
  |
  v
DEPLOYMENT
  |
  v
OBSERVED BEHAVIOR
  |
  v
DETECTION
```

Known facts should be distinguished from assumptions.

### 102. AI Model Compromise Root-Cause Analysis

Root-Cause Analysis determines why the compromise was possible.

Potential causes include:

- excessive privilege;
- stolen credentials;
- vulnerable pipeline;
- compromised supplier;
- weak repository security;
- inadequate change control.

### 103. AI Model Compromise Contributing-Factor Analysis

Contributing factors may include:

- insufficient monitoring;
- undocumented dependencies;
- weak segregation of duties;
- outdated infrastructure;
- inadequate testing;
- poor evidence retention.

Contributing factors should not automatically be treated as root causes.

### 104. AI Model Compromise Attack-Path Reconstruction

Attack-Path Reconstruction identifies how an attacker moved from initial access to model compromise.

```text id="p6m2qw"
INITIAL ACCESS
      |
      v
IDENTITY COMPROMISE
      |
      v
PRIVILEGE ESCALATION
      |
      v
MODEL REPOSITORY
      |
      v
MODEL MODIFICATION
      |
      v
DEPLOYMENT
      |
      v
PRODUCTION IMPACT
```

### 105. AI Model Compromise Access-Log Correlation

Access logs should be correlated across:

- identity systems;
- repositories;
- cloud platforms;
- CI/CD;
- deployment platforms;
- model-serving infrastructure.

### 106. AI Model Compromise Deployment-Log Correlation

Deployment logs should be correlated with:

- approved change requests;
- model versions;
- artifact hashes;
- deployment identities;
- timestamps.

### 107. AI Model Compromise Repository-Log Correlation

Repository activity should be compared against expected development and release activity.

Unexpected changes should be investigated.

### 108. AI Model Compromise Credential-Use Correlation

Credential use should be correlated with:

- identity;
- location;
- device;
- time;
- action;
- privilege.

### 109. AI Model Compromise Model-Version Correlation

Investigators should determine which model version was:

- approved;
- deployed;
- accessed;
- modified;
- active during the suspected incident.

### 110. AI Model Compromise Configuration Correlation

Configuration changes should be correlated with model changes to determine whether behavior could have resulted from configuration rather than model modification.

### 111. AI Model Compromise Dependency Analysis

Dependency Analysis evaluates whether compromise extended beyond the model itself.

Dependencies may include:

- libraries;
- containers;
- runtimes;
- operating systems;
- cloud services;
- APIs;
- datasets.

### 112. AI Model Compromise Software-Dependency Investigation

Software dependencies should be examined for:

- unauthorized changes;
- vulnerable versions;
- malicious packages;
- unexpected dependencies.

### 113. AI Model Compromise Model-Supply-Chain Investigation

Supply-chain investigation should evaluate:

- model source;
- publisher;
- repository;
- upstream dependencies;
- transfer mechanisms;
- supplier security.

### 114. AI Model Compromise Supplier Investigation

Where a supplier is involved, investigation should establish:

- what component was affected;
- when it was affected;
- what evidence exists;
- whether other customers or versions may be affected;
- what containment has occurred.

Supplier statements should be treated as evidence requiring appropriate assessment rather than automatically as independent assurance.

### 115. AI Model Compromise Fourth-Party Investigation

Fourth-party investigation examines upstream dependencies that may have contributed to compromise.

### 116. AI Model Compromise Behavioral Reproduction

Behavioral Reproduction attempts to reproduce suspicious model behavior using:

- preserved artifacts;
- equivalent configurations;
- relevant inputs;
- controlled environments.

### 117. AI Model Compromise Adversarial Validation

Adversarial Validation tests whether suspicious behavior can be reliably triggered.

Testing should remain controlled and authorized.

### 118. AI Model Compromise Backdoor Investigation

Backdoor Investigation examines whether the model contains hidden or conditional behavior that can be triggered under specific conditions.

Potential indicators may include:

- unusual trigger patterns;
- conditional outputs;
- anomalous behavior on specific inputs;
- unexplained privileged actions.

A suspicious behavior pattern does not automatically prove intentional malicious design.

### 119. AI Model Compromise Model-Differential Analysis

Model-Differential Analysis compares suspected and trusted versions to identify meaningful differences.

Possible comparison methods include:

- artifact comparison;
- weight comparison;
- configuration comparison;
- behavioral testing;
- dependency comparison.

### 120. AI Model Compromise Compromise-Confidence Assessment

Compromise-Confidence Assessment evaluates the strength of evidence supporting a compromise hypothesis.

Confidence levels may consider:

- direct evidence;
- corroborating evidence;
- behavioral evidence;
- integrity evidence;
- access evidence;
- alternative explanations.

### 121. AI Model Compromise Remediation

Remediation removes or neutralizes the compromise.

Potential actions include:

- replacing the model;
- rebuilding from trusted sources;
- revoking credentials;
- rebuilding affected infrastructure;
- removing malicious dependencies;
- correcting access controls.

### 122. AI Model Compromise Trusted-Model Restoration

Trusted-Model Restoration replaces the compromised model with an artifact whose:

- provenance;
- integrity;
- authorization;
- configuration;
- security state

have been appropriately established.

### 123. AI Model Compromise Recovery Validation

Recovery Validation confirms that the restored environment is trustworthy enough for its intended operational state.

Validation may include:

- artifact verification;
- configuration verification;
- access-control testing;
- behavioral testing;
- dependency validation;
- security monitoring.

### 124. AI Model Compromise Post-Recovery Monitoring

Post-Recovery Monitoring should continue after restoration to identify:

- recurrence;
- residual compromise;
- unauthorized access;
- abnormal behavior;
- repeated attack paths.

### 125. AI Model Compromise Incident Control Principle

**AI model compromise controls should protect the complete model trust chain—from acquisition and provenance through storage, modification, approval, deployment, runtime operation, monitoring, and recovery.**

A secure model-compromise capability should therefore combine:

- access control;
- artifact integrity;
- authenticity;
- provenance;
- change management;
- deployment security;
- runtime monitoring;
- behavioral validation;
- incident response;
- trusted recovery.

Model hash verification, digital signatures, repository controls, behavioral monitoring, quarantine, forensic investigation, red-team testing, recovery validation, and similar measures are generally **governance or implementation practices** unless a specific applicable law, regulation, contract, or authoritative requirement makes a particular activity mandatory.

Internal `AI-IMR-*` identifiers remain knowledge-base taxonomy identifiers and should not be represented as legal provisions or regulatory requirements.

## Part 3 — Continuous AI Model Compromise Monitoring, Advanced Detection, Response, Resilience and Assurance

### 126. AI Model Compromise Continuous Monitoring

Continuous monitoring provides ongoing visibility into model artifacts, repositories, deployment environments, identities, configurations, and runtime behavior.

Monitoring should be proportional to:

- model criticality;
- exposure;
- deployment architecture;
- threat environment;
- business impact.

Continuous monitoring does not automatically constitute continuous assurance.

### 127. AI Model Compromise Monitoring Coverage

Monitoring coverage should address the complete model trust chain:

```text id="m8q4kt"
MODEL SOURCE
     |
     v
REPOSITORY
     |
     v
ARTIFACT
     |
     v
BUILD / PIPELINE
     |
     v
REGISTRY
     |
     v
DEPLOYMENT
     |
     v
RUNTIME
     |
     v
MODEL OUTPUT
```

A monitoring program should identify coverage gaps across these stages.

### 128. AI Model Compromise Leading Indicator

Leading indicators provide early signals that compromise risk may be increasing.

Examples include:

- unusual privileged access;
- unexpected repository changes;
- failed integrity checks;
- abnormal deployment activity;
- unexplained signing-key usage.

### 129. AI Model Compromise Lagging Indicator

Lagging indicators measure events that have already occurred.

Examples include:

- confirmed unauthorized modification;
- compromised model deployments;
- credential compromise;
- integrity-control failures;
- repeated model compromise incidents.

Leading and lagging indicators should not be treated as interchangeable.

### 130. AI Model Compromise Artifact Integrity Monitoring

Artifact integrity monitoring compares stored or deployed artifacts against approved integrity references.

Possible references include:

- cryptographic hashes;
- signatures;
- trusted manifests;
- version metadata.

A mismatch should trigger investigation rather than automatically being classified as malicious compromise.

### 131. AI Model Compromise Signature Monitoring

Signature monitoring verifies that model artifacts remain associated with expected signing identities and valid signatures.

Monitoring should detect:

- invalid signatures;
- changed signing identities;
- expired or revoked certificates;
- unexpected signing events.

### 132. AI Model Compromise Hash-Drift Monitoring

Hash-drift monitoring identifies unexpected changes in model artifacts.

```text id="v2n7rx"
APPROVED HASH
      |
      v
DEPLOYED HASH
      |
      +---- MATCH ------> EXPECTED STATE
      |
      +---- MISMATCH ---> INVESTIGATION
```

Hash drift establishes an integrity discrepancy, not necessarily attacker attribution.

### 133. AI Model Compromise Repository Monitoring

Repository monitoring should identify unusual:

- uploads;
- downloads;
- deletions;
- permission changes;
- administrative actions;
- artifact replacements.

Monitoring should be correlated with authorized change activity.

### 134. AI Model Compromise Artifact-Registry Monitoring

Artifact registries should be monitored for unauthorized:

- publication;
- modification;
- deletion;
- tagging;
- promotion;
- version substitution.

### 135. AI Model Compromise CI/CD Monitoring

CI/CD monitoring should identify suspicious changes involving:

- pipeline definitions;
- build environments;
- credentials;
- dependencies;
- model artifacts;
- deployment stages.

### 136. AI Model Compromise Build-Environment Monitoring

Build environments should be monitored for unexpected:

- software installation;
- network communication;
- privileged activity;
- source-code changes;
- artifact generation.

### 137. AI Model Compromise Model-Training Environment Monitoring

Training environments should be monitored for unauthorized access or changes to:

- training code;
- datasets;
- model checkpoints;
- configuration;
- compute infrastructure;
- credentials.

### 138. AI Model Compromise Fine-Tuning Environment Monitoring

Fine-tuning environments should be monitored for unexpected changes to:

- adapters;
- fine-tuning datasets;
- training parameters;
- model checkpoints;
- pipelines.

### 139. AI Model Compromise Deployment Monitoring

Deployment monitoring should verify that production deployments correspond to approved model versions and deployment records.

Potential indicators include:

- unknown model versions;
- unexpected deployment identities;
- unauthorized deployment times;
- unexpected environments.

### 140. AI Model Compromise Runtime Monitoring

Runtime monitoring evaluates the operational environment in which a model executes.

Signals may include:

- unexpected processes;
- unusual network connections;
- unauthorized file changes;
- abnormal resource usage;
- unexpected service identities.

### 141. AI Model Compromise Model-Serving Monitoring

Model-serving infrastructure should be monitored for unauthorized changes to:

- serving containers;
- inference endpoints;
- model paths;
- runtime configuration;
- authentication;
- authorization.

### 142. AI Model Compromise Model-Endpoint Monitoring

Model endpoints should be monitored for unusual access patterns, including:

- unexpected clients;
- abnormal request volumes;
- unusual geographic patterns;
- privilege changes;
- unexpected administrative activity.

### 143. AI Model Compromise Identity Monitoring

Identity monitoring should correlate model-management actions with the identities that performed them.

Important attributes may include:

- user;
- service account;
- role;
- device;
- location;
- timestamp.

### 144. AI Model Compromise Privilege-Drift Monitoring

Privilege-drift monitoring identifies changes that give an identity greater access to:

- model repositories;
- artifact registries;
- deployment systems;
- signing infrastructure;
- production environments.

### 145. AI Model Compromise Credential-Use Monitoring

Credential monitoring should identify:

- unusual authentication;
- impossible or unexpected access patterns;
- credential reuse;
- abnormal service-account activity;
- unusual administrative actions.

### 146. AI Model Compromise Signing-Key Monitoring

Signing-key usage should be monitored because unauthorized signing activity can undermine model authenticity controls.

Indicators include:

- unexpected signing operations;
- unusual signing frequency;
- unauthorized signing identities;
- use outside approved environments.

### 147. AI Model Compromise Configuration-Drift Monitoring

Configuration drift monitoring identifies changes to:

- model-serving settings;
- security policies;
- permissions;
- environment variables;
- network controls;
- deployment parameters.

### 148. AI Model Compromise Model-Version Monitoring

Model-version monitoring identifies which model version is active in each environment.

The organization should be able to determine:

```text
MODEL
  |
  +-- Version
  |
  +-- Digest
  |
  +-- Environment
  |
  +-- Deployment
  |
  +-- Approval
  |
  +-- Owner
```

### 149. AI Model Compromise Model-Substitution Monitoring

Model-substitution monitoring detects situations where an approved model identifier is associated with an unexpected underlying artifact.

This is important because a version label alone does not establish artifact identity.

### 150. AI Model Compromise Behavioral Monitoring

Behavioral monitoring evaluates whether a model behaves consistently with its approved security and functional expectations.

Potential signals include:

- unexpected outputs;
- abnormal refusal behavior;
- unusual tool calls;
- unexpected instruction following;
- unexplained policy bypasses.

### 151. AI Model Compromise Behavioral-Baseline Monitoring

Behavioral baselines establish expected behavior against which changes can be evaluated.

Baselines should account for legitimate variability and should not treat every behavioral difference as evidence of compromise.

### 152. AI Model Compromise Differential-Behavior Monitoring

Differential monitoring compares:

- current model;
- approved baseline;
- previous version;
- reference model.

Significant unexplained differences should be investigated.

### 153. AI Model Compromise Trigger-Behavior Monitoring

Trigger-behavior monitoring looks for suspicious behavior that occurs only under particular conditions.

Examples include:

- specific input sequences;
- hidden triggers;
- unusual token patterns;
- specific tool requests;
- unusual context combinations.

### 154. AI Model Compromise Backdoor-Behavior Monitoring

Backdoor monitoring evaluates whether a model exhibits conditional behavior inconsistent with its approved design.

Detection should combine:

- behavioral testing;
- artifact analysis;
- provenance;
- version comparison;
- contextual evidence.

### 155. AI Model Compromise Tool-Use Monitoring

Where models can invoke tools, monitoring should identify:

- unauthorized tools;
- unusual tool frequency;
- unexpected tool sequences;
- privileged tool calls;
- tool calls inconsistent with the task.

### 156. AI Model Compromise Agent-Action Monitoring

Agentic systems require monitoring of actions initiated through model-driven workflows.

Monitoring should record:

- requested action;
- authorized action;
- actual action;
- target;
- identity;
- result.

### 157. AI Model Compromise Data-Access Monitoring

Data-access monitoring should identify unexpected model access to:

- databases;
- files;
- object stores;
- APIs;
- customer information;
- sensitive datasets.

### 158. AI Model Compromise Network-Behavior Monitoring

Network monitoring should identify unexpected communications from model-serving or model-management infrastructure.

Potential indicators include:

- unknown destinations;
- unexpected outbound traffic;
- unusual protocols;
- unexpected data volumes.

### 159. AI Model Compromise Exfiltration Monitoring

Exfiltration monitoring identifies suspicious movement of:

- model weights;
- source code;
- datasets;
- credentials;
- configuration;
- sensitive outputs.

### 160. AI Model Compromise Model-Theft Monitoring

Model-theft monitoring focuses on unauthorized acquisition of model artifacts.

Relevant signals include:

- unusual downloads;
- large artifact transfers;
- unauthorized repository access;
- unexpected external destinations.

### 161. AI Model Compromise Model-Extraction Correlation

Model-extraction activity should be correlated with other evidence when assessing whether an attacker obtained meaningful model information.

Model extraction and model compromise remain distinct concepts.

### 162. AI Model Compromise Supply-Chain Monitoring

Supply-chain monitoring should cover relevant upstream model and software dependencies.

Potential monitoring targets include:

- model providers;
- model repositories;
- package repositories;
- base images;
- libraries;
- build services.

### 163. AI Model Compromise Supplier-Change Monitoring

Supplier changes should be monitored for changes involving:

- model versions;
- hosting;
- dependencies;
- security controls;
- ownership;
- signing mechanisms.

### 164. AI Model Compromise Fourth-Party Monitoring

Where practical, organizations should monitor material fourth-party dependencies that could affect model integrity.

This can be difficult when upstream visibility is limited.

### 165. AI Model Compromise Threat-Intelligence Correlation

Threat intelligence may be correlated with model-compromise telemetry to identify:

- known malicious infrastructure;
- compromised dependencies;
- relevant attack techniques;
- exposed credentials;
- supplier compromises.

Threat intelligence is an input to investigation, not proof of compromise by itself.

### 166. AI Model Compromise Attack-Path Monitoring

Attack-path monitoring evaluates whether an observed sequence could provide an attacker with a path toward model compromise.

```text id="r5k8zp"
IDENTITY
   |
   v
PRIVILEGE
   |
   v
REPOSITORY
   |
   v
ARTIFACT
   |
   v
PIPELINE
   |
   v
DEPLOYMENT
   |
   v
RUNTIME
```

### 167. AI Model Compromise Cross-System Correlation

Model-compromise detection should correlate telemetry across:

- identity;
- repository;
- CI/CD;
- cloud;
- endpoint;
- network;
- model-serving systems.

### 168. AI Model Compromise Cross-Version Correlation

Comparing incidents across model versions can identify whether a compromise is:

- version-specific;
- deployment-specific;
- pipeline-related;
- systemic.

### 169. AI Model Compromise Cross-Environment Correlation

Development, test, staging, and production environments should be correlated where relevant.

A compromise appearing in multiple environments may indicate a shared dependency or common control failure.

### 170. AI Model Compromise Cross-Provider Correlation

Where multiple model providers are used, incidents should be analyzed across providers to identify common dependencies or common-mode risks.

### 171. AI Model Compromise Recurrence Monitoring

Recurrence monitoring identifies repeated compromise indicators after remediation.

Repeated events may indicate:

- incomplete eradication;
- persistent credentials;
- unresolved vulnerabilities;
- recurring supplier issues;
- ineffective controls.

### 172. AI Model Compromise Control-Failure Monitoring

Control-failure monitoring identifies situations in which expected preventive or detective controls did not operate effectively.

Examples include:

- failed signature validation;
- bypassed deployment approval;
- missing repository logging;
- ineffective access restrictions.

### 173. AI Model Compromise Compensating-Control Monitoring

Where a primary control is unavailable, compensating controls should be monitored to confirm that they remain effective.

### 174. AI Model Compromise Defense-in-Depth Monitoring

Defense-in-depth monitoring evaluates whether multiple independent layers remain operational.

```text id="z3w6mh"
IDENTITY CONTROL
       |
       v
REPOSITORY CONTROL
       |
       v
ARTIFACT INTEGRITY
       |
       v
DEPLOYMENT CONTROL
       |
       v
RUNTIME MONITORING
       |
       v
BEHAVIORAL VALIDATION
```

Failure of one layer should not automatically eliminate every remaining layer.

### 175. AI Model Compromise Governance-Failure Monitoring

Monitoring should also identify governance failures such as:

- expired approvals;
- missing ownership;
- incomplete inventories;
- unreviewed exceptions;
- outdated model records.

### 176. AI Model Compromise Policy-Exception Monitoring

Exceptions involving model integrity or deployment controls should be monitored for:

- expiration;
- scope;
- compensating controls;
- repeated use;
- residual risk.

### 177. AI Model Compromise Change Correlation

Model-compromise indicators should be correlated with authorized changes to distinguish:

- legitimate modification;
- configuration drift;
- accidental change;
- suspected malicious modification.

### 178. AI Model Compromise Deployment Correlation

Suspicious behavior should be correlated with deployment events to determine whether a behavioral change coincided with a new model or configuration.

### 179. AI Model Compromise Model-Version Correlation

Behavioral or security anomalies should be correlated with specific model versions to determine whether the issue is version-specific.

### 180. AI Model Compromise Tool-Version Correlation

Where tools interact with the model, tool-version changes should be considered when investigating behavioral changes.

### 181. AI Model Compromise Configuration-Change Correlation

Configuration changes should be examined alongside model changes because apparent model compromise may actually originate from:

- system configuration;
- permissions;
- tools;
- prompts;
- deployment settings.

### 182. AI Model Compromise Detection Automation

Detection can be automated using:

- integrity verification;
- repository monitoring;
- SIEM correlation;
- deployment gates;
- anomaly detection;
- behavioral testing.

Automation should not eliminate appropriate human investigation.

### 183. AI Model Compromise Classification Automation

Automated classification may assign preliminary categories such as:

- integrity anomaly;
- unauthorized access;
- suspicious deployment;
- behavioral anomaly;
- suspected compromise.

Final classification should remain subject to appropriate evidence and governance requirements.

### 184. AI Model Compromise Automated Containment

Automated containment may include:

- blocking deployment;
- disabling a model endpoint;
- revoking credentials;
- isolating an artifact;
- stopping a pipeline.

Automation should be proportional to the potential consequences of an incorrect containment decision.

### 185. AI Model Compromise Automation Dependency

Organizations should identify dependencies on automated detection and containment mechanisms.

An automated control that depends on the compromised environment may itself be unreliable.

### 186. AI Model Compromise Automation Failure

Failure modes should be considered for security automation, including:

- false positives;
- false negatives;
- unavailable telemetry;
- corrupted evidence;
- failed integrations;
- incorrect automated actions.

### 187. AI Model Compromise Taxonomy Interoperability

Incident information should use consistent classifications across:

- SOC;
- GRC;
- engineering;
- cloud security;
- model-risk management;
- supplier-risk management.

### 188. AI Model Compromise Incident Data Integration

Incident data should integrate relevant information from:

- SIEM;
- IAM;
- CI/CD;
- repositories;
- artifact registries;
- model platforms;
- ticketing systems.

### 189. AI Model Compromise Detection-Automation Quality

Automated detection should be evaluated for:

- precision;
- recall;
- coverage;
- latency;
- false positives;
- false negatives.

These metrics should be interpreted in the context of model criticality.

### 190. AI Model Compromise Classification Automation Quality

Automated classification should be evaluated against validated incident cases.

Classification confidence should be distinguishable from incident severity.

### 191. AI Model Compromise Explainability

Where automated systems influence compromise classification or containment, relevant decision logic and evidence should be sufficiently explainable for investigation and oversight.

### 192. AI Model Compromise Human Oversight

Human oversight should be retained where automated actions could materially affect:

- production availability;
- customer services;
- evidence;
- legal obligations;
- business operations.

### 193. AI Model Compromise Exception Management

Exceptions to model-compromise controls should have:

- documented justification;
- defined scope;
- owner;
- expiration;
- compensating controls;
- residual-risk assessment.

### 194. AI Model Compromise Investigation Dispute Management

When technical teams disagree about whether a model was compromised, the organization should maintain a structured evidence-based dispute process.

### 195. AI Model Compromise Investigation Independence

Investigation independence should be considered where the team responsible for the affected model also controls the evidence or remediation process.

### 196. AI Model Compromise Assurance

Assurance evaluates whether model-compromise controls are designed and operating effectively.

Assurance is broader than monitoring because it considers:

- control design;
- operating effectiveness;
- evidence;
- coverage;
- independence;
- limitations.

### 197. AI Model Compromise Control Sampling

Control sampling may examine a representative set of:

- models;
- deployments;
- repositories;
- changes;
- access events.

Sampling methodology should be risk-based.

### 198. AI Model Compromise Back-Testing

Back-testing evaluates detection and classification controls against historical or simulated compromise scenarios.

### 199. AI Model Compromise Control Effectiveness

Control effectiveness should consider whether controls actually reduce the likelihood or impact of model compromise.

A control being documented does not demonstrate that it is effective.

### 200. AI Model Compromise Continuous Assurance

Continuous assurance combines ongoing evidence collection with periodic evaluation of control effectiveness.

```text id="n4c7ty"
MONITOR
   |
   v
COLLECT EVIDENCE
   |
   v
ASSESS CONTROL
   |
   v
IDENTIFY GAP
   |
   v
REMEDIATE
   |
   v
REVALIDATE
```

Continuous monitoring and continuous assurance should remain distinct concepts.

### 201. AI Model Compromise Risk Dashboard

A risk dashboard may consolidate:

- integrity alerts;
- unauthorized changes;
- privileged access;
- failed validations;
- supplier issues;
- unresolved findings.

### 202. AI Model Compromise Key Risk Indicators

KRIs may include:

- percentage of models without verified provenance;
- number of integrity exceptions;
- privileged model-management accounts;
- unauthorized deployment attempts;
- unresolved model-integrity findings.

KRIs should be interpreted in relation to model inventory and exposure.

### 203. AI Model Compromise Key Performance Indicators

KPIs may measure:

- detection time;
- containment time;
- recovery time;
- validation completion;
- remediation completion;
- recovery-test success.

### 204. AI Model Compromise Trend Analysis

Trend analysis can identify changes in:

- integrity alerts;
- access anomalies;
- deployment violations;
- compromise attempts;
- control failures.

Trends should account for changes in model population and monitoring coverage.

### 205. AI Model Compromise Control-Failure Analysis

Control failures should be analyzed to determine:

- which control failed;
- why it failed;
- whether another control detected the issue;
- whether the failure was systemic;
- whether remediation is required.

### 206. AI Model Compromise Compensating Controls

Compensating controls may temporarily reduce risk when primary controls are unavailable.

They should be:

- documented;
- authorized;
- monitored;
- time-bounded where appropriate;
- reassessed.

### 207. AI Model Compromise Resilience

Model-compromise resilience requires the ability to continue safely when a model or supporting component cannot be trusted.

Possible capabilities include:

- trusted backup models;
- alternative providers;
- isolated recovery environments;
- manual fallback;
- tested rollback.

### 208. AI Model Compromise Maturity Assessment

Maturity assessment evaluates capability across dimensions such as:

- inventory;
- provenance;
- integrity;
- access control;
- monitoring;
- response;
- recovery;
- assurance.

### 209. AI Model Compromise Governance Integration

Model-compromise management should integrate with:

- cybersecurity;
- model risk;
- AI governance;
- privacy;
- safety;
- third-party risk;
- business continuity;
- disaster recovery;
- enterprise risk management.

### 210. AI Model Compromise Continuous-Assurance Principle

**Continuous model-compromise assurance requires evidence that model integrity, identity, provenance, access, deployment, runtime state, and behavior remain appropriately controlled over time.**

Monitoring provides signals. Assurance evaluates whether the control environment remains effective.

The following distinctions remain essential:

- monitoring ≠ assurance;
- integrity anomaly ≠ confirmed compromise;
- behavioral anomaly ≠ confirmed compromise;
- correlation ≠ causation;
- model version ≠ artifact identity;
- artifact identity ≠ behavioral integrity;
- supplier notification ≠ independent assurance;
- automated detection ≠ confirmed investigation;
- automated containment ≠ complete remediation;
- recovery ≠ trusted recovery;
- redundancy ≠ effective resilience;
- documentation ≠ demonstrated capability.

Internal `AI-IMR-*` identifiers remain knowledge-base taxonomy identifiers and do not represent statutory provisions, regulatory article numbers, certification requirements, or legal obligations. Where a model-compromise control is legally mandatory, it should be separately mapped to the applicable authoritative source, jurisdiction, organizational role, affected activity, effective date, and specific requirement.


