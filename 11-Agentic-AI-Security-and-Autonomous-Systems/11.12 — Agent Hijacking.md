# 11.12 — Agent Hijacking

## Part 1 — Foundations and Core Concepts

### 1. Definition of Agent Hijacking

Agent hijacking is the unauthorized manipulation or takeover of an AI agent's behavior, execution context, authority, or operational control.

The attacker may attempt to cause the agent to:

* perform unauthorized actions;
* follow malicious instructions;
* misuse legitimate tools;
* disclose protected information;
* alter its operating objectives;
* bypass established security controls.

Agent hijacking is therefore broader than simply compromising a model.

---

### 2. Agent Hijacking as an Agentic AI Security Threat

In conventional applications, compromising a component may provide access to a defined function.

In agentic systems, hijacking can influence:

```text
Perception
   ↓
Reasoning
   ↓
Planning
   ↓
Tool Selection
   ↓
Action
   ↓
External Effect
```

The attacker may therefore manipulate the agent's decision and execution chain.

---

### 3. Agent Hijacking vs Model Compromise

Model compromise concerns the model itself.

Agent hijacking concerns the broader operational system surrounding the model.

```text
Model Compromise
      ↓
Model Behavior

Agent Hijacking
      ↓
Agent Behavior
      ↓
Tools
      ↓
Memory
      ↓
Identity
      ↓
Actions
```

An agent can potentially be hijacked even when the underlying model infrastructure remains uncompromised.

---

### 4. Agent Hijacking vs Account Takeover

Traditional account takeover usually involves gaining control of a user's account.

Agent hijacking may instead involve manipulating:

* agent context;
* instructions;
* memory;
* tools;
* sessions;
* execution state;
* delegated authority.

The attacker may never need to obtain the agent's primary identity credential.

---

### 5. Agent Hijacking vs Prompt Injection

Prompt injection is one possible mechanism for influencing an agent.

Agent hijacking is the broader security outcome.

```text
Prompt Injection
       ↓
Agent Manipulation
       ↓
Agent Hijacking
       ↓
Unauthorized Behavior
```

Not every prompt injection results in successful hijacking, but successful manipulation of agent behavior may constitute an agent-hijacking condition.

---

### 6. Agent Hijacking as a Security Boundary Violation

An agent operates within defined security boundaries.

Hijacking occurs when an attacker causes the agent to operate outside its intended:

* purpose;
* context;
* authority;
* permissions;
* transaction limits;
* tool boundaries.

---

### 7. Agent Control Boundary

The control boundary defines what components can legitimately influence agent behavior.

```text
Trusted Control Plane
        │
        ▼
Agent Runtime
        │
        ├── Model
        ├── Context
        ├── Memory
        ├── Tools
        └── Actions
```

Unauthorized influence crossing this boundary can create hijacking risk.

---

### 8. Agent Execution Context

An agent's execution context may include:

* system instructions;
* user requests;
* retrieved information;
* tool results;
* memory;
* session state;
* environment variables;
* security policies.

Compromise of any security-sensitive component can potentially influence subsequent behavior.

---

### 9. Trusted vs Untrusted Agent Inputs

Agent inputs should be classified according to trust.

```text
Higher Trust
    │
    ├── Security Policy
    ├── Authorization Service
    ├── Identity Provider
    │
    ├── Business Systems
    │
    └── External Content
Lower Trust
```

Untrusted content must not automatically acquire the authority of trusted instructions.

---

### 10. Agent Instruction Hierarchy

Agentic systems often process multiple instruction sources.

For example:

```text
Security Policy
      ↓
System Instructions
      ↓
Application Instructions
      ↓
User Instructions
      ↓
External Content
```

Hijacking may occur when lower-trust content successfully overrides or manipulates higher-trust instructions.

---

### 11. Agent Objective Manipulation

An attacker may attempt to change the effective objective of an agent.

For example:

```text
Original Objective:
Process customer refunds according to policy.

Manipulated Objective:
Refund every request immediately.
```

Objective manipulation is dangerous because the agent may continue operating legitimately from the perspective of its compromised reasoning.

---

### 12. Agent Goal Hijacking

Goal hijacking occurs when an attacker causes an agent to pursue an unauthorized goal.

The malicious goal may be introduced through:

* user input;
* documents;
* websites;
* email;
* tool output;
* memory;
* another agent.

The agent may then perform legitimate actions toward an illegitimate objective.

---

### 13. Agent Context Hijacking

Context hijacking occurs when an attacker manipulates the information available to the agent so that it reaches unauthorized conclusions.

```text
Trusted Context
      +
Manipulated Context
      ↓
Incorrect Agent Decision
      ↓
Potentially Unauthorized Action
```

The attacker does not necessarily need to alter the model itself.

---

### 14. Agent Session Hijacking

Session hijacking involves gaining unauthorized control over an active agent session or manipulating the session's state.

Potential consequences include:

* impersonation;
* unauthorized instructions;
* access to session data;
* tool misuse;
* transaction execution.

Session identity must therefore remain strongly bound to the authorized principal and execution context.

---

### 15. Agent Runtime Hijacking

Runtime hijacking involves compromising the environment in which an agent executes.

Potential targets include:

* agent orchestration services;
* containers;
* virtual machines;
* execution workers;
* runtime configuration;
* environment variables.

Runtime compromise can provide a path to bypass application-level safeguards.

---

### 16. Agent Identity Hijacking

Identity hijacking occurs when an attacker causes actions to appear as though they originated from a legitimate agent.

```text
Attacker
   ↓
Spoofed / Compromised Identity
   ↓
Enterprise System
   ↓
Trusted Agent Assumption
```

Strong workload identity and attribution controls are therefore essential.

---

### 17. Agent Credential Hijacking

An attacker may obtain credentials associated with an agent.

Potential credentials include:

* API tokens;
* service credentials;
* certificates;
* cloud credentials;
* database credentials;
* delegated tokens.

Credential compromise can enable direct impersonation even without manipulating the model.

---

### 18. Agent Tool Hijacking

An attacker may manipulate the tools available to an agent or the way those tools are invoked.

Examples include:

* replacing a legitimate endpoint;
* modifying tool configuration;
* changing tool schemas;
* redirecting API requests;
* introducing a malicious tool.

Tool hijacking can convert legitimate agent capabilities into attack mechanisms.

---

### 19. Agent Memory Hijacking

Attackers may attempt to insert malicious information into persistent agent memory.

```text
Attacker Input
     ↓
Memory
     ↓
Future Agent Session
     ↓
Manipulated Behavior
```

Persistent memory creates a particularly important hijacking risk because malicious influence can survive beyond the original interaction.

---

### 20. Agent Context Persistence Risk

A hijacking condition may persist if malicious instructions or state are stored in:

* long-term memory;
* knowledge bases;
* workflow state;
* configuration;
* task queues.

Persistence can transform a temporary manipulation into a recurring compromise.

---

### 21. Agent-to-Agent Hijacking

In multi-agent systems, one compromised agent may attempt to influence another agent.

```text
Compromised Agent A
        ↓
Malicious Message
        ↓
Trusted Agent B
        ↓
Unauthorized Action
```

The receiving agent must not automatically trust messages merely because they originate from another registered agent.

---

### 22. Delegation Hijacking

An attacker may manipulate delegated authority so that a transaction or task is executed by another agent under apparently legitimate authority.

Potential consequences include:

* privilege propagation;
* unauthorized actions;
* authority amplification;
* loss of attribution.

Delegated authority must therefore remain explicitly bounded.

---

### 23. Agent Authority Hijacking

Authority hijacking occurs when an attacker causes an agent to operate with authority beyond its intended scope.

Examples include:

* accessing restricted records;
* executing administrative actions;
* modifying security policies;
* initiating high-value transactions.

The security architecture must prevent the agent from expanding its own authority.

---

### 24. Agent Behavior Hijacking

Agent behavior may be manipulated without compromising identity or credentials.

For example:

```text
Legitimate Agent
      ↓
Malicious Context
      ↓
Manipulated Reasoning
      ↓
Legitimate Credentials
      ↓
Unauthorized Action
```

This makes behavioral protection as important as credential protection.

---

### 25. Agent Hijacking Through Prompt Injection

Prompt injection may attempt to redirect an agent toward unauthorized actions.

Examples include instructions such as:

* ignore previous restrictions;
* reveal confidential information;
* call a privileged tool;
* change transaction parameters;
* disable security checks.

Independent enforcement must prevent successful injection from becoming unauthorized execution.

---

### 26. Agent Hijacking Through Indirect Prompt Injection

Indirect prompt injection occurs when malicious instructions are embedded in content the agent processes.

Potential sources include:

* websites;
* PDFs;
* emails;
* documents;
* issue trackers;
* knowledge bases;
* retrieved records.

```text
External Content
      ↓
Agent Retrieval
      ↓
Malicious Instruction
      ↓
Agent Reasoning
      ↓
Potential Hijacking
```

---

### 27. Agent Hijacking Through Tool Results

Tool results should be treated as potentially untrusted inputs.

A compromised or manipulated tool could return information designed to influence the agent.

```text
Tool
 ↓
Malicious Result
 ↓
Agent Reasoning
 ↓
Unsafe Decision
```

Tool output must not automatically acquire instruction authority.

---

### 28. Agent Hijacking Through Memory Poisoning

Memory poisoning can create persistent behavioral manipulation.

An attacker may attempt to insert false information such as:

```text
"Security approval has already been granted."
```

If later treated as authoritative, the stored information could influence future transactions.

Memory provenance and authorization boundaries are therefore critical.

---

### 29. Agent Hijacking Through Identity Confusion

An agent may receive conflicting identity information.

For example:

```text
User Identity
      ≠
Agent Identity
      ≠
Delegated Identity
```

If these identities become confused, the agent may perform an action under the wrong authority.

---

### 30. Agent Hijacking Through Authorization Confusion

An attacker may attempt to convince the agent that an action is authorized when it is not.

```text
Agent:
"The user said this is approved."

Authorization Service:
"DENY."
```

The authorization service must remain authoritative.

---

### 31. Agent Hijacking Through Tool Substitution

A malicious tool may be substituted for a trusted tool or an approved tool may be redirected to an unauthorized endpoint.

Tool identity and provenance must therefore be validated.

---

### 32. Agent Hijacking Through Configuration Manipulation

Agent configuration can influence:

* tools;
* permissions;
* prompts;
* endpoints;
* memory;
* execution limits.

Unauthorized configuration changes can therefore create a hijacking condition without directly modifying the model.

---

### 33. Agent Hijacking Through Workflow Manipulation

An attacker may manipulate the workflow surrounding an agent.

Examples include:

* changing task queues;
* inserting malicious jobs;
* altering workflow states;
* modifying approval status;
* changing execution order.

The agent may then execute actions according to a compromised workflow.

---

### 34. Agent Hijacking Through Event Manipulation

Event-driven agents may react automatically to external events.

If event integrity is compromised, an attacker may generate malicious events that trigger autonomous actions.

```text
False Event
    ↓
Agent Trigger
    ↓
Agent Decision
    ↓
Autonomous Action
```

Event authenticity must therefore be verified.

---

### 35. Agent Hijacking Through API Manipulation

APIs connecting agents to enterprise systems can become hijacking paths.

Attackers may attempt:

* request manipulation;
* endpoint substitution;
* parameter tampering;
* token abuse;
* replay;
* response manipulation.

API security must therefore be part of the agent security boundary.

---

### 36. Agent Hijacking Through External Connectivity

Internet access and external services can expose agents to hostile content and systems.

An agent capable of browsing, communicating, or interacting with external services has a broader hijacking surface than an isolated agent.

---

### 37. Agent Hijacking and Excessive Agency

Excessive agency increases the consequences of successful hijacking.

```text
Limited Agent
     ↓
Few Tools
     ↓
Small Impact

Highly Autonomous Agent
     ↓
Many Tools
     ↓
Persistent Access
     ↓
Large Impact
```

Reducing unnecessary autonomy limits hijacking consequences.

---

### 38. Agent Hijacking and Privilege Escalation

A hijacked agent may attempt to obtain greater privileges.

Possible paths include:

* requesting additional credentials;
* manipulating authorization;
* abusing delegated authority;
* exploiting tool permissions;
* convincing another agent to perform privileged actions.

Agent architecture should prevent autonomous self-escalation.

---

### 39. Agent Hijacking and Lateral Movement

A compromised agent may attempt to move across enterprise systems.

```text
Agent A
 ↓
Tool
 ↓
Application B
 ↓
Credential
 ↓
System C
```

Network segmentation, identity boundaries, and least privilege should limit this movement.

---

### 40. Agent Hijacking and Transaction Risk

If a hijacked agent has transaction authority, compromise can produce direct business consequences.

Examples include:

* unauthorized payments;
* fraudulent refunds;
* account changes;
* infrastructure modifications;
* sensitive communications.

Transaction security must therefore remain independent of agent reasoning.

---

### 41. Agent Hijacking and Data Exposure

A hijacked agent may be manipulated into retrieving or transmitting sensitive information.

Potential targets include:

* customer records;
* financial data;
* credentials;
* internal documents;
* security information.

Data access must remain independently authorized.

---

### 42. Agent Hijacking and Persistence

Persistence determines whether hijacking survives beyond the original attack.

Potential persistence mechanisms include:

* memory;
* configuration;
* scheduled tasks;
* workflow state;
* credentials;
* tool modifications;
* delegated permissions.

Security teams should distinguish temporary manipulation from persistent compromise.

---

### 43. Agent Hijacking and Blast Radius

Blast radius represents the maximum potential impact of a hijacked agent.

```text
Hijacked Agent
      ↓
Identity Boundary
      ↓
Tool Boundary
      ↓
Data Boundary
      ↓
Transaction Boundary
      ↓
Enterprise Impact
```

Strong boundaries should progressively reduce the impact.

---

### 44. Detectability of Agent Hijacking

Agent hijacking may be difficult to detect because the compromised agent may continue using legitimate:

* identities;
* credentials;
* tools;
* workflows.

Behavioral monitoring is therefore necessary in addition to traditional authentication monitoring.

---

### 45. Agent Hijacking Indicators

Potential indicators include:

* unexpected tool usage;
* unusual transaction patterns;
* abnormal instruction sequences;
* unauthorized destinations;
* unusual data access;
* repeated authorization failures;
* unexpected memory modifications;
* sudden changes in behavior.

These signals should be correlated across the agent lifecycle.

---

### 46. Agent Hijacking Accountability

A secure system should allow investigators to determine:

```text
Who initiated the interaction?
Which agent was involved?
What context influenced the decision?
Which tools were used?
What authority was available?
What transaction occurred?
What changed?
```

Without attribution, identifying the source and scope of hijacking becomes significantly harder.

---

### 47. Agent Hijacking as a Governance Object

Agent hijacking should be included in:

* AI risk assessments;
* threat models;
* security policies;
* incident-response plans;
* control frameworks;
* security testing;
* audit programs.

It should not be treated solely as a model-level security problem.

---

### 48. Agent Hijacking Lifecycle

Hijacking can be viewed as a lifecycle:

```text
Initial Access
     ↓
Influence
     ↓
Control
     ↓
Privilege / Capability Abuse
     ↓
Action
     ↓
Persistence
     ↓
Impact
```

Security controls should be capable of disrupting the lifecycle at multiple stages.

---

### 49. Core Principles of Agent Hijacking Security

The core principles are:

1. Treat agent context as a security boundary.
2. Separate trusted instructions from untrusted content.
3. Authenticate agent identities independently.
4. Authorize actions independently from model reasoning.
5. Restrict tools and permissions through least privilege.
6. Protect memory and persistent state.
7. Preserve attribution across delegated actions.
8. Monitor behavioral deviations.
9. Maintain independent emergency controls.
10. Limit the blast radius of a compromised agent.

---

### 50. Core Principle of Agent Hijacking

The fundamental principle is:

> **An attacker must not be able to convert influence over an agent into unauthorized enterprise authority.**

The secure architecture therefore separates:

```text
Influence
   ↓
Agent Reasoning
   ↓
Action Proposal
   ↓
Independent Authorization
   ↓
Bounded Execution
   ↓
Business Effect
```

Even if an attacker successfully manipulates the agent's reasoning, independent identity, authorization, transaction, and execution controls should prevent that manipulation from becoming uncontrolled enterprise action.

# Part 2 — Technical Security and Control Mechanisms

### 51. Agent Hijacking Security Control Architecture

Agent hijacking defenses should protect every layer between external input and autonomous execution.

```text id="8x4v3q"
External Input
      ↓
Input Security
      ↓
Context Isolation
      ↓
Agent Runtime
      ↓
Identity
      ↓
Authorization
      ↓
Tool Gateway
      ↓
Action Controls
      ↓
Protected System
      ↓
Monitoring / SOC
```

The architecture should assume that an attacker may successfully influence the agent and ensure that influence alone cannot produce unauthorized enterprise effects.

---

### 52. Agent Identity Control

Every agent should have a unique, verifiable identity.

Identity controls should prevent:

* identity spoofing;
* shared agent identities;
* unauthorized identity reuse;
* identity substitution.

The executing identity should be recorded with every security-sensitive operation.

---

### 53. Agent Authentication

Agent authentication should verify that the workload executing an operation is the legitimate agent associated with the claimed identity.

Authentication should occur before access to protected:

* tools;
* APIs;
* data;
* workflows;
* transaction services.

---

### 54. Strong Agent Authentication

High-risk agents should use strong workload authentication mechanisms.

Possible mechanisms include:

* mutual TLS;
* workload identity;
* signed tokens;
* certificates;
* hardware-backed identity where appropriate.

Authentication strength should correspond to the impact of the agent's available capabilities.

---

### 55. Fine-Grained Agent Authorization

Authorization should evaluate what the authenticated agent is allowed to do.

```text id="1cxp5w"
Agent Identity
      +
Requested Action
      +
Resource
      +
Context
      +
Risk
      ↓
Authorization Decision
```

Authentication should never be interpreted as unrestricted authority.

---

### 56. Independent Authorization Service

Authorization decisions should be made independently from the agent.

```text id="5o0f3j"
Agent
 ↓
Request
 ↓
Authorization Service
 ├── Identity
 ├── Policy
 ├── Resource
 ├── Context
 └── Risk
 ↓
ALLOW / DENY
```

This prevents a hijacked agent from granting itself additional authority.

---

### 57. Agent Access Allowlisting

Only approved agents should be permitted to access protected services.

Allowlisting should consider:

* agent identity;
* version;
* environment;
* workload;
* business purpose.

Unknown or unauthorized agent instances should be denied.

---

### 58. Authentication Method Allowlisting

Only approved authentication mechanisms should be accepted for sensitive agent operations.

For example:

```text id="6u9v9m"
Approved Workload Identity → ALLOW
Expired Token              → DENY
Unknown Certificate        → DENY
Untrusted Credential       → DENY
```

---

### 59. Authorization Scope Validation

Authorization scope should be checked against the requested action.

A valid identity with access to a system should not automatically receive access to every operation within that system.

---

### 60. Agent Role Validation

The agent's assigned role should be validated before execution.

```text id="fzx3l8"
Agent Role:
Customer Support

Requested:
Delete Production Database

→ Role Mismatch
→ DENY
```

Role validation should be performed by trusted security infrastructure.

---

### 61. Agent Permission Validation

Individual permissions should be validated at the point of use.

This prevents an attacker from exploiting unused or accidentally inherited privileges.

---

### 62. Transaction Authorization Limits

Transactions initiated by agents should have explicit limits.

Limits may include:

* maximum amount;
* number of records;
* number of actions;
* resource scope;
* transaction type.

Limits should be technically enforced.

---

### 63. Cumulative Authorization Limits

Controls should consider cumulative actions.

```text id="i1g3ap"
Action 1 → €500
Action 2 → €500
Action 3 → €500
Action 4 → €500
----------------
Total     → €2,000
```

An attacker should not be able to bypass a limit by dividing one prohibited operation into many permitted operations.

---

### 64. Time-Bounded Agent Authorization

Agent authority should expire automatically.

Short authorization windows reduce the period during which a hijacked agent can operate.

---

### 65. Short-Lived Agent Credentials

Agent credentials should preferably be temporary.

```text id="7s8f8v"
Credential Issued
      ↓
Limited Scope
      ↓
Limited Lifetime
      ↓
Automatic Expiration
```

Short-lived credentials reduce persistence after compromise.

---

### 66. Credential Isolation

Agent credentials should be isolated from:

* prompts;
* memory;
* model context;
* logs;
* tool results;
* user-visible outputs.

Credentials should never become persistent agent knowledge.

---

### 67. Secrets Management

Secrets should be managed through controlled enterprise mechanisms.

Agents should receive only the secrets necessary for an authorized operation and, where possible, only through brokered access.

---

### 68. Context Security Controls

Security-sensitive context should be separated from general agent context.

```text id="x0d0el"
Security Context
      │
      ├── Identity
      ├── Authorization
      ├── Policy
      └── Approval
       
Agent Context
      │
      ├── User Input
      ├── Documents
      ├── Tool Results
      └── Web Content
```

The second category should never be allowed to redefine the first.

---

### 69. Prompt and Instruction Integrity

System instructions and security policies should be protected against unauthorized modification.

Controls should include:

* version control;
* integrity checking;
* restricted modification;
* approval workflows;
* change monitoring.

---

### 70. Untrusted Input Isolation

Potentially hostile inputs should be explicitly isolated.

Examples include:

* websites;
* emails;
* uploaded documents;
* customer messages;
* external API responses;
* retrieved knowledge.

Untrusted content may inform reasoning but must not become authoritative security policy.

---

### 71. Prompt Injection Detection

Systems should detect attempts to manipulate agent instructions.

Detection may identify:

* instruction overrides;
* attempts to reveal system prompts;
* requests to disable controls;
* privilege escalation instructions;
* malicious tool-use directives.

Detection is an additional layer, not the primary authorization boundary.

---

### 72. Memory Isolation

Agent memory should be isolated according to:

* agent;
* user;
* tenant;
* session;
* sensitivity;
* business purpose.

A compromised memory store should not automatically expose unrelated agent contexts.

---

### 73. Memory Authorization

Agents should be authorized before reading or modifying persistent memory.

Security-sensitive memory should have stricter access requirements than ordinary conversational context.

---

### 74. Memory Integrity

Persistent memory should be protected against unauthorized modification.

Controls may include:

* integrity verification;
* provenance;
* versioning;
* restricted writes;
* anomaly detection.

---

### 75. Persistent Context Controls

Persistent context should have explicit lifecycle controls.

These should address:

* creation;
* modification;
* retention;
* expiration;
* deletion;
* authorization.

Persistent instructions should not silently become permanent authority.

---

### 76. Data Access Authorization

Agent data access should be independently authorized.

A hijacked agent should not be able to use legitimate credentials to retrieve arbitrary enterprise data simply because the data is technically accessible.

---

### 77. Data Minimization

Agents should receive only the information required for their approved function.

This limits the consequences of successful hijacking.

---

### 78. Field-Level Data Protection

Sensitive fields should be separately protected.

```text id="8d05lq"
Customer Record
 ├── Name          → Allowed
 ├── Order Status  → Allowed
 ├── Payment Data  → Restricted
 └── Credentials   → Denied
```

Hijacking one agent should not expose an entire database.

---

### 79. Tenant Isolation

Agents operating in multi-tenant environments should be prevented from crossing tenant boundaries.

Tenant identity should be independently validated before data or transaction access.

---

### 80. Session Isolation

Agent sessions should be isolated to prevent hijacking of one session from providing access to another.

Controls should bind sessions to appropriate:

* identities;
* tokens;
* execution contexts;
* tenants.

---

### 81. Network Segmentation

Agent runtimes should be separated from sensitive enterprise systems.

```text id="b7v2n5"
Agent Runtime
      ↓
Agent Security Gateway
      ↓
Controlled Network Zone
      ↓
Protected Service
```

Network segmentation limits lateral movement after hijacking.

---

### 82. Egress Controls

Outbound communication from agents should be restricted to approved destinations.

Controls should prevent a hijacked agent from freely communicating with attacker-controlled infrastructure.

---

### 83. External Communication Controls

Agents capable of sending external communications should have controls over:

* recipient;
* destination;
* content classification;
* attachment;
* frequency;
* channel.

---

### 84. Code Execution Isolation

Agents capable of executing code should operate within isolated environments.

Controls may include:

* sandboxing;
* containerization;
* restricted filesystem access;
* network restrictions;
* CPU and memory limits.

This prevents a hijacked agent from converting code execution into unrestricted host access.

---

### 85. Database Security Controls

Database access should be tightly scoped.

Controls should include:

* restricted schemas;
* restricted tables;
* query controls;
* row-level security;
* read/write separation;
* transaction limits.

---

### 86. Read/Write Separation

Read and write access should be independently controlled.

```text id="q7z1ga"
READ
 ↓
Information Access

WRITE
 ↓
State Modification

DELETE / ADMIN
 ↓
Critical Authority
```

A hijacked read-only agent should not automatically become a destructive agent.

---

### 87. Human Approval Controls

High-risk actions should require human approval before execution.

Approval should occur through an independent mechanism rather than through conversational confirmation alone.

---

### 88. Action Preview

The system should present the exact action that will be executed.

The preview should identify:

* target;
* action;
* parameters;
* affected resources;
* transaction value;
* expected consequence.

---

### 89. Dual Authorization

Critical operations should require two independent authorization decisions.

This creates an additional barrier if one identity, agent, or control path is compromised.

---

### 90. Reversibility Controls

Where possible, autonomous actions should support:

* rollback;
* cancellation;
* compensation;
* restoration.

Reversibility reduces the impact of successful hijacking.

---

### 91. Agent Execution Limits

Agents should have limits on:

* execution duration;
* action count;
* transaction volume;
* resource consumption;
* concurrent operations.

These controls constrain runaway behavior.

---

### 92. Loop Detection

The system should detect repeated autonomous behavior.

```text id="r5vv79"
Action A
 ↓
Action B
 ↓
Action A
 ↓
Action B
 ↓
...
```

A hijacked agent may exhibit unusual repetitive execution patterns.

---

### 93. Recursive Delegation Controls

Agents should not be able to create unlimited chains of delegated authority.

Controls should limit:

* delegation depth;
* delegated scope;
* delegated duration;
* cumulative impact.

---

### 94. Agent-to-Agent Authentication

Agents communicating with one another should mutually authenticate.

The receiving agent should verify the identity of the sender before accepting security-sensitive requests.

---

### 95. Agent-to-Agent Message Integrity

Agent-to-agent messages should be protected against unauthorized modification.

Security-sensitive attributes should include:

* sender;
* recipient;
* action;
* authority;
* parameters;
* expiration.

---

### 96. Delegated Authority Constraints

Delegated authority should be explicitly constrained.

```text id="1v4c9s"
Agent A
 ↓
Delegates
 ↓
Agent B
 ↓
Reduced / Equal Authority
```

Agent B must not receive broader authority than the delegation permits.

---

### 97. Tool Result Validation

Tool results should be validated before influencing security-sensitive decisions.

An attacker who compromises a tool should not automatically control the agent's subsequent actions.

---

### 98. Output Validation Before Action

Model-generated outputs should pass through deterministic validation before they become executable requests.

```text id="6sm2cn"
Model Output
     ↓
Schema Validation
     ↓
Security Policy
     ↓
Business Rules
     ↓
Authorization
     ↓
Execution
```

---

### 99. Business Rule Enforcement

Business rules must be enforced by authoritative systems.

For example:

```text id="8nh1wu"
Refund ≤ Original Payment
Payment Recipient = Approved
Discount ≤ Authorized Limit
Production Change = Approved Window
```

The agent should not be trusted to enforce these rules through reasoning alone.

---

### 100. Resource Controls

Resources available to a potentially hijacked agent should be explicitly limited.

Controls may constrain:

* compute;
* storage;
* API calls;
* database operations;
* cloud resources;
* number of affected records.

---

### 101. Rate Limiting

Rate limiting restricts the speed at which a hijacked agent can perform actions.

```text id="i2jzkg"
Normal:
100 requests/minute

Abnormal:
10,000 requests/minute

→ Throttle / Block
```

---

### 102. Concurrency Controls

Concurrent operations should be limited.

This prevents a hijacked agent from launching large numbers of simultaneous operations before security teams can intervene.

---

### 103. Timeout Controls

Agent operations should have explicit timeouts.

Long-running operations should automatically terminate or require renewed authorization when their permitted execution period expires.

---

### 104. Retry Controls

Retries should be bounded and evaluated for safety.

A hijacked agent should not be able to create an infinite retry loop against a transaction or API.

---

### 105. Circuit Breakers

Circuit breakers should automatically stop operations when suspicious thresholds are exceeded.

Potential triggers include:

* repeated failures;
* excessive volume;
* unusual destinations;
* authorization failures;
* abnormal transaction values.

---

### 106. Emergency Agent Controls

Emergency controls should allow security teams to rapidly restrict an agent.

Controls may include:

* suspend agent;
* disable tools;
* revoke credentials;
* block network access;
* stop workflows;
* deny transactions.

---

### 107. Agent Kill Switch

The kill switch must operate independently of the potentially hijacked agent.

```text id="r4z1nd"
Security Operator
       ↓
Kill Switch
       ↓
Agent Execution Stopped
       ↓
Credentials Revoked
       ↓
Tools / Transactions Blocked
```

The agent should not be capable of disabling its own kill switch.

---

### 108. Credential Revocation

Compromised credentials should be revocable rapidly.

Revocation should propagate across relevant:

* sessions;
* tools;
* APIs;
* workloads;
* delegated identities.

---

### 109. Agent Isolation During Incident Response

During suspected hijacking, the agent should be isolated while preserving sufficient evidence for investigation.

Isolation may include:

* network quarantine;
* tool suspension;
* credential revocation;
* transaction blocking;
* memory write protection.

---

### 110. Agent Isolation

Security architecture should provide isolation boundaries between agents.

A compromised customer-service agent should not automatically gain access to:

* finance agents;
* infrastructure agents;
* privileged administrative agents.

---

### 111. Behavioral Hijacking Monitoring

Security monitoring should establish behavioral baselines and identify deviations.

Indicators include:

* new tool usage;
* unusual transaction patterns;
* abnormal data access;
* unexpected destinations;
* unusual operating times;
* changes in action frequency.

---

### 112. Agent Hijacking Activity Logging

Security-relevant agent activity should be logged.

Logs should capture:

```text id="i1upw0"
Agent Identity
Session
Initiator
Instruction Source
Tool
Action
Target
Authorization
Result
Timestamp
```

---

### 113. Tamper-Resistant Hijacking Audit Logging

Logs should be protected against modification by compromised agents or administrators operating within the affected environment.

Controls may include:

* append-only storage;
* centralized collection;
* immutable retention;
* cryptographic integrity.

---

### 114. Agent Hijacking Security Observability

Security teams should be able to correlate the complete chain:

```text id="ub39eu"
Input
 ↓
Context
 ↓
Agent Decision
 ↓
Tool Invocation
 ↓
Authorization
 ↓
Action
 ↓
Business Effect
```

This is essential for distinguishing normal autonomous behavior from hijacking.

---

### 115. Agent Hijacking Risk Detection

Detection should combine multiple signals.

Potential indicators include:

* prompt injection;
* identity anomalies;
* memory changes;
* abnormal tool usage;
* privilege escalation attempts;
* unusual transactions;
* unexpected external communications.

No single indicator should be treated as definitive in isolation.

---

### 116. Secure Hijacking Failure Handling

When hijacking is suspected, the system should fail safely.

```text id="2d8iqr"
Suspicious Behavior
       ↓
Risk Evaluation
       ↓
Restrict / Stop
       ↓
Revoke Authority
       ↓
Preserve Evidence
       ↓
Investigate
```

The priority should be preventing additional unauthorized effects.

---

### 117. Core Engineering Principle

The core engineering principle is:

> **A successfully manipulated agent must still be unable to independently convert that manipulation into unauthorized enterprise authority.**

This requires independent enforcement across:

```text id="1gq5op"
Identity
   ↓
Authentication
   ↓
Authorization
   ↓
Context Security
   ↓
Tool Controls
   ↓
Transaction Controls
   ↓
Execution Limits
   ↓
Monitoring
   ↓
Emergency Response
```

Agent hijacking is therefore controlled not by assuming the agent will always behave correctly, but by architecting the enterprise so that **incorrect agent behavior remains bounded, observable, interruptible, and recoverable**.

# Part 3 — Governance, Risk, GRC, and Operational Controls

### 118. Agent Hijacking Governance Framework

Organizations should establish a formal governance framework for agent hijacking risk.

The framework should address:

* agent ownership;
* hijacking threat scenarios;
* risk classification;
* security requirements;
* monitoring;
* incident response;
* evidence;
* exception management;
* assurance;
* lifecycle reauthorization.

Agent hijacking should be governed as an enterprise AI security risk rather than treated solely as an application defect.

---

### 119. Agent Hijacking Ownership and Accountability

Each production agent should have clearly defined ownership.

Ownership should cover:

* business purpose;
* security responsibility;
* technical operation;
* approved capabilities;
* risk acceptance;
* incident response.

The agent itself cannot be considered accountable for security decisions.

---

### 120. Enterprise Agent Hijacking Risk Inventory

Organizations should maintain an inventory of agents and their exposure to hijacking.

The inventory should identify:

| Attribute         | Example                |
| ----------------- | ---------------------- |
| Agent             | Customer Service Agent |
| Business Function | Customer Support       |
| Autonomy          | High                   |
| External Inputs   | Email / Web            |
| Tools             | CRM / Ticketing        |
| Persistent Memory | Yes                    |
| Transactions      | Refunds                |
| Risk Tier         | High                   |
| Owner             | Customer Operations    |

The inventory should identify which agents could produce material business effects if hijacked.

---

### 121. Agent Hijacking Classification

Hijacking scenarios should be classified according to the mechanism and impact.

Possible classifications include:

* prompt-based manipulation;
* context manipulation;
* memory poisoning;
* session hijacking;
* credential compromise;
* tool hijacking;
* identity spoofing;
* workflow manipulation;
* runtime compromise;
* agent-to-agent hijacking.

Classification supports consistent risk treatment.

---

### 122. Agent Hijacking Risk Tiering

Hijacking risk should be evaluated according to both likelihood and potential impact.

```text id="2e9r17"
Low
 ↓
Limited tools / limited data

Moderate
 ↓
Operational access

High
 ↓
Sensitive data / transactions

Critical
 ↓
Privileged / destructive / financial authority
```

Higher-risk agents should receive stronger preventive and detective controls.

---

### 123. Agent Hijacking Approval Governance

High-risk agents should require formal approval before production deployment.

Approval should consider:

* business purpose;
* autonomy level;
* available tools;
* transaction authority;
* external connectivity;
* memory;
* sensitive-data access;
* emergency controls.

---

### 124. Agent Hijacking Purpose Limitation

Agents should have a clearly defined purpose.

For example:

```text id="o5h8g8"
Approved Purpose:
Resolve customer support tickets.

Not Automatically Authorized:
Modify billing policies.
Change administrator roles.
Access unrelated customer records.
```

Purpose limitation reduces opportunities for successful hijacking to produce unrelated impacts.

---

### 125. Agent Authority Governance

Agent authority should be explicitly documented.

Governance should identify:

* permitted actions;
* permitted resources;
* permitted tools;
* permitted transactions;
* maximum impact;
* duration of authority.

Authority should be independently enforceable.

---

### 126. Agent-to-Tool Access Governance

Organizations should govern which tools each agent may access.

Tool access reviews should consider:

* business necessity;
* sensitivity;
* privilege;
* transaction capability;
* external exposure;
* hijacking consequences.

---

### 127. Agent Hijacking Access Governance

Access governance should evaluate not only whether an agent can access a system, but what it can do after access is obtained.

```text id="qjv1j0"
System Access
      ≠
Unlimited System Authority
```

This distinction is fundamental to limiting hijacking impact.

---

### 128. Least-Privilege Hijacking Governance

Agent capabilities should follow least privilege.

Governance should ensure that a hijacked agent has access only to the minimum:

* data;
* tools;
* transactions;
* credentials;
* network destinations;
* execution resources.

---

### 129. Agent Capability Governance

Capabilities should be explicitly approved and mapped to business purpose.

The governance process should identify:

```text id="4f1ygu"
Capability
   ↓
Business Purpose
   ↓
Required Authority
   ↓
Security Risk
   ↓
Approved Scope
```

Unnecessary capabilities should not be deployed.

---

### 130. Capability Combination Governance

Governance should evaluate dangerous combinations of individually legitimate capabilities.

For example:

```text id="3u0x1q"
Read Customer Data
+
Modify Account
+
Issue Refund
+
Send External Email
```

The combination may create a much greater hijacking risk than any single capability.

---

### 131. Permission Inheritance Governance

Agent permissions inherited from:

* users;
* roles;
* applications;
* parent agents;
* workflows

should be explicitly reviewed.

An agent should not inherit broad privileges simply because its execution context is associated with a highly privileged user.

---

### 132. Delegation Governance

Agent-to-agent delegation should be governed through explicit policies.

Governance should define:

* permitted delegators;
* permitted delegates;
* delegated capabilities;
* transaction limits;
* expiration;
* revocation.

---

### 133. Authority Propagation Governance

Authority should not automatically expand as it propagates through agents and systems.

```text id="y0j7d1"
Original Authority
       ↓
Delegated Authority
       ↓
Equal or Reduced Authority
```

Any increase in authority should require a new independent authorization decision.

---

### 134. Data Governance for Agent Hijacking

Data governance should account for the possibility that an agent becomes hijacked.

Controls should address:

* data classification;
* data minimization;
* sensitive data;
* tenant isolation;
* retention;
* external transfer;
* monitoring.

The goal is to reduce the information available to a compromised agent.

---

### 135. Sensitive Data Hijacking Governance

Agents with access to sensitive data should receive enhanced security governance.

Governance should define:

* permitted data classes;
* approved purposes;
* access conditions;
* output restrictions;
* external transmission restrictions;
* monitoring requirements.

---

### 136. Memory Governance

Persistent agent memory should be treated as a governed security asset.

Governance should define:

* what may be stored;
* who may write memory;
* who may read it;
* retention periods;
* provenance;
* integrity requirements;
* deletion requirements.

---

### 137. Context Governance

Agent context should be governed according to trust level.

```text id="7mq1hf"
Trusted Security Context
        ↓
Controlled Business Context
        ↓
External / Untrusted Content
```

Lower-trust context should not override higher-trust security controls.

---

### 138. Credential Governance

Credentials available to agents should be governed through enterprise identity and secrets-management processes.

Governance should address:

* issuance;
* scope;
* lifetime;
* rotation;
* storage;
* access;
* revocation.

---

### 139. Network Governance

Network connectivity should be governed according to hijacking risk.

Agents should use:

* approved network zones;
* controlled egress;
* restricted destinations;
* monitored connections;
* segmented environments.

---

### 140. Human Oversight Governance

Human oversight requirements should be defined according to hijacking risk and business impact.

High-impact agents may require:

* human approval;
* dual control;
* real-time monitoring;
* intervention capability;
* post-action review.

---

### 141. Hijacking Response Governance

Organizations should define when suspected hijacking triggers automatic containment.

Possible triggers include:

* confirmed prompt manipulation;
* abnormal privilege use;
* suspicious credential activity;
* unusual tool invocation;
* abnormal transactions;
* unexpected external communication.

---

### 142. High-Impact Agent Governance

High-impact agents should receive stronger governance requirements.

Examples include agents capable of:

* financial transactions;
* privileged administration;
* production changes;
* sensitive-data processing;
* identity management;
* external contractual communications.

---

### 143. Destructive Capability Governance

Agents capable of destructive operations should be explicitly governed.

Examples include:

* deleting records;
* terminating accounts;
* destroying infrastructure;
* modifying security controls.

Such capabilities should normally have stronger authorization and human oversight.

---

### 144. Autonomous Action Governance

Governance should define which actions may occur autonomously and which require escalation.

```text id="o7d6g7"
Low Impact
 → Autonomous

Moderate Impact
 → Autonomous + Monitoring

High Impact
 → Human Approval

Critical Impact
 → Restricted / Dual Control
```

---

### 145. Agent Hijacking Security Requirements

Security requirements should be documented before production deployment.

Requirements should address:

* identity;
* authentication;
* authorization;
* context security;
* memory;
* tool access;
* network access;
* transaction controls;
* monitoring;
* emergency response.

---

### 146. Agent Hijacking Threat Modeling

Threat modeling should explicitly evaluate hijacking paths.

A model should consider:

```text id="4m9y70"
Entry Point
    ↓
Influence Mechanism
    ↓
Agent Manipulation
    ↓
Capability Abuse
    ↓
Privilege / Authority
    ↓
Action
    ↓
Impact
```

Threat modeling should identify controls capable of interrupting each stage.

---

### 147. Agent Secure Development Lifecycle

Agent development should incorporate hijacking security throughout the lifecycle.

```text id="8bq4ir"
Design
 ↓
Threat Model
 ↓
Security Requirements
 ↓
Development
 ↓
Adversarial Testing
 ↓
Security Review
 ↓
Deployment
 ↓
Monitoring
```

Hijacking controls should not be added only after deployment.

---

### 148. Model Change Governance

Changes to models should be treated as potentially security-relevant.

Governance should assess whether a model change affects:

* instruction following;
* refusal behavior;
* tool selection;
* action generation;
* context interpretation;
* security-boundary adherence.

---

### 149. Prompt and Instruction Change Governance

Changes to:

* system prompts;
* agent instructions;
* policies;
* behavioral constraints

should be version-controlled, reviewed, tested, and approved.

A prompt change can materially alter hijacking resistance.

---

### 150. Tool Change Governance

Changes to agent tools should trigger security review when they affect:

* permissions;
* endpoints;
* schemas;
* transaction capabilities;
* data access;
* external connectivity.

A newly added tool can substantially increase hijacking impact.

---

### 151. Data-Source Change Governance

Changes to data sources should be assessed for:

* trustworthiness;
* provenance;
* integrity;
* sensitivity;
* injection exposure;
* effect on agent decisions.

New external sources should not automatically become trusted agent inputs.

---

### 152. Agent Configuration Management

Agent configuration should be centrally managed and controlled.

Configuration may include:

* tools;
* permissions;
* prompts;
* memory settings;
* network access;
* execution limits;
* approval thresholds.

Unauthorized configuration changes should be detectable.

---

### 153. Agent Hijacking Security Testing

Testing should verify that hijacking attempts cannot produce unauthorized outcomes.

Testing should include:

* prompt injection;
* context manipulation;
* memory poisoning;
* tool abuse;
* privilege escalation;
* credential misuse;
* session manipulation.

---

### 154. Adversarial Hijacking Testing

Adversarial testing should actively attempt to take control of agent behavior.

Scenarios may include:

```text id="8ivn1k"
Malicious User
Malicious Document
Malicious Website
Malicious Tool Result
Compromised Agent
Stolen Credential
Malicious Agent
      ↓
Hijacking Attempt
```

The objective is to determine whether manipulation can cross security boundaries.

---

### 155. Hijacking Control Effectiveness Testing

Controls should be tested against explicit security objectives.

| Control            | Effectiveness Test            |
| ------------------ | ----------------------------- |
| Context isolation  | Inject malicious instruction  |
| Authorization      | Request prohibited action     |
| Memory integrity   | Modify trusted memory         |
| Tool allowlist     | Invoke unauthorized tool      |
| Credential control | Attempt credential reuse      |
| Kill switch        | Attempt action after shutdown |

A control should be considered effective only when it demonstrably prevents or limits the intended attack.

---

### 156. Agent Hijacking Monitoring Governance

Organizations should define which behaviors require monitoring.

Monitoring should include:

* unusual tool use;
* abnormal data access;
* unusual transaction patterns;
* identity anomalies;
* memory changes;
* authorization failures;
* unexpected external communication.

---

### 157. Agent Hijacking Key Risk Indicators

Potential KRIs include:

* hijacking attempts;
* successful manipulation events;
* unauthorized tool invocations;
* abnormal privilege requests;
* unexpected memory modifications;
* unusual external destinations;
* emergency shutdown events;
* authorization bypass attempts.

---

### 158. Security Baseline Compliance

Each production agent should comply with an approved security baseline.

Baseline controls may include:

* unique identity;
* strong authentication;
* least privilege;
* tool allowlisting;
* context isolation;
* memory controls;
* logging;
* monitoring;
* emergency shutdown.

---

### 159. Continuous Hijacking Control Monitoring

Security controls should be continuously monitored where practical.

Monitoring should verify that:

* authorization remains active;
* tools remain approved;
* permissions remain scoped;
* logging remains operational;
* kill switches remain available;
* security configurations remain intact.

---

### 160. Agent Hijacking Drift

Hijacking resistance can degrade over time.

Drift may result from:

* model updates;
* prompt changes;
* new tools;
* expanded permissions;
* new integrations;
* configuration changes;
* changed data sources.

Security drift should trigger reassessment.

---

### 161. Hijacking Incident Classification

Hijacking incidents should be classified by severity and outcome.

Examples include:

* attempted manipulation;
* successful behavioral manipulation;
* unauthorized tool use;
* credential compromise;
* unauthorized data access;
* unauthorized transaction;
* persistent agent compromise;
* cross-agent compromise.

---

### 162. Agent Hijacking Incident Response Integration

Hijacking response should integrate with the enterprise incident-response framework.

```text id="1j5f2a"
Detect
 ↓
Validate
 ↓
Contain
 ↓
Revoke Authority
 ↓
Isolate Agent
 ↓
Assess Impact
 ↓
Recover
 ↓
Investigate
 ↓
Remediate
```

Response procedures should prioritize stopping continued autonomous actions.

---

### 163. SOC Integration

The SOC should receive sufficient telemetry to identify agent hijacking.

Relevant signals include:

* agent identity;
* user identity;
* tool usage;
* authorization;
* network activity;
* memory modifications;
* transactions;
* configuration changes.

---

### 164. Audit Evidence

Agent hijacking controls should generate auditable evidence.

Evidence may include:

* threat models;
* risk assessments;
* access reviews;
* authorization records;
* configuration history;
* testing results;
* monitoring events;
* incident records.

---

### 165. Governance Control Matrix

A governance control matrix should connect hijacking risks with preventive and detective controls.

| Risk                 | Control                   | Evidence                |
| -------------------- | ------------------------- | ----------------------- |
| Prompt hijacking     | Context isolation         | Test results            |
| Tool hijacking       | Tool allowlist            | Configuration           |
| Credential hijacking | Short-lived credentials   | IAM records             |
| Memory poisoning     | Memory integrity          | Audit records           |
| Privilege escalation | Independent authorization | Decision logs           |
| Runaway behavior     | Circuit breaker           | Alert / shutdown record |

---

### 166. Risk-to-Control Traceability

Each identified hijacking risk should map to an enforceable control.

```text id="m1lh54"
Hijacking Threat
      ↓
Risk
      ↓
Control Objective
      ↓
Security Control
      ↓
Implementation
      ↓
Test
      ↓
Evidence
```

This enables governance teams to demonstrate that identified risks are actively managed.

---

### 167. GRC Integration

Agent hijacking should be integrated into enterprise GRC processes.

Relevant areas include:

* AI risk registers;
* control libraries;
* policy management;
* risk assessments;
* exceptions;
* remediation;
* audit;
* assurance.

This allows agent hijacking to become part of the organization's broader risk-management structure.

---

### 168. Hijacking Exception Management

Exceptions to hijacking security requirements should be formally documented.

Each exception should identify:

* affected agent;
* control gap;
* business justification;
* risk;
* compensating controls;
* risk owner;
* expiration date.

Exceptions should be temporary and periodically reviewed.

---

### 169. Residual Hijacking Risk Acceptance

Some hijacking risk may remain after controls are implemented.

Residual risk should be:

* documented;
* quantified or qualitatively assessed;
* assigned to an authorized risk owner;
* formally accepted where appropriate;
* periodically reviewed.

The agent or AI model must never be the party accepting enterprise risk.

---

### 170. Agent Hijacking Reauthorization

Agents should undergo reauthorization after significant changes or at defined intervals.

Triggers may include:

* model changes;
* tool additions;
* permission changes;
* new data sources;
* new external integrations;
* major incidents;
* changes in business purpose.

Reauthorization verifies that the agent remains within its approved security boundary.

---

### 171. Agent Hijacking Retirement Governance

When an agent is retired, its hijacking attack surface should be eliminated.

Retirement should include:

* disabling the agent;
* revoking credentials;
* removing tool access;
* terminating sessions;
* disabling workflows;
* removing unnecessary memory;
* updating the agent inventory.

Retirement should also verify that the former agent cannot continue executing autonomous actions.

---

### 172. Independent Hijacking Assurance

High-risk agents should undergo independent assurance.

Assessment should evaluate:

* hijacking resistance;
* authorization independence;
* context isolation;
* memory security;
* tool controls;
* transaction boundaries;
* monitoring;
* emergency response.

Independent assurance provides confidence that the agent's security controls operate as designed.

---

### 173. Agent Hijacking Governance Maturity Model

Organizations can assess their hijacking governance maturity as follows:

```text id="0xiw8y"
Level 1 — Ad Hoc
Limited awareness of hijacking risk

Level 2 — Defined
Basic agent inventory and policies

Level 3 — Controlled
Threat modeling, testing, access controls, monitoring

Level 4 — Managed
Continuous control monitoring and GRC integration

Level 5 — Adaptive
Dynamic risk controls, continuous adversarial testing,
behavioral detection, and lifecycle reauthorization
```

---

### 174. Enterprise Agent Hijacking Operating Model

A mature operating model distributes responsibilities across organizational functions.

```text id="u4xv91"
Business Owner
      │
      ├── Purpose / Risk
      │
AI Platform
      │
      ├── Agent Architecture
      │
Security / GRC
      │
      ├── Controls / Governance
      │
SOC
      │
      ├── Detection / Response
      │
Engineering
      │
      ├── Remediation
      │
Audit
      │
      └── Independent Assurance
```

This prevents hijacking risk from being owned solely by the AI development team.

---

### 175. Governance Evidence Model

Governance evidence should demonstrate that hijacking risks are actively controlled.

Evidence should allow the organization to establish:

```text id="u7c7r2"
What agent?
      ↓
What authority?
      ↓
What inputs?
      ↓
What controls?
      ↓
What monitoring?
      ↓
What incidents?
      ↓
What remediation?
```

Evidence should be retained according to enterprise, regulatory, and contractual requirements.

---

### 176. Enterprise Security Principle

The enterprise security principle is:

> **An agent must remain securely bounded even when its behavior, context, memory, credentials, or execution environment is compromised.**

Security architecture should therefore assume that agent manipulation is possible while preventing that manipulation from becoming unrestricted enterprise authority.

---

### 177. Strategic Governance Principle

The strategic governance principle is:

> **Govern agent hijacking as a failure of the complete agent control environment, not merely as a failure of model behavior.**

The governance model should continuously evaluate the relationship between:

```text id="4gq6i8"
Agent
 ↓
Identity
 ↓
Context
 ↓
Memory
 ↓
Tools
 ↓
Authority
 ↓
Actions
 ↓
Business Impact
```

Effective governance ensures that compromise at one layer does not automatically compromise the layers above or below it.

# Part 4 — Advanced Enterprise Architecture, Assurance, and Operationalization

### 178. Enterprise Agent Hijacking Security Architecture

An enterprise architecture for agent hijacking must assume that an attacker may successfully manipulate an agent's reasoning, context, memory, or execution behavior.

The architecture should therefore establish independent security boundaries around the agent.

```text
                  ENTERPRISE SECURITY CONTROL PLANE
 ┌──────────────────────────────────────────────────────────────┐
 │ Identity │ Authorization │ Policy │ Risk │ Monitoring │ SOC │
 └──────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │ Agent Security   │
                    │     Gateway      │
                    └────────┬─────────┘
                             │
                    ┌────────▼─────────┐
                    │  Agent Runtime   │
                    │ ┌──────────────┐ │
                    │ │ Model        │ │
                    │ │ Context      │ │
                    │ │ Memory       │ │
                    │ │ Planner      │ │
                    │ └──────────────┘ │
                    └────────┬─────────┘
                             │
                    ┌────────▼─────────┐
                    │ Tool / Action    │
                    │ Enforcement      │
                    └────────┬─────────┘
                             │
                             ▼
                    Protected Enterprise
                       Systems / Data
```

The critical architectural principle is that the agent must not control all security decisions governing its own execution.

---

### 179. Enterprise Agent Hijacking Security Gateway

A dedicated security gateway can mediate interactions between agents and protected enterprise resources.

The gateway may enforce:

* agent identity;
* authentication;
* authorization;
* tool allowlisting;
* action validation;
* transaction limits;
* data controls;
* egress policies;
* rate limits;
* emergency blocking.

The gateway should remain outside the agent's reasoning authority.

---

### 180. Agent Hijacking Control Plane

The control plane should provide centralized security management for agents.

It may manage:

```text id="x4h5e2"
Agent Identity
      ↓
Agent Policy
      ↓
Permissions
      ↓
Tool Access
      ↓
Risk Thresholds
      ↓
Approval Requirements
      ↓
Emergency Controls
```

The control plane should be independently protected from agent runtimes.

---

### 181. Hijacking Policy-as-Code

Security policies should be represented as machine-enforceable rules wherever practical.

Example:

```text id="2g8v5x"
IF agent = CustomerSupport
AND action = refund
AND amount > approved_limit
THEN require_human_approval
```

Policy-as-code reduces dependence on the agent correctly interpreting security requirements.

---

### 182. Zero Trust Agent Hijacking Architecture

Every agent request should be treated as potentially untrusted until independently verified.

The architecture should continuously evaluate:

* who is acting;
* which agent is acting;
* what is being requested;
* what resource is targeted;
* what authority exists;
* what context applies;
* what risk is present.

Trust should never be granted merely because an agent has previously behaved correctly.

---

### 183. Dynamic Agent Authorization

Authorization should be capable of changing according to real-time conditions.

```text id="u2s0xw"
Agent Identity
      +
Current Context
      +
Requested Action
      +
Risk Score
      +
Transaction Value
      ↓
Dynamic Authorization
      ↓
ALLOW / DENY / ESCALATE
```

This allows the architecture to respond to emerging evidence of hijacking.

---

### 184. Context-Aware Authorization Architecture

Authorization decisions should consider the context surrounding an action.

Relevant attributes may include:

* initiating user;
* agent identity;
* session;
* location or environment;
* data sensitivity;
* action type;
* transaction value;
* current risk state.

A legitimate agent may therefore receive different authorization depending on circumstances.

---

### 185. Agent Trust Zones

Agents should be placed into security zones according to their capabilities and risk.

```text id="y1k4g9"
Zone 1 — Low Risk
Limited information access

Zone 2 — Operational
Business-system access

Zone 3 — Sensitive
Sensitive-data access

Zone 4 — High Impact
Financial / privileged actions
```

Movement between zones should require explicit authorization.

---

### 186. Privilege Segmentation

Agent privileges should be segmented so that hijacking one capability does not automatically expose all others.

For example:

```text id="8d9v3r"
Customer Data
     │
     ├── Read Service
     │
     ├── Update Service
     │
     └── Financial Service
```

Each capability should have its own security boundary.

---

### 187. Secure Agent-to-Tool Hijacking Architecture

Tools should be accessed through controlled interfaces rather than directly from unrestricted agent runtime environments.

```text id="j0j7nm"
Agent
 ↓
Tool Request
 ↓
Tool Security Gateway
 ↓
Authorization
 ↓
Parameter Validation
 ↓
Approved Tool
```

The gateway should prevent unauthorized tool substitution and invocation.

---

### 188. Tool Invocation Security

Every sensitive tool invocation should be independently evaluated.

The security layer should validate:

* calling agent;
* requested function;
* parameters;
* resource;
* authorization;
* transaction risk.

The agent's claim that an operation is necessary should not constitute authorization.

---

### 189. Tool Result Security

Tool results should remain untrusted until validated.

```text id="0v5d2b"
Tool Result
      ↓
Integrity / Schema Validation
      ↓
Trust Classification
      ↓
Agent Context
```

This reduces the risk of tool-result-based hijacking.

---

### 190. Multi-Agent Trust Architecture

In multi-agent environments, trust relationships should be explicit.

```text id="2j9s0r"
Agent A
   │
   │ authenticated request
   ▼
Agent B
   │
   │ independently authorized action
   ▼
Enterprise System
```

Registration as an approved agent should not imply unrestricted trust.

---

### 191. Secure Delegation Architecture

Delegation should preserve the original authority chain.

```text id="4j7w0v"
User
 ↓
Agent A
 ↓
Delegated Authority
 ↓
Agent B
 ↓
Restricted Action
```

Agent B should not receive more authority than Agent A was permitted to delegate.

---

### 192. Agent Attribution Plane

Every autonomous action should maintain attribution across the complete execution chain.

Attribution should correlate:

* initiating user;
* initiating session;
* agent;
* delegated agent;
* tool;
* authorization decision;
* transaction;
* resulting system change.

This provides forensic visibility after suspected hijacking.

---

### 193. Credential Isolation Architecture

Credentials should be separated from agent reasoning and memory.

```text id="p6h2ly"
Agent
  │
  │ Request
  ▼
Credential Broker
  │
  │ Short-Lived Credential
  ▼
Protected Service
```

The agent should receive only the minimum credential authority necessary for the specific operation.

---

### 194. Short-Lived Agent Authority

Agent authority should expire automatically.

A hijacked agent should therefore have a limited period in which compromised authority can be exercised.

Short-lived authority should be combined with:

* narrow scopes;
* transaction limits;
* revocation;
* continuous monitoring.

---

### 195. Agent Hijacking Lifecycle Architecture

Security controls should operate throughout the agent lifecycle.

```text id="j3v5fp"
Design
 ↓
Registration
 ↓
Authorization
 ↓
Deployment
 ↓
Operation
 ↓
Monitoring
 ↓
Reauthorization
 ↓
Retirement
```

Hijacking resistance should be continuously maintained rather than established only during initial deployment.

---

### 196. Hijacking Integrity Controls

Security-critical agent components should have integrity protections.

These may include:

* signed configurations;
* immutable policies;
* cryptographic hashes;
* protected prompts;
* configuration monitoring;
* trusted deployment pipelines.

Unauthorized changes should generate security events.

---

### 197. Hijacking Provenance Architecture

The architecture should maintain provenance for information influencing agent decisions.

Provenance should identify:

* source;
* timestamp;
* trust classification;
* transformation;
* retrieval path;
* agent or system that introduced the information.

This allows investigators to identify how malicious influence entered the agent.

---

### 198. Agent Hijacking Risk Scoring

Agents and individual actions can be assigned dynamic hijacking-risk scores.

Potential factors include:

```text id="r2h4wm"
Input Risk
+
Context Risk
+
Memory Risk
+
Identity Risk
+
Tool Risk
+
Transaction Risk
+
Behavioral Anomaly
=
Hijacking Risk
```

Risk scores should support adaptive controls rather than replace deterministic security policies.

---

### 199. Adaptive Hijacking Controls

Controls should become more restrictive when evidence of hijacking increases.

For example:

```text id="w4s2hk"
Normal
 ↓
Enhanced Monitoring
 ↓
Restricted Tools
 ↓
Human Approval
 ↓
Action Blocking
 ↓
Agent Isolation
```

Adaptive controls can reduce exposure while avoiding unnecessary disruption during normal operation.

---

### 200. Autonomous Transaction Protection

Transactions initiated by agents should have independent protection.

The transaction security layer should evaluate:

* amount;
* recipient;
* resource;
* frequency;
* authorization;
* risk;
* reversibility.

A manipulated agent should not be able to directly convert reasoning into unrestricted financial or operational transactions.

---

### 201. High-Impact Hijacking Protection

High-impact actions should have stronger controls.

Examples include:

* financial transfers;
* privileged account changes;
* production changes;
* security-policy modifications;
* deletion of critical data;
* external contractual commitments.

Such actions may require explicit human authorization or dual control.

---

### 202. Human Approval Architecture

Human approval should be implemented outside the agent's conversational reasoning.

```text id="8v7zcy"
Agent Proposal
      ↓
Risk Evaluation
      ↓
Approval Service
      ↓
Authorized Human
      ↓
Independent Confirmation
      ↓
Execution
```

The agent should not be able to fabricate or self-approve human authorization.

---

### 203. Approval Integrity

Approval mechanisms should verify that approval was:

* issued by the correct person;
* associated with the correct transaction;
* issued within the valid time period;
* not modified;
* not replayed.

Approval must be cryptographically or technically bound to the action whenever appropriate.

---

### 204. Dual Control and Segregation of Duties

Critical actions should require independent authorization from separate parties or control paths.

This reduces the likelihood that a hijacked agent or compromised individual control path can independently complete a high-impact operation.

---

### 205. Agent Hijacking Kill-Switch Architecture

The kill switch should exist outside the agent runtime.

```text id="6o2r2s"
SOC / Security Operator
          │
          ▼
   Emergency Control
          │
    ┌─────┴─────┐
    ▼           ▼
Stop Agent   Revoke Authority
    │           │
    └─────┬─────┘
          ▼
   Block Further Actions
```

The agent must not be able to override the mechanism.

---

### 206. Blast-Radius Reduction

Architecture should deliberately limit the maximum impact of a hijacked agent.

Controls include:

* segmented identities;
* restricted tools;
* limited data;
* transaction thresholds;
* network isolation;
* short-lived authority;
* human approval.

The objective is to make compromise survivable.

---

### 207. Agent Circuit Breakers

Circuit breakers should automatically interrupt agent activity when predefined risk conditions occur.

Triggers may include:

* excessive transactions;
* repeated authorization failures;
* unusual destinations;
* abnormal tool usage;
* suspicious behavioral patterns.

---

### 208. Continuous Agent Hijacking Security Testing

Security testing should continue after deployment.

Testing should evaluate whether new:

* models;
* prompts;
* tools;
* data sources;
* integrations;
* configurations

introduce new hijacking paths.

---

### 209. Regression Testing for Agent Hijacking

Security regression tests should verify that previously mitigated hijacking scenarios remain blocked.

```text id="m4v8gj"
Known Attack
     ↓
Security Test
     ↓
Expected Result = BLOCK
     ↓
Deployment Gate
```

A model or configuration update should not silently weaken existing protections.

---

### 210. Continuous Adversarial Hijacking Testing

Organizations should continuously simulate realistic hijacking scenarios.

Testing should include:

* direct prompt manipulation;
* indirect prompt injection;
* malicious documents;
* poisoned memory;
* tool-result manipulation;
* agent impersonation;
* delegated-authority abuse;
* credential compromise.

---

### 211. Behavioral Baselines

Each agent should have an expected behavioral baseline.

The baseline may include:

* normal tools;
* normal transaction sizes;
* normal destinations;
* normal execution frequency;
* normal data-access patterns;
* normal operating periods.

Significant deviations should generate investigation signals.

---

### 212. Agent Hijacking Security Observability

Observability should provide visibility across the entire autonomous execution chain.

```text id="2f1kq7"
Input
 ↓
Context
 ↓
Decision
 ↓
Tool
 ↓
Authorization
 ↓
Transaction
 ↓
Business Effect
```

Security teams should be able to reconstruct this chain during an investigation.

---

### 213. Security Telemetry

Telemetry should capture security-relevant events such as:

* identity events;
* authorization decisions;
* tool invocations;
* memory modifications;
* context changes;
* network connections;
* transaction activity;
* configuration changes.

Telemetry should be correlated using a common agent and execution identifier.

---

### 214. SIEM and SOC Integration

Agent telemetry should integrate with enterprise security operations.

The SOC should be capable of detecting correlations such as:

```text id="0f4m3c"
Prompt Injection
      +
New Tool Usage
      +
Abnormal Data Access
      +
Large Transaction
      ↓
Potential Agent Hijacking
```

This provides stronger detection than analyzing any single event.

---

### 215. Agent Hijacking Incident Response Playbook

A dedicated playbook should define the response to suspected hijacking.

```text id="m5o7cp"
Detect
 ↓
Validate
 ↓
Classify
 ↓
Restrict
 ↓
Revoke
 ↓
Isolate
 ↓
Preserve Evidence
 ↓
Investigate
 ↓
Recover
 ↓
Reauthorize
```

The response should prioritize preventing continued autonomous actions.

---

### 216. Agent Compromise and Hijacking Containment

Containment should occur at multiple layers.

Potential actions include:

* stop agent execution;
* revoke credentials;
* disable tools;
* block network egress;
* freeze transactions;
* isolate memory;
* terminate sessions;
* disable delegated agents.

Layered containment reduces residual attack paths.

---

### 217. Cryptographic Agent Integrity

Security-critical agent components should use cryptographic integrity protections where appropriate.

These may protect:

* configuration;
* identity;
* policies;
* messages;
* deployment artifacts;
* memory records;
* audit events.

Cryptographic integrity helps distinguish authorized changes from unauthorized manipulation.

---

### 218. Agent Configuration Integrity

Configuration changes should be monitored and controlled.

Security-sensitive configuration includes:

* tools;
* permissions;
* endpoints;
* prompts;
* memory settings;
* approval thresholds;
* network policies.

Unauthorized changes should trigger alerts and potentially automatic containment.

---

### 219. Agent Hijacking Supply-Chain Assurance

The agent supply chain should be assessed for hijacking opportunities.

Components may include:

* models;
* frameworks;
* agent runtimes;
* plugins;
* tools;
* APIs;
* packages;
* datasets;
* prompts.

Compromised dependencies can create indirect agent-control paths.

---

### 220. Model Provider Assurance

Organizations should assess model providers for controls relevant to agent integrity.

Assessment areas may include:

* model change management;
* security practices;
* availability;
* data handling;
* version management;
* incident notification;
* supply-chain security.

---

### 221. Tool and API Provider Assurance

External tools and APIs should be evaluated according to their potential hijacking impact.

Assessment should consider:

* provider security;
* authentication;
* authorization;
* data handling;
* endpoint integrity;
* change management;
* incident response.

---

### 222. Agent Hijacking Control Testing

Controls should be periodically tested against defined attack scenarios.

Testing should establish whether:

```text id="5t0w0v"
Hijacking Attempt
      ↓
Detection
      ↓
Authorization Boundary
      ↓
Action Control
      ↓
Containment
```

operates as designed.

---

### 223. Independent Security Assessment

High-risk autonomous systems should receive independent security assessments.

The assessment should examine both preventive and detective controls.

It should specifically determine whether agent manipulation can cross:

* identity boundaries;
* authorization boundaries;
* tool boundaries;
* data boundaries;
* transaction boundaries.

---

### 224. Risk-Based Hijacking Assurance Frequency

Assurance frequency should correspond to risk.

```text id="1g3fph"
Low Risk
→ Periodic Review

Moderate Risk
→ Regular Testing

High Risk
→ Frequent Assessment

Critical Risk
→ Continuous / Near-Continuous Assurance
```

High-impact agents should not rely solely on annual security reviews.

---

### 225. Continuous Agent Hijacking Drift Detection

The architecture should continuously detect changes that may increase hijacking risk.

Potential drift includes:

* additional tools;
* broader permissions;
* new data sources;
* model updates;
* prompt changes;
* new integrations;
* changed network access.

Detected drift should trigger reassessment where appropriate.

---

### 226. Enterprise Agent Hijacking Security Dashboard

Security dashboards should provide an enterprise view of hijacking exposure.

Example:

| Metric                           | Status |
| -------------------------------- | ------ |
| Agents at Risk                   | 12     |
| High-Risk Agents                 | 4      |
| Hijacking Attempts               | 37     |
| Successful Manipulations         | 2      |
| Unauthorized Tool Attempts       | 19     |
| Emergency Stops                  | 3      |
| Agents Requiring Reauthorization | 6      |

The dashboard should support operational decision-making rather than merely reporting technical events.

---

### 227. Agent Hijacking Security Metrics

Useful metrics include:

* hijacking attempts detected;
* successful hijacking events;
* time to detect;
* time to contain;
* unauthorized tool invocation attempts;
* privilege escalation attempts;
* abnormal memory modifications;
* emergency shutdown frequency;
* control failure rate.

Metrics should distinguish attempted attacks from successful security-boundary violations.

---

### 228. Agent Hijacking Assurance Package

A high-risk agent should maintain an assurance package containing evidence such as:

```text id="e0qz3k"
Risk Assessment
Threat Model
Security Architecture
Identity Configuration
Authorization Policies
Tool Inventory
Hijacking Test Results
Monitoring Configuration
Incident Playbook
Kill-Switch Validation
Access Reviews
Reauthorization Records
```

This provides evidence that hijacking controls are designed, implemented, tested, and maintained.

---

### 229. Lifecycle Reauthorization

Agents should be reauthorized when material changes occur.

Triggers may include:

* model replacement;
* new tools;
* new permissions;
* new data sources;
* new external connectivity;
* business-purpose changes;
* major incidents;
* material behavioral changes.

Reauthorization should confirm that the agent's current authority remains justified.

---

### 230. Agent Hijacking Retirement

Retiring an agent should eliminate its remaining ability to act.

The retirement process should:

1. stop execution;
2. revoke credentials;
3. disable tools;
4. terminate active sessions;
5. remove scheduled tasks;
6. disable delegated authority;
7. address persistent memory;
8. update the enterprise inventory.

---

### 231. Enterprise Security Integration

Agent hijacking controls should integrate with existing enterprise security capabilities.

Relevant systems include:

* IAM;
* PAM;
* API gateways;
* SIEM;
* SOAR;
* DLP;
* EDR;
* network security;
* secrets management;
* GRC platforms.

Agent security should become part of the existing enterprise control ecosystem rather than operate as an isolated AI security function.

---

### 232. Agent Hijacking Architecture Traceability

Every major hijacking risk should map to an architectural control.

```text id="0z5d93"
Threat
 ↓
Risk
 ↓
Security Requirement
 ↓
Architecture Control
 ↓
Implementation
 ↓
Test
 ↓
Evidence
```

This creates traceability from threat identification through operational assurance.

---

### 233. Agent Security Boundary Enforcement

The architecture must enforce explicit boundaries between:

* model behavior;
* agent context;
* agent identity;
* authorization;
* tools;
* data;
* transactions.

The agent should never be the final authority over the boundaries protecting enterprise resources.

---

### 234. Model Behavior Is Not Security Authorization

A model may determine:

> "This action appears appropriate."

That does not mean:

> "This action is authorized."

The architecture should enforce:

```text id="7s9v5x"
Model Decision
      ↓
Action Proposal
      ↓
Independent Authorization
      ↓
Security Policy
      ↓
Execution
```

This distinction prevents successful model manipulation from becoming automatic authorization.

---

### 235. Agent Capability Is Not Agent Authority

An agent may technically possess a capability without being authorized to exercise it in every context.

For example:

```text id="5j2p4u"
Capability:
Issue Refund

Authority:
Refund ≤ €500
Approved Customer
Approved Reason
Approved Session
```

Capability defines what the system can potentially do; authority defines what it is permitted to do now.

---

### 236. Autonomous Execution Must Remain Bounded

Autonomy should always operate within enforceable boundaries.

Those boundaries should include:

* identity;
* authorization;
* tools;
* data;
* transaction limits;
* execution duration;
* network access;
* human approval.

Autonomy without enforceable boundaries creates unacceptable hijacking exposure.

---

### 237. Agent Compromise Must Not Equal Enterprise Compromise

A compromised agent should not provide unrestricted access to the enterprise.

```text id="t9l6u4"
Compromised Agent
       ↓
Limited Identity
       ↓
Limited Permissions
       ↓
Limited Tools
       ↓
Limited Data
       ↓
Limited Transactions
       ↓
Contained Enterprise Impact
```

This principle is central to blast-radius reduction.

---

### 238. Autonomous Systems Require Independent Control

Security-critical controls must remain independent from the agent they protect.

Independent controls should include:

* authorization;
* credential management;
* transaction enforcement;
* monitoring;
* emergency shutdown;
* network controls;
* policy enforcement.

The agent may propose actions, but independent enterprise controls must determine whether those actions can occur.

---

### 239. Continuous Agentic Security Assurance

Agent hijacking security must be continuously assured throughout the operational lifecycle.

Assurance should combine:

```text id="l7w6k2"
Threat Modeling
      +
Security Testing
      +
Behavioral Monitoring
      +
Control Validation
      +
Drift Detection
      +
Incident Response
      +
Reauthorization
      ↓
Continuous Hijacking Assurance
```

The strategic objective is not to assume that an agent can never be hijacked. It is to ensure that **even when an agent is manipulated or compromised, its authority remains independently controlled, its actions remain bounded, its behavior remains observable, and enterprise impact remains contained.**



