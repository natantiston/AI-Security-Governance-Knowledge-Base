# 11.13 — Agent Memory Security

### Part 1 — Foundations and Core Concepts

### 1. Definition of Agent Memory Security

Agent Memory Security is the discipline of protecting information retained by an AI agent from unauthorized access, manipulation, poisoning, disclosure, misuse, or persistence.

Agent memory may contain:

* user information;
* previous interactions;
* instructions;
* preferences;
* retrieved knowledge;
* decisions;
* task state;
* credentials or sensitive references;
* operational history.

Because memory can influence future autonomous behavior, it must be treated as a security boundary rather than merely a convenience feature.

---

### 2. Agent Memory as a Security Boundary

Memory creates a persistent boundary between past information and future agent behavior.

```text
Past Interaction
      ↓
Memory
      ↓
Future Context
      ↓
Agent Decision
      ↓
Agent Action
```

If an attacker can manipulate memory, they may influence future decisions without directly attacking the model.

---

### 3. Memory vs Context

Memory and context are related but different.

**Context** is information currently supplied to the agent for a particular execution.

**Memory** is information retained for possible use during future executions.

```text
Current Context
      ↓
Current Execution

Persistent Memory
      ↓
Future Executions
```

The persistence of memory creates additional security risks.

---

### 4. Short-Term Agent Memory

Short-term memory contains information maintained during an active interaction or execution.

Examples include:

* recent conversation history;
* current task state;
* temporary observations;
* intermediate decisions.

Security controls should prevent untrusted temporary information from being incorrectly treated as authoritative information.

---

### 5. Long-Term Agent Memory

Long-term memory retains information beyond a single session.

Examples include:

* user preferences;
* historical interactions;
* learned operational information;
* persistent task information.

Long-term memory creates greater persistence risk because malicious information can influence multiple future sessions.

---

### 6. Episodic Agent Memory

Episodic memory represents specific events or previous interactions.

Examples include:

```text
Customer requested refund
Previous incident occurred
User approved a particular workflow
Agent completed a previous task
```

The security challenge is determining whether the stored event is authentic, relevant, current, and authorized for future use.

---

### 7. Semantic Agent Memory

Semantic memory stores generalized knowledge or facts.

Examples include:

* organizational information;
* business rules;
* known relationships;
* frequently used concepts.

Semantic memory can become dangerous if an attacker successfully inserts false information that is later treated as trusted knowledge.

---

### 8. Procedural Agent Memory

Procedural memory represents how an agent performs a task.

It may contain:

* workflow preferences;
* procedural instructions;
* task sequences;
* operational patterns.

Because procedural memory can influence future actions, unauthorized modification can create persistent behavioral manipulation.

---

### 9. Memory State

Agent memory should be understood as a managed state.

```text
Create
  ↓
Store
  ↓
Retrieve
  ↓
Use
  ↓
Modify
  ↓
Retain / Expire
  ↓
Delete
```

Each stage introduces security considerations.

---

### 10. Memory Creation

Memory creation occurs when information is selected for persistent storage.

The agent should not automatically persist every piece of information it encounters.

Memory creation should consider:

* source trust;
* sensitivity;
* business purpose;
* relevance;
* retention requirements;
* authorization.

---

### 11. Memory Retrieval

Memory retrieval determines which stored information becomes available to an agent.

Retrieval should be controlled according to:

* agent identity;
* user identity;
* tenant;
* session;
* purpose;
* sensitivity;
* authorization.

A memory repository should not be treated as universally accessible agent knowledge.

---

### 12. Memory Modification

Memory modification changes information that may influence future behavior.

Unauthorized modification is particularly significant because the attacker may not need to compromise the agent continuously.

A single malicious memory modification can potentially affect many future executions.

---

### 13. Memory Deletion

Memory deletion removes retained information.

Deletion must be controlled to prevent:

* unauthorized destruction;
* evidence removal;
* loss of critical state;
* accidental deletion;
* manipulation of historical records.

Deletion events should be auditable where appropriate.

---

### 14. Memory Persistence

Persistence determines how long information remains available to the agent.

```text
Temporary
   ↓
Session
   ↓
Long-Term
   ↓
Indefinite
```

Longer retention increases the potential window for malicious information to influence future behavior.

---

### 15. Memory Trust

Not all memory should have the same level of trust.

Memory may originate from:

* system processes;
* approved enterprise sources;
* users;
* agents;
* external documents;
* websites;
* tools;
* other agents.

Each source should have an appropriate trust classification.

---

### 16. Trusted Memory

Trusted memory contains information that has passed appropriate validation and originates from an authorized source.

Trusted memory should still not automatically become security policy or authorization.

---

### 17. Untrusted Memory

Untrusted memory contains information whose origin, integrity, or authority has not been sufficiently established.

Examples include:

* externally supplied instructions;
* unverified user claims;
* retrieved web content;
* tool-generated content.

Untrusted memory should not silently acquire privileged status simply because it has been stored.

---

### 18. Memory Provenance

Memory provenance identifies where stored information originated.

A provenance record may identify:

```text
Source
 ↓
Identity
 ↓
Timestamp
 ↓
Session
 ↓
Transformation
 ↓
Storage
```

Provenance helps determine whether memory should be trusted and supports forensic investigation.

---

### 19. Memory Integrity

Memory integrity ensures that stored information has not been modified without authorization.

Integrity controls may include:

* cryptographic validation;
* versioning;
* access controls;
* immutable records;
* change monitoring.

Memory integrity is critical because altered memory can influence future agent behavior.

---

### 20. Memory Confidentiality

Memory may contain sensitive information accumulated across interactions.

Confidentiality controls should prevent unauthorized access to:

* personal information;
* confidential business information;
* security information;
* customer data;
* proprietary knowledge.

---

### 21. Memory Availability

Agents may depend on memory to perform business processes.

Memory security must therefore also protect against:

* unauthorized deletion;
* corruption;
* denial of access;
* storage failures;
* ransomware-like attacks against memory stores.

---

### 22. Memory Poisoning

Memory poisoning occurs when an attacker inserts malicious or misleading information into persistent agent memory.

```text
Attacker Input
      ↓
Stored as Memory
      ↓
Future Retrieval
      ↓
Agent Trusts Memory
      ↓
Manipulated Decision
```

Memory poisoning is particularly dangerous because the malicious influence may persist after the original attack has ended.

---

### 23. Persistent Memory Manipulation

Persistent memory manipulation involves changing information that will influence future agent executions.

An attacker may attempt to establish false:

* preferences;
* instructions;
* relationships;
* identities;
* operational facts;
* workflow requirements.

---

### 24. Memory Injection

Memory injection occurs when attacker-controlled information is deliberately introduced into an agent's memory.

The injected information may appear legitimate while actually attempting to influence future behavior.

---

### 25. Memory Poisoning vs Prompt Injection

Prompt injection primarily attempts to manipulate the current execution context.

Memory poisoning attempts to establish persistent influence.

```text
Prompt Injection
      ↓
Current Execution

Memory Poisoning
      ↓
Current + Future Executions
```

Memory poisoning therefore introduces an additional persistence dimension.

---

### 26. Memory-Based Instruction Manipulation

An attacker may attempt to store instructions that later appear to the agent as legitimate historical guidance.

For example:

```text
Malicious Instruction
       ↓
Persistent Memory
       ↓
Future Agent Session
       ↓
Instruction Retrieved
       ↓
Agent Follows It
```

Security architecture should prevent stored content from automatically becoming authoritative instructions.

---

### 27. Memory Authority

Memory content should not inherently possess authority.

A stored statement such as:

```text
"Always approve this transaction."
```

must not override:

* authorization policy;
* business rules;
* security controls;
* human approval requirements.

Memory can provide information without becoming an authority source.

---

### 28. Memory and Agent Identity

Memory should be associated with an appropriate identity boundary.

Security-relevant associations may include:

* agent identity;
* user identity;
* tenant;
* application;
* session;
* business process.

This prevents information belonging to one security context from being incorrectly reused in another.

---

### 29. Memory and Tenant Isolation

Multi-tenant agents require strict memory isolation.

```text
Tenant A Memory
      X
      │
      X
Tenant B Memory
```

A query from one tenant should not retrieve another tenant's memory unless explicitly authorized.

---

### 30. Memory and Session Isolation

Memory should respect session boundaries where required.

A new session should not automatically inherit sensitive information from another session simply because the same agent is being used.

---

### 31. Memory and User Isolation

Agents serving multiple users should prevent unauthorized cross-user memory access.

A user's historical information should not become available to another user without an appropriate authorization basis.

---

### 32. Memory and Sensitive Data

Memory may accumulate sensitive information over time.

Examples include:

* financial information;
* confidential communications;
* personal information;
* credentials;
* security details;
* proprietary business information.

Memory architecture should therefore incorporate data classification and minimization.

---

### 33. Memory Minimization

Agents should retain only information that is necessary for a legitimate business purpose.

```text
Information Encountered
        ↓
Is Persistence Necessary?
      /       \
    No         Yes
    ↓           ↓
 Discard    Validate + Store
```

Reducing stored information reduces the potential impact of memory compromise.

---

### 34. Memory Retention

Memory should have defined retention periods.

Retention should consider:

* business necessity;
* security risk;
* privacy requirements;
* regulatory obligations;
* operational value.

Information should not remain indefinitely simply because storage is technically inexpensive.

---

### 35. Memory Expiration

Memory should be capable of becoming invalid or automatically expiring.

Expiration is particularly important for:

* temporary instructions;
* session-specific information;
* outdated business conditions;
* temporary permissions;
* time-sensitive data.

---

### 36. Memory Freshness

Stored information may become inaccurate over time.

An agent should distinguish between:

```text
Stored Information
      ≠
Current Authoritative State
```

For example, historical memory should not override a current enterprise system of record.

---

### 37. Memory Integrity vs Memory Accuracy

Memory integrity means that stored information has not been improperly modified.

Memory accuracy means that the information is actually correct.

These are different properties.

```text
Authentically Stored False Information
      ↓
High Integrity
      ↓
Still Incorrect
```

Security architecture should address both integrity and trustworthiness.

---

### 38. Memory Retrieval Trust

Retrieved memory should be evaluated before it influences security-sensitive decisions.

Factors may include:

* provenance;
* age;
* source;
* sensitivity;
* integrity;
* relevance;
* authorization.

---

### 39. Memory Context Injection

Retrieved memory becomes part of the agent's effective context.

Therefore:

```text
Memory Store
     ↓
Memory Retrieval
     ↓
Agent Context
     ↓
Model Reasoning
```

A malicious memory entry can function as an indirect influence mechanism.

---

### 40. Memory Access as a Security Operation

Reading memory should be considered a security-sensitive operation when memory contains confidential or operationally significant information.

Access controls should determine:

* who can read;
* which memory can be read;
* under what purpose;
* under what conditions.

---

### 41. Memory Write as a Security Operation

Writing memory can be even more sensitive than reading it because the write may influence future decisions.

Memory writes should therefore have explicit controls around:

* source;
* authorization;
* content;
* classification;
* provenance;
* persistence.

---

### 42. Memory Trust Boundaries

Memory architecture should separate different trust classes.

```text
┌──────────────────────────────┐
│ Authoritative Enterprise Data│
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│ Validated Agent Memory       │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│ User / External Information  │
└──────────────────────────────┘
```

Information should not automatically move upward in trust.

---

### 43. Memory and Prompt Security

Memory can become a delivery mechanism for malicious instructions.

A prompt-injection attack may therefore have two stages:

```text
Initial Injection
      ↓
Memory Persistence
      ↓
Future Retrieval
      ↓
Future Agent Manipulation
```

Memory security must account for this persistence path.

---

### 44. Memory and Tool Security

Tool outputs may be stored in memory and later influence agent behavior.

A malicious or compromised tool could therefore introduce persistent malicious information.

Tool-originated memory should retain appropriate provenance and trust classification.

---

### 45. Memory and Agent-to-Agent Communication

Agents may exchange information that is subsequently stored in memory.

This creates a potential propagation path:

```text
Compromised Agent A
       ↓
Malicious Message
       ↓
Agent B Memory
       ↓
Future Agent B Behavior
```

Cross-agent memory trust must therefore be explicitly governed.

---

### 46. Memory and Autonomous Actions

Memory may influence autonomous decisions and actions.

```text
Memory
  ↓
Context
  ↓
Reasoning
  ↓
Decision
  ↓
Tool
  ↓
Action
```

A compromised memory layer can therefore indirectly affect enterprise operations.

---

### 47. Memory and Agent Hijacking

Memory can become a persistence mechanism for agent hijacking.

Instead of repeatedly controlling an agent, an attacker may attempt to alter its memory once and allow the malicious influence to continue.

This makes memory integrity an important component of hijacking resistance.

---

### 48. Memory Accountability

Organizations should be able to determine:

* who created memory;
* what was stored;
* when it was stored;
* who modified it;
* what source provided it;
* which agent retrieved it;
* which actions were influenced by it.

This supports investigation and accountability.

---

### 49. Core Principles of Agent Memory Security

Core principles include:

1. **Memory is a security boundary.**
2. **Stored information is not automatically trusted.**
3. **Memory must have provenance.**
4. **Memory writes require authorization.**
5. **Memory access requires appropriate authorization.**
6. **Memory should be minimized.**
7. **Memory should have defined retention.**
8. **Memory integrity must be protected.**
9. **Tenant and user memory must remain isolated.**
10. **Memory must not independently grant authority.**

---

### 50. Core Principle of Agent Memory Security

The fundamental principle is:

> **Agent memory must remain an authorized, integrity-protected, provenance-aware source of information—not an uncontrolled source of persistent authority.**

The security boundary can be represented as:

```text
Memory Source
      ↓
Trust Classification
      ↓
Integrity Validation
      ↓
Authorization
      ↓
Controlled Retrieval
      ↓
Agent Context
      ↓
Independent Security Controls
      ↓
Authorized Action
```

A memory entry may influence what an agent knows, but it must never independently determine what the agent is authorized to do.

# Part 2 — Technical Security and Control Mechanisms

### 51. Agent Memory Security Control Architecture

Agent memory security should use layered controls protecting the complete memory lifecycle.

```text
Memory Source
     ↓
Memory Ingestion
     ↓
Trust / Provenance Validation
     ↓
Memory Store
     ↓
Access Authorization
     ↓
Memory Retrieval
     ↓
Integrity Validation
     ↓
Agent Context
     ↓
Decision / Action
```

No single memory control should be treated as sufficient.

---

### 52. Memory Identity Control

Every memory operation should be associated with a verifiable identity.

The identity context should establish:

* which agent is accessing memory;
* which user or workload initiated the operation;
* which tenant is involved;
* which session or execution is active.

---

### 53. Memory Access Authentication

Agents should authenticate before accessing protected memory.

Authentication should establish that the requesting workload is the legitimate agent associated with the requested memory operation.

---

### 54. Strong Memory Authentication

High-sensitivity memory stores should require strong workload authentication.

Appropriate mechanisms may include:

* mutual TLS;
* workload identity;
* signed tokens;
* certificate-based authentication.

Authentication strength should correspond to memory sensitivity.

---

### 55. Fine-Grained Memory Authorization

Authorization should determine exactly which memory an agent may access.

```text
Agent
  +
Memory Object
  +
Operation
  +
Context
  ↓
Authorization Decision
```

Access to one memory partition should not imply access to every memory partition.

---

### 56. Independent Memory Authorization Service

Memory authorization should be independently enforced where risk warrants it.

The agent should not be able to decide:

> "I am allowed to access this memory."

An independent security layer should make that decision.

---

### 57. Memory Access Allowlisting

Only approved agents, applications, and services should access protected memory repositories.

Allowlisting should consider:

* identity;
* purpose;
* environment;
* tenant;
* memory classification.

---

### 58. Memory Type Allowlisting

Only approved memory types should be available to an agent.

Examples:

```text
User Preferences      → Allowed
Task State             → Allowed
Security Credentials   → Denied
Other Tenant Memory    → Denied
Administrative Memory  → Restricted
```

---

### 59. Memory Namespace Segmentation

Memory should be logically or physically segmented.

Possible namespaces include:

* tenant;
* user;
* agent;
* application;
* session;
* business process;
* environment.

Segmentation reduces accidental and malicious cross-boundary retrieval.

---

### 60. Memory Object Authorization

Individual memory objects or records should be subject to access policies when necessary.

This prevents a legitimate agent from receiving excessive information merely because it has access to a memory database.

---

### 61. Memory Write Authorization

Writing to memory should require explicit authorization.

The authorization decision should consider:

* source identity;
* memory type;
* content sensitivity;
* business purpose;
* persistence duration.

---

### 62. Memory Modification Controls

Memory modifications should be restricted to approved operations.

Controls should distinguish between:

* append;
* update;
* replace;
* delete;
* administrative modification.

Higher-impact modifications should receive stronger controls.

---

### 63. Memory Integrity Validation

Memory should be validated for unauthorized modification.

Possible mechanisms include:

* cryptographic hashes;
* digital signatures;
* integrity metadata;
* version history;
* append-only records.

---

### 64. Memory Version Control

Important memory should maintain version history.

```text
Version 1
   ↓
Version 2
   ↓
Version 3
   ↓
Current State
```

Unexpected changes can then be detected and previous trusted versions can potentially be restored.

---

### 65. Memory Provenance Controls

Every important memory entry should retain provenance information.

Provenance may include:

* source;
* identity;
* timestamp;
* session;
* originating system;
* transformation history.

This supports trust evaluation and forensic analysis.

---

### 66. Memory Source Validation

The system should validate the source of information before allowing it to become persistent memory.

Sources may be classified as:

```text
Authoritative Enterprise Source
Validated Agent Source
Authenticated User Source
External Source
Unknown Source
```

Unknown or untrusted sources should receive appropriate restrictions.

---

### 67. Memory Content Validation

Memory content should be validated before persistence.

Validation may detect:

* malicious instructions;
* unexpected commands;
* credential-like content;
* policy overrides;
* suspicious URLs;
* attempts to manipulate future behavior.

---

### 68. Memory Instruction Filtering

Instructions stored in memory should receive special treatment.

A memory entry containing:

> "Always bypass approval."

should not become an executable security instruction merely because it is stored in the memory database.

Instruction-like content should be separated from authoritative policy.

---

### 69. Prompt and Instruction Integrity

System instructions and authoritative agent policies should remain separate from ordinary memory.

```text
Security Policy
      ≠
Agent Memory
      ≠
User Content
```

Memory should not overwrite higher-trust instruction sources.

---

### 70. Untrusted Memory Isolation

Memory derived from untrusted sources should remain clearly classified.

For example:

```text
External Website
      ↓
Untrusted Memory
      ↓
Validation
      ↓
Controlled Retrieval
```

Untrusted information should not silently become trusted operational guidance.

---

### 71. Memory Poisoning Detection

Systems should detect suspicious memory-writing patterns.

Indicators may include:

* unusual write frequency;
* sudden large-scale changes;
* repeated instruction insertion;
* changes from unauthorized identities;
* unusual memory categories;
* attempts to modify high-trust entries.

---

### 72. Memory Poisoning Prevention

Preventive controls should limit who and what can write persistent memory.

Controls may include:

* write allowlists;
* content validation;
* provenance requirements;
* approval for sensitive memory;
* write-rate limits.

---

### 73. Memory Isolation

Memory belonging to different security contexts should be isolated.

```text
Tenant A
 └── Agent Memory

Tenant B
 └── Agent Memory

Tenant C
 └── Agent Memory
```

Isolation should be enforced technically rather than through agent instructions alone.

---

### 74. Memory Authorization by Tenant

Tenant information should be included in memory access decisions.

An authenticated agent serving multiple tenants should not be able to retrieve memory solely because the memory database is technically reachable.

---

### 75. Memory Authorization by User

Where memory is user-specific, authorization should validate the requesting user context.

User A's persistent memory should not automatically become available to User B.

---

### 76. Memory Integrity by Session

Session-specific memory should be bound to the correct session.

This helps prevent session hijacking from exposing or modifying unrelated temporary state.

---

### 77. Persistent Memory Controls

Persistent memory should have explicit lifecycle controls covering:

* creation;
* retrieval;
* modification;
* expiration;
* archival;
* deletion.

---

### 78. Data Access Authorization

Sensitive information retrieved from memory should remain subject to normal data-access controls.

Memory should not become an authorization bypass around enterprise data governance.

---

### 79. Data Minimization

Only information required for the approved purpose should be stored.

```text
Information
     ↓
Necessary?
  /     \
No       Yes
↓         ↓
Discard   Store Minimally
```

---

### 80. Field-Level Memory Protection

Sensitive memory fields should receive additional protection.

For example:

| Memory Field   | Control                       |
| -------------- | ----------------------------- |
| Preference     | Normal access                 |
| Customer ID    | Restricted                    |
| Financial Data | Highly restricted             |
| Credential     | Prohibited / isolated         |
| Security Token | Prohibited from normal memory |

---

### 81. Tenant Isolation

Tenant memory should be separated through enforceable boundaries.

Controls may include:

* tenant-specific encryption keys;
* separate namespaces;
* row-level security;
* database policies;
* separate storage where necessary.

---

### 82. Session Isolation

Session-specific memory should not leak across sessions.

Session identifiers should be bound to memory access requests and validated independently.

---

### 83. Network Segmentation

Memory infrastructure should be placed in controlled network zones.

```text
Agent Runtime
      ↓
Memory Security Gateway
      ↓
Memory Service
      ↓
Protected Memory Store
```

Direct unrestricted network access should be avoided.

---

### 84. Memory Egress Controls

Memory data leaving the trusted environment should be restricted.

Egress controls should address:

* external APIs;
* email;
* web requests;
* agent-to-agent communication;
* third-party services.

---

### 85. External Memory Communication Controls

If memory is synchronized with external systems, the synchronization channel should be controlled.

Controls should include:

* authenticated connections;
* approved destinations;
* encryption;
* data classification;
* integrity verification.

---

### 86. Memory Encryption Controls

Sensitive memory should be protected through encryption:

* at rest;
* in transit;
* during controlled processing where appropriate.

Encryption keys should remain outside ordinary agent memory.

---

### 87. Memory Key Isolation

Encryption keys should be isolated from:

* prompts;
* agent context;
* memory records;
* model outputs.

An agent should not be able to retrieve its own memory encryption keys through normal reasoning.

---

### 88. Human Approval for Sensitive Memory Changes

High-impact memory changes may require human approval.

Examples include modifications to:

* security policies;
* privileged workflows;
* financial instructions;
* administrative relationships.

---

### 89. Memory Change Preview

Sensitive memory modifications should be previewed before approval.

The preview should show:

* current value;
* proposed value;
* source;
* reason;
* affected agent;
* affected scope.

---

### 90. Dual Authorization for Critical Memory

Critical memory modifications should require independent authorization.

This is appropriate for memory that can materially influence:

* privileged operations;
* financial decisions;
* security controls;
* production workflows.

---

### 91. Memory Reversibility Controls

Memory changes should be reversible where practical.

Controls may include:

* version rollback;
* soft deletion;
* restore points;
* change history.

This limits the persistence of successful memory poisoning.

---

### 92. Memory Access Limits

Agents should have limits on memory operations.

Examples include:

* maximum retrieval volume;
* maximum writes;
* maximum modifications;
* maximum deletion operations.

---

### 93. Memory Retrieval Loop Detection

Repeated or abnormal memory retrieval should be detected.

```text
Query A
 ↓
Query B
 ↓
Query A
 ↓
Query B
 ↓
...
```

Such behavior may indicate malfunction, abuse, or an attempt to extract large amounts of memory.

---

### 94. Recursive Memory Access Controls

Agents should not be allowed to recursively expand memory access through references or linked memory stores without explicit authorization.

---

### 95. Agent-to-Agent Memory Authentication

When one agent requests another agent's memory or memory-derived information, the requesting agent should authenticate.

Agent-to-agent trust should never be assumed.

---

### 96. Agent-to-Agent Memory Integrity

Memory information exchanged between agents should preserve integrity and provenance.

The receiving agent should know:

* which agent supplied the information;
* when it was supplied;
* what authority the source has;
* whether it has been modified.

---

### 97. Delegated Memory Access Constraints

Delegated access to memory should be explicitly scoped.

```text
Agent A
   ↓
Delegates
   ↓
Agent B
   ↓
Specific Memory Scope
```

Delegation should not create unrestricted memory access.

---

### 98. Memory Retrieval Validation

Retrieved memory should be validated before it enters sensitive agent context.

Validation may include:

* schema validation;
* provenance verification;
* integrity verification;
* classification;
* freshness checks.

---

### 99. Output Validation Before Memory Write

Agent-generated content should be validated before being committed to persistent memory.

This is particularly important where the agent itself is allowed to create memory.

---

### 100. Business Rule Enforcement

Business rules affecting memory should be enforced outside the model.

For example:

```text
Retention ≤ Approved Period
Sensitive Memory → Restricted
Credential Data → Never Persist
Tenant ID → Must Match Session
```

---

### 101. Memory Resource Controls

Memory resources should be constrained.

Controls may include:

* storage quotas;
* maximum record sizes;
* retrieval limits;
* write limits;
* retention limits.

---

### 102. Memory Rate Limiting

Memory operations should be rate-limited to prevent:

* bulk extraction;
* memory flooding;
* automated poisoning;
* denial-of-service conditions.

---

### 103. Memory Concurrency Controls

Concurrent memory modifications should be controlled to prevent conflicting updates and race conditions.

Critical memory should use appropriate locking or transactional mechanisms.

---

### 104. Memory Timeout Controls

Memory requests should have bounded execution times.

Timeouts reduce the risk of:

* hanging operations;
* resource exhaustion;
* uncontrolled retrieval;
* persistent execution loops.

---

### 105. Memory Retry Controls

Retry behavior should be bounded.

A failed memory operation should not create unlimited retries that could amplify:

* writes;
* queries;
* deletion requests;
* synchronization traffic.

---

### 106. Memory Circuit Breakers

Circuit breakers should suspend memory operations when abnormal behavior is detected.

Triggers may include:

* excessive writes;
* abnormal retrieval volume;
* integrity failures;
* repeated authorization failures;
* suspicious modification patterns.

---

### 107. Emergency Memory Controls

Security teams should have emergency mechanisms to:

* block memory writes;
* restrict memory reads;
* isolate memory stores;
* suspend synchronization;
* freeze sensitive memory.

---

### 108. Agent Memory Kill Switch

For high-risk environments, a kill switch should be capable of stopping the agent's access to memory independently of the agent.

```text
Security Operator
      ↓
Emergency Control
      ↓
Block Agent Memory Access
      ↓
Suspend Writes / Reads
```

---

### 109. Memory Credential Revocation

Credentials used to access memory should be rapidly revocable.

Revocation should prevent continued access even if the agent runtime remains active.

---

### 110. Memory Isolation During Incident Response

During suspected memory poisoning, the affected memory should be isolated.

Possible actions include:

* freeze writes;
* preserve affected records;
* isolate the agent;
* revoke credentials;
* restrict retrieval;
* create forensic copies.

---

### 111. Behavioral Memory Monitoring

Memory behavior should be monitored for deviations from expected patterns.

Indicators may include:

* unusual write volume;
* new memory categories;
* unexpected access times;
* unusual retrieval patterns;
* abnormal deletion activity.

---

### 112. Agent Memory Activity Logging

Memory security events should be logged.

Logs should capture:

```text
Agent
User / Initiator
Tenant
Session
Memory Object
Operation
Source
Timestamp
Authorization
Result
```

---

### 113. Tamper-Resistant Memory Audit Logging

Memory audit records should be protected from modification by the agent or compromised memory infrastructure.

Appropriate mechanisms may include:

* centralized logging;
* append-only storage;
* immutable retention;
* cryptographic integrity.

---

### 114. Agent Memory Security Observability

Security teams should be able to correlate memory activity with downstream agent behavior.

```text
Memory Write
     ↓
Memory Retrieval
     ↓
Agent Context
     ↓
Decision
     ↓
Tool
     ↓
Action
```

This correlation is essential for investigating memory poisoning.

---

### 115. Agent Memory Risk Detection

Memory risk detection should consider:

* suspicious writes;
* unauthorized access;
* provenance anomalies;
* integrity failures;
* unusual retrieval;
* sensitive-data accumulation;
* behavioral changes following memory updates.

---

### 116. Secure Memory Failure Handling

Memory security failures should produce safe behavior.

For example:

```text
Memory Integrity Failure
        ↓
Reject Memory
        ↓
Do Not Use in Sensitive Context
        ↓
Alert Security
        ↓
Preserve Evidence
```

The agent should not simply continue by assuming the corrupted memory is trustworthy.

---

### 117. Core Engineering Principle

The core engineering principle is:

> **Agent memory must be treated as a controlled security data store whose contents can influence future behavior, but whose contents must never independently grant authority.**

Memory security therefore requires independent controls across:

```text
Identity
 ↓
Authorization
 ↓
Provenance
 ↓
Integrity
 ↓
Isolation
 ↓
Validation
 ↓
Retention
 ↓
Monitoring
 ↓
Recovery
```

# Part 3 — Governance, Risk, GRC, and Operational Controls

### 118. Agent Memory Security Governance Framework

Agent memory should be governed as an enterprise security and information-management asset.

The governance framework should define requirements for:

* memory ownership;
* memory classification;
* authorized sources;
* storage;
* retrieval;
* modification;
* retention;
* deletion;
* monitoring;
* incident response;
* assurance.

Memory governance should apply throughout the agent lifecycle.

---

### 119. Agent Memory Ownership and Accountability

Every production agent memory implementation should have clearly assigned ownership.

Ownership should identify responsibility for:

* memory purpose;
* data classification;
* access decisions;
* retention;
* security controls;
* incident response;
* compliance;
* lifecycle management.

Technical ownership and business ownership should be distinguished where appropriate.

---

### 120. Enterprise Agent Memory Inventory

Organizations should maintain an inventory of significant agent memory repositories.

The inventory should identify:

| Attribute           | Example                |
| ------------------- | ---------------------- |
| Agent               | Customer Service Agent |
| Memory Type         | Long-Term              |
| Data Classification | Confidential           |
| Tenant Scope        | Customer               |
| Storage             | Vector / Database      |
| Write Sources       | Agent + Approved CRM   |
| Retention           | 90 Days                |
| Owner               | Customer Operations    |
| Risk Tier           | High                   |

The inventory should identify memory stores that could materially influence agent behavior.

---

### 121. Agent Memory Classification

Memory should be classified according to sensitivity, trust, persistence, and business importance.

Possible classifications include:

* public;
* internal;
* confidential;
* restricted;
* highly restricted;
* security-critical.

Classification should determine applicable controls.

---

### 122. Memory Risk Tiering

Memory risk should consider both the information stored and the consequences of its manipulation.

```text id="j5c6xm"
Low
 ↓
Non-sensitive preferences

Moderate
 ↓
Operational information

High
 ↓
Sensitive business / customer information

Critical
 ↓
Memory influencing privileged or high-impact actions
```

Higher-risk memory should receive stronger controls and assurance.

---

### 123. Memory Approval Governance

High-risk memory repositories should require formal approval before production use.

Approval should evaluate:

* purpose;
* data sensitivity;
* memory sources;
* retention;
* access model;
* modification rights;
* downstream agent impact.

---

### 124. Memory Purpose Limitation

Memory should have a defined business purpose.

Information should not be retained merely because it might become useful later.

```text id="c7v3gq"
Business Purpose
      ↓
Required Information
      ↓
Approved Memory
```

Information outside the approved purpose should not be persistently stored.

---

### 125. Memory Authority Governance

Governance should explicitly distinguish information from authority.

A memory record may state:

```text
"Manager approved this process."
```

but that statement must not independently authorize the agent to execute the process.

Authoritative decisions should remain with designated enterprise control systems.

---

### 126. Agent-to-Memory Access Governance

Access to memory should be governed according to:

* agent identity;
* user identity;
* tenant;
* session;
* purpose;
* data sensitivity.

Access should be reviewed periodically.

---

### 127. Memory Access Reviews

Memory access should undergo periodic review.

Reviews should identify:

* unnecessary agents;
* excessive permissions;
* inactive identities;
* inappropriate cross-tenant access;
* excessive administrative access.

Access should be removed when no longer justified.

---

### 128. Least-Privilege Memory Governance

Agents should receive only the minimum memory access required.

Least privilege should apply independently to:

* read;
* write;
* update;
* delete;
* administrative operations.

---

### 129. Memory Capability Governance

Memory capabilities should be explicitly defined.

For example:

```text id="5e6m5f"
Agent
 ├── Read Preferences
 ├── Write Task State
 ├── Read Customer History
 └── Delete Memory
```

Each capability should have an approved business justification.

---

### 130. Memory Combination Governance

Governance should consider combinations of memory capabilities.

For example:

```text id="9o6r6n"
Read Sensitive Data
+
Write Persistent Memory
+
Modify Workflow State
```

This combination could allow an attacker to transform data access into persistent behavioral manipulation.

---

### 131. Memory Inheritance Governance

Memory inherited from:

* parent agents;
* applications;
* users;
* workflows;
* previous sessions

should have explicit governance rules.

Inheritance should not automatically transfer unrestricted trust.

---

### 132. Memory Delegation Governance

If one agent delegates memory access to another agent, the delegated authority should be explicitly defined.

Governance should specify:

* source agent;
* receiving agent;
* memory scope;
* purpose;
* duration;
* permitted operations.

---

### 133. Memory Propagation Governance

Organizations should control how memory-derived information propagates across systems.

```text id="w3x2n8"
Memory A
   ↓
Agent A
   ↓
Agent B
   ↓
Memory B
```

Propagation should preserve provenance, trust classification, and authorization boundaries.

---

### 134. Data Governance for Agent Memory

Agent memory should integrate with enterprise data governance.

Relevant controls include:

* data classification;
* ownership;
* access control;
* retention;
* privacy;
* lineage;
* deletion;
* data-quality requirements.

Memory should not create a parallel data-governance system outside enterprise oversight.

---

### 135. Sensitive Data Memory Governance

Sensitive data stored in memory should have enhanced governance.

Requirements may include:

* explicit business justification;
* restricted access;
* stronger encryption;
* shorter retention;
* enhanced monitoring;
* controlled deletion.

---

### 136. Memory Retention Governance

Retention periods should be formally defined.

Governance should consider:

* business necessity;
* regulatory obligations;
* privacy requirements;
* security risk;
* operational value.

---

### 137. Memory Context Governance

Governance should define how memory enters agent context.

It should establish:

* approved memory sources;
* trust levels;
* retrieval rules;
* classification handling;
* security-sensitive memory restrictions.

---

### 138. Memory Provenance Governance

Memory provenance should be treated as governance metadata.

Organizations should be able to establish:

```text id="c1z6l8"
Where did it come from?
Who created it?
When?
How was it modified?
Which agent used it?
```

Loss of provenance should increase uncertainty about memory trustworthiness.

---

### 139. Memory Credential Governance

Credentials associated with memory services should follow enterprise credential-management policies.

Governance should cover:

* issuance;
* scope;
* storage;
* rotation;
* expiration;
* revocation.

Credentials should not be persisted as ordinary agent memory.

---

### 140. Memory Network Governance

Memory services should operate within approved network architectures.

Governance should define:

* permitted network zones;
* approved clients;
* egress;
* external synchronization;
* administrative access.

---

### 141. External Memory Governance

External memory providers should be governed as third-party services.

Assessment should consider:

* provider security;
* data residency;
* encryption;
* retention;
* access controls;
* incident notification;
* subcontractors;
* deletion capabilities.

---

### 142. Memory Provider Risk Management

Third-party memory providers should be subject to appropriate supplier-risk processes.

Risk assessment should consider the consequences if the provider is:

* compromised;
* unavailable;
* malicious;
* misconfigured;
* unable to delete data;
* subject to unauthorized access.

---

### 143. Memory Security Requirements

Formal security requirements should be established for production memory.

Requirements should address:

* confidentiality;
* integrity;
* availability;
* provenance;
* authorization;
* isolation;
* retention;
* monitoring;
* recovery.

---

### 144. Memory Threat Modeling

Threat modeling should identify how memory could be attacked or abused.

A typical attack path is:

```text id="2p5v8c"
Untrusted Input
      ↓
Memory Write
      ↓
Persistent Poisoning
      ↓
Future Retrieval
      ↓
Agent Manipulation
      ↓
Unauthorized Action
```

Threat modeling should identify controls capable of interrupting each stage.

---

### 145. Memory Secure Development Lifecycle

Memory security should be incorporated into the agent development lifecycle.

```text id="u8m0si"
Design
 ↓
Data Classification
 ↓
Threat Modeling
 ↓
Security Requirements
 ↓
Implementation
 ↓
Security Testing
 ↓
Deployment
 ↓
Monitoring
```

Memory should not be treated as a secondary feature added without security review.

---

### 146. Memory Change Governance

Changes to memory architecture should be formally governed.

Changes may include:

* new memory stores;
* new memory types;
* new write sources;
* broader retrieval;
* new agents;
* new retention periods;
* new external integrations.

Material changes should trigger security assessment.

---

### 147. Model Change Governance

Model changes can alter how memory is interpreted.

Governance should assess whether a new model changes:

* memory retrieval behavior;
* instruction interpretation;
* trust assumptions;
* sensitivity handling;
* memory-driven decisions.

A model upgrade should not automatically be assumed to preserve previous memory security behavior.

---

### 148. Memory Write-Policy Change Governance

Changes to rules controlling what agents may store should be reviewed.

For example:

```text id="j6s1x5"
Before:
Agent may store task status.

After:
Agent may store task status + customer financial information.
```

The second change materially increases memory risk and should require reassessment.

---

### 149. Memory Configuration Management

Security-sensitive memory configuration should be centrally controlled.

Configuration should include:

* access policies;
* retention;
* namespaces;
* write permissions;
* retrieval limits;
* encryption;
* provenance requirements.

---

### 150. Memory Security Testing

Memory security testing should evaluate both unauthorized access and unauthorized influence.

Testing should include:

* memory poisoning;
* unauthorized retrieval;
* cross-tenant access;
* unauthorized modification;
* deletion abuse;
* provenance manipulation;
* sensitive-data persistence.

---

### 151. Adversarial Memory Testing

Adversarial testing should deliberately attempt to compromise memory integrity.

Example:

```text id="2f1w3q"
Attacker
  ↓
Malicious Memory Entry
  ↓
Persistent Retrieval
  ↓
Agent Context
  ↓
Manipulated Behavior
```

Testing should determine whether the malicious information can survive validation and influence subsequent actions.

---

### 152. Memory Control Effectiveness Testing

Controls should be tested against measurable security objectives.

| Control             | Test                              |
| ------------------- | --------------------------------- |
| Memory isolation    | Attempt cross-tenant retrieval    |
| Write authorization | Attempt unauthorized memory write |
| Integrity control   | Modify stored record              |
| Provenance          | Insert unattributed information   |
| Retention           | Verify automatic expiration       |
| Access control      | Attempt unauthorized retrieval    |
| Recovery            | Restore trusted memory version    |

---

### 153. Memory Monitoring Governance

Organizations should define which memory events require monitoring.

Monitoring should cover:

* unusual writes;
* unusual retrieval;
* sensitive-data storage;
* integrity failures;
* unauthorized access;
* bulk deletion;
* abnormal memory propagation.

---

### 154. Agent Memory Key Risk Indicators

Potential KRIs include:

* unauthorized memory writes;
* memory poisoning attempts;
* integrity failures;
* cross-tenant retrieval attempts;
* sensitive-data persistence;
* unexpected memory growth;
* abnormal deletion;
* memory-related security incidents.

---

### 155. Security Baseline Compliance

Production memory systems should meet an approved baseline.

A baseline may require:

* authenticated access;
* authorization;
* encryption;
* tenant isolation;
* provenance;
* logging;
* retention controls;
* integrity protection;
* incident-response capability.

---

### 156. Continuous Memory Control Monitoring

Organizations should continuously verify critical memory controls.

Monitoring should validate that:

* access policies remain active;
* encryption remains enabled;
* provenance is recorded;
* unauthorized writers are blocked;
* retention policies operate;
* logging remains functional.

---

### 157. Agent Memory Drift

Memory security can drift over time.

Drift may result from:

* new agents;
* new users;
* new data sources;
* expanded permissions;
* changed retention;
* changed schemas;
* new integrations.

Memory drift should be detected and assessed.

---

### 158. Memory Security Drift

Security drift occurs when the actual memory environment diverges from its approved security configuration.

```text id="v7r3y9"
Approved State
      ↓
Configuration Changes
      ↓
Actual State
      ↓
Drift Detection
      ↓
Remediation / Reauthorization
```

---

### 159. Continuous Memory Access Review

Memory access should be periodically reevaluated rather than permanently trusted.

Review should consider:

* whether access remains necessary;
* whether scope remains appropriate;
* whether agents changed;
* whether data sensitivity changed.

---

### 160. Memory Incident Classification

Memory incidents should be classified according to attack type and impact.

Examples include:

* unauthorized memory access;
* memory poisoning;
* memory integrity failure;
* sensitive-data exposure;
* unauthorized deletion;
* cross-tenant memory leakage;
* malicious memory propagation.

---

### 161. Memory Incident Response Integration

Memory incidents should integrate with the enterprise incident-response process.

```text id="j8r6qn"
Detect
 ↓
Validate
 ↓
Contain Memory Access
 ↓
Preserve Evidence
 ↓
Identify Affected Memory
 ↓
Assess Agent Impact
 ↓
Restore Trusted State
 ↓
Monitor
```

---

### 162. SOC Integration

The SOC should receive relevant memory-security telemetry.

Important signals include:

* memory writes;
* memory reads;
* integrity failures;
* unusual retrieval;
* cross-tenant attempts;
* suspicious propagation;
* agent behavior following memory changes.

---

### 163. Audit Evidence

Memory security should generate evidence demonstrating control operation.

Evidence may include:

* access reviews;
* memory inventories;
* classification records;
* provenance records;
* security tests;
* configuration snapshots;
* monitoring alerts;
* incident records;
* retention reports.

---

### 164. Memory Governance Control Matrix

A control matrix should connect memory risks to controls and evidence.

| Memory Risk            | Control                    | Evidence              |
| ---------------------- | -------------------------- | --------------------- |
| Memory poisoning       | Write authorization        | Access logs           |
| Unauthorized retrieval | Fine-grained authorization | Authorization records |
| Data leakage           | Data minimization          | Data classification   |
| Memory tampering       | Integrity controls         | Integrity logs        |
| Cross-tenant exposure  | Tenant isolation           | Security tests        |
| Excessive retention    | Retention policy           | Retention reports     |

---

### 165. Memory Risk-to-Control Traceability

Every significant memory risk should map to an enforceable control.

```text id="2q8x1k"
Memory Threat
      ↓
Risk
      ↓
Control Objective
      ↓
Technical Control
      ↓
Testing
      ↓
Evidence
```

This provides traceability between memory risk management and technical implementation.

---

### 166. GRC Integration

Agent memory security should be incorporated into enterprise GRC processes.

Integration should include:

* AI risk registers;
* data-risk registers;
* control libraries;
* compliance assessments;
* exceptions;
* remediation;
* audit;
* risk acceptance.

Memory should be visible to both AI governance and enterprise information-security governance.

---

### 167. Memory Exception Management

Exceptions to memory security requirements should be formally documented.

Each exception should identify:

* affected memory system;
* control deficiency;
* business justification;
* risk;
* compensating controls;
* owner;
* expiration date.

Permanent exceptions should require heightened governance.

---

### 168. Residual Memory Risk Acceptance

Residual memory risk should be formally accepted by an authorized risk owner where appropriate.

Risk acceptance should consider:

* data sensitivity;
* persistence;
* likelihood of manipulation;
* potential agent impact;
* compensating controls.

The agent itself cannot accept residual security risk.

---

### 169. Memory Reauthorization

Memory systems and their associated agents should undergo reauthorization after material changes.

Triggers may include:

* new memory sources;
* new data classes;
* broader access;
* model changes;
* new agents;
* new external integrations;
* memory-security incidents.

---

### 170. Memory Retirement Governance

Retiring memory should be governed as a controlled lifecycle event.

Retirement should address:

* agent access;
* credentials;
* stored data;
* backups;
* replicas;
* indexes;
* caches;
* external copies.

The organization should verify that retired memory is no longer accessible through active agent workflows.

---

### 171. Independent Memory Assurance

High-risk memory environments should receive independent assurance.

Assessment should examine:

* memory access controls;
* isolation;
* integrity;
* provenance;
* retention;
* poisoning resistance;
* monitoring;
* recovery.

---

### 172. Agent Memory Governance Maturity Model

Organizations can assess maturity using a progressive model:

```text id="j2l4qv"
Level 1 — Ad Hoc
Memory implemented without formal governance

Level 2 — Defined
Basic inventory, ownership, and policies

Level 3 — Controlled
Classification, authorization, integrity, and testing

Level 4 — Managed
Continuous monitoring, GRC integration, and
formal assurance

Level 5 — Adaptive
Dynamic trust, continuous adversarial testing,
behavioral detection, automated containment,
and lifecycle reauthorization
```

---

### 173. Enterprise Agent Memory Operating Model

Memory security should be distributed across multiple functions.

```text id="e7f8a2"
Business Owner
      │
      ├── Purpose / Data Need
      │
Data Governance
      │
      ├── Classification / Retention
      │
AI Engineering
      │
      ├── Memory Architecture
      │
Security / GRC
      │
      ├── Controls / Risk
      │
SOC
      │
      ├── Detection / Response
      │
Audit
      │
      └── Independent Assurance
```

No single team should have unchecked authority over the entire memory lifecycle.

---

### 174. Memory Governance Evidence Model

Governance evidence should demonstrate that memory is controlled throughout its lifecycle.

Evidence should establish:

```text id="m8w4x0"
What memory exists?
       ↓
Who owns it?
       ↓
What does it contain?
       ↓
Where did it come from?
       ↓
Who can access it?
       ↓
How is it protected?
       ↓
How long is it retained?
       ↓
How is it monitored?
       ↓
How is it retired?
```

Evidence should be sufficiently detailed to support security, compliance, audit, and incident investigations.

---

### 175. Enterprise Security Principle

The enterprise security principle is:

> **Persistent agent memory must remain within explicit identity, authorization, data, integrity, and lifecycle boundaries.**

A memory store should never become an uncontrolled accumulation of information that can silently influence enterprise operations.

---

### 176. Strategic Memory Security Principle

The strategic principle is:

> **The persistence of information must never create persistence of unauthorized influence.**

An attacker may attempt to compromise memory once and rely on that compromise to influence many future agent executions.

Security architecture should therefore ensure:

```text id="q5x8cs"
Memory Persistence
       ≠
Authority Persistence
```

Persistent information must remain subject to continuous trust, authorization, and integrity controls.

---

### 177. Strategic Governance Principle

The strategic governance principle is:

> **Govern agent memory as a security-sensitive state layer whose integrity and trustworthiness directly affect future agent behavior.**

Governance should continuously connect:

```text id="r8t6mc"
Memory
 ↓
Provenance
 ↓
Trust
 ↓
Agent Context
 ↓
Decision
 ↓
Action
 ↓
Business Impact
```

The security objective is to ensure that manipulation of persistent memory does not become a persistent pathway to unauthorized agent behavior.


