# 11.16 — Human Approval Gates and Human Oversight

## 1. Definition of Human Approval Gates

A human approval gate is a security control that requires an authorized human to review and approve an agent-proposed action before that action can be executed.

In agentic AI systems, approval gates provide an independent decision boundary between:

```text
Agent Recommendation
        |
        v
Human Approval Gate
        |
        v
Authorized Execution
```

The purpose is not to make humans approve every agent activity, but to ensure that appropriately selected high-risk actions cannot be completed solely through autonomous agent behavior.

---

## 2. Human Oversight as an Agentic AI Security Control

Human oversight is a security mechanism that allows authorized personnel to monitor, review, intervene in, or stop autonomous agent activity.

It provides an additional control layer when:

* Agent decisions have significant consequences.
* Actions are difficult to reverse.
* Sensitive data is involved.
* Financial value is involved.
* Privileged systems are affected.
* Security boundaries may be crossed.
* Agent confidence is low.
* Risk exceeds autonomous execution thresholds.

Human oversight should therefore be treated as part of the security architecture rather than merely an operational convenience.

---

## 3. Human Approval vs Human Oversight

Human approval and human oversight are related but different controls.

| Control            | Primary Function                             |
| ------------------ | -------------------------------------------- |
| Human approval     | Authorizes a specific action                 |
| Human oversight    | Monitors ongoing agent behavior              |
| Human intervention | Changes or stops agent behavior              |
| Human review       | Evaluates decisions or outcomes              |
| Human escalation   | Transfers a decision to an authorized person |

Approval is generally **pre-action**.

Oversight may be:

* Pre-action
* During execution
* Post-action
* Continuous

---

## 4. Approval as an Independent Security Boundary

An agent should not be able to approve its own proposed action.

The approval mechanism must be logically and operationally independent from the agent.

```text
+-------------+
| AI Agent    |
+------+------+
       |
       | Proposed Action
       v
+----------------------+
| Approval Control     |
+----------+-----------+
           |
           | Approved
           v
+----------------------+
| Authorization Layer  |
+----------+-----------+
           |
           v
+----------------------+
| Target System        |
+----------------------+
```

This prevents the agent from converting its own recommendation into authorization.

---

## 5. Human Approval and Agent Authority

Human approval should not become a mechanism through which an agent obtains unrestricted authority.

The approval must remain:

* Specific
* Limited
* Contextual
* Time-bound
* Traceable
* Revocable where possible

For example, approval to:

```text
Transfer €5,000 to Supplier A
```

should not imply authorization to:

```text
Transfer unlimited funds to any supplier.
```

---

## 6. Approval Thresholds

Approval thresholds define when autonomous execution must stop and human authorization must occur.

Thresholds may be based on:

* Financial value
* Data sensitivity
* Business impact
* Security impact
* Transaction type
* Privilege level
* Reversibility
* Regulatory significance
* Risk score

Example:

```text
Low Risk
   -> Autonomous

Medium Risk
   -> Enhanced Validation

High Risk
   -> Human Approval

Critical Risk
   -> Dual Approval
```

Thresholds should be formally defined rather than determined dynamically by the agent itself.

---

## 7. Risk-Based Human Approval

Not every agent action requires human intervention.

A risk-based model evaluates the proposed action before determining the required level of human involvement.

Conceptually:

```text
Action
  |
  v
Risk Evaluation
  |
  +---- Low ------> Autonomous
  |
  +---- Medium ---> Oversight
  |
  +---- High -----> Approval
  |
  +---- Critical --> Dual Approval / Block
```

This approach preserves the efficiency benefits of autonomous systems while maintaining stronger controls for high-risk operations.

---

## 8. Human-in-the-Loop

Human-in-the-loop means the agent cannot complete a defined action until a human provides authorization.

Example:

```text
Agent
  |
  | Proposed Action
  v
Human Reviewer
  |
  +--> Approve
  |
  +--> Reject
  |
  +--> Modify
  |
  +--> Escalate
```

The human therefore becomes an explicit decision point in the execution path.

---

## 9. Human-on-the-Loop

Human-on-the-loop means the agent can operate autonomously while humans continuously monitor its behavior and retain the ability to intervene.

Example:

```text
              +----------------+
              | Human Oversight|
              +-------+--------+
                      |
                      v
Agent ---> Decision ---> Action
  ^                       |
  |                       |
  +---- Monitoring -------+
```

This model is appropriate when requiring approval for every action would create unacceptable operational delays.

---

## 10. Human Approval Authority

Not every human is authorized to approve every agent action.

Approval authority should be determined by:

* Role
* Responsibility
* Organizational authority
* Security clearance where applicable
* Business ownership
* Transaction authority
* Separation-of-duties requirements

An agent should not select an approver merely because that person is available.

---

## 11. Approval Identity

Every approval should be bound to a verified human identity.

The system should establish:

```text
Who approved?
      +
What was approved?
      +
When was it approved?
      +
Under what authority?
```

Approval records should therefore be associated with authenticated identities rather than generic accounts.

---

## 12. Approval Context

A human reviewer must receive sufficient context to make an informed security decision.

Relevant context may include:

* Agent identity
* Original user request
* Proposed action
* Target system
* Target resource
* Data involved
* Transaction value
* Security risk
* Reason for the action
* Previous related actions
* Delegation chain
* Policy requiring approval

Approval without sufficient context can become a superficial control.

---

## 13. Action Preview

Before approval, the system should present the actual action the agent intends to perform.

For example:

```text
Agent:
Finance-Agent-01

Action:
Create payment

Beneficiary:
Supplier A

Amount:
€8,500

Source Account:
Corporate Account X

Reason:
Invoice INV-20481

Risk:
High

Approval Required:
Yes
```

The human should approve the concrete action rather than an abstract statement such as:

```text
"Allow the agent to continue."
```

---

## 14. Approval vs Recommendation

An agent may recommend an action, but the recommendation itself is not authorization.

```text
Agent Recommendation
        |
        v
Human Review
        |
        v
Authorization Decision
```

The human approval must be an independent security decision.

This distinction prevents the system from treating model confidence or persuasive reasoning as equivalent to authorization.

---

## 15. Approval Integrity

Approval integrity ensures that an approval cannot be altered, substituted, replayed, or detached from the action that was reviewed.

The approval should be bound to relevant parameters such as:

* Agent
* Action
* Resource
* Transaction
* Amount
* Recipient
* Time
* Policy context

A change to a material parameter should invalidate the previous approval.

---

## 16. Approval Expiration

Human approvals should generally have defined validity periods.

Example:

```text
Approval Created
      |
      v
Valid for 10 Minutes
      |
      v
Execution
      |
      v
Approval Expires
```

An expired approval should not automatically authorize later execution.

This reduces the risk of delayed or replayed approvals.

---

## 17. Approval Replay Risk

An approval should not be reusable for multiple independent actions unless that behavior has been explicitly authorized.

For example:

```text
Approval #1001
   |
   +--> Transaction A     ALLOWED
   |
   +--> Transaction B     DENIED
   |
   +--> Transaction C     DENIED
```

This prevents an agent from capturing a legitimate approval and repeatedly applying it to unauthorized transactions.

---

## 18. Approval Scope

Approval scope defines exactly what the human has authorized.

Scope can include:

* Specific agent
* Specific tool
* Specific resource
* Specific transaction
* Specific amount
* Specific recipient
* Specific action
* Specific time period

The principle is:

**Approval should grant only the authority explicitly approved.**

---

## 19. Approval Escalation

When an action exceeds an approver's authority or risk threshold, the system should escalate it.

Example:

```text
Agent Request
      |
      v
Manager Approval
      |
      | Above Authority
      v
Director Approval
      |
      | Critical
      v
Security / Executive Approval
```

Escalation should be determined by policy rather than by the agent.

---

## 20. Approval Delegation

Approval responsibilities may sometimes be delegated, but delegation must itself be controlled.

A delegated approver should have:

* Defined authority
* Defined scope
* Defined duration
* Authenticated identity
* Appropriate business role

Delegation should not allow an individual to approve actions that exceed the authority of the original approval chain.

---

## 21. Segregation of Duties

Human approval should preserve segregation-of-duties requirements.

An individual who creates or controls a sensitive transaction may be prohibited from approving the same transaction.

For example:

```text
Agent Creates Payment
        |
        v
Requester Reviews
        |
        X
Requester Cannot Approve
        |
        v
Independent Approver
```

Agentic systems should inherit existing enterprise segregation-of-duties requirements rather than bypassing them.

---

## 22. Dual Approval

Critical actions may require two independent human approvals.

Examples include:

* Large financial transactions
* Privileged identity changes
* Production security changes
* Destructive operations
* Critical infrastructure actions

Example:

```text
Agent Proposal
      |
      +----> Approver A
      |
      +----> Approver B
                |
          Both Required
                |
                v
           Authorization
```

Dual approval reduces dependence on a single human decision.

---

## 23. Human Approval and Least Privilege

Approval should follow the principle of least privilege.

A human should receive only the authority necessary to evaluate and approve the relevant action.

The approval mechanism should not grant broad administrative permissions merely because the user has approved one agent transaction.

---

## 24. Human Approval and Agent Identity

The approval process must clearly identify which agent generated the proposed action.

For example:

```text
Initiating User
      |
      v
Agent A
      |
      v
Agent B
      |
      v
Proposed Action
      |
      v
Human Approval
```

The approver should know whether the action came from:

* A trusted internal agent
* A delegated agent
* An external agent
* A newly registered agent
* An agent operating under elevated authority

---

## 25. Approval and Delegated Authority

Where multiple agents are involved, the approval system should preserve the delegation chain.

Example:

```text
User
 |
 v
Agent A
 |
 | Delegated Authority
 v
Agent B
 |
 | Proposed Action
 v
Human Approver
```

The approver should be able to determine whether Agent B actually possesses the authority necessary to request the action.

Human approval must not conceal invalid delegation.

---

## 26. Human Approval and Prompt Injection

Prompt injection may cause an agent to propose actions that the legitimate user never intended.

Therefore, human approval should not simply ask:

```text
"Approve what the agent wants to do?"
```

Instead, the interface should clearly identify:

* Original user intent
* Agent interpretation
* Proposed action
* Security implications
* Relevant inputs
* Data sources
* Transaction parameters

This enables the human to detect when the agent's proposed action diverges from legitimate intent.

---

## 27. Human Oversight and Agent Manipulation

Agents can potentially influence human reviewers through:

* Misleading explanations
* Selective information
* Excessive confidence
* Omitted context
* Urgency claims
* Repeated approval requests
* Manipulated summaries

Human oversight therefore requires protection against **automation bias** and agent-generated misinformation.

The approval interface should present objective security-relevant information independently of the agent's narrative.

---

## 28. Automation Bias

Automation bias occurs when humans excessively trust automated recommendations.

In agentic systems, this can result in:

```text
Agent Recommendation
       |
       v
Human Assumes Correctness
       |
       v
Approval
       |
       v
Unsafe Action
```

Human approval is ineffective if the human simply approves whatever the agent proposes.

Organizations should therefore design approval processes that encourage meaningful verification.

---

## 29. Human Approval Fatigue

Excessive approval requests can reduce the effectiveness of human oversight.

If users receive hundreds of approvals per day, they may begin approving requests without adequate review.

Therefore:

```text
Too Few Approvals
       -> Excessive Autonomous Risk

Too Many Approvals
       -> Approval Fatigue

Risk-Based Approvals
       -> Balanced Control
```

Approval thresholds should be calibrated to focus human attention on genuinely important actions.

---

## 30. Human Oversight Coverage

Organizations should define which agent activities require human oversight.

Coverage may include:

| Activity                       | Possible Oversight |
| ------------------------------ | ------------------ |
| Low-risk read                  | Monitoring         |
| Sensitive data access          | Approval           |
| Financial transaction          | Approval           |
| Privileged change              | Dual approval      |
| Destructive action             | Mandatory approval |
| Security policy change         | Dual approval      |
| Critical infrastructure action | Human-controlled   |

Coverage should be documented as part of the agent security policy.

---

## 31. Human Oversight Boundaries

Human oversight should have clearly defined boundaries.

The organization should establish:

* What humans can approve
* What humans can reject
* What humans can modify
* What humans can stop
* What humans can override
* What requires escalation
* What can never be overridden

Without explicit boundaries, human intervention can itself become an uncontrolled privilege mechanism.

---

## 32. Human Override

Human override allows an authorized human to intervene in autonomous execution.

Examples include:

```text
STOP
PAUSE
REJECT
REVOKE
ESCALATE
ROLL BACK
```

Override capabilities should themselves require authentication and authorization.

A human override should not become an unrestricted administrative backdoor.

---

## 33. Human Intervention During Execution

For long-running autonomous workflows, intervention may be required while the agent is already executing.

Example:

```text
Agent Workflow
     |
     v
Step 1
     |
     v
Step 2
     |
     v
Human Intervention
     |
     +--> Continue
     +--> Modify
     +--> Stop
     +--> Escalate
```

The architecture should support interruption at meaningful control points.

---

## 34. Human Approval for Irreversible Actions

Irreversible or difficult-to-reverse actions generally warrant stronger human control.

Examples include:

* Permanent deletion
* Financial transfer
* Destructive infrastructure changes
* Account termination
* Permanent configuration changes
* Legal or regulatory submissions

The more difficult an action is to reverse, the stronger the justification for human authorization should be.

---

## 35. Human Approval for High-Impact Actions

High-impact actions should have explicit approval requirements based on business and security consequences.

A high-impact action may affect:

* Safety
* Financial assets
* Customer rights
* Sensitive information
* Production availability
* Security controls
* Regulatory obligations

Agent autonomy should decrease as potential impact increases.

---

## 36. Approval Failure Handling

The system must define what happens when approval fails.

Possible outcomes include:

```text
Approval Denied
      |
      +--> Stop Action
      |
      +--> Return to Agent
      |
      +--> Request Clarification
      |
      +--> Escalate
```

The agent must not interpret a denial as permission to find another path to the same action.

---

## 37. Approval Timeout Handling

If a human does not respond within the required time, the system should follow a predefined policy.

For security-sensitive actions, the default should generally be:

```text
No Approval
     |
     v
No Execution
```

Silence should not be interpreted as authorization unless a formally governed workflow explicitly defines otherwise.

---

## 38. Human Approval and Fail-Safe Behavior

Approval mechanisms should fail safely.

If the approval service becomes unavailable:

```text
Approval Service Unavailable
          |
          v
High-Risk Action
          |
          v
BLOCK
```

The system should not automatically bypass human approval because the approval infrastructure is temporarily unavailable.

---

## 39. Human Approval and Business Continuity

Fail-safe approval controls must still consider business continuity.

Organizations may define controlled alternatives such as:

* Secondary approvers
* Emergency approval procedures
* Backup approval systems
* Emergency operating procedures
* Predefined continuity authorities

These alternatives must remain governed and auditable.

---

## 40. Human Approval Logging

Every approval event should generate an auditable record.

Relevant information includes:

* Approver identity
* Agent identity
* Original request
* Proposed action
* Approval decision
* Timestamp
* Approval scope
* Risk score
* Policy requiring approval
* Transaction identifier
* Relevant authorization context

This creates evidence of how autonomous actions were authorized.

---

## 41. Human Approval Non-Repudiation

For high-impact operations, organizations may require stronger evidence that an approval was genuinely issued by the authorized person.

Controls may include:

* Strong authentication
* Cryptographic signing
* Tamper-resistant records
* Secure timestamps
* Transaction binding
* Immutable audit trails

The objective is to establish reliable evidence of the approval event.

---

## 42. Human Oversight Monitoring

Organizations should monitor the effectiveness of human oversight itself.

Metrics may include:

* Approval rate
* Rejection rate
* Escalation rate
* Approval response time
* Approval volume
* Override frequency
* Approval fatigue indicators
* Post-approval incident rate
* Incorrect approval rate

Oversight should be treated as a measurable security control.

---

## 43. Human Approval Abuse

Approval mechanisms can themselves be abused.

Potential risks include:

* Compromised approver accounts
* Unauthorized delegation
* Approval replay
* Social engineering
* Approval flooding
* Approval interface manipulation
* Privilege escalation
* Collusion
* Inadequate review

Therefore, the human approval layer requires its own security controls.

---

## 44. Human Approval and Zero Trust

Human approval should follow Zero Trust principles.

The system should continuously verify:

```text
Approver Identity
      +
Approver Authority
      +
Requested Action
      +
Approval Context
      +
Current Risk
```

Being an authorized employee does not automatically make every approval valid.

---

## 45. Human Oversight Across Agent Chains

In multi-agent environments, oversight must account for the entire execution chain.

Example:

```text
User
 |
 v
Agent A
 |
 v
Agent B
 |
 v
Agent C
 |
 v
Tool
 |
 v
Transaction
```

A human approving Agent B's request should understand the downstream consequences when Agent B will delegate further to Agent C or invoke a high-impact tool.

Approval should not become a blind checkpoint within a complex autonomous chain.

---

## 46. Approval Context Integrity

The information presented to a human approver must be protected from manipulation.

The approval interface should independently retrieve or verify critical information such as:

* Transaction amount
* Target
* Agent identity
* Authorization scope
* Data classification
* Risk level

The agent should not be the sole source of truth for the information used to approve its own action.

---

## 47. Human Approval and Separation of Decision Authority

The architecture should separate:

```text
Model Decision
       |
       v
Agent Recommendation
       |
       v
Security Authorization
       |
       v
Human Approval
       |
       v
Execution
```

Depending on risk, some of these functions may be combined operationally, but security-critical authority should remain independently controlled.

This separation prevents one compromised component from controlling the entire action chain.

---

## 48. Human Oversight as a Governance Object

Human oversight should be formally governed.

Governance should define:

* Which agents require oversight
* Which actions require approval
* Approval thresholds
* Authorized approvers
* Escalation procedures
* Override rules
* Emergency procedures
* Logging requirements
* Review frequency
* Control effectiveness measures

Human oversight should therefore be included in the enterprise AI governance framework.

---

## 49. Core Principles of Human Approval Gates and Human Oversight

The following principles should guide enterprise implementation:

1. **Approval must be independent of agent behavior.**
2. **Human approval must authorize a specific action, not unlimited authority.**
3. **High-impact actions require stronger oversight.**
4. **Approval must be bound to the action being approved.**
5. **Approver identity and authority must be verified.**
6. **Approval must preserve least privilege.**
7. **Segregation of duties must remain effective.**
8. **Approval interfaces must provide trustworthy context.**
9. **Human oversight must be resistant to automation bias.**
10. **Approval volume must be controlled to prevent approval fatigue.**
11. **Denial must not trigger autonomous attempts to bypass the decision.**
12. **Approval failures must fail safely.**
13. **Emergency overrides must remain controlled and auditable.**
14. **Every material approval must be attributable.**
15. **Human oversight must be continuously evaluated for effectiveness.**

---

## 50. Core Principle of Human Approval Gates and Human Oversight

**Human approval is a security boundary, not a formality.**

An autonomous agent may analyze information, formulate a recommendation, and prepare an action, but where governance requires human authorization, the agent must stop at the approval boundary.

The fundamental control model is:

```text
                    AGENT
                      |
                      v
              Proposed Action
                      |
                      v
             Risk Evaluation
                      |
          +-----------+-----------+
          |                       |
       Low Risk               High Risk
          |                       |
          v                       v
   Autonomous Control      Human Approval Gate
                                  |
                         +--------+--------+
                         |                 |
                      APPROVE           REJECT
                         |                 |
                         v                 v
                  Authorization         STOP
                         |
                         v
                     Execution
```

The governing principle is:

**The agent may recommend, but the authorized human and independent security controls determine whether a high-impact action may proceed.**

## Part 2 — Security Controls

### 51. Human Approval Gate Security Control Architecture

Human approval gates should be implemented as independent security enforcement points between autonomous agent decisions and high-impact execution.

```text
+------------------+
|     AI Agent     |
+--------+---------+
         |
         | Proposed Action
         v
+------------------------+
| Risk & Policy Engine   |
+-----------+------------+
            |
       Approval Required
            |
            v
+------------------------+
| Human Approval Gateway |
+-----------+------------+
            |
       +----+----+
       |         |
    APPROVE    REJECT
       |         |
       v         v
 Authorization   STOP
       |
       v
+------------------------+
| Target System / Tool   |
+------------------------+
```

The agent must not be able to bypass, modify, or disable the approval control.

---

### 52. Approval Identity Control

Every approval must be associated with a unique, authenticated human identity.

The control should establish:

* Who approved the action.
* Which account was used.
* Whether the identity was authenticated.
* Whether the person possessed approval authority.
* When the approval occurred.
* Which action was approved.

Generic or shared approval identities should not be used for security-sensitive actions.

---

### 53. Strong Approver Authentication

High-impact approvals should require strong authentication appropriate to the risk.

Possible controls include:

* Phishing-resistant MFA
* Hardware security keys
* Device-bound authentication
* Strong enterprise identity authentication
* Step-up authentication

The authentication assurance should increase with the impact of the action being approved.

---

### 54. Approver Authorization

Authentication establishes who the approver is; authorization determines whether that person is permitted to approve the action.

Authorization should evaluate:

```text
Approver Identity
      +
Role
      +
Approval Authority
      +
Action Type
      +
Transaction Value
      +
Risk Level
```

An authenticated employee must not automatically be treated as an authorized approver.

---

### 55. Fine-Grained Approval Authority

Approval authority should be granular.

For example:

```text
Manager A
  -> Approve expenses <= €5,000

Director B
  -> Approve expenses <= €50,000

Executive C
  -> Approve expenses > €50,000
```

The system should prevent an approver from exceeding the limits defined by policy.

---

### 56. Independent Approval Service

Approval decisions should be enforced by an independent service or control layer.

The agent should not be responsible for determining:

```text
"Was I approved?"
```

Instead:

```text
Agent
  |
  v
Approval Service
  |
  v
Authorization Service
  |
  v
Execution
```

This prevents an agent from falsely claiming that approval was granted.

---

### 57. Approval Policy Enforcement

Approval policies should be enforced automatically.

Policies may specify:

* Which actions require approval.
* Which risk levels require approval.
* Who may approve.
* How many approvals are required.
* Maximum transaction value.
* Approval expiration.
* Escalation requirements.
* Emergency procedures.

The agent should not be able to redefine these policies.

---

### 58. Approval Requirement Allowlisting

Organizations should maintain an explicit allowlist of actions that may proceed autonomously and actions that require human authorization.

Example:

| Action                   | Control                     |
| ------------------------ | --------------------------- |
| Read public information  | Autonomous                  |
| Read internal data       | Policy-based                |
| Modify customer record   | Approval depending on risk  |
| Delete record            | Mandatory approval          |
| Transfer funds           | Mandatory approval          |
| Modify IAM privileges    | Dual approval               |
| Disable security control | Prohibited or dual approval |

This creates a predictable authorization boundary.

---

### 59. Action Classification

Before approval requirements are determined, the proposed action should be classified.

Classification factors may include:

* Action type
* Resource
* Data sensitivity
* Financial value
* Business impact
* Security impact
* Reversibility
* Regulatory impact
* Operational criticality

This allows approval requirements to be risk-based rather than arbitrary.

---

### 60. Transaction Risk Evaluation

Where an agent proposes a transaction, the system should calculate transaction risk independently of the agent's explanation.

Example:

```text
Transaction
    |
    +--> Amount
    +--> Recipient
    +--> Resource
    +--> Data
    +--> Privilege
    +--> Reversibility
    +--> Frequency
    +--> Context
    |
    v
Risk Score
```

The risk score can determine whether human approval is required.

---

### 61. Approval Scope Validation

Before execution, the system must confirm that the approved scope matches the requested action.

For example:

```text
Approved:
Delete File A

Requested:
Delete File B

Result:
DENY
```

An approval should never be interpreted as a generic permission to perform similar actions.

---

### 62. Approval Parameter Validation

Critical parameters should be validated before execution.

Parameters may include:

* Amount
* Recipient
* Account
* Resource
* Environment
* Tool
* Data set
* Destination
* Action type

If any security-relevant parameter changes after approval, the action should normally require reapproval.

---

### 63. Approval-to-Transaction Binding

The approval should be cryptographically or logically bound to the specific transaction.

Conceptually:

```text
Approval
   |
   +--> Agent ID
   +--> Action
   +--> Resource
   +--> Parameters
   +--> Transaction ID
   +--> Timestamp
   |
   v
Authorized Transaction
```

This prevents an approval from being copied to another transaction.

---

### 64. Approval Token Security

Where approval tokens are used, they should be:

* Short-lived
* Non-transferable where possible
* Scoped
* Audience-restricted
* Bound to the transaction
* Protected against replay
* Revocable where appropriate

An approval token should represent only the authority explicitly granted.

---

### 65. Approval Expiration

Approval decisions should have defined expiration periods.

```text
Approval Issued
      |
      v
Valid Period
      |
      v
Execution
      |
      v
Expiration
```

If the action is not executed within the approved period, the system should require a new authorization.

---

### 66. Approval Replay Protection

A completed approval should not automatically authorize another transaction.

The system should track:

* Approval identifier
* Transaction identifier
* Approval state
* Execution state
* Timestamp

Once consumed:

```text
Approval #8472
     |
     v
Transaction #5521
     |
     v
CONSUMED
```

The same approval should not be reusable.

---

### 67. Approval State Management

Approval workflows should maintain explicit states.

Example:

```text
PROPOSED
   |
   v
PENDING APPROVAL
   |
   +--> APPROVED --> AUTHORIZED --> EXECUTED
   |
   +--> REJECTED
   |
   +--> EXPIRED
   |
   +--> CANCELLED
```

This prevents ambiguous approval states from being interpreted as authorization.

---

### 68. Human Approval Controls for Sensitive Data

Access to highly sensitive information may require human approval depending on the use case.

Examples include:

* Financial records
* Sensitive customer information
* Credentials
* Security investigation data
* Legal information
* Highly confidential corporate data

The approval should specify the permitted data scope and intended purpose.

---

### 69. Data Minimization During Approval

The approval interface should provide sufficient information for a decision without unnecessarily exposing sensitive information.

Instead of displaying an entire confidential dataset, the system may present:

```text
Customer:
ID-20481

Requested Action:
Export 25 records

Data Classification:
Confidential

Destination:
Approved CRM System

Risk:
High
```

The approval process itself must not become a data-exfiltration channel.

---

### 70. Approval Context Integrity

The information presented to the human approver must be independently validated.

Critical information should not rely exclusively on an agent-generated summary.

For example:

```text
Agent says:
"Low-value payment."

Independent system:
Amount = €85,000

Result:
REQUIRE REVIEW
```

The approval layer should obtain security-critical attributes from trusted systems.

---

### 71. Original User Intent Validation

Where an action originates from a human request, the system should preserve the relationship between the original request and the proposed action.

Example:

```text
User:
"Prepare a supplier report."

Agent:
"Delete supplier records."

Approval Gateway:
Intent mismatch detected
        |
        v
DENY / ESCALATE
```

Human approval should not automatically legitimize actions that clearly exceed the original purpose.

---

### 72. Prompt Injection-Aware Approval Controls

Approval systems should assume that the agent may have been influenced by malicious instructions.

Potential sources include:

* Web content
* Emails
* Documents
* Tool results
* Memory
* Other agents
* External APIs

The approval system should therefore distinguish:

```text
Trusted Authorization Context
          from
Agent-Generated Content
```

Agent-generated claims should not independently establish approval authority.

---

### 73. Approval Risk Escalation

Approval requirements should increase when risk increases.

Example:

```text
Risk Score
   |
   +-- Low ------> Autonomous
   |
   +-- Moderate -> Human Review
   |
   +-- High -----> Human Approval
   |
   +-- Critical -> Dual Approval / Block
```

Risk escalation should be controlled by security policy.

---

### 74. Dynamic Approval Requirements

Approval requirements may change during execution.

For example:

```text
Initial Request
      |
      v
Low Risk
      |
      v
Agent discovers sensitive data
      |
      v
Risk increases
      |
      v
Approval Required
```

The agent should not continue under the original authorization after a material risk change.

---

### 75. Step-Up Approval

A workflow can require stronger approval when an action crosses a higher-risk threshold.

Example:

```text
€1,000
  |
  v
Manager Approval

€10,000
  |
  v
Director Approval

€100,000
  |
  v
Dual Executive Approval
```

This supports risk-proportionate authorization.

---

### 76. Human Approval for Irreversible Actions

Irreversible actions should receive stronger controls.

Examples:

* Permanent deletion
* Destructive infrastructure changes
* Financial transfers
* Account termination
* Security policy deletion
* Permanent data modification

Where feasible:

```text
Irreversible Action
       |
       v
Mandatory Approval
       |
       v
Execution
```

---

### 77. Reversibility Controls

Where possible, agent actions should be designed to remain reversible.

Instead of:

```text
DELETE
```

the system may support:

```text
QUARANTINE
ARCHIVE
DISABLE
STAGE
ROLLBACK
```

Reversibility reduces the consequences of incorrect autonomous execution and may reduce the required approval level for some operations.

---

### 78. Human Approval for Privileged Actions

Actions affecting privileged security boundaries should require enhanced controls.

Examples include:

* IAM changes
* Administrator creation
* Privilege escalation
* Security policy changes
* Firewall changes
* Production access changes
* Credential administration

The agent should not be able to use human approval to circumvent privileged access management.

---

### 79. Dual Authorization Controls

Critical operations should require two independent approvals.

The system should verify that:

```text
Approver A != Approver B
```

and that both possess the required authority.

The two approvals should be independently authenticated and recorded.

---

### 80. Segregation-of-Duties Enforcement

Approval controls should integrate with enterprise segregation-of-duties policies.

For example:

```text
Agent prepares transaction
          |
          v
Requester
          |
          X
Cannot approve own request
          |
          v
Independent Approver
```

The system should automatically prevent prohibited combinations of responsibilities.

---

### 81. Approval Conflict Detection

The system should detect conflicts such as:

* Requester approving own action
* Agent owner approving a prohibited transaction
* Same person providing both required approvals
* Approver lacking authority
* Approver outside permitted organizational scope

A detected conflict should result in rejection or escalation.

---

### 82. Approval Delegation Controls

Temporary approval delegation should be explicitly controlled.

A delegation record should include:

```text
Delegator
Delegate
Authority
Scope
Start Time
End Time
Restrictions
```

Delegated approval authority should automatically expire.

---

### 83. Approval Queue Security

Approval queues themselves are security-sensitive.

Controls should protect against:

* Unauthorized queue access
* Request modification
* Request deletion
* Priority manipulation
* Approval reassignment
* Queue flooding
* Approval spoofing

An attacker should not be able to manipulate the queue to bypass the intended approver.

---

### 84. Approval Notification Integrity

Notifications requesting approval should clearly identify:

* The requesting agent
* The action
* The target
* The risk
* The approval deadline
* The transaction identifier

Approvers should be directed to a trusted approval interface rather than relying on potentially manipulated notification content.

---

### 85. Secure Approval Interface

The approval interface should minimize ambiguity.

A secure interface should clearly distinguish:

```text
WHAT THE AGENT PROPOSED
          |
          v
WHAT POLICY REQUIRES
          |
          v
WHAT THE HUMAN IS AUTHORIZING
          |
          v
WHAT WILL ACTUALLY EXECUTE
```

Material differences should prevent approval.

---

### 86. Approval Confirmation

For high-impact actions, the system may require explicit confirmation after displaying critical parameters.

Example:

```text
I confirm:

Agent: Finance-Agent-01
Action: Payment
Amount: €25,000
Recipient: Supplier A
Account: Corporate Account X

[ APPROVE ]
[ REJECT ]
```

This reduces accidental approval.

---

### 87. Approval Rate Limiting

Approval systems should limit excessive approval activity.

Controls may detect:

```text
100 approvals
within 2 minutes
```

and trigger:

* Additional verification
* Temporary restriction
* Security alert
* Manual review

This helps prevent compromised approver accounts from authorizing large numbers of agent actions.

---

### 88. Approval Concurrency Controls

Concurrent approval requests should be controlled when they affect the same resource or transaction.

For example:

```text
Agent A ---> Change Resource X
Agent B ---> Change Resource X
```

The system should prevent conflicting approvals from producing an unsafe final state.

---

### 89. Approval Timeout Controls

Approval requests should have defined timeouts.

If the deadline expires:

```text
PENDING
   |
   v
TIMEOUT
   |
   v
NO AUTHORIZATION
```

The agent should not interpret timeout as approval.

---

### 90. Approval Cancellation

Authorized personnel should be able to cancel pending approvals when:

* The transaction is no longer required.
* Risk has changed.
* The agent has behaved abnormally.
* The target changed.
* The request was submitted incorrectly.
* A security incident is suspected.

Cancellation should invalidate the pending authorization.

---

### 91. Approval Revocation

Where technically possible, approvals should be revocable before execution.

For example:

```text
Approved
   |
   v
Security Alert
   |
   v
Approval Revoked
   |
   v
Execution Blocked
```

This is particularly important for delayed or long-running workflows.

---

### 92. Human Override Controls

Human override capabilities should themselves be protected through:

* Strong authentication
* Role-based authorization
* Explicit scope
* Logging
* Approval requirements for critical overrides
* Separation of duties

A human override should not create an unmonitored administrative pathway.

---

### 93. Emergency Approval Controls

Emergency procedures may permit expedited authorization for critical situations.

However, emergency access should still require:

* Defined emergency criteria
* Authorized personnel
* Time limitation
* Strong authentication
* Logging
* Post-event review

Emergency procedures should not become permanent bypass mechanisms.

---

### 94. Agent Execution Limits During Approval

An agent should not continue taking unrestricted actions while waiting for human approval.

For example:

```text
Action Pending Approval
        |
        v
Agent State Restricted
        |
        +--> No Alternate Path
        +--> No Privilege Escalation
        +--> No Delegation Bypass
        +--> No Repeated Submission
```

This prevents the agent from circumventing the approval boundary.

---

### 95. Approval Bypass Prevention

The architecture should detect attempts to bypass human approval.

Examples include:

* Calling a different tool
* Delegating to another agent
* Splitting one transaction into smaller transactions
* Changing parameters after approval
* Switching execution environments
* Using an alternate API
* Reissuing rejected requests

Example:

```text
Rejected Payment: €100,000
       |
       v
Agent attempts:
20 × €5,000
       |
       v
Cumulative Transaction Control
       |
       v
BLOCK
```

Approval controls must therefore consider the broader action chain, not only individual requests.

---

### 96. Approval-Aware Agent-to-Agent Delegation

An agent should not be able to bypass approval by transferring the task to another agent.

```text
Agent A
   |
   | Approval Required
   X
   |
   +----> Agent B
              |
              X
        Cannot bypass approval
```

Delegation must preserve the original approval requirement.

---

### 97. Approval-Aware Tool Invocation

Tools invoked after human approval must execute only within the approved scope.

If the approved action is:

```text
Update Customer A
```

the tool authorization layer should reject:

```text
Update Customer B
```

even if the agent attempts to make the change after receiving legitimate approval.

---

### 98. Output Validation Before Approved Action

Before execution, the system should validate the final agent-generated output against the approved action.

```text
Approved Action
      |
      v
Final Agent Output
      |
      v
Compare
      |
   +--+--+
   |     |
 Match  Mismatch
   |     |
   v     v
Execute Block / Reapprove
```

This is critical because an agent's final execution parameters may differ from the information originally presented to the human.

---

### 99. Business Rule Enforcement

Human approval should not bypass business rules.

For example:

```text
Human Approval
      +
Policy Violation
      |
      v
DENY
```

Security controls should remain capable of rejecting an action even after human approval when the action violates mandatory enterprise policies.

---

### 100. Core Approval Enforcement Principle

The central security control model is:

```text
Agent proposes
      |
      v
Independent risk evaluation
      |
      v
Approval policy
      |
      +---- Autonomous
      |
      +---- Human approval
      |
      +---- Dual approval
      |
      +---- Deny
      |
      v
Independent authorization
      |
      v
Final action validation
      |
      v
Execution
```

Human approval must therefore be treated as **one controlled authorization layer within a defense-in-depth architecture**, not as a substitute for identity, authorization, least privilege, transaction controls, or independent security enforcement.

## Part 3 — Governance

### 118. Human Approval and Oversight Governance Framework

Human approval and oversight governance defines how the enterprise determines when autonomous agent activity requires human involvement, who may provide that involvement, how decisions are recorded, and how the effectiveness of the controls is evaluated.

The governance framework should establish:

* Approval requirements
* Oversight requirements
* Approval authority
* Risk thresholds
* Escalation rules
* Segregation of duties
* Emergency procedures
* Evidence requirements
* Review frequency
* Control effectiveness requirements

Human oversight should therefore be governed as an enterprise security control rather than treated as an informal operational practice.

---

### 119. Human Oversight Ownership and Accountability

Every human approval process should have clearly assigned ownership.

Responsibilities should be divided among:

| Responsibility                 | Typical Owner           |
| ------------------------------ | ----------------------- |
| Business approval policy       | Business Owner          |
| Security approval requirements | Security / CISO         |
| Agent configuration            | AI / Engineering        |
| Identity controls              | IAM                     |
| Approval platform              | Platform Owner          |
| Risk thresholds                | Risk / Governance       |
| Audit evidence                 | GRC                     |
| Incident intervention          | SOC / Incident Response |

No single team should necessarily control every aspect of a high-impact approval workflow.

---

### 120. Enterprise Human Approval Inventory

The organization should maintain an inventory of agent activities that require human approval or oversight.

The inventory should identify:

* Agent
* Business process
* Proposed action
* Risk classification
* Approval requirement
* Approver role
* Number of approvals
* Approval threshold
* Escalation path
* Exception status
* Review date

Example:

```text id="i6a1vr"
Agent: Finance-Agent-01
Action: Payment
Risk: High
Approval: Required
Approver: Finance Manager
Threshold: > €5,000
Dual Approval: > €50,000
```

---

### 121. Human Approval Classification

Approval requirements should be classified according to the nature and impact of the action.

Possible classifications include:

* Informational
* Operational
* Sensitive
* High-impact
* Critical
* Prohibited

Classification should consider both security and business consequences.

---

### 122. Human Oversight Risk Tiering

Agent activities should be assigned oversight tiers.

Example:

```text id="n9e4ap"
Tier 1 — Low Risk
        |
        v
Autonomous + Monitoring

Tier 2 — Moderate Risk
        |
        v
Enhanced Oversight

Tier 3 — High Risk
        |
        v
Mandatory Human Approval

Tier 4 — Critical
        |
        v
Dual Approval / Human-Controlled
```

Risk tiering should be formally documented and periodically reviewed.

---

### 123. Human Approval Governance Board

Organizations with extensive agentic AI deployments may establish a governance body responsible for high-impact approval policies.

Responsibilities may include:

* Defining approval standards
* Reviewing high-risk agent use cases
* Establishing approval thresholds
* Reviewing exceptions
* Evaluating control effectiveness
* Reviewing major incidents
* Approving changes to critical oversight models

The board should not replace operational ownership; it should provide governance and challenge.

---

### 124. Approval Purpose Limitation

Human approval must be limited to the business and security purpose for which it was granted.

For example:

```text id="4br5u6"
Purpose:
Approve supplier payment

Does NOT authorize:
- Supplier creation
- Bank-account modification
- Unlimited payments
- Privilege changes
```

Purpose limitation prevents a legitimate approval from becoming an unrestricted authority mechanism.

---

### 125. Agent Authority Governance

Governance must explicitly distinguish between:

* Agent capability
* Agent permission
* Agent authority
* Human approval
* Final authorization

An agent should never receive broader authority merely because a human approved one action.

The governing principle is:

**Approval does not automatically expand agent authority.**

---

### 126. Agent-to-Tool Access Governance

Where an approved agent action involves tools, governance must define the relationship between:

```text id="b0bq7h"
Agent
  |
  v
Approval
  |
  v
Tool
  |
  v
Resource
```

The tool authorization layer should independently enforce the approved scope.

Human approval should not bypass tool security requirements.

---

### 127. Human Approval Access Governance

Access to approval functions should be governed like privileged access.

Controls should define:

* Eligible approvers
* Roles
* Approval limits
* Business units
* Transaction categories
* Emergency authority
* Delegation rules
* Review requirements

Approval privileges should be granted according to least privilege.

---

### 128. Human Approval Access Reviews

Approval privileges should be periodically reviewed.

Reviews should verify:

* Approver still holds the required role.
* Approval authority remains appropriate.
* Delegations remain valid.
* Former employees have been removed.
* Temporary privileges have expired.
* Segregation-of-duties conflicts do not exist.

High-impact approval privileges should generally receive more frequent review.

---

### 129. Approval Lifecycle Governance

Approval authority should have a defined lifecycle:

```text id="sm0c5a"
Requested
   |
   v
Reviewed
   |
   v
Approved
   |
   v
Provisioned
   |
   v
Active
   |
   v
Reviewed
   |
   +--> Renewed
   |
   +--> Suspended
   |
   +--> Revoked
   |
   v
Retired
```

Approval authority should not remain permanently active without periodic validation.

---

### 130. Approver Creation Governance

Creation of new approver privileges should require formal authorization.

The organization should validate:

* Identity
* Employment status
* Role
* Business responsibility
* Approval authority
* Segregation-of-duties conflicts
* Required training
* Risk level

Approver access should be provisioned only after these requirements are satisfied.

---

### 131. Approval Credential Governance

Approval credentials must be managed as security-sensitive credentials.

Governance should address:

* Authentication requirements
* MFA
* Credential lifecycle
* Device requirements
* Credential rotation where applicable
* Revocation
* Emergency access
* Compromise response

High-risk approval should never depend on weak or shared authentication.

---

### 132. Approval Policy Governance

Approval policies should be formally owned and version controlled.

Policy changes should identify:

* Who changed the policy
* Why it changed
* What changed
* Effective date
* Approval authority
* Security impact
* Testing performed

Changes to approval thresholds can materially change enterprise risk and therefore require appropriate governance.

---

### 133. Approval Attribute Governance

Approval decisions may depend on attributes such as:

* User role
* Business unit
* Transaction value
* Data classification
* Agent identity
* Resource sensitivity
* Risk score

These attributes must come from authoritative enterprise sources where possible.

The agent should not be trusted to define its own approval attributes.

---

### 134. Agent Delegation Governance

Delegation between agents must preserve approval requirements.

For example:

```text id="l9u4ts"
Agent A
   |
   | Approval Required
   v
Agent B
   |
   X
Cannot remove requirement
```

Governance should define:

* Whether delegation is permitted.
* Maximum delegation depth.
* Whether approval follows delegated actions.
* Whether delegated agents can initiate approval requests.
* Whether delegated authority can be further delegated.

---

### 135. Transitive Approval Authority Governance

Approval authority must not propagate automatically through an agent chain.

```text id="2m0vgn"
Human
  |
  v
Agent A
  |
  v
Agent B
  |
  v
Agent C
```

Agent C should not inherit human approval authority merely because it is downstream from Agent A.

Every authority transition must be explicitly governed.

---

### 136. Data Governance for Human Approval

Approval workflows should comply with enterprise data governance.

Governance should define:

* Data minimization
* Data classification
* Access restrictions
* Retention
* Encryption
* Disclosure
* Auditability
* Data residency where applicable

Approval interfaces should expose only the information necessary for the decision.

---

### 137. Sensitive Data Approval Governance

Where agent actions involve sensitive information, governance should define when human approval is required.

Examples include:

* Financial information
* Confidential business information
* Sensitive customer information
* Credentials
* Security information
* Legal information

Approval requirements should be based on both data sensitivity and intended action.

---

### 138. Context Governance

Governance should define which contextual information may influence approval decisions.

Relevant context may include:

* Original user request
* Agent identity
* Delegation chain
* Current workflow
* Resource
* Transaction
* Security state
* Risk score

The context presented to the approver should be trustworthy and attributable.

---

### 139. Agent Memory Governance

Persistent memory can influence future actions and therefore approval requirements.

Governance should establish:

* Which memory can influence high-impact actions.
* Who can modify relevant memory.
* How memory provenance is established.
* How memory poisoning is detected.
* How historical context is validated.

An agent should not be allowed to justify a sensitive action solely through untrusted persistent memory.

---

### 140. Credential Governance

Credentials used by agents and approval systems should be governed separately.

Governance should cover:

* Credential ownership
* Scope
* Lifetime
* Storage
* Rotation
* Revocation
* Delegation
* Incident response

Human approval should not result in uncontrolled credential exposure to the agent.

---

### 141. Network Governance

Agent approval architecture should be integrated with network security governance.

Requirements may include:

* Segmentation
* Restricted communication paths
* Secure approval endpoints
* Egress restrictions
* Administrative access controls
* Monitoring

An agent should not be able to bypass the approval system by communicating directly with the target system.

---

### 142. External Approver and External Agent Governance

External parties may participate in agent workflows.

Examples include:

* External service providers
* Partners
* Contractors
* Customer-facing agents
* Third-party AI agents

Governance should define whether external actors can:

* Request approval
* Receive approval results
* Submit transactions
* Participate in delegated workflows
* Access approval context

External participation should be explicitly authorized.

---

### 143. Approval Provider Risk Management

Third-party approval platforms should undergo risk assessment.

Assessment areas may include:

* Authentication
* Authorization
* Availability
* Data protection
* Auditability
* Change management
* Incident response
* Integration security
* Provider access

A failure or compromise of the approval platform could affect high-impact autonomous actions.

---

### 144. Human Approval Security Requirements

Security requirements should be formally defined for approval workflows.

Requirements may include:

* Strong authentication
* Fine-grained authorization
* Transaction binding
* Approval expiration
* Replay protection
* Segregation of duties
* Immutable audit logging
* Secure failure handling
* Approval bypass prevention
* Emergency controls

These requirements should be traceable to identified risks.

---

### 145. Human Oversight Threat Modeling

Approval and oversight workflows should undergo threat modeling.

Threat scenarios may include:

* Compromised agent
* Compromised approver
* Approval replay
* Approval spoofing
* Social engineering
* Approval fatigue
* Interface manipulation
* Delegation abuse
* Transaction substitution
* Approval bypass
* Agent-generated misinformation

The threat model should examine the complete path from agent decision to final execution.

---

### 146. Human Approval Secure Development Lifecycle

Approval functionality should be included in the secure development lifecycle.

Security activities should include:

```text id="qf0cwl"
Requirements
    |
    v
Threat Modeling
    |
    v
Secure Design
    |
    v
Implementation
    |
    v
Security Testing
    |
    v
Deployment
    |
    v
Continuous Monitoring
```

Changes to approval mechanisms should not be deployed without appropriate security validation.

---

### 147. Model Change Governance

Changes to an agent model may alter:

* Recommendations
* Decision patterns
* Tool selection
* Risk interpretation
* Approval frequency
* Proposed transaction types

Therefore, material model changes should trigger evaluation of existing approval controls.

---

### 148. Prompt and Instruction Change Governance

Changes to system prompts, policies, instructions, or agent objectives can affect when and how agents request approval.

Governance should require assessment of:

* Approval triggers
* Escalation behavior
* Action scope
* Human-readable explanations
* Bypass resistance

Prompt changes should not silently weaken approval boundaries.

---

### 149. Tool Change Governance

Changes to tools can alter the consequences of approved actions.

Governance should review:

* New capabilities
* New parameters
* New write functions
* New transaction limits
* New data access
* New external connectivity

A tool upgrade that increases impact should trigger reassessment of approval requirements.

---

### 150. Data-Source Change Governance

Changes to data sources can affect agent decisions and therefore human approval.

Governance should evaluate:

* Source trust
* Data classification
* Data freshness
* Data integrity
* External influence
* Security implications

A new untrusted data source should not silently become an input to high-impact autonomous actions.

---

### 151. Approval Configuration Management

Approval configurations should be managed as controlled security configurations.

This includes:

* Approval thresholds
* Approver groups
* Risk rules
* Escalation paths
* Transaction limits
* Expiration periods
* Dual-control requirements

Configuration changes should be authenticated, authorized, tested, and logged.

---

### 152. Human Approval Security Testing

Approval workflows should be tested against security requirements.

Testing should verify:

* Unauthorized approvers are rejected.
* Expired approvals are rejected.
* Modified transactions require reapproval.
* Replayed approvals are rejected.
* Delegation cannot bypass approval.
* Segregation-of-duties conflicts are blocked.
* Kill switches work.
* Approval failures fail safely.

---

### 153. Adversarial Human Approval Testing

Adversarial testing should attempt to defeat approval controls.

Scenarios may include:

* Agent attempts to manipulate the approver.
* Agent modifies parameters after approval.
* Agent submits repeated requests.
* Agent delegates to bypass approval.
* Agent exploits alternate tools.
* Agent exploits approval-token reuse.
* Agent generates misleading summaries.

Testing should demonstrate that approval remains an effective security boundary under hostile conditions.

---

### 154. Approval Control Effectiveness Testing

Organizations should evaluate whether approval controls actually reduce risk.

Testing should measure:

* Correct approval enforcement
* False approvals
* False denials
* Bypass attempts
* Approval latency
* Post-approval incidents
* Unauthorized actions
* Human decision quality

A control that technically exists but does not meaningfully reduce risk should not be considered effective.

---

### 155. Human Oversight Control Effectiveness

Human oversight should be evaluated as a measurable security control.

Assessment should determine whether humans can:

* Detect abnormal behavior
* Understand proposed actions
* Identify malicious instructions
* Stop unsafe execution
* Escalate correctly
* Respond within required time

The existence of an oversight dashboard does not prove that meaningful oversight exists.

---

### 156. Human Approval Monitoring Governance

Approval activity should be continuously monitored for anomalies.

Monitoring may detect:

* Excessive approvals
* Unusual approval times
* Unusual approvers
* Repeated approvals
* High-risk approvals
* Approval bursts
* Rejected actions followed by alternate attempts

Example:

```text id="v42wra"
Normal:
10 approvals/hour

Observed:
800 approvals/hour

        |
        v
Anomaly Detection
        |
        v
Security Review
```

---

### 157. Human Approval Key Risk Indicators

Useful KRIs include:

| KRI                            | Example   |
| ------------------------------ | --------- |
| Approval bypass attempts       | 12/month  |
| High-risk approvals            | 245/month |
| Rejected actions               | 81/month  |
| Approval replay attempts       | 4         |
| Approval conflicts             | 3         |
| Emergency approvals            | 17        |
| Approval latency               | 8 minutes |
| Unauthorized approver attempts | 6         |
| Post-approval incidents        | 2         |
| Approval fatigue indicator     | Elevated  |

KRIs should identify increasing risk before a material incident occurs.

---

### 158. Security Baseline Compliance

Approval workflows should be evaluated against an enterprise security baseline.

The baseline may require:

* Strong authentication
* Least privilege
* Approval logging
* Transaction binding
* Expiration
* Segregation of duties
* Monitoring
* Incident response
* Emergency controls

Exceptions should be formally documented and risk assessed.

---

### 159. Continuous Approval Control Monitoring

Approval controls should be monitored continuously.

Monitoring should verify:

```text id="p3n0es"
Policy
  |
  v
Configuration
  |
  v
Runtime Enforcement
  |
  v
Actual Approval Behavior
```

Differences between approved policy and runtime behavior should generate appropriate alerts.

---

### 160. Human Oversight Drift

Oversight can degrade over time.

Examples include:

* New agents added without approval rules.
* New transaction types introduced.
* Approval thresholds increased.
* Approvers change roles.
* New tools bypass existing workflows.
* Approval queues become overloaded.

Oversight drift should be continuously identified and corrected.

---

### 161. Human Oversight Incident Classification

Incidents involving approval controls should be classified consistently.

Examples:

* Approval bypass
* Unauthorized approval
* Approval manipulation
* Approval replay
* Approver compromise
* Segregation-of-duties violation
* Human oversight failure
* Approval-system compromise
* Emergency-control abuse

Classification supports consistent response and reporting.

---

### 162. Human Approval Incident Response Integration

Approval-related incidents should integrate into the enterprise incident-response process.

Response actions may include:

```text id="oj0o6j"
Detect
  |
  v
Suspend Approval
  |
  v
Revoke Approver Credentials
  |
  v
Suspend Agent
  |
  v
Stop Transactions
  |
  v
Preserve Evidence
  |
  v
Investigate
```

Incident response should address both the agent and the human authorization layer.

---

### 163. SOC Integration

The SOC should receive security-relevant approval events.

Examples include:

* High-risk approval
* Approval bypass attempt
* Unauthorized approver
* Excessive approval activity
* Emergency override
* Approval replay
* Suspicious agent behavior before approval

This allows human approval to become part of enterprise security monitoring.

---

### 164. Audit Evidence

Approval governance should generate sufficient evidence to demonstrate control operation.

Evidence may include:

* Approval policies
* Approver authorization records
* Approval events
* Rejection records
* Escalations
* Dual approvals
* Override events
* Configuration changes
* Control testing
* Review records

Evidence should be protected against unauthorized modification.

---

### 165. Governance Control Matrix

The organization should maintain a control matrix connecting risks to approval and oversight controls.

| Risk                      | Control                    | Owner          | Evidence              |
| ------------------------- | -------------------------- | -------------- | --------------------- |
| Unauthorized agent action | Human approval             | Business Owner | Approval record       |
| Approval spoofing         | Strong authentication      | IAM            | Authentication log    |
| Approval replay           | Transaction binding        | Security       | Approval token record |
| Excessive authority       | Approval scope             | IAM/Security   | Authorization policy  |
| Single-person abuse       | Dual approval              | Business       | Dual approval record  |
| Bypass                    | Independent gateway        | Security       | Gateway logs          |
| Approval fatigue          | Approval-volume monitoring | Governance     | KRI                   |

This creates traceability from risk to implemented control.

---

### 166. Risk-to-Control Traceability

Every material approval requirement should be traceable to an identified risk.

Example:

```text id="gqgqv6"
Risk
  |
  v
Unauthorized Autonomous Transaction
  |
  v
Security Requirement
  |
  v
Mandatory Human Approval
  |
  v
Approval Gateway
  |
  v
Runtime Enforcement
  |
  v
Audit Evidence
```

This demonstrates why the control exists and how it is enforced.

---

### 167. GRC Integration

Human approval governance should integrate with enterprise GRC processes.

Integration areas include:

* Risk registers
* Control libraries
* Compliance assessments
* Exceptions
* Audit findings
* Remediation
* Risk acceptance
* Control testing
* Evidence management

Agent approval requirements should therefore become part of the organization's broader governance framework.

---

### 168. Approval Exception Management

Exceptions to human approval requirements should be formally governed.

An exception should identify:

* Affected agent
* Affected action
* Reason
* Duration
* Risk
* Compensating controls
* Owner
* Approval authority
* Expiration date

Permanent exceptions should be strongly discouraged for high-impact autonomous activity.

---

### 169. Residual Approval Risk Acceptance

Where human approval controls cannot fully mitigate a risk, residual risk should be explicitly accepted by an authorized risk owner.

The acceptance should document:

* Remaining risk
* Business justification
* Compensating controls
* Risk owner
* Expiration/review date
* Monitoring requirements

The agent itself must never be the risk acceptance authority.

---

### 170. Human Approval Reauthorization

Approval requirements should be periodically revalidated.

Reauthorization should determine whether:

* The action still requires approval.
* The risk classification remains accurate.
* The approver remains authorized.
* Thresholds remain appropriate.
* Dual-control requirements remain necessary.
* Agent capabilities have changed.
* Tools or data sources have changed.

Material changes should trigger reauthorization before continued operation.

---

### 171. Human Oversight Retirement Governance

Human oversight processes should be formally retired when the underlying agent workflow is retired or redesigned.

Retirement should include:

* Disabling approval rules
* Removing obsolete approvers
* Revoking approval credentials
* Closing approval queues
* Preserving required evidence
* Updating governance records
* Validating that no dependent agent remains active

This prevents obsolete approval pathways from becoming security weaknesses.

---

### 172. Independent Human Oversight Assurance

High-risk approval systems should undergo independent assurance.

Assessment may examine:

* Approval policy
* Technical enforcement
* Identity controls
* Segregation of duties
* Transaction binding
* Logging
* Human factors
* Bypass resistance
* Incident response

Independent assurance provides greater confidence than self-assessment alone.

---

### 173. Human Approval Governance Maturity Model

A maturity model can evaluate enterprise capability.

| Level          | Capability                                                        |
| -------------- | ----------------------------------------------------------------- |
| 1 — Initial    | Ad hoc human approval                                             |
| 2 — Defined    | Documented approval rules                                         |
| 3 — Managed    | Risk-based approval and monitoring                                |
| 4 — Integrated | Enterprise IAM, GRC, SOC integration                              |
| 5 — Adaptive   | Continuous risk-based oversight and autonomous control adjustment |

The objective is to progress from manual checkpoints toward measurable, adaptive, and independently enforced oversight.

---

### 174. Enterprise Human Oversight Operating Model

A mature operating model distributes responsibilities across business, security, technology, and governance functions.

```text id="v7w2zr"
Business Owner
     |
     v
Defines Business Impact
     |
     +----------------+
     |                |
     v                v
Security          Governance
     |                |
     v                v
Controls          Risk / Policy
     |                |
     +-------+--------+
             |
             v
       Approval Platform
             |
             v
           Agent
             |
             v
          Execution
```

The operating model should clearly define ownership, escalation, accountability, and evidence responsibilities.

---

### 175. Governance Evidence Model

Human approval governance should maintain evidence across the entire control lifecycle.

Evidence should demonstrate:

```text id="7m08aq"
Requirement
    |
    v
Risk Assessment
    |
    v
Approval Policy
    |
    v
Approver Authorization
    |
    v
Approval Event
    |
    v
Execution
    |
    v
Monitoring
    |
    v
Control Testing
```

This provides a defensible record showing that high-impact autonomous activity remained subject to governed human authorization.

---

### 176. Enterprise Security Principle

**Human oversight must remain an independent security control over autonomous authority.**

An agent may generate recommendations, prepare transactions, communicate with other agents, and execute low-risk activities autonomously. However, when enterprise policy requires human involvement, the human approval boundary must remain technically enforceable and independent from agent behavior.

The architecture should therefore ensure:

```text id="f1os0p"
Agent Capability
      ≠
Agent Authority
      ≠
Human Approval
      ≠
Final Authorization
```

Each represents a distinct security concept and should be governed accordingly.

---

### 177. Strategic Governance Principle

**Human involvement must be risk-based, meaningful, attributable, and resistant to bypass.**

The objective of human oversight is not to place a human in every autonomous workflow. The objective is to ensure that the level of human control corresponds to the potential impact of autonomous activity.

A mature enterprise should therefore govern the relationship as:

```text id="zq9c4j"
Low Impact
   |
   v
Autonomous Execution
   +
Monitoring

Moderate Impact
   |
   v
Enhanced Human Oversight

High Impact
   |
   v
Mandatory Human Approval

Critical Impact
   |
   v
Dual Approval / Human-Controlled Execution
```

Human approval should remain **purpose-limited, least-privileged, independently authenticated, transaction-bound, auditable, continuously monitored, and enforceable even when the agent itself is compromised**.

# Part 4 — Security Architecture

## 178. Enterprise Human Approval and Oversight Security Architecture

Enterprise human approval architecture establishes the technical boundaries that determine when autonomous agent activity can proceed independently and when human authorization is required.

A reference architecture can be represented as:

```text id="2f7q1x"
+----------------------------------------------------------------+
|                    Enterprise Governance Plane                 |
|                                                                |
| Risk | Policy | GRC | Compliance | Audit | Business Authority |
+-------------------------------+--------------------------------+
                                |
                                v
+----------------------------------------------------------------+
|                  Agent Security Control Plane                   |
|                                                                |
| Identity | Authorization | Risk | Policy | Delegation | Limits |
+-------------------------------+--------------------------------+
                                |
                                v
+----------------------------------------------------------------+
|                    Approval Decision Gateway                   |
|                                                                |
| Approval Rules | Context | Thresholds | SoD | Expiration       |
+-------------------------------+--------------------------------+
                                |
                                v
+----------------------------------------------------------------+
|                    Human Approval Interface                    |
|                                                                |
| Action Preview | Risk | Context | Approve | Reject | Escalate  |
+-------------------------------+--------------------------------+
                                |
                                v
+----------------------------------------------------------------+
|                    Execution Enforcement                       |
|                                                                |
| Tool Gateway | API Gateway | Transaction Control | PAM         |
+-------------------------------+--------------------------------+
                                |
                                v
                       Target Enterprise
```

The architecture must ensure that the agent cannot directly bypass the approval boundary.

---

## 179. Human Approval Security Gateway

The human approval security gateway is the enforcement point between an agent's proposed action and authorized execution.

It should evaluate:

* Agent identity
* Original user identity
* Requested action
* Resource
* Risk
* Transaction value
* Approval requirement
* Approver authority
* Approval state
* Delegation chain
* Security policy

The gateway should return an explicit decision:

```text id="3q6t7z"
ALLOW AUTONOMOUS
REQUIRE APPROVAL
REQUIRE DUAL APPROVAL
DENY
```

The agent should never be able to override the gateway's decision.

---

## 180. Human Oversight Control Plane

The oversight control plane manages policies governing human intervention.

It should maintain:

* Approval thresholds
* Approver roles
* Risk rules
* Escalation paths
* Dual-control requirements
* Emergency procedures
* Approval expiration
* Override rules
* Monitoring requirements

This separates oversight policy from the runtime behavior of individual agents.

---

## 181. Policy-as-Code for Human Approval

Human approval requirements should be represented as machine-enforceable policies.

Example:

```text id="g6v5z8"
IF
    action = "financial_transfer"
AND amount > 5000
THEN
    human_approval = REQUIRED

IF
    amount > 50000
THEN
    dual_approval = REQUIRED

IF
    action = "disable_security_control"
THEN
    execution = BLOCKED
```

Policy-as-code enables:

* Version control
* Automated testing
* Consistent enforcement
* Change tracking
* Rollback
* Evidence generation

---

## 182. Zero Trust Human Approval Architecture

Human approval should follow Zero Trust principles.

The system should verify:

```text id="y9g8rb"
Approver Identity
      +
Approver Authority
      +
Requested Action
      +
Current Context
      +
Risk
      +
Approval Scope
```

Being authenticated does not automatically make a person authorized to approve a particular action.

Similarly, being authorized to approve one action does not establish authority over all agent actions.

---

## 183. Dynamic Approval Authorization

Approval requirements should be capable of changing based on current risk.

For example:

```text id="7zv1kp"
Normal Risk
    |
    v
Autonomous Execution

Elevated Risk
    |
    v
Human Approval

Critical Risk
    |
    v
Dual Approval / Block
```

Dynamic authorization can consider:

* Transaction value
* Current threat level
* Agent behavior
* Resource sensitivity
* User context
* Data classification
* Delegation depth
* Previous activity

---

## 184. Context-Aware Approval Architecture

Approval decisions should incorporate relevant security context.

Context may include:

* Original user request
* Agent identity
* Agent chain
* Business process
* Target resource
* Data involved
* Previous actions
* Current security state
* Transaction history

The approval system should distinguish trusted security metadata from agent-generated narrative.

---

## 185. Human Approval Trust Zones

Approval workflows should be separated according to risk.

Example:

```text id="j9k1cg"
+----------------------------------+
| Low-Risk Autonomous Zone         |
| Monitoring                       |
+----------------------------------+

+----------------------------------+
| Moderate-Risk Oversight Zone     |
| Human Visibility / Intervention  |
+----------------------------------+

+----------------------------------+
| High-Risk Approval Zone          |
| Mandatory Human Authorization    |
+----------------------------------+

+----------------------------------+
| Critical-Control Zone            |
| Dual Approval / Human Control    |
+----------------------------------+
```

An agent should not move an action into a lower-control zone simply by changing its workflow or delegating it.

---

## 186. Privilege Segmentation for Approval

Approval authority should be segmented by:

* Action type
* Transaction value
* Business unit
* Data sensitivity
* Environment
* Agent
* Resource
* Risk tier

This prevents an approver with narrow authority from becoming a universal authorization point.

---

## 187. Secure Agent-to-Approval Architecture

Agent requests should pass through a controlled interface before reaching human approvers.

```text id="t8k2vh"
Agent
  |
  v
Request Validation
  |
  v
Risk Evaluation
  |
  v
Approval Policy
  |
  v
Human Approval
  |
  v
Independent Authorization
  |
  v
Execution
```

The agent should not directly control what information is presented to the approver.

---

## 188. Approval Invocation Security

Every approval request should be securely associated with:

* Requesting agent
* Original user
* Action
* Target
* Transaction
* Risk assessment
* Approval policy
* Timestamp
* Correlation identifier

This allows the system to determine exactly what the human was asked to approve.

---

## 189. Approval Result Security

Approval results should be treated as security-sensitive information.

An approval result should be:

* Authenticated
* Integrity-protected
* Scope-limited
* Time-bound
* Transaction-bound
* Protected against replay

A compromised agent must not be able to fabricate:

```text id="8c3rpa"
APPROVED
```

and bypass the approval service.

---

## 190. Multi-Agent Approval Trust Architecture

When multiple agents participate in a workflow, the approval architecture must preserve the entire chain.

```text id="f5x2bn"
User
 |
 v
Agent A
 |
 v
Agent B
 |
 v
Agent C
 |
 v
Approval Gateway
 |
 v
Target System
```

The approval gateway should know which agent actually requested the action and which agents participated in the decision chain.

Trust should not automatically propagate between agents.

---

## 191. Secure Approval Delegation Architecture

Where approval authority is delegated, the delegation must be explicitly constrained.

```text id="a2k7ws"
Approver A
    |
    | Limited Delegation
    v
Approver B
    |
    +--> Specific Action
    +--> Specific Threshold
    +--> Specific Period
    +--> No Further Delegation
```

Delegated approval authority should not become unrestricted authority.

---

## 192. Human Identity and Attribution Plane

The attribution plane connects:

```text id="5y8xqf"
User
  |
  v
Agent
  |
  v
Proposed Action
  |
  v
Approver
  |
  v
Authorization
  |
  v
Execution
```

The system should preserve attribution across the entire chain.

This allows investigators to establish:

* Who initiated the workflow.
* Which agent proposed the action.
* Who approved it.
* What was approved.
* Which authorization decision was made.
* What actually executed.

---

## 193. Approval Credential Isolation

Approval credentials must be isolated from agents.

An agent should never have access to:

* Approver passwords
* MFA secrets
* Hardware-token credentials
* Approval signing keys
* Approval session tokens

The agent can request approval, but cannot authenticate as the approver.

---

## 194. Short-Lived Approval Authority

Approval authority and approval tokens should be time-bound where appropriate.

```text id="3m8q6v"
Approval Granted
      |
      v
Short Validity Period
      |
      v
Action Executed
      |
      v
Authority Expires
```

This limits the consequences of credential theft or delayed execution.

---

## 195. Approval Lifecycle Architecture

Approval requests should have a controlled lifecycle:

```text id="b3y7mf"
PROPOSED
   |
   v
VALIDATED
   |
   v
PENDING
   |
   +--> APPROVED
   |       |
   |       v
   |   AUTHORIZED
   |       |
   |       v
   |    EXECUTED
   |
   +--> REJECTED
   |
   +--> EXPIRED
   |
   +--> CANCELLED
```

Invalid states must never result in execution.

---

## 196. Approval Integrity Controls

Approval integrity ensures that what the human approved is exactly what the system executes.

Controls should bind:

* Agent
* Action
* Resource
* Parameters
* Transaction
* Time
* Approval identity

Any material change should invalidate the approval.

---

## 197. Approval Provenance Architecture

The system should maintain provenance from the original request to final execution.

```text id="x7k4nq"
Original User Intent
       |
       v
Agent Interpretation
       |
       v
Agent Recommendation
       |
       v
Risk Assessment
       |
       v
Human Approval
       |
       v
Authorization
       |
       v
Execution
```

This allows organizations to reconstruct why and how a high-impact autonomous action occurred.

---

## 198. Approval Risk Scoring

Approval decisions should be supported by independent risk scoring.

Possible factors include:

* Financial value
* Data sensitivity
* Privilege level
* Reversibility
* Agent trust state
* Delegation depth
* Target criticality
* Behavioral anomalies
* External dependencies

A conceptual model:

```text id="m5x8qd"
Action Risk =
Impact
+
Authority
+
Data
+
Agent Behavior
+
Delegation
+
External Exposure
```

The result can determine the required approval level.

---

## 199. Adaptive Human Oversight Controls

Oversight should increase automatically when risk increases.

```text id="r7z3hc"
Normal
  |
  v
Monitor

Elevated
  |
  v
Enhanced Monitoring

High
  |
  v
Human Approval

Critical
  |
  v
Dual Approval / Stop
```

Adaptive controls reduce unnecessary human intervention for low-risk operations while strengthening protection around high-impact activity.

---

## 200. Autonomous Transaction Protection

Human approval should integrate with transaction security.

Before execution, the system should verify:

* Approved transaction
* Transaction amount
* Recipient
* Resource
* Current authorization
* Approval validity
* Business rules
* Transaction state

A valid approval should never override independent transaction-security controls.

---

## 201. High-Impact Approval Protection

High-impact actions should receive stronger architectural controls.

Examples include:

* Financial transfers
* Production changes
* Privileged identity modifications
* Destructive operations
* Security-control changes
* Sensitive data exports

Possible architecture:

```text id="w6x0sb"
High-Impact Request
       |
       v
Independent Risk Evaluation
       |
       v
Human Approval
       |
       v
Dual Authorization
       |
       v
Final Validation
       |
       v
Execution
```

---

## 202. Human Approval Architecture

The approval interface should be designed as a security control rather than a generic workflow screen.

It should clearly display:

* Agent identity
* Original request
* Proposed action
* Target
* Parameters
* Risk
* Approval reason
* Delegation chain
* Expiration
* Consequences

The approver should be able to:

```text id="7f5h2s"
APPROVE
REJECT
ESCALATE
CANCEL
```

where permitted by policy.

---

## 203. Approval Integrity

Approval integrity requires that the human decision remain bound to the exact action presented.

For example:

```text id="0x6f4p"
Approved:
€10,000 → Supplier A

Execution Request:
€10,000 → Supplier B

Result:
DENY
```

The approval system should not allow material parameters to change silently after approval.

---

## 204. Dual Control and Segregation of Duties

Critical agent actions should support independent approval by multiple authorized parties.

```text id="c5n8jd"
                 Agent
                   |
                   v
             Critical Action
                   |
          +--------+--------+
          |                 |
          v                 v
     Approver A         Approver B
          |                 |
          +--------+--------+
                   |
              Both Valid
                   |
                   v
              Execution
```

The system should verify that the required approvers are genuinely independent.

---

## 205. Approval Suspension Architecture

The enterprise should be able to suspend approval authority or pending approvals during security incidents.

Possible triggers include:

* Approver compromise
* Agent compromise
* Security incident
* Policy violation
* Abnormal transaction activity
* Identity compromise

Suspension should immediately prevent affected approvals from authorizing execution.

---

## 206. Approval Revocation Architecture

Previously issued approvals should be revocable where execution has not yet occurred.

```text id="4y8n2v"
APPROVED
   |
   v
Security Alert
   |
   v
REVOKED
   |
   v
Execution Blocked
```

Revocation should propagate to dependent execution systems.

---

## 207. Approval Blast-Radius Reduction

Approval architecture should limit the impact of compromised human or agent identities.

Controls include:

* Transaction limits
* Approval scope
* Short-lived authorization
* Segmentation
* Dual approval
* Credential isolation
* Resource restrictions

A compromised approver should not automatically gain authority over every agent workflow.

---

## 208. Approval Circuit Breakers

Circuit breakers should automatically suspend approval-driven execution when abnormal behavior occurs.

Triggers may include:

* Excessive approvals
* Unusual transaction volume
* Repeated policy violations
* Abnormal agent behavior
* Approval replay
* Sudden changes in approval patterns

```text id="2w6m9a"
Abnormal Activity
      |
      v
Circuit Breaker
      |
      v
Suspend Execution
      |
      v
Security Investigation
```

---

## 209. Continuous Approval Security Testing

Approval mechanisms should be continuously tested.

Testing should include:

* Unauthorized approvers
* Expired approvals
* Approval replay
* Parameter modification
* Approval bypass
* Delegation abuse
* Queue manipulation
* Credential compromise
* Kill-switch behavior

Testing should verify both technical enforcement and workflow correctness.

---

## 210. Approval Regression Testing

Changes to:

* Models
* Agents
* Tools
* Policies
* APIs
* Identity systems
* Approval thresholds

should trigger regression testing.

The objective is to ensure that a system change does not accidentally remove an existing approval requirement.

---

## 211. Continuous Adversarial Approval Testing

Adversarial testing should attempt to manipulate the human approval process.

Scenarios should include:

```text id="8v3yq1"
Agent Misrepresentation
Approval Replay
Transaction Substitution
Approver Spoofing
Approval Flooding
Delegation Bypass
Tool Substitution
Parameter Manipulation
```

The test objective is to demonstrate that the human approval boundary remains effective against hostile agent behavior.

---

## 212. Approval Behavioral Baselines

Organizations should establish normal patterns for approval activity.

Baseline attributes may include:

* Approval volume
* Approval timing
* Typical transaction values
* Typical agents
* Typical business units
* Typical approval duration
* Rejection rates
* Escalation frequency

Anomalies should be investigated according to risk.

---

## 213. Human Oversight Observability

Security teams should be able to observe both agent activity and human oversight activity.

Observability should include:

```text id="q6y1pa"
Agent Activity
     |
     +--> Proposed Actions
     |
     +--> Approval Requests
     |
     +--> Approval Decisions
     |
     +--> Overrides
     |
     +--> Executions
     |
     +--> Rejections
```

This enables correlation between autonomous activity and human authorization.

---

## 214. Security Telemetry

Telemetry should capture security-relevant events such as:

* Approval requests
* Approval decisions
* Rejections
* Escalations
* Expirations
* Overrides
* Approver authentication
* Policy changes
* Approval failures
* Bypass attempts
* Transaction execution

Each event should include a correlation identifier wherever possible.

---

## 215. SIEM and SOC Integration

Approval telemetry should be integrated with enterprise SIEM and SOC platforms.

```text id="m3s8kp"
Agent
  |
  v
Approval Gateway
  |
  v
Security Telemetry
  |
  v
SIEM
  |
  v
Detection
  |
  v
SOC
  |
  v
Response
```

The SOC should be able to detect suspicious combinations such as:

```text
Compromised Agent
+
Unusual Approver
+
High-Value Transaction
=
Critical Alert
```

---

## 216. Human Approval Incident Response Playbook

Incident response should define specific actions for approval-related incidents.

```text id="n5k4cx"
Detect
  |
  v
Classify
  |
  v
Suspend Approval
  |
  v
Revoke Credentials
  |
  v
Stop Transactions
  |
  v
Isolate Agent
  |
  v
Preserve Evidence
  |
  v
Investigate
  |
  v
Restore Safely
```

The playbook should address both technical and human authorization components.

---

## 217. Agent Compromise and Approval Containment

If an agent is compromised, all pending and recently issued approvals associated with the agent should be evaluated.

Containment may include:

* Suspending the agent
* Cancelling pending approvals
* Revoking delegated authority
* Blocking transactions
* Revalidating recent approvals
* Isolating downstream systems

This prevents a compromised agent from continuing to exploit legitimate approval mechanisms.

---

## 218. Cryptographic Approval Integrity

High-impact approvals may require cryptographic protection.

Possible controls include:

* Digital signatures
* Secure timestamps
* Transaction hashes
* Cryptographic approval tokens
* Strong identity binding

For example:

```text id="k1z8mr"
Approval
   |
   +--> Transaction Hash
   +--> Approver Identity
   +--> Timestamp
   +--> Scope
   |
   v
Cryptographically Bound Decision
```

Cryptographic controls should complement—not replace—authorization policy.

---

## 219. Approval Configuration Integrity

Approval configurations should be protected against unauthorized changes.

Configuration should include:

* Thresholds
* Approvers
* Risk rules
* Escalation
* Dual-control requirements
* Expiration
* Emergency procedures

Changes should require:

* Authentication
* Authorization
* Review
* Testing
* Logging
* Version control

---

## 220. Human Oversight Supply-Chain Assurance

Third-party components supporting human oversight should be assessed.

This includes:

* Approval platforms
* Identity providers
* Workflow systems
* Notification systems
* Risk engines
* Policy engines
* Transaction platforms

A compromised dependency could undermine the approval boundary even when the agent itself remains secure.

---

## 221. Identity Provider Assurance

Identity providers supporting approver authentication should receive appropriate assurance.

Assessment areas include:

* Authentication security
* MFA
* Session protection
* Privileged access
* Availability
* Credential recovery
* Logging
* Incident response

The identity provider represents a foundational component of the approval architecture.

---

## 222. Approval Platform Assurance

Approval platforms should be assessed for:

* Authentication
* Authorization
* Transaction integrity
* Availability
* Auditability
* Configuration security
* Data protection
* Change management
* Incident response

Critical approval infrastructure should receive assurance proportional to the actions it controls.

---

## 223. Human Approval Control Testing

Control testing should verify that:

```text id="h6p9sw"
Correct Approver
       +
Correct Action
       +
Correct Scope
       +
Correct Time
       +
Correct Risk
       =
Valid Approval
```

Testing should include both positive and negative scenarios.

---

## 224. Independent Security Assessment

High-impact human oversight architecture should receive independent security assessment.

Assessment areas may include:

* Architecture
* Identity
* Authorization
* Approval workflow
* Segregation of duties
* Transaction binding
* Bypass resistance
* Logging
* Incident response
* Human-factor security

Independent assessment should challenge both technical controls and governance assumptions.

---

## 225. Risk-Based Oversight Assurance Frequency

Oversight assurance should be proportional to risk.

```text id="k7d4mn"
Low Risk
   -> Annual

Moderate Risk
   -> Semiannual

High Risk
   -> Quarterly

Critical Risk
   -> Continuous Monitoring
      + Frequent Testing
```

Significant changes or incidents should trigger additional assessment regardless of the normal schedule.

---

## 226. Continuous Approval Security Drift Detection

Approval controls can drift from their approved design.

Examples:

* New agents bypass approval.
* Thresholds are increased.
* Approvers change roles.
* New tools execute outside approval workflows.
* Dual-control requirements are removed.
* New transaction types are introduced.

The system should continuously compare:

```text id="e0w7sn"
Approved Architecture
        |
        v
Current Configuration
        |
        v
Runtime Behavior
        |
        v
Drift Detection
```

Material drift should trigger remediation or formal exception handling.

---

## 227. Enterprise Human Oversight Security Dashboard

The enterprise should maintain centralized visibility into oversight effectiveness.

Metrics may include:

* Active approval workflows
* Pending approvals
* High-risk approvals
* Rejected actions
* Approval bypass attempts
* Emergency overrides
* Approval latency
* Approval anomalies
* Suspended agents
* Approval-control failures

Example:

```text id="a8s3zf"
+------------------------------------------------------+
|        HUMAN OVERSIGHT SECURITY DASHBOARD            |
+------------------------------------------------------+
| Pending | High Risk | Rejected | Overrides | Alerts |
|   84    |    17     |    29    |     3     |   11   |
+------------------------------------------------------+
| Active Agents | Approval Bypass | Dual Approval |
|     642       |        2        |      31       |
+------------------------------------------------------+
```

---

## 228. Human Oversight Security Metrics

Key metrics should measure both control operation and control effectiveness.

Examples include:

* Percentage of high-risk actions receiving approval
* Unauthorized approval attempts
* Approval bypass attempts
* Approval replay attempts
* Average approval latency
* Rejection rate
* Escalation rate
* Emergency override rate
* Approval-related incidents
* Post-approval security incidents
* Percentage of approval rules tested
* Percentage of approvers reviewed
* Percentage of high-risk workflows independently assessed

Metrics should be connected to defined risk objectives.

---

## 229. Human Oversight Assurance Package

High-risk agent workflows should maintain an assurance package containing:

* Agent identity
* Business purpose
* Risk classification
* Approval policy
* Approval thresholds
* Authorized approvers
* Segregation-of-duties rules
* Threat model
* Architecture
* Security testing
* Approval evidence
* Monitoring configuration
* Exceptions
* Risk acceptance
* Reauthorization history

This provides evidence that human oversight is operational rather than merely documented.

---

## 230. Lifecycle Reauthorization

Human approval requirements should be periodically reauthorized.

The review should determine whether:

* The agent still requires the same autonomy.
* Approval thresholds remain appropriate.
* Approvers remain authorized.
* The business process has changed.
* Agent capabilities have changed.
* Tool capabilities have changed.
* Risk has changed.
* Oversight remains effective.

Reauthorization should result in one of:

```text id="s6z1wy"
CONTINUE
REDUCE AUTONOMY
INCREASE OVERSIGHT
CHANGE APPROVERS
SUSPEND
RETIRE
```

---

## 231. Human Oversight Retirement

When an agent or workflow is retired, associated approval controls should also be retired.

This should include:

1. Disable approval workflows.
2. Remove obsolete approvers.
3. Revoke approval credentials.
4. Cancel pending requests.
5. Remove obsolete policies.
6. Preserve required evidence.
7. Update governance records.
8. Confirm no dependent workflow remains active.

Retirement prevents obsolete approval pathways from becoming attack surfaces.

---

## 232. Enterprise Security Integration

Human approval architecture should integrate with enterprise security systems.

```text id="u1y8xw"
Human Oversight
      |
      +--> IAM
      +--> PAM
      +--> API Security
      +--> Transaction Security
      +--> Data Security
      +--> SIEM
      +--> SOAR
      +--> GRC
      +--> SOC
      +--> Incident Response
```

Human oversight should therefore operate as an integrated enterprise security capability.

---

## 233. Approval Architecture Traceability

Every approval requirement should be traceable from business need to runtime enforcement.

```text id="v4m7ka"
Business Requirement
        |
        v
Risk
        |
        v
Security Requirement
        |
        v
Approval Policy
        |
        v
Architecture
        |
        v
Runtime Enforcement
        |
        v
Evidence
```

This demonstrates why an approval control exists and how the organization proves that it operates.

---

## 234. Human Approval Boundary Enforcement

The approval boundary must be technically enforceable.

The architecture should prevent:

* Agent bypass
* Tool bypass
* API bypass
* Delegation bypass
* Parameter substitution
* Approval replay
* Unauthorized override
* Direct execution

The essential architecture is:

```text id="c8w5vt"
Agent
  |
  v
Proposed Action
  |
  v
Approval Boundary
  |
  +---- DENY
  |
  +---- APPROVAL REQUIRED
  |          |
  |          v
  |       Human
  |          |
  |          v
  +------ Authorization
             |
             v
          Execution
```

---

## 235. Model Behavior Is Not Security Authorization

The model may generate:

```text id="p9r2kx"
"The manager approved this."
```

That statement has no authorization value unless independently verified.

The security architecture must obtain approval status from a trusted authorization system.

```text id="z5n7qy"
Model Claim
    |
    X
    |
    v
Trusted Approval Record
    |
    v
Authorization Decision
```

Agent-generated assertions must never become security evidence by themselves.

---

## 236. Agent Capability Is Not Agent Authority

An agent may have the technical capability to request an approval, but that does not give it authority to determine:

* Who approves
* Whether approval is required
* What threshold applies
* Whether approval was granted
* Whether approval can be bypassed

The architecture must independently enforce these decisions.

```text id="b3m9wc"
Agent Capability
       |
       X
       |
Agent Authority
       |
       X
       |
Approval Authority
```

These are separate security concepts.

---

## 237. Autonomous Execution Must Remain Bounded

Human oversight architecture must establish explicit boundaries around autonomous execution.

These include:

* Action scope
* Transaction value
* Time
* Data
* Tools
* Delegation
* Resources
* Environment
* Communication
* Execution duration

```text id="w8q4nf"
             AUTONOMOUS AGENT
                    |
        +-----------+-----------+
        |           |           |
      Scope        Time       Impact
        |           |           |
      Bound        Bound       Bound
        |           |           |
        +-----------+-----------+
                    |
                    v
             Controlled Action
```

Autonomy should never be equivalent to unrestricted authority.

---

## 238. Agent Compromise Must Not Equal Enterprise Compromise

A compromised agent should not be capable of defeating human oversight and obtaining unrestricted enterprise authority.

The architecture should provide:

```text id="m2v7cz"
Compromised Agent
       |
       v
Restricted Identity
       |
       v
Limited Authority
       |
       v
Approval Boundary
       |
       v
Human Authorization
       |
       v
Independent Enforcement
       |
       v
Contained Impact
```

If an agent attempts to bypass approval by changing tools, delegating to another agent, modifying parameters, or creating multiple smaller transactions, independent controls must detect and block the attempt.

---

## 239. Autonomous Systems Require Independent Human Control

Human oversight must remain independent of the autonomous system it governs.

The final architecture should therefore separate:

```text id="n7x4ks"
                 AGENT
                   |
                   v
            Recommendation
                   |
                   v
             Risk Engine
                   |
                   v
           Approval Gateway
                   |
                   v
             HUMAN REVIEW
                   |
                   v
        Independent Authorization
                   |
                   v
          Execution Enforcement
                   |
                   v
               TARGET
```

The fundamental architectural principle is:

**The agent may recommend and prepare an action, but it must not control the mechanism that determines whether the action is authorized.**

Human approval, independent authorization, least privilege, transaction controls, monitoring, and emergency intervention must operate as separate but coordinated security boundaries.



